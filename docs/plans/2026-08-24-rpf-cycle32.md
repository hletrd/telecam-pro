# Review-plan-fix cycle 32 — implementation plan

Date: 2026-08-24
Source: `.context/reviews/_aggregate.md`, cycle 32
Status: complete; all fourteen findings fixed, configured gates green, signed commits pushed

## Repository rules applied

- Preserve measured PMA110 behavior, enumerated multi-device routes, EN/KO presentation, quiet
  Sony-style camera UX, exact media ownership/durability, and wrapper-owned release evidence.
- Every cycle-32 finding is scheduled below. Nothing is deferred, silently dropped,
  severity-downgraded, or introduced as unrelated scope.
- Completed plans remain non-destructive archives in `docs/plans/`; no prior plan is moved or deleted.
- Use GPG-signed Conventional Commits with gitmoji, pull with rebase before every push, and do not
  deploy because `DEPLOY_MODE=none`.
- Perform all work in `/private/tmp/find-x9-rpf32.SEkU6E/repo`. Preserve the shared main worktree and
  all pre-existing dirty changes without touching, staging, stashing, resetting, or checking them out.

## P1 — gate review on native recording finalization (AGG32-01)

- [x] Publish exact recording-finalization ownership separately from starting/active REC state.
- [x] Keep review disabled defensively and in Compose until release or quarantine reaches terminal.
- [x] Test held native release, terminal enablement, and absence of a false second Stop affordance.

## P2 — align retirement-registry capacity with family units (AGG32-02)

- [x] Bound distinct family keys at the durable journal limit and separately bound owner fan-out.
- [x] Preserve rollback/idempotence and cross-Engine exact notification.
- [x] Test 64 families with multiple listeners and rejection only at the 65th distinct family.

## P3 — retry accepted nonterminal family retirement safely (AGG32-03)

- [x] Give retryable `RETAINED` outcomes a conflated, bounded, backed-off process retry owner.
- [x] Avoid spinning on genuine live rows and prevent released Engines from remaining strongly owned.
- [x] Test transient query/marker failures, later terminal recovery, release, and capacity reclamation.

## P4 — authorize deletion of restored ownerless media (AGG32-04)

- [x] Route only legacy owner-unverified rows through an Activity-owned
  `MediaStore.createDeleteRequest` consent flow while preserving current app-owned durability.
- [x] Reconcile approval, cancellation, disappearance, and launch failure truthfully without issuing
  a redundant unauthorized delete.
- [x] Add EN/KO status and pure/Robolectric routing-result coverage; record device validation as open.

## P5 — make recreate smoke prove a replacement preview (AGG32-05)

- [x] Observe ordered Ready invalidation and a newer generation after recreation.
- [x] Require a producer-fed frame oracle for the replacement surface within a bounded timeout.

## P6 — protect viewport-relative focal-rail fading (AGG32-06)

- [x] Test fixed trailing-edge fade coordinates across nonterminal offsets and disappearance at end.
- [x] Keep the production modifier order as the single source exercised by the regression.

## P7 — test rendered AF terminal cues (AGG32-07)

- [x] Extract Canvas-consumed cue geometry and assert nonempty, distinct, in-bounds CHECK/CROSS paths.
- [x] Verify positive ink/keyline widths and visibility over bright/dark fixtures.

## P8 — complete the ViewModel review-gate truth table (AGG32-08)

- [x] Cover all four starting/recording combinations at the defensive ViewModel boundary.

## P9 — expose a quiet accessible horizon-level state (AGG32-09)

- [x] Add stable, coarsened level/left/right semantics without a sensor-rate live region.
- [x] Add paired EN/KO strings and Compose tests for disabled, level, directions, and locale behavior.

## P10 — clamp still-review pan to fitted content (AGG32-10)

- [x] Single-source fitted bitmap/viewport geometry and clamp every pan, zoom, point-centering, size,
  and orientation transition.
- [x] Pure-test portrait/landscape letterboxing, 1x/4x/12x, corners, and excessive pans.

## P11 — recognize double-taps with spatial and temporal validity (AGG32-11)

- [x] Add the supported minimum/maximum interval and touch-slop distance to the unified gesture owner.
- [x] Test valid-near, far, too-fast, timed-out, drag, and pinch sequences.

## P12 — keep one durable viewfinder accessibility identity (AGG32-12)

- [x] Move current AF state onto the existing viewfinder `stateDescription`, make the Canvas
  decorative, and retain only the small terminal change announcer.
- [x] Test exactly one viewfinder focus stop across idle/searching/focused/failed states.

## P13 — document device-enumerated Lens-tab choices (AGG32-13)

- [x] Qualify 0.6x/1x/3x/10x as PMA110-specific and describe capability-filtered presets generally.
- [x] Add a documentation contract rejecting the unqualified fixed-list claim.

## P14 — qualify optional private UX policy links (AGG32-14)

- [x] Mark `docs/UX_POLICY.md` as optional/when-present and identify committed fallback authority.
- [x] Enforce the qualifier for current public docs in `tools/check_docs.py` tests.

## Quality gates and delivery

- [x] Run focused tests for every fix, documentation/tool contracts, and `git diff --check`.
- [x] Run `./gradlew :app:assembleDebug :app:testDebugUnitTest :app:lintDebug` against the whole repo
  and root-fix every error/actionable warning without suppression.
- [x] Run `./gradlew :app:lintRelease :app:assembleRelease :app:bundleRelease` against clean source
  and root-fix every error/actionable warning without suppression.
- [x] Commit and push each independent review, plan, fix, test, and documentation increment using
  GPG-signed Conventional Commits plus gitmoji, pulling with rebase before each push.
- [x] Update this plan with exact evidence and mark complete only after every item is done.

## Deferred findings

None.

## Completion evidence

- All fourteen aggregate findings were implemented in the isolated cycle clone; no source, index,
  process, or pre-existing dirty delta in the shared main worktree was touched.
- Review stays unavailable from REC admission through the exact native/container/microphone
  finalization terminal. The shutter still leaves Stop state immediately, and deterministic tests
  prove change-gated ownership publication plus defensive/Compose admission.
- Retained-family bookkeeping now counts distinct families separately from bounded listener fan-out,
  unregisters released Engines, distinguishes authoritative survivors from retryable uncertainty,
  and owns one conflated 250 ms–30 s retry without spinning on live rows.
- Restored owner-unverified media uses Android's system-owned delete request; approval never issues a
  redundant direct delete, while cancel/failure/disappearance preserves exact provider truth. API
  33/36 consent behavior remains honestly open as FIELD_CHECKS E2.
- Recreation instrumentation now requires ordered Ready invalidation, a newer Surface generation,
  and a real producer-fed replacement frame. The focal fade test protects rendered viewport
  coordinates through a controlled reverse-order failure.
- AF terminal glyphs have Canvas-consumed checked geometry; AF and coarsened EN/KO horizon state now
  share one durable viewfinder accessibility identity. Still review clamps every transform to fitted
  content and validates double taps by minimum/maximum time plus spatial proximity.
- Focused JVM/Robolectric/Compose/instrumentation-compilation suites passed for every lane.
  `verifyPartitionACoverage` passed at 99.83% with the exact existing 13-line residual manifest;
  96 public documentation checks passed and 21 optional-private checks skipped by design.
- `./gradlew :app:assembleDebug :app:testDebugUnitTest :app:lintDebug` passed against the whole repo.
- From a clean committed source tree with the existing local upload-signing setup,
  `./gradlew :app:lintRelease :app:assembleRelease :app:bundleRelease` passed, including R8, signed
  APK assembly, and signed AAB assembly. Direct Gradle outputs remain developer-only; no immutable
  release evidence or deployment is claimed.
- The existing reviewed Gradle report-aggregation and upstream native-symbol-strip warnings remain
  governed by prior plan records; this cycle introduced no new unresolved gate warning.
- Eleven review/plan/implementation commits were GPG-signed, pull-rebased, and pushed before this
  closeout. This closeout is the twelfth and final signed/pushed cycle commit.
- Deployment and physical-device mutation were not performed (`DEPLOY_MODE=none`).

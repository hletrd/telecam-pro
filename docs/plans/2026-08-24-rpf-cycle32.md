# Review-plan-fix cycle 32 — implementation plan

Date: 2026-08-24
Source: `.context/reviews/_aggregate.md`, cycle 32
Status: in progress

## Repository rules applied

- Preserve measured PMA110 behavior, enumerated multi-device routes, EN/KO presentation, quiet
  Sony-style camera UX, exact media ownership/durability, and wrapper-owned release evidence.
- Every cycle-32 finding is scheduled below. Nothing is deferred, silently dropped,
  severity-downgraded, or introduced as unrelated scope.
- Completed plans remain non-destructive archives in `docs/plans/`; no prior plan is moved or deleted.
- Use GPG-signed Conventional Commits with gitmoji, pull with rebase before every push, and do not
  deploy because `DEPLOY_MODE=none`.
- Perform all work in `/private/tmp/find-x9-rpf32.SEkU6E/repo`. Preserve the shared main worktree and
  all pre-existing dirty changes without touching, staging, stashing, resetting, or checking them out.

## P1 — gate review on native recording finalization (AGG32-01)

- [ ] Publish exact recording-finalization ownership separately from starting/active REC state.
- [ ] Keep review disabled defensively and in Compose until release or quarantine reaches terminal.
- [ ] Test held native release, terminal enablement, and absence of a false second Stop affordance.

## P2 — align retirement-registry capacity with family units (AGG32-02)

- [ ] Bound distinct family keys at the durable journal limit and separately bound owner fan-out.
- [ ] Preserve rollback/idempotence and cross-Engine exact notification.
- [ ] Test 64 families with multiple listeners and rejection only at the 65th distinct family.

## P3 — retry accepted nonterminal family retirement safely (AGG32-03)

- [ ] Give retryable `RETAINED` outcomes a conflated, bounded, backed-off process retry owner.
- [ ] Avoid spinning on genuine live rows and prevent released Engines from remaining strongly owned.
- [ ] Test transient query/marker failures, later terminal recovery, release, and capacity reclamation.

## P4 — authorize deletion of restored ownerless media (AGG32-04)

- [ ] Route only legacy owner-unverified rows through an Activity-owned
  `MediaStore.createDeleteRequest` consent flow while preserving current app-owned durability.
- [ ] Reconcile approval, cancellation, disappearance, and launch failure truthfully without issuing
  a redundant unauthorized delete.
- [ ] Add EN/KO status and pure/Robolectric routing-result coverage; record device validation as open.

## P5 — make recreate smoke prove a replacement preview (AGG32-05)

- [ ] Observe ordered Ready invalidation and a newer generation after recreation.
- [ ] Require a producer-fed frame oracle for the replacement surface within a bounded timeout.

## P6 — protect viewport-relative focal-rail fading (AGG32-06)

- [ ] Test fixed trailing-edge fade coordinates across nonterminal offsets and disappearance at end.
- [ ] Keep the production modifier order as the single source exercised by the regression.

## P7 — test rendered AF terminal cues (AGG32-07)

- [ ] Extract Canvas-consumed cue geometry and assert nonempty, distinct, in-bounds CHECK/CROSS paths.
- [ ] Verify positive ink/keyline widths and visibility over bright/dark fixtures.

## P8 — complete the ViewModel review-gate truth table (AGG32-08)

- [ ] Cover all four starting/recording combinations at the defensive ViewModel boundary.

## P9 — expose a quiet accessible horizon-level state (AGG32-09)

- [ ] Add stable, coarsened level/left/right semantics without a sensor-rate live region.
- [ ] Add paired EN/KO strings and Compose tests for disabled, level, directions, and locale behavior.

## P10 — clamp still-review pan to fitted content (AGG32-10)

- [ ] Single-source fitted bitmap/viewport geometry and clamp every pan, zoom, point-centering, size,
  and orientation transition.
- [ ] Pure-test portrait/landscape letterboxing, 1x/4x/12x, corners, and excessive pans.

## P11 — recognize double-taps with spatial and temporal validity (AGG32-11)

- [ ] Add the supported minimum/maximum interval and touch-slop distance to the unified gesture owner.
- [ ] Test valid-near, far, too-fast, timed-out, drag, and pinch sequences.

## P12 — keep one durable viewfinder accessibility identity (AGG32-12)

- [ ] Move current AF state onto the existing viewfinder `stateDescription`, make the Canvas
  decorative, and retain only the small terminal change announcer.
- [ ] Test exactly one viewfinder focus stop across idle/searching/focused/failed states.

## P13 — document device-enumerated Lens-tab choices (AGG32-13)

- [ ] Qualify 0.6x/1x/3x/10x as PMA110-specific and describe capability-filtered presets generally.
- [ ] Add a documentation contract rejecting the unqualified fixed-list claim.

## P14 — qualify optional private UX policy links (AGG32-14)

- [ ] Mark `docs/UX_POLICY.md` as optional/when-present and identify committed fallback authority.
- [ ] Enforce the qualifier for current public docs in `tools/check_docs.py` tests.

## Quality gates and delivery

- [ ] Run focused tests for every fix, documentation/tool contracts, and `git diff --check`.
- [ ] Run `./gradlew :app:assembleDebug :app:testDebugUnitTest :app:lintDebug` against the whole repo
  and root-fix every error/actionable warning without suppression.
- [ ] Run `./gradlew :app:lintRelease :app:assembleRelease :app:bundleRelease` against clean source
  and root-fix every error/actionable warning without suppression.
- [ ] Commit and push each independent review, plan, fix, test, and documentation increment using
  GPG-signed Conventional Commits plus gitmoji, pulling with rebase before each push.
- [ ] Update this plan with exact evidence and mark complete only after every item is done.

## Deferred findings

None.

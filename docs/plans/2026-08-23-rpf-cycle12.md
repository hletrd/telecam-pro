# Review-plan-fix cycle 12 — implementation plan

Date: 2026-08-23
Source: `.context/reviews/_aggregate.md`, cycle 12
Status: active; implementation and host gates complete, immutable release gate pending

## Repository rules applied

- Preserve PMA110 behavior byte-for-byte and derive non-PMA110 behavior only from enumerated
  capabilities; no device behavior may be newly claimed without current-device evidence.
- Correctness, native ownership, data-loss/privacy, and evidence-integrity findings are mandatory.
- New user-facing prose requires matching English and Korean resources and the quiet Sony
  Alpha/Xperia Pro surface. This plan adds no banners, coach marks, or marketing copy.
- Deployment and device work are disabled. Do not reuse an old ADB serial.
- Completed plan files remain in place. Moving/deleting them requires destructive-action
  confirmation, so their completed status is the repository's non-destructive archive marker.
- Preserve exact coverage, immutable release boundaries, dependency verification, GPG signing,
  Conventional Commits with gitmoji, pull-with-rebase before push, and no force push.

Every aggregate finding AGG12-01 through AGG12-14 is scheduled below or explicitly deferred under
the repository's recorded owner decision. No finding is silently dropped or severity-downgraded.

## Phase 1 — close native recording and topology discovery ownership gaps

- [x] **P1 / AGG12-01:** add a release-visible recorder setup-classification owner before
  `RecordingPreNativeAllocationAttempt` becomes `CLAIMED`. Bind the exact recorder/native attempt
  before vendor setup, classify every exit as released or quarantined, make pause/release wait for a
  bounded terminal decision, and prevent replacement Camera2/GL admission while classification is
  pending. Add a real Engine barrier test for claim -> blocked native setup -> release -> replacement.
- [x] **P2 / AGG12-02:** mark every incomplete/id-list-failed route snapshot unresolved, without
  changing accepted-session optical truth. Add integration coverage for complete inventory ->
  same-id identity epoch -> exhausted partial retries -> recovered same-id callback -> complete truth.

Acceptance: no old native recorder setup can overlap a replacement graph, and a recovered camera
service can converge partial inventory without requiring a new id/epoch or process restart.

## Phase 2 — make delete/rejected-output capacity represent unresolved work only

- [x] **P3 / AGG12-03:** retain family-delete markers through producer terminality, then run an exact
  bounded Images/Video absence query and retire only authoritative absence. Keep unresolved markers
  fail-closed and prove more than 64 successful capture/delete cycles in one process.
- [x] **P4 / AGG12-04:** ensure every already-admitted rejected HEIF/JPEG/DNG/video output retains an
  exact owner even when the ordinary retry set is full. Reserve per-capture worst-case capacity or
  durably spill overflow identity before returning; add capacity-one multi-sibling and restart
  adoption tests.

Acceptance: healthy long sessions do not hit a false capacity cliff, and no failed output can be
forgotten or adopted after process recreation.

## Phase 3 — bind device evidence to singular executable/artifact identities

- [x] **P5 / AGG12-05:** replace caller-self-issued child proof with outer-held authority that a
  direct caller cannot mint. Require that authority before imports and add a correct-digest forged
  child refusal test plus the existing legitimate outer -> child success coverage.
- [x] **P6 / AGG12-06:** make device-evidence debug compilation and embedded source provenance consume
  one immutable source owner. Add a Gradle integration barrier proving transient source replacement
  cannot compile beneath an older manifest.
- [x] **P7 / AGG12-07:** no-follow open and snapshot one regular APK inode, hash during the copy, and
  run aapt/aapt2/ZIP/source inspection exclusively on that private copy. Test early/late A -> B -> A
  swaps and symlink/non-regular inputs.
- [x] **P8 / AGG12-08:** freeze the exact case-owned report evidence set; descriptor-walk it with
  no-follow, same-inode bounded reads; reject missing, unexpected, symlink, and special-file members;
  and write the attestation pair through controlled descriptor-relative outputs. Test file and parent
  swaps, disappearance, addition, symlink, and FIFO cases.

Acceptance: source, bytecode, APK contracts, executed harness code, and completed report evidence
each have one immutable identity across every authority that attests them.

## Phase 4 — make the authoritative host gate behaviorally complete

- [x] **P9 / AGG12-09:** change the final whitespace gate to cover HEAD through the complete working
  tree (staged and unstaged). Replace/supplement source-string assertions with exact command-plan and
  temporary-Git-repository tests that prove both defect classes fail.

Acceptance: a staged-only invalid patch cannot pass `tools/verify_host.py`, and the meta-test proves
behavior rather than the presence of inert text.

## Phase 5 — align shutter and focus accessibility presentation with real admission

- [x] **P10 / AGG12-11:** include photo output-admission truth in the primary shutter's visual-health
  model while preserving Timer Cancel and recording Stop behavior. Add real Compose assertions for
  disabled semantics and alpha in healthy/unhealthy/admission-refused states.
- [x] **P11 / AGG12-12:** derive viewfinder custom focus actions from the same Ready, region-count,
  AUTO-AF, non-Manual admission facts used by the Engine; expose Reset only while a held tap point
  exists. Add Compose coverage for capable/incapable, Ready/not-Ready, Manual, held, and unheld states.

Acceptance: touch, keyboard, TalkBack/Switch Access semantics, and visual affordance never promise an
action that the Engine will refuse as a no-op.

## Phase 6 — repair and pin architecture authority

- [x] **P12 / AGG12-13:** replace the removed `CameraEngine.centerCrop` reference with top-level
  `centerCropBox` plus `StillCapturePipeline`'s one-pass crop/rotation application; add a docs check
  for both owners.
- [x] **P13 / AGG12-14:** add `Zoom & the Hybrid Camera Routes` to the architecture TOC, renumber the
  following entries, and add an H2-to-TOC completeness contract.

Acceptance: the architecture is navigable and names the current allocation/geometry owners exactly.

## Phase 7 — gates, delivery, and evidence

- [x] Run `./gradlew :app:assembleDebug :app:testDebugUnitTest :app:lintDebug` against the whole repo.
- [ ] Run the configured release tasks through the repository's immutable release wrapper when the
  direct Gradle release entry points fail closed by design.
- [x] Run `python3 tools/verify_host.py`, the complete tool/coverage/harness/docs suites, Python
  compilation, both staged and unstaged diff checks, and exact Partition-A coverage verification.
- [x] Root-fix every error and cleanly actionable warning; add no suppression.
- [ ] Commit independent concerns with fine-grained GPG-signed Conventional Commit + gitmoji
  messages; `git pull --rebase` before each push and push immediately. Do not deploy.
- [ ] Mark completed items and record exact gate/commit evidence here.

## Planned commit sequence

`fix(camera)`, `fix(storage)`, `fix(attestation)`, `build(provenance)`, `test(gate)`,
`fix(ui)`, `docs(architecture)`, and `docs(plan)`, split further when one commit would mix concerns.

## Interim implementation evidence

- The authoritative host gate passed from the complete implementation: **1,703** JVM/Robolectric/
  Compose tests, debug assembly/lint, **35** tool/release tests, **9** coverage-tool tests, **169**
  device-harness self-tests, **91** documentation checks, Python compilation, and the complete
  HEAD-to-working-tree whitespace check.
- Coverage is **51.91% overall (12,129/23,366)** and **99.78% Partition A (5,969/5,982)**. The exact
  existing **13-line / 9-class** residual manifest is unchanged. Six gate issues were fixed: two
  integration test/compile errors and four newly reachable coverage lines.
- Seven independent implementation commits through `8515074` are GPG-signed, pull-rebased, and
  pushed. Deployment and physical-device work remain disabled.

## Deferred findings

### D1 / AGG12-10 — owner-cleared media provenance spoofing

- **Original severity / confidence:** Medium / High (unchanged).
- **Citation:** `app/src/main/kotlin/me/hletrd/telecampro/storage/LatestCaptureReducer.kt:101-157,
  179-208,275-327`; `MediaStoreWriter.kt:164-230`.
- **Reason permitted by repository policy:** `docs/BACKLOG.md:213-216` records the explicit owner
  decision: **“Reinstall gallery restore — RESOLVED same day (user decision)”** and requires any
  visual-media grant to restore captures from the previous install. Removing automatic owner-cleared
  restore would directly reverse that product decision and the published privacy permission purpose.
- **Risk retained:** a deliberately forged TeleCam-shaped owner-cleared row can win last-capture
  display ownership. Its scope remains FILE_ONLY, so it does not gain capture-family deletion.
- **Exit criterion:** reopen when the owner explicitly reverses the reinstall-restore decision, asks
  for a user-confirmed import flow, or Android exposes authenticated historical media provenance that
  distinguishes this app's pre-uninstall rows from forged null-owner rows.
- **Future policy:** any later implementation remains bound by PMA110 preservation, EN+KO UX,
  privacy/Data Safety consistency, signed Conventional commits, and non-destructive Git rules.

No other finding is deferred.

## Residual warnings

None accepted at planning time. Any configured-gate warning that cannot be fixed cleanly must be
recorded here with original severity/confidence, concrete reason, and a reopening criterion.

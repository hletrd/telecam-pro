# Review-plan-fix cycle 1 implementation plan (new loop, 2026-07-17)

Date: 2026-07-17
Source: `.context/reviews/_aggregate.md` at HEAD `99c778e4b216` + the uncommitted WIP
"TELE finder PIP" diff (6 files, ~79 lines) that this cycle absorbs per the run directive
("review them as part of Prompt 1, commit them fixed as part of Prompt 3 — do not stash or
discard").
Deployment mode: **per-cycle** (deploy to PMA110 at `127.0.0.1:5599` + on-device functional
verification after gates are green).

This plan schedules all 21 aggregate findings (1 Critical, 2 High, 11 Medium, 7 Low). Every
finding is either scheduled below or explicitly deferred in § Deferrals with its original
severity/confidence, citation, reason, and exit criterion. No finding is silently dropped.
Carried-over cycle-10 findings (AGG-6..AGG-9, AGG-15) that were reviewed but never planned are
scheduled here.

Run constraints:

- JDK 21; gates this cycle: `:app:assembleDebug`, `:app:testDebugUnitTest`, `:app:lintDebug`
  (whole repo). Errors block commits; warnings fixed best-effort or recorded as deferrals.
- Every commit is GPG-signed (`-S`), Conventional Commits + gitmoji, no AI co-author trailer,
  `git pull --rebase` before every push, one concern per commit, push after each slice.
- Device work IS in scope this cycle (per-cycle deploy + verification), driven only through
  serial `127.0.0.1:5599`.
- No dependency/toolchain/permission change.

Status: ☐ todo · ◐ in progress · ☑ done · ⏸ deferred

## Phase 1 — Make the TELE finder PIP honest, safe, and user-controlled (the WIP)

- ☑ **P1.1 / AGG-1 (Critical/High) + AGG-14 partial** — Re-scope the finder to what the single
  stream can truthfully show: the FULL current camera frame (the widest available field — wider
  than the main view while GL zoom compensation or punch-in magnifies past the HAL field,
  identical otherwise). Correct ALL false "UNZOOMED wide 3× field" comments
  (`GlPipeline.kt:673-677,690`, `CameraState.kt:226-231`, `CameraScreen.kt:377`,
  `CameraEngine.kt:603`; verify `FlipRenderer.kt:132`) to describe actual behavior and the
  single-stream limitation. File the REAL wide-finder design (second wide stream, or
  HAL-zoom-cap + GL-main-zoom split with AE/metering/video implications) as a new BACKLOG item
  requiring an owner decision + device A/B. Do NOT label the PIP "wide" or "unzoomed" anywhere.
- ☑ **P1.2 / AGG-5 (Medium/High)** — Gate the finder behind a user toggle, default OFF: new
  `ExtraSettings.teleFinder` (or equivalent) threaded `ProSheet` Assist tab → `CameraActions` →
  `CameraViewModel` → `CameraEngine` → `RendererConfig`, persisted via `SettingsStore`
  (synchronous commit path), exactly like `punchIn`. Engine gating becomes
  `toggle && teleconverterMode`. Add SettingsStore round-trip coverage.
- ☑ **P1.3 / AGG-2 (High/Medium)** — Isolate the finder draw: own `runCatching` +
  `try/finally { glDisable(GL_SCISSOR_TEST) }` so a finder-only failure degrades to "no PIP this
  frame" — never a preview-health event, never leaked context scissor state into
  encoder/analysis draws.
- ☑ **P1.4 / AGG-4 (Medium/High)** — Unify the finder geometry:
  (a) extract a pure `finderRect(boxW, boxH, fraction, margin)` seam (repo pattern: `coverScale`)
  used by the GL px path and the Compose dp path; fix the padding-before-fillMaxWidth ~6% border
  shrink; (b) unit-test the seam and GL/Compose agreement; (c) pin the overlay to a
  layout-direction-independent bottom-left anchor (RTL); (d) gate the finder to
  `AspectRatio.W4_3` for v1 (16:9 mask layering/framing conflict — documented in code);
  (e) push `gl.setTeleFinder(false)` at TC-off intent time so the GL PIP cannot outlive the
  Compose border during reconfigure; (f) square/clipped border corners so the border traces the
  GL rect.
- ☑ **P1.5 / AGG-14 (Low/High)** — Extend `RendererConfigStoreTest` with `teleFinder` in both
  snapshot tests; add the new pure-seam tests from P1.4.

## Phase 2 — Zoom pipeline correctness (user-priority TODO #1)

- ☑ **P2.1 / AGG-3 (High/Medium)** — Invalidate the hardware-slider glide on scale remaps:
  `zoomEaseTarget = null` (+ remove ticker callback) in `onModeChange`,
  `onToggleTeleconverter`, and `onLens`, mirroring the documented `zoomPendingRatio` invariant.
  Add/extend a test if a pure seam exists (ease-target invalidation table).
  `ui/CameraViewModel.kt:963-1010,1034-1064,1120-1180`.
- ☑ **P2.2 / AGG-10 (Medium/High)** — Stop the gesture-end wrong-zoom window: ensure the
  boost-flip `startPreview` rebuild carries the exact final zoom (write it into controller
  controls before/with the rebuild) and skip the now-redundant second submit; same single-submit
  treatment at gesture start if achievable without changing the boost contract.
  `camera/CameraEngine.kt:1820-1832`, `camera/CameraController.kt` boost/fast-path region.
- ☑ **P2.3 / AGG-11 (Medium/Medium)** — Make the zoom RMW atomic with packet writers:
  perform `controls = controls.copy(zoomRatio = z)` under the same monitor the
  rollback/normalize writers use. `camera/CameraEngine.kt:1853-1880`.
- ☑ **P2.4 / AGG-17 (Low/Medium)** — Change-gate `onZoomResult` forwarding (only forward a
  halZoom that differs from the last sent). `camera/CameraController.kt` capture callback.

## Phase 3 — Carried-over cycle-10 correctness findings

- ☑ **P3.1 / AGG-6 (Medium/High)** — Standby meter: classify `AudioRecord.read` results via the
  existing `AudioReadPolicy` seam — zero = retry, negative = terminal (exit + exactly-once
  release; bounded, backed-off recreation only while standby intent still wants a meter). Add an
  Android-free standby-read policy test (positive/zero/all negative constants/disable race/
  bounded retry). `camera/CameraEngine.kt:3155-3200`; `video/AudioReadPolicy.kt`.
- ☑ **P3.2 / AGG-7 (Medium/High)** — `CaptureOutputTracker.record`: never assign REVIEW to a
  capture `trimCaptures()` just evicted — recheck membership post-trim; fall back to the newest
  retained capture. Add the exact 5-step regression from the cycle-10 report plus
  pin/delete-scope consistency assertions. `ui/CaptureOutputTracker.kt:84-108`.
- ☑ **P3.3 / AGG-8 (Medium/High)** — Lens page truth: 3× caption conditional on TELE state
  (`70 mm` off / `70 mm + TC = 300 mm` on); fix the stale lens-bundling comments/KDoc
  (`LensChoice`, `CameraActions`, ProSheet); pure caption test. `ui/controls/ProSheet.kt:769-798`.
- ☑ **P3.4 / AGG-9 (Medium/High)** — GlPipeline preview draw/swap failure branch: contain a
  failed `clearPreviewOutput` with the same policy as the acquisition branch (encoder active →
  fail encoder owner; else terminal EGL reset), preserving the original failure as primary
  context; seam test for the disposition policy where practical. `gl/GlPipeline.kt`
  preview-failure branch.
- ☑ **P3.5 / AGG-15 (Low/High)** — Per-tab `ScrollState` (or reset-to-top on tab switch) in the
  settings sheet. `ui/controls/ProSheet.kt:207-233`.

## Phase 4 — Capture/recording performance with correctness edges

- ☑ **P4.1 / AGG-12 (Medium/High)** — `StillSnapshot.packYuv420ToNv21`: row-wise
  `System.arraycopy` fast paths (pixelStride-1 planes; NV21-shaped interleaved chroma), generic
  fallback retained, fix the false "cheap memcpy" comment, extend `StillSnapshotYuvTest` with
  fast-path shapes (incl. rowStride > width). `capture/StillSnapshot.kt:87-98`.
- ☑ **P4.2 / AGG-13 (Medium/High)** — Move REC admission off the main thread onto the recorder
  executor while preserving: the synchronous ownership check, mic-handoff ordering, accepted
  controller/session snapshot + atomic publication recheck, and stop-behind-start ordering.
  Publish `isRecordingStarting` optimistically. If the ownership restructure proves unsafe to
  land this cycle, record a deferral per the rules instead of landing a half-measure.
  `camera/CameraEngine.kt:2369-2527`; `ui/CameraViewModel.kt:1485`.

## Phase 5 — Storage/security/docs hygiene

- ☑ **P5.1 / AGG-16 (Low/Medium)** — Add the explicit `OWNER_PACKAGE_NAME = packageName`
  predicate to `cleanupOrphanedPending`, mirroring the read path.
  `storage/MediaStoreWriter.kt:220-257`.
- ☑ **P5.2 / AGG-18 (Low/High)** — Fix `docs/BACKLOG.md` "final cycle" wording; add the new
  wide-finder design item (from P1.1); record this cycle's durable deferrals there.
- ☑ **P5.3** — Sync `CLAUDE.md`/`docs/ARCHITECTURE.md` with the landed behavior: the finder's
  honest contract + toggle, ease-target invalidation invariant, standby-read policy, REC
  admission threading, tracker review-membership invariant. Comments stay dense per convention.

## Phase 6 — Gates, deploy, on-device verification

- ◐ **P6.1** — After each slice: focused JVM suites. Before deploy: full
  `:app:assembleDebug :app:testDebugUnitTest :app:lintDebug` green on the whole repo.
- ◐ **P6.2** — Run DEPLOY_CMD exactly (`adb -s 127.0.0.1:5599 install -r ... && am start ...`).
- ◐ **P6.3 / includes AGG-21 (Medium/Medium)** — On-device verification (with the serial): wake,
  launch, real preview screencap; pinch zoom in/out; lens/mode switching; TELE toggle + finder
  toggle behavior (default OFF; when ON: border/content alignment, bottom-left chrome overlap
  check per AGG-21); still capture; logcat scan for crashes/ANRs/camera errors.

## Deferrals (strict records)

- ⏸ **AGG-19 (Low/Medium, needs-manual-validation)** — `FINDER_MIN_ZOOM = 1.15` likely too low
  for the finder's purpose. `camera/CameraState.kt:232` (`FINDER_MIN_ZOOM`). Reason: threshold
  tuning is a product/on-device judgment that depends on the AGG-1 re-scope landing first and a
  deliberate framing session with the physical converter mounted; the toggle (P1.2, default OFF)
  removes any forced exposure. Not a correctness/security/data-loss item, so deferral is
  permitted by the repo's own pattern for device-judgment items (docs/BACKLOG.md "Residual Field
  Checks … do not require a code or metadata change unless the result exposes a defect").
  Exit criterion: a device session with the converter mounted picks a threshold (or a
  zoomComp-based gate) and updates the constant + test.
- ⏸ **AGG-20 (Low/Medium, needs-manual-validation)** — App-side AE ticks pay full
  repeating-request rebuilds; post-gesture re-center emits a submit train.
  `ui/CameraViewModel.kt:1688-1719`, `camera/AutoExposure.kt:111-134`,
  `camera/CameraController.kt:806-820`. Reason: the fix (sensor-keys-only fast path) changes the
  AE request path on a HAL with documented request-swap fragility, and the visible benefit needs
  on-device measurement to justify that risk. This matches the repo's existing deferral class for
  perf items needing device measurement: docs/BACKLOG.md "D-5..D-9 perf items needing device
  measurement" (durable record, § Deferred from review-plan-fix cycle 2). Severity preserved:
  Low/Medium. Exit criterion: an on-device measurement session that quantifies the post-gesture
  photo-P stall train; if visible, implement an `updateSensorValues` fast path with the same
  cached-builder pattern as zoom.
- Existing durable items keep their recorded exit criteria: BACKLOG D-1..D-18 (incl. D-16 OPPO
  Maven credential = aggregate S1), residual field checks, R8/CameraUnit/Play operations.
- Deferred work remains bound by repo policy when picked up: GPG-signed commits, Conventional
  Commits + gitmoji, no co-author trailer, pull-rebase before push, latest-stable toolchain,
  device verification before claiming camera/GL fixes done.

## Aggregate coverage map (scheduled exactly once)

AGG-1→P1.1, AGG-2→P1.3, AGG-3→P2.1, AGG-4→P1.4, AGG-5→P1.2, AGG-6→P3.1, AGG-7→P3.2, AGG-8→P3.3,
AGG-9→P3.4, AGG-10→P2.2, AGG-11→P2.3, AGG-12→P4.1, AGG-13→P4.2, AGG-14→P1.5, AGG-15→P3.5,
AGG-16→P5.1, AGG-17→P2.4, AGG-18→P5.2, AGG-19→deferred, AGG-20→deferred, AGG-21→P6.3.

## Plan archiving

Per repo convention (see 2026-07-16 cycle-9 plan § Deferral): completed dated plans under
`docs/plans/` ARE the durable archive and are never moved or deleted. All prior plans
(2026-07-02 … 2026-07-16 cycle 9) are fully implemented and stand as archived history; no file
move is safe or needed. The interrupted "cycle 10" review had no plan; its findings are absorbed
above (AGG-6..9, AGG-15) so no orphan plan remains.

## Progress

- Prompt 1: complete — 13/13 reviewer reports + `_aggregate.md`; baseline gates green with the
  WIP (assembleDebug PASS, 479/479 tests, lintDebug clean).
- Prompt 2: complete — this plan; 19 findings scheduled, 2 deferred with records above; no
  implementation started before this plan was written.
- Prompt 3: implementation complete — all 19 scheduled items landed in 13 signed source
  slices (fc5b1a3..68efd71) plus this docs slice; every intermediate commit tree compiles and
  the post-implementation working tree was verified byte-identical to the gate-verified state
  after hunk-split staging. P4.2 landed in full (admission on the serial recorder executor,
  synchronous in-flight gate, stop-during-start latch). Focused suites ran per slice; a full
  assembleDebug + 494/494 tests (65 suites) passed on the complete tree before splitting.
  Final whole-repo gates, per-cycle deploy to 127.0.0.1:5599, and on-device verification
  (P6.1-P6.3) are recorded below once executed.

# Review-Plan-Fix Cycle 1 Plan — 2026-07-10

> **Archived plan.** All scheduled items landed 2026-07-10 (or were re-scoped to the recorded
> deferrals in place) — see the **Progress log** at the bottom for the authoritative per-phase
> completion state with commit hashes. The `☐` checkbox marks below are the historical "was queued"
> ledger, NOT open work. Active work: `docs/plans/2026-07-10-rpf-cycle2.md`; active release backlog:
> `docs/BACKLOG.md`.

Derived from `.context/reviews/_aggregate.md` (13-agent fan-out; gitignored raw snapshots). Rule:
every aggregate finding is **scheduled** below or **deferred with recorded reason, original severity,
and an exit criterion** — nothing silently dropped. Work follows repo policy (CLAUDE.md + global git
rules): GPG-signed `-S` commits, Conventional Commits + gitmoji, no `Co-Authored-By`, fine-grained
one-concern commits, `git pull --rebase` before every push.

Gate objective (all three must stay green after every commit):
`:app:assembleDebug` · `:app:testDebugUnitTest` · `:app:lintDebug` (baseline: all green, 63/63 tests,
zero lint issues — qa-adversary verified with fresh `--rerun-tasks` runs).

**Device constraint:** the PMA110 is unreachable this session. Per CLAUDE.md ("Verify on device
before claiming a camera/GL/orientation fix is done… If the device is unreachable, say so and mark
the item pending"), this cycle schedules only statically-verifiable changes; any scheduled item whose
*visual/pixel/timing* effect needs hardware carries a `[device-verify pending]` tag, and changes that
would *alter* device-verified behavior without a way to re-verify are deferred, not half-shipped.
Note: any code change invalidates the recorded release AAB hash — `docs/play-console-submit.md` must
be refreshed after the next full release gate on hardware (tracked there; not a this-cycle item).

**Plan archive status:** `docs/plans/2026-07-02-review-fixes.md` and
`docs/plans/2026-07-03-cycle1-fixes.md` already carry the "Archived plan" header and are fully
resolved (every item ☑ or ⏸ with exit criteria) — nothing further to archive. This file becomes the
active plan and gets archived the same way when done.

Status: ☐ todo · ◐ in progress · ☑ done (gates green) · ⏸ deferred
**→ See the Progress log at the bottom for the authoritative per-phase completion state (all
scheduled items landed 2026-07-10 except the two explicitly re-scoped to deferrals in place).**

---

## Phase 1 — Crash-class guards (aggregate A1-A7; no happy-path behavior change)

- ☐ **P1.1 / A1** (Critical/High, debugger) — `CameraController.capturePhoto()` `CameraController.kt:613-670`:
  wrap the request-build + `session.capture()` block in `runCatching`; on failure invoke the pending
  callback's `onError` and clear the `pending` slot so the shutter never wedges ("Capture already in
  progress" forever). Mirrors the guards every sibling path already has.
- ☐ **P1.2 / A2** (High/High, debugger) — `VideoRecorder.kt:114-140,316-329`: release the locally
  created video/audio `MediaCodec` (and input surface) when configure/createInputSurface/start throws
  before field assignment; rethrow after cleanup.
- ☐ **P1.3 / A3** (High/High, debugger) — `MediaReview.kt:341`: guard the
  `setOnPreparedListener { it.start() }` body with `runCatching` (matches every other MediaPlayer
  call in the file); closing review during async prepare must not crash.
- ☐ **P1.4 / A4** (Med-High/Medium, debugger) — `CameraSelector2.kt:47`: wrap
  `manager.cameraIdList` in `runCatching { … }.getOrDefault(emptyArray())` (the codebase's other
  call site is already guarded).
- ☐ **P1.5 / A5** (Med-High/Med-High, debugger; prior BUG-2) — `CaptureCapabilities.kt:113-115`:
  guard the `getOrElse` fallback's second `getCameraCharacteristics` call; on failure return the
  defensive-default caps instead of crashing.
- ☐ **P1.6 / A6** (Medium/Medium, debugger+tracer) — `VideoRecorder.kt:165-213`: when the drain
  thread is still alive after the stop-timeout, mark failure but skip `stop()`/`release()` on the
  wedged codec (deliberate leak on a rare error path beats a native-race SIGSEGV); comment why.

## Phase 2 — Correctness & data-integrity (A9-A26 subset)

- ☐ **P2.1 / A9** (Medium/High, tracer) — stamp JPEG EXIF from the per-shot `TotalCaptureResult`
  available in `onPhoto` instead of live `controller.lastIso/lastExposureNs`
  (`CameraEngine.kt:738-787,1030-1051`).
- ☐ **P2.2 / A10** (Medium, qa-adversary) — on settings restore, revalidate `videoFrameRate` via
  `VideoFrameRate.availableFor(...)` and `videoCodec` via `EncoderCaps.availableCodecs()` before
  pushing to the engine (`CameraViewModel.applyLoaded`), closing the FPS_120-SIGABRT / APV landmines.
- ☐ **P2.3 / A11** (Low-Med, qa-adversary) — `ColorProfiles.apvFormat()`: set `KEY_COLOR_TRANSFER`
  explicitly (CLAUDE.md invariant: "Tag a transfer explicitly, always").
- ☐ **P2.4 / A12 + D6** (Medium/High, tracer+critic+perf) — persistence: add a trailing debounced
  `scheduleSettingsSave()` (~500 ms, synchronous commit at fire time) and call it from
  `updateControls` and the video-settings setters; replace the per-drag-frame immediate save in
  `onAudioGain` with the same scheduler. Fixes the Recents-kill loss window AND the ~60-key sync
  commit per drag frame in one mechanism.
- ☐ **P2.5 / A13** (Medium/Medium, tracer+code-reviewer) — mic-ownership handoff: guard
  `setStandbyAudioMonitor` against a recorder whose async teardown is still in flight (teardown
  in-progress flag cleared when the stop future completes), so a fast pause→resume cannot start a
  second concurrent `AudioRecord` (`CameraEngine.kt:813-818,896-913,1064`).
- ☐ **P2.6 / A14** (Medium/High, tracer) — zoom writers: manual zoom input (pinch/dial/slider)
  cancels the hardware-slide glide target so the ~30 Hz ticker stops dragging zoom back to a stale
  target (`CameraViewModel.kt:555-585`).
- ☐ **P2.7 / A15** (Low-Med/Medium, tracer) — `MediaReview.kt:314-361`: react to `uri` changes in
  the video player `AndroidView` (release + recreate the `MediaPlayer` in `update` when the uri
  differs). `[device-verify pending: playback]`
- ☐ **P2.8 / A16 + G-test** (High/High, test-engineer) — `ProControls.formatShutterSpeed`: fix the
  [0.667 s, 1.0 s) → `"1/1s"` display bug; add boundary unit tests.
- ☐ **P2.9 / A17 + G-test** (Med-High/High, test-engineer) — `ManualControls.kelvinTintToRggbGains`:
  clamp output gains to a sane documented bound; unit tests for 2000 K/extremes.
  `[device-verify pending: WB rendering at extreme kelvin]`
- ☐ **P2.10 / A19** (Medium, qa-adversary) — wire `controls.jpegQuality` into
  `HeifCapture.writeHeif(quality = …)` so the quality slider governs the DEFAULT (HEIF) output too;
  update the slider tooltip ("Compression quality for HEIF/JPEG photos").
- ☐ **P2.11 / A20** (Medium/High, critic) — review Delete removes ALL outputs of the last capture
  (track the DNG sibling URI alongside `lastMediaUri`), not just the HEIF.
- ☐ **P2.12 / A21** (Low/High, critic) — tighten `cleanupOrphanedPending` LIKE to `'DCIM/X9Tele/%'`.
- ☐ **P2.13 / A22** (Low/High, architect) — `pause()` stops an active timelapse (a backgrounded
  timelapse only no-ops and resumes with silent gaps today).
- ☐ **P2.14 / A23** (Low/Medium, code-reviewer) — `applyLoaded`: sync `controls.fps` from the
  restored `videoFrameRate` (mirror `onVideoFrameRate`).
- ☐ **P2.15 / A24** (Low/Medium, code-reviewer) — `stopRecording`: re-apply the mode-appropriate
  preview GL transfer that `startRecording` overwrote (AVC → null loses the LOG preview cue).
  `[device-verify pending: preview look]`
- ☐ **P2.16 / A25** (Low/Medium, code-reviewer) — set `starting = false` once `started = true`.
- ☐ **P2.17 / A26** (Low/High, code-reviewer) — gate `programShouldRunAppSide` on
  `exposureMode == PROGRAM` (all consumers already gate; makes the flag mean what its KDoc says).
  Confirm `refreshProgramAppSide` is invoked on exposure-mode changes (CLAUDE.md says it is).
- ⏸ **P2.18 / A18 (dormant half)** (Medium/High-math, test-engineer) — RE-SCOPED to deferred D-22:
  the O-Log2 inverse branch threshold (`Shaders.kt:108-114`, dormant uTransfer=3). Reason: the
  inverse must be the exact inverse of whatever the FORWARD curve resolution decides (deferred D-7,
  device-LUT-gated); fixing the dormant threshold against a forward curve that may itself change
  would be churn. Both halves resolve together. Severity Medium preserved. Exit: same as D-7 — the
  white-paper numeric analysis + device gray-ramp LUT comparison, then fix forward boundary and
  inverse threshold in one change.

## Phase 3 — Release accuracy / packaging / security hardening (B1-B10)

- ☐ **P3.1 / B1** (High, critic+security) — move `oplus.ocs.camera`/`oplus.ocs.base` to
  `debugImplementation`; split `OcsProbe` behind a source-set pair (`src/debug` real,
  `src/release` no-op launcher) so release neither links nor references the OEM SDK. Verify BOTH
  `assembleDebug` and `assembleRelease` compile.
- ☐ **P3.2 / B3** (Medium, security) — add `tools:node="remove"` for `INTERNET` and
  `ACCESS_NETWORK_STATE` in the manifest; verify the processed manifest still lists only
  CAMERA/RECORD_AUDIO.
- ☐ **P3.3 / B4** (Medium, qa-adversary) — fix the manifest comment's wrong package
  (`com.hletrd.telecampro` → `me.hletrd.telecampro`).
- ☐ **P3.4 / B6** (Low/High, security+qa) — gate the enumerated release-path informational logs
  (notably the ~1/s 3A line `CameraController.kt:504` and the `VideoRecorder` info logs) behind
  `BuildConfig.DEBUG`; keep genuine warning/error logs.
- ☐ **P3.5 / B5** (High, document-specialist) — switch heifwriter `1.2.0-alpha01` → stable `1.1.0`
  (stable exists; the "no stable 1.1.0 exists" claim is false). If the build/API check fails, revert
  and instead correct CLAUDE.md with the real reason the alpha is required. Either way update the
  CLAUDE.md toolchain row. Try removing the `@SuppressLint("RestrictedApi")` in `HeifCapture.kt`.
  `[device-verify pending: one HEIF capture on hardware before release]`
- ☐ **P3.6 / B2 + B10** (High, critic) — rewrite the Data-Safety/listing wording: lead with the
  enforceable "declares no INTERNET permission" fact; drop the absolute "no network-capable SDKs"
  claim (true again for release after P3.1, but keep the defensible phrasing); soften the Sound
  Focus/Stage listing line to match its unverified-acoustics status. Align `docs/play-data-safety.md`,
  `docs/play-store-listing.md`, `PRIVACY.md`.
- ☐ **P3.7 / B8** (Major/device-gated, critic) — extract a pure
  `RotationMath.videoOrientationHint(deviceOrientation)` helper used by `startRecording` →
  `VideoRecorder`, with unit tests pinning the current normalization. The on-device both-landscapes
  check remains open in `docs/BACKLOG.md` Residual Field Checks (exit criterion for the sign).
- ☐ **P3.8 / B9 (docs half)** (Major claim, critic) — correct "Open Gate = full 4:3 sensor" in
  README/ARCHITECTURE to the device-verified reality (4:3 aspect recording, 2560×1920 verified on
  the tele). The code-side cap decision is deferred — see Deferred D-6.

## Phase 4 — Documentation / comment drift sweep (C1-C13; all Confirmed)

- ☐ **P4.1 / C1** — `VideoRecorder` class KDoc: replace the false "NO orientation hint" claim with
  the correct two-part scheme (GL bakes afocal 180°; muxer hint carries device tilt).
- ☐ **P4.2 / C2** — purge stale gyro-EIS-is-active comments: `CameraController.kt:410-411`,
  `CameraEngine.kt:505`, `CameraState.kt:189-191` (LensChoice), `CaptureCapabilities.kt:85` (GYRO),
  `GyroEis.kt:11-22` header, `CameraEngine.setTransfer` docblock opener.
- ☐ **P4.3 / C3** — `ProControls.kt:540`: invert the wrong "LOG drives the native HAL log" comment
  to the settled GL O-Log2 fact.
- ☐ **P4.4 / C4** — ARCHITECTURE `DCIM/Pictures` → `DCIM/X9Tele` (2 sites).
- ☐ **P4.5 / C5** — CLAUDE.md "~12 Hz" → 40 ms/25 Hz; same for `CameraViewModel.kt:73`.
- ☐ **P4.6 / C6** — reconcile the bitrate story (code target ≈99.4 Mbps @4K30 MAX, 120 Mbps clamp;
  "134 Mbps" reframed as measured VBR overshoot or removed) across ARCHITECTURE/CameraState
  KDoc/README.
- ☐ **P4.7 / C7** — fix the "JPEG straight-through / crop is HEIF-only" claims in
  `CameraState.PhotoFormats` KDoc + ARCHITECTURE to match `saveJpegAsync` reality.
- ☐ **P4.8 / C8** — align `VideoStabMode` label story (docs say "OIS Std/Enhanced"; code ships
  "Standard"/"Active") — update the docs to the shipped labels.
- ☐ **P4.9 / C9-C13** — remaining comment fixes: `MainActivity.kt:183-188` stale key-event block;
  `FlipRenderer.kt:14-16` KDoc precision; `ManualDials.kt:73` five→six dials; ARCHITECTURE module-map
  omissions (OcsProbe, AutoExposure, AudioInputInspector, ui/review, standby-audio thread, tickers);
  `ProControls.kt:552` "10-bit" phrasing; `play-store-listing.md:98` icon wording.

## Phase 5 — Dead code & duplication (E6 + A-EV)

- ☐ **P5.1** — remove dead code (4 agents): `MainActivity.CAMERA_BUTTon_KEYS`,
  `VendorTagInspector.valueString`, `ProControls.shutterNsToSlider`/`sliderToShutterNs`,
  `MediaStoreWriter.savePhotoBytes`, `CameraActions.onFps` (+ ViewModel impl + preview stub).
- ☐ **P5.2** — consolidate the duplicated `fnSlotValue`/`autoShutterText`/`autoIsoText`/`next*`
  cyclers from `ProSheet.kt` + `ManualDials.kt` into one shared internal location; while there, fix
  the EV readout to use `caps.evStep` (code-reviewer Medium + feature-dev-cr — the drift this
  duplication caused).
- ☐ **P5.3** — consolidate `TELECONVERTER_MAGNIFICATION` (300/70) to a single shared constant
  (4 sites: CameraEngine, CameraController, CameraViewModel literal, Overlays literal).

## Phase 6 — Performance quick wins (statically-safe subset of D)

- ☐ **P6.1 / D5** (Medium/High, perf) — stop registering the gyroscope in `GyroEis` (its integration
  output feeds the removed GL-EIS path and has zero consumers; gravity sensor keeps roll/orientation).
  Verify no consumer by grep before removing.
- ☐ **P6.2 / D9** (Medium/Medium, perf) — waveform overlay: batch the up-to-8,192 `drawCircle` calls
  with `drawPoints` (same geometry). `[device-verify pending: visual parity]`
- ☐ **P6.3 / D10-15 subset** (Low, perf) — gate `gl.setAeMetering` posts on value change
  (`CameraViewModel.kt:972`); skip the unity-gain PCM rewrite loop in
  `VideoRecorder.applyGainAndLevel` when gain==1; recreate the analysis executor in
  `GlPipeline.start()` so the pipeline isn't single-use; move `onDeleteLastMedia`'s
  `contentResolver.delete` off the main thread.
- ☐ **P6.4 / D3 partial + A13's join** — move `startRecording`'s standby-meter join off the
  main-thread critical path where trivially safe (bounded wait relocation only; full startRecording
  offload is Deferred D-3).

## Phase 7 — UX / accessibility (statically-safe subset of F)

- ☐ **P7.1 / F2** (High, designer) — add `Modifier.semantics`/`progressSemantics` (role, value,
  range, set-value action) to `RulerSlider` and `CameraSlider` so every dial/slider is visible to
  TalkBack. Additive; no visual change.
- ◐ **P7.2 / F3+F5** (High/Med-High, designer) — minimum touch targets. DONE: `TeleChip` hit area
  36→48 dp (outer click box; 36 dp pill visual unchanged). RE-SCOPED to deferred D-20: the
  `DialChip`/`MemoryRecallStrip`/`MiniTextButton` bumps — those sit inside dense dial/strip layouts
  where a 48 dp reservation reflows neighbors, which cannot be visually judged off-device
  (CLAUDE.md device rule). Severity Med-High preserved. Exit: next device UX pass with screenshots.
- ☐ **P7.3 / F6 partial** (Med-High, designer) — gate `onCapturePhoto` on `cameraReady` exactly the
  way `startRecording` already is (consistency; prevents capture attempts on a dead session).
- ☐ **P7.4 / F1** (High, designer; UX_POLICY-mandated) — surface AE/AWB/AF lock states as compact
  OSD glyphs ("AEL"/"AWL"/"AFL") in the existing OSD row — policy says important states belong in the
  OSD. `[device-verify pending: visuals]`
- ⏸ **P7.5 / F4** (High, designer) — RE-SCOPED to deferred D-21: surface `CONTROL_AF_STATE`
  through controller→engine→state and tint the tap-AF reticle on FOCUSED/NOT_FOCUSED. Reason: the
  useful thresholds/timing of AF-state transitions on this HAL (PASSIVE_SCAN chatter vs FOCUSED)
  are only observable on hardware; shipping an indicator tuned blind risks a flickering reticle —
  the exact quiet-viewfinder violation UX policy forbids. Severity High preserved. Exit: implement
  during the next device session with live AF-state observation.

## Phase 8 — Tests (G; after the refactors above)

- ☐ **P8.1 / G1+G2** (Critical/High coverage) — refactor `AutoExposure.driveProgram`/`driveIso`/
  `driveShutterNs` signatures from `Range<…>` to plain `Long/Int` bounds (the repo's own established
  pattern; `Range` throws "not mocked" in unit tests), update callers, and add tests: ISO-clamp
  handoff, handheld ceiling, 1/10 s dark ceiling, brightness-neutral shutter slide.
- ☐ **P8.2 / G3** — extract `programShouldRunAppSide`/`preferredProgramShutterNs` as pure top-level
  functions; test the P-mode HAL-AE-vs-app-side matrix (photo/video × flash modes).
- ☐ **P8.3 / G4** — extract the session fallback-ladder attempt→(useRaw, useHlg, previewOnly)
  booleans (`CameraController.kt:283-289`) as a pure function; test the ladder ordering.
- ☐ **P8.4 / G6-G12 subset** — tests for `videoStabControlMode` (testable today), `centerCrop`
  rect math (extract from CameraEngine), `applyGainAndLevel` (with P6.3's unity-gain change),
  `formatShutterSpeed` boundaries (P2.8), `kelvinTintToRggbGains` clamps (P2.9),
  `videoOrientationHint` (P3.7).

---

## Deferred — recorded, NOT implemented this cycle

Severities preserved from the aggregate; none are downgraded. Per the deferred-fix rules,
correctness items below are deferred ONLY under the repo's own device-verification rule
(CLAUDE.md "Working conventions": *"Verify on device before claiming a camera/GL/orientation fix is
done. Compilation ≠ correct pixels… If the device is unreachable, say so and mark the item
pending."*) — i.e., the fix cannot be validated (or even safely landed) without the PMA110, which is
unreachable this session. Structural refactors are additionally deferred under CLAUDE.md's hot-path
caution (*"Hot paths (edited most, touch carefully)"*) and the architect's own ship-soon trade-off.

- ⏸ **D-1 / A8** [Med-High/High(invocation) Med(crash)] — serialize all `openCamera` paths onto
  `setupExecutor` (3-thread open today; `resume()` opens on main against documented policy;
  `CameraEngine.kt:156,452,551,921`). Reason: hot-path lifecycle threading change in the exact zone
  of historical keyguard/disconnect crashes; requires on-device launch/lens-switch/resume
  re-verification (repo rule quoted above). Exit: implement + device session re-runs cold start,
  keyguard launch, rapid lens switch, background/foreground without `CAMERA_DISCONNECTED`/HAL error.
- ⏸ **D-2 / A7** [Medium/Medium, prior BUG-5 carried again] — stale-`Surface` bind in the async
  startup window (`CameraEngine.kt:114-161`). Reason: needs a device repro to prove the fix
  (surface-recreate race); statically the closure rewrite is easy but unverifiable. Exit:
  device-repro (rapid rotate/fold of the TextureView during cold start) passes with the fix.
- ⏸ **D-3 / D3+D4 (perf)** [Medium/High] — move `startRecording` (MediaStore insert + 2× codec
  configure + AudioRecord init) and `pause()`'s bounded `controller.close()` join off the main
  thread. Reason: reorders lifecycle-critical operations documented as race-prone (CLAUDE.md
  lifecycle-races section); needs on-device ANR/latency measurement to prove benefit and no
  regression. Exit: device session shows record-start latency unchanged or better and no new
  lifecycle race under rapid start/stop + backgrounding.
- ⏸ **D-4 / D1 (perf) + code-reviewer GL readback** [High/High mechanism; works on device today] —
  full-res `glReadPixels` after `swapBuffers`, before the encoder draw (`GlPipeline.kt:285-363`).
  Reason: the current sequence is device-verified working (scopes + app-side AE ship on it); any
  reorder/FBO/downscale change alters GL timing on the hottest path and is exactly what CLAUDE.md
  says must be re-verified on hardware. Exit: downscaled-FBO (or pre-swap) readback implemented +
  device profile shows no encoder jitter and scopes/AE still meter correctly.
- ⏸ **D-5 / D2 (perf)** [High/High] — monolithic unstable `CameraUiState` → whole-screen
  recomposition at 6-30 Hz. Reason: structural Compose state refactor across the two biggest UI
  files days before a release-ready tag; regression surface is the entire UI; needs device frame
  profiling to validate. Exit: state split/stability annotations land with device frame-time
  comparison and a full manual UI pass.
- ⏸ **D-6 / B9 (code half)** [Major/Medium] — Open-Gate 3840-px cap vs "full sensor" (docs corrected
  this cycle in P3.8). Reason: raising the cap changes the recorded format on device-verified
  hardware paths (HAL stream-combination risk documented throughout CLAUDE.md); which 4:3 sizes the
  tele actually advertises is unknowable off-device. Exit: device session enumerates
  `StreamConfigurationMap` 4:3 video sizes >3840 and verifies a recording at the larger size, or
  confirms 2560×1920 is the tele's max and closes the item as docs-only.
- ⏸ **D-7 / A18 (forward half)** [Medium/High-math] — O-Log2 FORWARD curve non-monotonicity
  (~0.0006 in R at the 0.006 segment boundary, `Shaders.kt:73-86`). Reason: the constants are the
  official OPPO white-paper values and the encoded output is device-LUT-verified per CLAUDE.md
  ("LUT-accurate"); changing the shipping OETF without an on-device LUT comparison risks breaking
  documented color behavior for a sub-milli artifact. Exit: numeric analysis reproduced against the
  white paper + device gray-ramp recording compared against the official LUT before/after.
- ⏸ **D-8 / E1** [Medium/High] — state triplication guardrail (`engine.applyExtras(ExtraSettings)`
  single push-point). Reason: structural refactor of the restore path in a hot file; P2.2/P2.14
  already touch `applyLoaded` minimally this cycle — stacking a redesign on top multiplies risk.
  Exit: implement before the NEXT persisted setting is added (architect's stated trigger), with the
  restore matrix re-tested on device.
- ⏸ **D-9 / E2-E4** [Medium] — CameraEngine god-object split (`StillWriter` extraction), AE-loop
  relocation out of the ViewModel, controller interface seam for CameraUnit. Reason: architect's own
  recommendation is to defer structural extraction until the CameraUnit backlog work begins ("this
  ships soon"); pure maintainability, no correctness deficit. Exit: schedule as the prerequisite
  commit(s) of BACKLOG item "Authenticated CameraUnit path" / the HEIF-EXIF feature.
- ⏸ **D-10 / E5** [Low] — stringly-typed `onStatus` → sealed `EngineEvent`. Reason: architect rates
  it fine for v1; UI has no consumer that needs error classes yet. Exit: first UX requirement that
  branches on error class (e.g. Retry affordance from F-deferred work).
- ⏸ **D-11 / F7** [Medium] — counter-rotate `ExposureMeter`/half-press label/`TimerCountdown`.
  Reason: the rotate-vs-fixed split is device-verified UX (CLAUDE.md documents the 2026-07-08 sign
  verification and the wide-element layout constraint); changing which elements rotate needs on-device
  visual confirmation in all four orientations. Exit: device session verifies the three elements in
  both landscapes.
- ⏸ **D-12 / F8-F10, F11** [Medium/Low] — mode-label scrim, custom-styled permission/delete dialogs,
  cold-launch feedback, long-press accessibility labels. Reason: visual-design changes needing
  device screenshots to evaluate against the Sony-reference UX policy; not correctness. Exit: next
  device UX pass; implement with screenshot evidence.
- ⏸ **D-13 / D10 (frame coalescing), D12 (cached AE builder), D14 remainder** [Low] — GL hot-path
  micro-optimizations. Reason: perf-reviewer itself marks D12 an accepted design; coalescing alters
  frame delivery on the device-verified preview path. Exit: device frame-profiling session.
- ⏸ **D-14 / D7+D8 (perf)** [Medium] — DNG write off the camera thread (`DngCreator.writeByteBuffer`
  + copy) and single-decode HEIF+JPEG pipeline. Reason: capture-path restructuring whose output
  integrity (valid DNG/HEIF files) is only provable by pulling real captures per CLAUDE.md's
  output-file verification convention. Exit: device session pulls and validates DNG+HEIF after the
  refactor (exiftool/dngvalidate + visual).
- ⏸ **D-15 / B7** [Low/High] — committed public read-only OPPO Maven credential
  (`settings.gradle.kts:26-27`). Reason: documented-public, read-only, build-time-only credential
  (security reviewer: "no rotation needed"); moving it to a property with a committed default gains
  nothing today and risks breaking clean-checkout builds. NOT a secret-exposure finding. Exit: fold
  into the CameraUnit auth work (BACKLOG #4) when real (non-public) credentials appear — move both
  to `gradle.properties`/env at that point.
- ⏸ **D-16 / B11** [External/Needs-manual] — publish + verify `github.com/hletrd/telecam-pro` and
  the GitHub Pages privacy-policy URL before Play submission. Reason: external operator action, not
  repository code; already the operator's step 2 in `docs/play-console-submit.md`. Exit: URLs
  resolve publicly; listing links verified in the console.
- ⏸ **D-17 / G5, G-SettingsStore, G-Range-blocked remainder** [Medium] — glyph-rotation math tests
  (needs a small extraction), SettingsStore round-trip tests (needs instrumentation or a
  SharedPreferences fake — new test infra), remaining `Range`-blocked helpers not refactored in
  P8.1. Reason: test-infra scope beyond this cycle's refactor budget; the highest-value targets
  (P8.1-P8.4) land first. Exit: next cycle's test batch; infra decision (fake vs instrumentation)
  recorded in the plan that schedules it.
- ⏸ **D-18 / critic m6 (APV dead code removal)** [Low/High] — the plumbed-but-unreachable APV path.
  Reason: documented as intentionally kept for diagnostics (CLAUDE.md: "defined but intentionally
  EXCLUDED"); P2.3 fixes its one dangerous property (missing transfer tag) without deleting the
  diagnostic surface. Exit: revisit when MediaMuxer gains APV-in-MP4 support or the diagnostic value
  lapses.
- ⏸ **D-19 / critic m9 (persist sub-4K video resolution), m11 (keep-screen-on toggle)** [Low] —
  product-scope additions, both already tracked in BACKLOG "Deferred Beyond v1"/optional product
  work. Exit: product decision post-v1.
- ⏸ **D-20 / F5 remainder (re-scoped from P7.2)** [Med-High/designer] — `DialChip` (~30 dp),
  `MemoryRecallStrip` (~28 dp), `MiniTextButton` (~23 dp) touch-target bumps. Reason: they sit
  inside dense dial/strip layouts where a 48 dp reservation reflows neighbors — unjudgeable
  off-device (CLAUDE.md device rule). The TeleChip half of P7.2 shipped. Exit: next device UX pass
  with screenshots.
- ⏸ **D-21 / F4 (re-scoped from P7.5)** [High/designer] — surface `CONTROL_AF_STATE` through
  controller→engine→state and tint the tap-AF reticle on FOCUSED/NOT_FOCUSED. Reason: AF-state
  transition timing on this HAL (PASSIVE_SCAN chatter vs FOCUSED) is only observable on hardware;
  an indicator tuned blind risks a flickering reticle — the quiet-viewfinder violation UX policy
  forbids. Exit: implement during a device session with live AF-state observation.
- ⏸ **D-22 / A18 dormant half (re-scoped from P2.18)** [Medium/test-engineer] — the O-Log2 inverse
  branch threshold (`Shaders.kt`, dormant uTransfer=3). Reason: the inverse must exactly invert
  whatever the FORWARD curve resolution (D-7) decides; fixing it first would be churn. Exit: same
  as D-7 — white-paper numeric analysis + device gray-ramp LUT comparison, then fix forward
  boundary and inverse threshold in one change.

---

## Progress log

**2026-07-10 — cycle 1 implementation complete.** Gates green throughout (assembleDebug +
testDebugUnitTest + lintDebug; both variants compile; merged release manifest re-verified clean).

- ☑ Phase 1 (all): P1.1-P1.6 — commits `42a2ece` (controller/selector/caps guards),
  `b53004c` (VideoRecorder leak/wedge), `bdbff17` (MediaReview).
- ☑ Phase 2: P2.1-P2.17 done — `42a2ece` (engine/VM correctness batch), `96a6bc5` (orphan sweep),
  `b53004c` (APV transfer, orientation-hint helper), `2d95994` (EV step + shutter readout).
  P2.18 re-scoped to deferred D-22 (see above).
- ☑ Phase 3: P3.1 `e1fde5a` (OCS out of release), P3.2+P3.3 `cad573a` (manifest), P3.4 in
  `42a2ece`/`b53004c` (log gating), P3.5 `cb30bd9` (heifwriter stable 1.1.0 — one on-device HEIF
  capture check pending before the next release gate), P3.6+P3.8 docs commit, P3.7 in `b53004c`
  (+ RotationMathTest pin).
- ☑ Phase 4 (all): C1-C13 swept across CLAUDE.md / ARCHITECTURE.md / README / play docs / in-code
  comments (docs commit).
- ☑ Phase 5 (all): dead code removed (`5abd1f0`, `2d95994`, `96a6bc5`); helpers consolidated into
  ControlCycles.kt; TELECONVERTER_MAGNIFICATION single-sourced.
- ☑ Phase 6: P6.1 `d6f3860` (gyro unregistered); P6.2 waveform drawPoints batching; P6.3
  (setAeMetering gate, unity-gain skip, GlPipeline executor recreate, off-main delete); P6.4 covered
  by the teardown-flag rework (the standby join stays, now guarded). [device-verify pending: waveform
  visual parity]
- ◐ Phase 7: P7.1 slider semantics + P7.3 capture gate + P7.4 lock OSD tags + P7.2's TeleChip DONE;
  P7.2 remainder → D-20, P7.5 → D-21 (see re-scope notes above). [device-verify pending: OSD tags,
  TalkBack pass]
- ☑ Phase 8: P8.1-P8.4 — AutoExposure drive tests (incl. driveProgram program line), fallback-ladder
  tests, videoStab resolution tests, WB gain clamp tests, centerCrop tests, audio gain tests,
  P-routing matrix tests, shutter-format tests, muxer-hint tests. Suite grew 63 → 100+ tests, all
  green.

**Residual device checks added by this cycle** (append to the BACKLOG list when this plan archives):
one HEIF capture on heifwriter 1.1.0; waveform overlay visual parity after drawPoints batching;
OSD lock tags + TeleChip layout on the 3168 px screen; TalkBack pass over the new slider semantics.
Note: code changed since the recorded release AAB — `docs/play-console-submit.md`'s hash must be
refreshed after the next full on-device release gate (per BACKLOG policy).

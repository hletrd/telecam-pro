# Review-plan-fix cycle 31 — implementation plan

Date: 2026-08-24
Source: `.context/reviews/_aggregate.md`, cycle 31
Status: complete; all eleven findings fixed, configured gates green, signed commits pushed

## Repository rules applied

- Preserve measured PMA110 behavior, capability-routed Android 13+ behavior, EN/KO presentation,
  quiet-camera UX, media durability, and wrapper-owned release evidence.
- Every cycle-31 finding is scheduled below. Nothing is deferred, silently dropped,
  severity-downgraded, or introduced as new scope under a deferred label.
- Completed plans remain non-destructive archives in `docs/plans/`; no completed plan requires a
  move or deletion.
- Use GPG-signed Conventional Commits with gitmoji, pull with rebase before every push, and do not
  deploy because `DEPLOY_MODE=none`.
- Perform all work in the isolated clean cycle clone. Preserve the shared main worktree and every
  pre-existing CameraEngine/storage/UI/test/tool delta without touching, staging, stashing,
  resetting, restoring, checking out, or terminating its processes.

## P1 — route process retirement to the exact local family owner (AGG31-01)

- [x] Replace stale-closure retirement publication with a bounded process-owned mapping from each
  family to its registered local deletion owner(s), without retaining obsolete Engines unboundedly.
- [x] Ensure a process scan cannot remove a family marker until the exact local bookkeeping can be
  reconciled, and make late/duplicate retirement idempotent.
- [x] Add a deterministic two-Engine overflow schedule proving Engine A's accepted rescan retires
  Engine B's unresolved output and restores B admission exactly once.

## P2 — separate provider-row deletion from marker cleanup (AGG31-02)

- [x] Return a typed known-output deletion result that distinguishes deleted/already absent,
  confirmed survivor, provider uncertainty, and DISCARD-marker cleanup failure.
- [x] Restore review only for an authoritatively present URI; retain cleanup retry for absent rows
  without creating a phantom review owner.
- [x] Cover positive-delete plus marker-remove failure, already-absent plus marker-remove failure,
  confirmed survivor, and unknown-provider outcomes through the ViewModel composition.

## P3 — keep TextureView callbacks off the blocking terminal gate (AGG31-03)

- [x] Record preview Surface/generation identity synchronously, then hand blocking gate admission
  to the serialized setup lane.
- [x] Recheck terminal state, current Surface, preview generation, and exact GL owner before binding;
  keep close-vs-native-acquisition ordering unchanged.
- [x] Add an interleaving regression proving a simulated main-thread callback returns while the
  terminal gate is held and stale queued binds are discarded.

## P4 — restore the exact Partition-A coverage contract (AGG31-04)

- [x] Cover the retired-facade/process fallback, invalid OTHER measured-host fallback,
  ViewModel-dispatcher facade counters, and default process thread factory.
- [x] Use the reviewed residual manifest only for genuinely non-executable lines with explicit
  source-region rationale; do not lower thresholds or loosen drift enforcement.
- [x] Re-run the authoritative coverage task and full host gate.

## P5 — test production-owned optics rollback (AGG31-05)

- [x] Drive a deterministic owned failure through the real `CameraEngine.rollbackOptics` boundary
  instead of pre-restoring Engine state or invoking the callback manually.
- [x] Assert Engine declaration, controller magnification input, UI declaration packet, and
  superseded rollback behavior from the same transition.
- [x] Keep the existing pure reducer regressions as complementary coverage.

## P6 — make callback-drain synchronization deterministic (AGG31-06)

- [x] Replace the fixed sleep in `EngineCallbackSinkTest` with a bounded rendezvous or narrow test
  seam proving the closer reached and is blocked in `closeAndDrain`.
- [x] Prove the drain cannot finish until the admitted callback lease releases, then completes
  exactly once afterward.

## P7 — align device-harness documentation with current evidence (AGG31-07)

- [x] Describe the existing two-pointer instrumented pinch probe accurately and distinguish its
  diagnostic logging from an automated pass/fail assertion and subjective feel check.
- [x] Mark front still mirror/capture-rotation evidence as device-verified while naming the actual
  remaining automated oracle gap.
- [x] Add documentation contracts that keep those evidence-state descriptions synchronized.

## P8 — prevent review from obscuring live video capture (AGG31-08)

- [x] Disable the review target visually and semantically while recording is starting or active,
  while preserving timelapse review behavior.
- [x] Add a defensive ViewModel refusal for non-Compose callers so prior-video playback cannot open
  into a live AudioRecord take.
- [x] Add Compose/policy and ViewModel regressions for idle, starting, and active states.

## P9 — make ToggleRow responsive (AGG31-09)

- [x] Reserve trailing Switch width or stack adaptively at the same compact/large-font boundary as
  value rows, preserving 48 dp activation, enabled state, semantics, and RTL behavior.
- [x] Add EN/KO 320 dp, 2x-font bounds and activation tests for checked/unchecked and
  enabled/disabled rows.

## P10 — add a non-color AF terminal-state cue (AGG31-10)

- [x] Preserve Sony-style state colors but give focused and failed reticles distinct geometry or
  glyphs, with an outline that survives arbitrary preview content.
- [x] Preserve the existing spoken live-region result and quiet viewfinder hierarchy.
- [x] Add deterministic state-presentation tests for scanning, focused, and failed states.

## P11 — keep the focal-rail fade at the viewport edge (AGG31-11)

- [x] Put `trailingEdgeFadeScrollHint` before `horizontalScroll` in the FocalRail modifier chain.
- [x] Add a constrained overflowing-rail regression proving fade presence while forward scrolling
  is possible and disappearance at the end.

## Quality gates and delivery

- [x] Run focused tests for every fix, documentation/tool contracts, and `git diff --check`.
- [x] Run `./gradlew :app:assembleDebug :app:testDebugUnitTest :app:lintDebug` against the whole repo
  and root-fix every error/actionable warning without suppression.
- [x] Run `./gradlew :app:lintRelease :app:assembleRelease :app:bundleRelease` against clean source
  and root-fix every error/actionable warning without suppression.
- [x] Run `python3 tools/verify_host.py` because AGG31-04 proves the authoritative gate is currently
  red; finish only when it is green.
- [x] Commit and push each independent fix and review/plan update using signed Conventional Commits
  plus gitmoji, always pulling with rebase before each push.
- [x] Update this plan with exact evidence and mark it complete only after every item is done.

## Deferred findings

None.

## Completion evidence

- All five focused implementation lanes passed their targeted JVM, Robolectric, Compose, lint, tool,
  and documentation checks before central integration. The shared dirty main worktree was never
  touched; every source edit, build, commit, and push ran in the isolated cycle clone.
- Cross-Engine retirement now registers exact local owners before marker durability and reconciles
  every owner through one bounded process registry. The deterministic one-worker/two-Engine overflow
  schedule proves an old accepted scan restores a replacement Engine's admission exactly once.
- Known-output deletion now reports provider presence and DISCARD-marker cleanup independently;
  positive-delete/already-absent cleanup failures retain retry metadata without restoring phantom
  media, while confirmed survivors and provider uncertainty remain truthful.
- TextureView callbacks publish identity and return before blocking terminal admission; the setup
  lane rechecks Surface generation, lifecycle, quarantine, and GL identity on both sides of the
  gate. The rollback integration test now executes the real Engine rollback boundary.
- Review is unavailable during recording admission/REC, ToggleRow preserves its Switch at compact
  2x font in EN/KO and RTL, AF terminal states carry check/cross geometry plus a keyline, and the
  focal-rail overflow fade stays at the viewport edge.
- `./gradlew :app:assembleDebug :app:testDebugUnitTest :app:lintDebug` passed against the whole repo.
- `python3 tools/verify_host.py` passed: debug and androidTest APK assembly, full JVM/Robolectric/
  Compose suite, lint, exact coverage, 80 tool/release tests, nine coverage-tool tests, 182 harness
  tests, 94 documentation checks, Python compilation, and diff validation. Partition A is 99.83%
  (7,558/7,571) with the reviewed 13-line residual manifest exact; six unexpected cycle-30 misses
  were fixed by executable tests (`GATE_FIXES=6`).
- The release gate passed from clean committed source:
  `./gradlew :app:lintRelease :app:assembleRelease :app:bundleRelease` completed lint, R8, signed APK
  assembly, and signed AAB assembly. The initial pre-commit attempt correctly stopped at the clean-
  source guard; the unchanged gate passed after fine-grained commits. Direct Gradle outputs remain
  developer-only and no immutable release evidence is claimed.
- Twelve review/plan/implementation commits were GPG-signed, pull-rebased, and pushed before this
  closeout. This closeout is the final signed/pushed cycle commit.
- Deployment and physical-device mutation were not performed (`DEPLOY_MODE=none`).

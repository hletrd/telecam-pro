# Review-plan-fix cycle 31 — implementation plan

Date: 2026-08-24
Source: `.context/reviews/_aggregate.md`, cycle 31
Status: planned; implementation not started

## Repository rules applied

- Preserve measured PMA110 behavior, capability-routed Android 13+ behavior, EN/KO presentation,
  quiet-camera UX, media durability, and wrapper-owned release evidence.
- Every cycle-31 finding is scheduled below. Nothing is deferred, silently dropped,
  severity-downgraded, or introduced as new scope under a deferred label.
- Completed plans remain non-destructive archives in `docs/plans/`; no completed plan requires a
  move or deletion.
- Use GPG-signed Conventional Commits with gitmoji, pull with rebase before every push, and do not
  deploy because `DEPLOY_MODE=none`.
- Perform all work in the isolated clean cycle clone. Preserve the shared main worktree and every
  pre-existing CameraEngine/storage/UI/test/tool delta without touching, staging, stashing,
  resetting, restoring, checking out, or terminating its processes.

## P1 — route process retirement to the exact local family owner (AGG31-01)

- [ ] Replace stale-closure retirement publication with a bounded process-owned mapping from each
  family to its registered local deletion owner(s), without retaining obsolete Engines unboundedly.
- [ ] Ensure a process scan cannot remove a family marker until the exact local bookkeeping can be
  reconciled, and make late/duplicate retirement idempotent.
- [ ] Add a deterministic two-Engine overflow schedule proving Engine A's accepted rescan retires
  Engine B's unresolved output and restores B admission exactly once.

## P2 — separate provider-row deletion from marker cleanup (AGG31-02)

- [ ] Return a typed known-output deletion result that distinguishes deleted/already absent,
  confirmed survivor, provider uncertainty, and DISCARD-marker cleanup failure.
- [ ] Restore review only for an authoritatively present URI; retain cleanup retry for absent rows
  without creating a phantom review owner.
- [ ] Cover positive-delete plus marker-remove failure, already-absent plus marker-remove failure,
  confirmed survivor, and unknown-provider outcomes through the ViewModel composition.

## P3 — keep TextureView callbacks off the blocking terminal gate (AGG31-03)

- [ ] Record preview Surface/generation identity synchronously, then hand blocking gate admission
  to the serialized setup lane.
- [ ] Recheck terminal state, current Surface, preview generation, and exact GL owner before binding;
  keep close-vs-native-acquisition ordering unchanged.
- [ ] Add an interleaving regression proving a simulated main-thread callback returns while the
  terminal gate is held and stale queued binds are discarded.

## P4 — restore the exact Partition-A coverage contract (AGG31-04)

- [ ] Cover the retired-facade/process fallback, invalid OTHER measured-host fallback,
  ViewModel-dispatcher facade counters, and default process thread factory.
- [ ] Use the reviewed residual manifest only for genuinely non-executable lines with explicit
  source-region rationale; do not lower thresholds or loosen drift enforcement.
- [ ] Re-run the authoritative coverage task and full host gate.

## P5 — test production-owned optics rollback (AGG31-05)

- [ ] Drive a deterministic owned failure through the real `CameraEngine.rollbackOptics` boundary
  instead of pre-restoring Engine state or invoking the callback manually.
- [ ] Assert Engine declaration, controller magnification input, UI declaration packet, and
  superseded rollback behavior from the same transition.
- [ ] Keep the existing pure reducer regressions as complementary coverage.

## P6 — make callback-drain synchronization deterministic (AGG31-06)

- [ ] Replace the fixed sleep in `EngineCallbackSinkTest` with a bounded rendezvous or narrow test
  seam proving the closer reached and is blocked in `closeAndDrain`.
- [ ] Prove the drain cannot finish until the admitted callback lease releases, then completes
  exactly once afterward.

## P7 — align device-harness documentation with current evidence (AGG31-07)

- [ ] Describe the existing two-pointer instrumented pinch probe accurately and distinguish its
  diagnostic logging from an automated pass/fail assertion and subjective feel check.
- [ ] Mark front still mirror/capture-rotation evidence as device-verified while naming the actual
  remaining automated oracle gap.
- [ ] Add documentation contracts that keep those evidence-state descriptions synchronized.

## P8 — prevent review from obscuring live video capture (AGG31-08)

- [ ] Disable the review target visually and semantically while recording is starting or active,
  while preserving timelapse review behavior.
- [ ] Add a defensive ViewModel refusal for non-Compose callers so prior-video playback cannot open
  into a live AudioRecord take.
- [ ] Add Compose/policy and ViewModel regressions for idle, starting, and active states.

## P9 — make ToggleRow responsive (AGG31-09)

- [ ] Reserve trailing Switch width or stack adaptively at the same compact/large-font boundary as
  value rows, preserving 48 dp activation, enabled state, semantics, and RTL behavior.
- [ ] Add EN/KO 320 dp, 2x-font bounds and activation tests for checked/unchecked and
  enabled/disabled rows.

## P10 — add a non-color AF terminal-state cue (AGG31-10)

- [ ] Preserve Sony-style state colors but give focused and failed reticles distinct geometry or
  glyphs, with an outline that survives arbitrary preview content.
- [ ] Preserve the existing spoken live-region result and quiet viewfinder hierarchy.
- [ ] Add deterministic state-presentation tests for scanning, focused, and failed states.

## P11 — keep the focal-rail fade at the viewport edge (AGG31-11)

- [ ] Put `trailingEdgeFadeScrollHint` before `horizontalScroll` in the FocalRail modifier chain.
- [ ] Add a constrained overflowing-rail regression proving fade presence while forward scrolling
  is possible and disappearance at the end.

## Quality gates and delivery

- [ ] Run focused tests for every fix, documentation/tool contracts, and `git diff --check`.
- [ ] Run `./gradlew :app:assembleDebug :app:testDebugUnitTest :app:lintDebug` against the whole repo
  and root-fix every error/actionable warning without suppression.
- [ ] Run `./gradlew :app:lintRelease :app:assembleRelease :app:bundleRelease` against clean source
  and root-fix every error/actionable warning without suppression.
- [ ] Run `python3 tools/verify_host.py` because AGG31-04 proves the authoritative gate is currently
  red; finish only when it is green.
- [ ] Commit and push each independent fix and review/plan update using signed Conventional Commits
  plus gitmoji, always pulling with rebase before each push.
- [ ] Update this plan with exact evidence and mark it complete only after every item is done.

## Deferred findings

None.

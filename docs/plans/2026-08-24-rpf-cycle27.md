# Review-plan-fix cycle 27 — implementation plan

Date: 2026-08-24
Source: `.context/reviews/_aggregate.md`, cycle 27
Status: complete; all four findings fixed, configured gates green, signed commits pushed

## Repository rules applied

- Preserve measured PMA110 behavior, capability-routed Android 13+ behavior, EN/KO presentation,
  quiet-camera UX, media durability, and wrapper-owned release evidence.
- Every cycle-27 finding is scheduled below. Nothing is deferred, silently dropped, severity-
  downgraded, or introduced as new scope under a deferred label.
- Completed plans remain non-destructive archives in `docs/plans/`; no completed plan requires a
  move or deletion.
- Use GPG-signed Conventional Commits with gitmoji, pull with rebase before every push, and do not
  deploy because `DEPLOY_MODE=none`.
- Preserve and never stage the unrelated, incorrect `CameraEngine.kt` worktree delta inherited from
  an earlier agent. Restoring it is authorization-blocked by the user's destructive-action rule.

## P1 — make the host gate self-contained in committed source (AGG27-01)

- [x] Split `tools/check_docs.py` into always-available committed/public checks and explicitly
  optional internal-document checks when ignored private docs are absent.
- [x] Preserve the full local internal-doc suite when those files exist; absence must skip only the
  private checks, never silently weaken public resource/build/privacy contracts.
- [x] Add a tool contract that exports committed HEAD into a disposable directory and proves the
  documentation gate starts and completes without borrowing ignored workspace files.
- [x] Run focused Python tests and the documentation checker in both normal-workspace and exported-
  HEAD shapes.

## P2 — remove synchronous marker retirement from the global registry lock (AGG27-02)

- [x] Extend exact-family authority so the final producer/publication-claim recheck and marker
  removal stay same-family ordered without holding `familyAuthorityRegistryLock` over preference
  I/O.
- [x] Keep global locking limited to short registry/user-count bookkeeping; preserve publication
  claims registered before exact-family monitor acquisition and producer-lease terminality.
- [x] Add deterministic blocked-remove tests proving unrelated-family publication and producer
  admission complete, plus same-family publication still retains the deletion marker.
- [x] Confirm the existing architecture wording remains accurate; no prose change was required.

## P3 — carry survivor provenance through file-only deletion (AGG27-03)

- [x] Store `MediaProvenance` per prior output in `CaptureOutputTracker` and carry it through
  `CaptureDeletePlan` and survivor selection.
- [x] Return/publish a typed survivor packet so `CameraViewModel` updates URI, provenance, and delete
  scope atomically after both restore branches.
- [x] Add reducer/tracker/ViewModel regressions for owned-processed + unverified-DNG and the reverse
  ordering; verify review/TalkBack provenance follows the promoted survivor.

## P4 — keep output-format chips reachable at large font scale (AGG27-04)

- [x] Reuse the existing RTL-aware horizontal-scroll and trailing-overflow affordance for the
  HEIF/JPEG/DNG multi-select row without adding exclusive `selectableGroup` semantics.
- [x] Bring the selected format into view while retaining per-chip context semantics and 48 dp
  targets.
- [x] Add a 320 dp, `fontScale=2f` Compose regression that scrolls to and activates every format
  chip and verifies accessible names.

## Quality gates and delivery

- [x] Run focused tests for each fix, `git diff --check`, and any documentation/tool contracts.
- [x] Run `./gradlew :app:assembleDebug :app:testDebugUnitTest :app:lintDebug` against the whole
  repository and root-fix every error/actionable warning without suppression.
- [x] Run `./gradlew :app:lintRelease :app:assembleRelease :app:bundleRelease` from the clean pushed
  detached worktree, excluding the unrelated incorrect `CameraEngine.kt` delta without altering it.
- [x] Update this plan with exact evidence and commit/push each independent fix using signed
  Conventional Commits plus gitmoji, always pulling with rebase before push.

## Deferred findings

None.

## Completion evidence

- Normal maintainer documentation gate: 118/118 checks passed. Exported committed-tree shape:
  89 checks passed with 21 explicitly named private-document skips. Tool contracts: 8/8 passed.
- Focused storage, provenance, ViewModel/review semantics, and responsive Compose suites passed;
  the combined focused Gradle run completed successfully.
- Whole-repository debug gate passed: `:app:assembleDebug`, `:app:testDebugUnitTest`, and
  `:app:lintDebug` completed successfully.
- Clean pushed-HEAD release gate passed: `:app:lintRelease`, `:app:assembleRelease`, and
  `:app:bundleRelease` completed successfully, including R8 and signed APK/AAB packaging.
- Six implementation/review/plan commits were GPG-signed, pull-rebased, and pushed before this
  closeout. This closeout is the final signed/pushed cycle commit.
- The original main worktree's unrelated `CameraEngine.kt` delta remained byte-for-byte preserved,
  unstaged, and absent from all cycle-27 commits and gates. No process was killed and no destructive
  restoration was performed.
- Deployment and physical-device mutation were not performed (`DEPLOY_MODE=none`).

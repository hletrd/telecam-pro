# Review-plan-fix cycle 11 — implementation plan

Date: 2026-08-23
Source: `.context/reviews/_aggregate.md`, cycle 11
Status: implementation and host gates complete; immutable release gate pending

## Repository rules applied

- Preserve PMA110 Camera2/HAL behavior byte-for-byte; generic/removable-camera behavior derives only
  from enumerated capability and remains unvalidated until measured.
- Correctness, privacy/data-loss, native ownership, and evidence-integrity findings are mandatory and
  are not deferred.
- New user-facing prose requires English and Korean resources and must preserve the quiet Sony
  Alpha/Xperia Pro surface. Prefer truthful state over banners or helper overlays.
- Deployment and device work are disabled for this invocation; no current device serial is supplied.
- Completed plan files remain in place. Moving or deleting them requires destructive-action
  confirmation, so archival is represented by each file's completed status rather than relocation.
- Preserve exact coverage, dependency verification, immutable release boundaries, GPG signing,
  Conventional Commits with gitmoji, pull-with-rebase before push, and no force push.

No finding is deferred. Every aggregate item AGG11-01 through AGG11-15 is scheduled below.

## Phase 1 — make family deletion typed, asynchronous, bounded, and resurrection-safe

- [x] **P1 / AGG11-01:** carry `CaptureFamilyKey`, media kind, and delete scope through the
  ViewModel→Engine deletion contract. Register live VIDEO identity before allocation, seed restored
  canonical family identity, and reserve the retained-still late-sibling gate for families that can
  actually produce late still outputs. Keep FILE_ONLY behavior truthful.
- [x] Project genuine capture-admission refusal into shutter enablement/status semantics, without
  leaving an enabled Ready control that cannot capture. Cover English and Korean presentation.
- [x] **P2 / AGG11-05, AGG11-06:** preserve the synchronous in-memory tombstone but move durable
  journal commit/retry off the UI lane onto an ordered owner. Bound persistent family markers and
  restart query work process-wide; retire only after every producer is terminal and exact family
  absence is authoritative, otherwise fail closed before growth.
- [x] **P3 / AGG11-07:** route every rejected HEIF/JPEG/DNG/video output through the typed durable
  discard contract, keep unresolved cleanup bounded, and prevent launch adoption after failure.
- [x] **P4 / AGG11-13 (deletion portion):** add production-composition tests for live/restored still
  and video, late siblings, Engine replacement, provider/journal failures, hundreds of deletions,
  bounded query/cardinality work, UI-thread identity, and launch recovery.

Acceptance: a legitimate video/restored delete cannot poison the shutter; delete acknowledgement is
durable without UI-thread fsync/sleep; journal/query work is finite; rejected media never resurrects.

## Phase 2 — keep topology truth and lease ownership coherent through REC

- [x] **P5 / AGG11-03:** transfer the topology lease explicitly when recorder ownership is published.
  Pre-publication failure may release it; post-publication attach/drain failure must retain it until
  checked native finalization or quarantine.
- [x] **P6 / AGG11-04:** separate discovery/cache invalidation from accepted-session optical truth.
  Preserve the current route's optical presets/finder gate throughout the REC lease and atomically
  replace them only when deferred topology convergence commits.
- [x] **P7 / AGG11-12:** prune consumed identity epochs for absent removable-camera ids after complete
  inventory while preserving same-id replacement invalidation and a bounded pending-removal set.
- [x] **P8 / AGG11-13 (topology portion):** add injectable topology/recorder boundaries and
  Engine-level tests for attach/removal/replacement, incomplete inventory, callback coalescing,
  harmless REC changes plus zoom, publication failure, native teardown, quarantine, release, cache
  truth, Ready continuity, and one latest-wins convergence.

Acceptance: no topology action or accepted-optics mutation can cross a live/native REC owner; the
GL/Compose finder predicates remain identical; removable-id history is bounded.

## Phase 3 — bind device-harness execution to private, regular, checkout-aware bytes

- [x] **P9 / AGG11-02:** derive default repository artifacts from `SOURCE_HARNESS_ROOT.parent`, while
  imports, case registration, and executed manifests remain rooted at the private snapshot.
- [x] **P10 / AGG11-10:** replace pathname `lstat`→read pairs with descriptor-relative no-follow
  open, same-descriptor `fstat`, and bounded reads; reject file and parent-component swaps,
  symlinks, and special files.
- [x] **P11 / AGG11-11:** remove the caller-spoofable environment-only child trust decision. Bind
  child mode to a parent-created private execution root using a one-shot inherited proof/dedicated
  child entrypoint and require execution/source roots to differ.
- [x] Add real outer→child subprocess tests for no-`--apk`, argv/return propagation, digest mismatch,
  preset-env spoofing, mutation between hash/import, file/parent symlink swaps, report-root routing,
  and cleanup.

Acceptance: every documented command reaches checkout artifacts, while executable harness bytes can
only come from the attested private regular-file snapshot.

## Phase 4 — make immutable release inputs and verified upload bytes singular

- [x] **P12 / AGG11-08:** make artifact verification consume one private no-follow regular-file
  snapshot across ZIP/keytool/jarsigner/bundletool authorities and fail if the source path identity or
  digest changes before success (or emit an exclusive digest-qualified verified upload copy).
- [x] **P13 / AGG11-09:** reject tracked symlink/non-regular packageable inputs in both the immutable
  Python wrapper and Gradle release guard; copy/hash from validated descriptors. Keep local signing
  configuration on its explicit separate allowlist.
- [x] Add source/artifact swap tests at early and late tool boundaries, relative/absolute symlinks,
  parent swaps, special files, and cross-host deterministic-source assertions.

Acceptance: a clean commit cannot import external mutable source bytes, and an upload-ready result
names the exact AAB bytes every verifier inspected.

## Phase 5 — close remaining test and documentation evidence gaps

- [x] **P14 / AGG11-13:** inject the post-native storage dispatcher/tail into real CameraEngines and
  prove one process-wide worker/backlog bound, non-blocking next admission, exact callback identity,
  overflow disposition, release behavior, and accepted old-Engine completion.
- [x] **P15 / AGG11-14:** replace the reducer test's 25 ms negative timing assertion with positive
  attempted/contended handshakes.
- [x] **P16 / AGG11-15:** reconcile `CLAUDE.md`, `docs/ARCHITECTURE.md`, and ViewModel ownership
  comments with the actual admission→process allocator→recorder sequence; qualify route inventory as
  attempted before first open with bounded later convergence. Pin both with documentation checks.

Acceptance: integration tests exercise shipping owners rather than helpers alone, concurrency tests
prove their interleavings, and authoritative docs no longer overclaim ordering or inventory.

## Phase 6 — gates, delivery, and evidence

- [x] Run `./gradlew :app:assembleDebug :app:testDebugUnitTest :app:lintDebug` against the whole repo.
- [ ] Run the configured release tasks through the repository-required immutable wrapper if direct
  Gradle tasks fail closed by design.
- [x] Run repository host/tool/harness/docs verification and `git diff --check`.
- [x] Root-fix every error and cleanly actionable warning; do not add suppressions.
- [ ] Commit independent concerns with fine-grained GPG-signed Conventional Commit + gitmoji
  messages; pull with rebase before every push, then push immediately. Do not deploy.
- [ ] Mark every task complete and record exact gate/commit evidence here.

## Planned commit sequence

`fix(storage)`, `test(storage)`, `fix(camera)`, `test(camera)`, `fix(attestation)`,
`test(attestation)`, `fix(release)`, `test(release)`, `test(video)`, `docs(architecture)`, and
`docs(plan)`, split further where a commit would mix independent concerns.

## Deferred findings

None.

## Residual warnings

None accepted at planning time. Any gate warning that cannot be root-fixed must receive a compliant
deferred record before the cycle may complete.

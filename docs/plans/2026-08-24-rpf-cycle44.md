# Review-plan-fix cycle 44 — implementation plan

Date: 2026-08-24
Source: `.context/reviews/_aggregate.md`, cycle 44
Status: complete; all nine findings fixed and every configured gate green

## Repository rules applied

- Preserve measured PMA110 Camera2 behavior, capability-enumerated generic-device paths, exact
  lifecycle/native ownership, durable media truth, the quiet Sony-style viewfinder, EN/KO parity,
  and explicit field-evidence limits.
- Schedule every one of the nine aggregate findings below. Nothing is silently dropped, deferred,
  severity-downgraded, or relabeled as optional.
- Keep completed historical plans as non-destructive archives in `docs/plans/`; no prior plan is
  moved, deleted, or reopened by this cycle.
- Work only in `/tmp/find-x9-ultra-cycle44.GMMtvi`; never modify, stash, reset, check out, clean, or
  build from the shared dirty `main` checkout.
- Use fine-grained GPG-signed Conventional Commits with gitmoji, pull with rebase before every push,
  and do not deploy because `DEPLOY_MODE=none`.

## P1 — give camera-input blocking identity-owned state (AGG44-01, AGG44-06)

- [x] Replace the shared last-writer Boolean with an explicit owner set/reducer so Compose-modal,
  microphone permission, media permission, camera policy, and ownerless-delete releases can remove
  only their own block.
- [x] Preserve synchronous countdown/input cancellation on the first acquired owner and publish the
  derived blocked bit only when aggregate ownership changes.
- [x] Restore microphone rationale/system-request ownership before composition, consume the pending
  action exactly once on grant or denial, and keep an ownerless result inert.
- [x] Add deterministic lifecycle/state-machine coverage for both pending actions and both phases,
  including replacement-screen publication, cross-owner overlap, exactly-once continuation, and
  terminal owner clearing; retain Bundle tests as serialization-only evidence.

## P2 — require positive pseudo-ZSL timing evidence (AGG44-02)

- [x] Make deep repeating YUV admission require a positive minimum-frame duration at or below the
  existing 24 fps ceiling; unavailable/failed timing falls back to the shallow real-capture path.
- [x] Preserve PMA110's measured positive ~33 ms logical/front admission and the existing monotonic
  deep → shallow → HAL-JPEG fallback ladder.
- [x] Replace the zero-is-safe unit contract and add route/session tests proving unavailable timing
  cannot allocate the deep reader or attach it to the repeating request.

## P3 — make a running timelapse visibly and semantically stoppable (AGG44-03)

- [x] Pass `timelapseRunning` through the primary-shutter presentation and provide localized EN/KO
  “Stop timelapse” semantics plus a non-color stop/running shape.
- [x] Route a running-timelapse press to stop before still-target/countdown admission, and keep that
  stop action enabled while camera Ready is false.
- [x] Add policy, Compose, keyboard/accessibility, paint, ViewModel, and Engine admission tests for
  idle/start/running/recovery/stop behavior without changing ordinary PHOTO/VIDEO geometry.

## P4 — separate small alarm text from recording-shape red (AGG44-04)

- [x] Keep `CameraColors.Record` for non-text REC/shutter/meter shapes and use a role-appropriate
  small-text color that clears 4.5:1 on the canonical worst-case live-frame plate.
- [x] Apply the compliant role to low-battery percentage and running-timelapse text without changing
  unrelated recording paint.
- [x] Extend HUD contrast tests to enumerate every actual small-text foreground separately from the
  3:1 non-text recording-shape contract.

## P5 — make camera condition statuses event-owned progress (AGG44-05)

- [x] Classify reconfiguring, preview/camera recovering, and retrying conditions as untimed
  `CameraStatusLifecycle.PROGRESS` rather than timer-driven events.
- [x] Retire an owned progress condition on exact Ready, rollback/exhaustion/pause, or replacement by
  a newer status without allowing stale Ready to swallow a newer message.
- [x] Update `CLAUDE.md`/architecture wording to name the typed lifecycle seam and add fast-ready,
  slow-recovery, terminal, and replacement tests.

## P6 — exercise the real repeated-zoom submit chain (AGG44-07)

- [x] Introduce the smallest test seam needed to observe Engine zoom start/movement/quiet/end
  submission without weakening Camera2 ownership or changing device behavior.
- [x] Drive production scheduling through first 250 ms landing, inward/same-direction movement,
  second 250 ms landing, and 700 ms end; assert exact wire ratios and absence of duplicate submits.
- [x] Keep the pure transition tests as local law coverage and add a regression that fails if
  `noteZoomMovement` or quiet scheduling is disconnected from production.

## P7 — register the front pseudo-ZSL soak in the canonical field ledger (AGG44-08)

- [x] Add a named front PHOTO/SINGLE sustained-idle and memory-pressure check with immutable APK/
  source identity, delivered cadence/frame gaps, camera errors, memory/thermal delta, and post-soak
  capture success criteria; update dashboard/open counts without claiming it passed.
- [x] Teach `tools/check_docs.py` to bind active `CLAUDE.md` “remains a field check” obligations to
  exact `docs/FIELD_CHECKS.md` identities, with a mutation test for this route.

## P8 — make shutter-focus paint coverage exercise production wiring (AGG44-09)

- [x] Capture the same real `ShutterButton` before and after keyboard focus over bright and dark
  backgrounds; assert production edge pixels change.
- [x] Compute representative/minimum contrast from captured pixels rather than color constants,
  while retaining the standalone indicator test only as component-level diagnostics.

## Quality gates and delivery

- [x] Run focused tests for each owner/policy/paint/status/docs seam plus `git diff --check`.
- [x] Run `./gradlew :app:assembleDebug :app:testDebugUnitTest :app:lintDebug` against the whole repo.
- [x] Run `./gradlew :app:lintRelease :app:assembleRelease :app:bundleRelease` against the whole repo.
- [x] Root-fix every gate error and warning without suppression.
- [x] Commit and push each independent review, plan, implementation, test, documentation, and
  closeout change with signed Conventional Commits plus gitmoji, pulling with rebase before push.
- [x] Mark this plan complete only after exact evidence is delivered.

## Deferred findings

None. All nine cycle-44 findings are scheduled for this cycle.

## Completion evidence

- All nine deduplicated findings were implemented; none was deferred. Camera input now derives from
  identity-owned Compose/review/permission/policy/delete tokens, and the microphone command/phase is
  one exactly-once saved transition owner across recreation.
- Deep pseudo-ZSL requires positive ≤1/24 s timing evidence. PMA110's measured ~33 ms route remains
  admitted; unavailable metadata now fails closed to shallow real capture.
- A running timelapse has EN/KO stop semantics, a non-color stop square, full-health presentation,
  and stop-before-capture admission even while camera Ready is false. Critical 11/12 sp HUD text
  uses a 4.89:1 alarm role while recording shapes retain their separate 3:1 red contract.
- Camera bring-up, reconfiguration, recovery, and retry conditions are untimed progress identities
  retired by exact Ready/terminal/pause/replacement events.
- Production-wiring coverage now holds microphone ownership across a replacement Compose release,
  drives zoom through the real 16/250/700 ms ViewModel→Engine chain, and captures the same real
  shutter before/after focus. That last test exposed and fixed overlapping borders that rendered
  only 2.57:1 contrast on a dark frame.
- `docs/FIELD_CHECKS.md` now owns open A5 for the front PHOTO/SINGLE sustained pseudo-ZSL soak, and
  the committed docs gate mutation-tests every active CLAUDE field-check claim against an exact open
  ledger identity. No unrun device evidence is claimed.
- The first full debug gate exposed a host-load race in the capacity test's own blocker setup. The
  deterministic start-before-supersede fix passed focused rerun and the complete second gate.
- Configured debug gate passed with 2,047 JVM/Robolectric/Compose tests plus debug assembly and lint.
  Configured release lint, signed APK, and signed AAB gate passed using read-only links to existing
  ignored signing inputs. The prebuilt native strip notice is expected packaging diagnostics, not
  an Android-lint/compiler warning.
- `python3 tools/verify_host.py` passed: Partition A 8,098/8,113 lines (99.82%), 110 tooling tests,
  nine coverage-tool tests, 184 device-harness self-tests, 140 documentation checks, Python
  compilation, Android debug assembly/test/lint, and `git diff --check` were all green.
- Eleven fine-grained GPG-signed Conventional Commits with gitmoji were pushed after pull-with-rebase
  before this closeout; this completion record is the twelfth. Deployment was not run
  (`DEPLOY_MODE=none`), and no device action was taken.
- Work remained in `/tmp/find-x9-ultra-cycle44.GMMtvi`. The shared dirty `main` checkout was never
  edited, staged, stashed, reset, checked out, cleaned, or used for a build; its pre-existing source,
  test, tooling, and review deltas remain untouched.

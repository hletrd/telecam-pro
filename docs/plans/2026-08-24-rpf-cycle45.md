# Review-plan-fix cycle 45 — implementation plan

Date: 2026-08-24
Source: `.context/reviews/_aggregate.md`, cycle 45
Status: complete; all ten findings fixed and every configured gate green

## Repository rules applied

- Preserve measured PMA110 Camera2 behavior, capability-enumerated generic-device paths, exact
  lifecycle/native ownership, durable media truth, the quiet Sony-style viewfinder, EN/KO parity,
  and explicit field-evidence limits.
- Schedule every one of the ten aggregate findings below. Nothing is silently dropped, deferred,
  severity-downgraded, or relabeled as optional.
- Keep completed historical plans in `docs/plans/` as the repository's non-destructive archive; no
  completed plan is deleted, moved, or reopened.
- Work only in `/tmp/find-x9-ultra-cycle45.4l2DdI/repo`; never modify, stash, reset, check out,
  clean, or build from the shared dirty `main` checkout.
- Use fine-grained GPG-signed Conventional Commits with gitmoji, pull with rebase before every push,
  and do not deploy because `DEPLOY_MODE=none`.

## P1 — make review replacement lifecycle-safe (AGG45-01)

- [x] Move the exact open-review identity needed to reconstruct the overlay into ViewModel-owned
  state, or atomically retire the exact review before policy replacement, so URI, provenance,
  delete scope, pin, `REVIEW` owner, and visible overlay cannot diverge.
- [x] Preserve Activity recreation, close/delete, late-sibling, and exact-family semantics.
- [x] Add production-composition coverage that opens review, replaces `CameraScreen` with the policy
  gate, restores it, and proves the same review returns or every exact owner retires together.

## P2 — bind progress retirement to current Ready ownership (AGG45-02)

- [x] Recheck Ready publication identity at the progress-retirement boundary, or fold camera-ready
  and progress ownership into one generation-aware reducer.
- [x] Ensure stale Ready cannot clear a newer NotReady operation's untimed condition while current
  Ready, pause, terminal, and replacement paths still retire their own progress.
- [x] Add a latch-controlled Ready-N / NotReady-N+1 / newer-progress interleaving regression.

## P3 — bound latest-capture restore admission (AGG45-03)

- [x] Introduce a ViewModel-local single-flight owner with one conflated pending refresh; keep
  blocking provider work application-context-only and publication generation-safe.
- [x] Reject/drop pending intent on teardown without interrupting a potentially wedged Binder call,
  and do not run a pending refresh once media is already restored.
- [x] Add deterministic saturation coverage proving one active plus at most one pending restore, at
  most one useful follow-up, no post-clear publication, and no unbounded request graph.

## P4 — preserve terminal camera verdicts from unhealthy hardware actions (AGG45-04)

- [x] Apply primary-action admission to physical shutter/REC execution while continuing to consume
  owned key edges and preserving active recording/timelapse stop semantics.
- [x] Ensure terminal exhausted-camera guidance cannot be replaced by an ownerless untimed
  reconfiguration status in either PHOTO or idle VIDEO.
- [x] Add real ViewModel hardware-entry tests for exhausted PHOTO/VIDEO, active REC, and active
  timelapse states.

## P5 — make active timelapse interval truth match the UI (AGG45-05)

- [x] Apply an accepted interval edit at the next scheduling boundary after any in-flight shot,
  using synchronized current settings without changing the owning run generation.
- [x] Keep recovery, stop, and restart schedules consistent with the displayed/persisted interval.
- [x] Test start at interval A, live edit to B, capture completion/recovery, stop/edit, and restart.

## P6 — prove microphone lifecycle wiring through real Activity recreation (AGG45-06)

- [x] Add a testable MainActivity permission/request boundary that does not weaken production
  permission or ViewModel ownership.
- [x] Drive actual Robolectric/ActivityScenario saved-state recreation for ENABLE_AUDIO and
  START_RECORDING in rationale and system-request phases, through grant and denial.
- [x] Assert restored input ownership, replacement-screen overlap, exactly-once continuation, and
  terminal clearing; keep existing helpers labeled as serialization/state-law coverage.

## P7 — require meaningful rendered focus-ring coverage (AGG45-07)

- [x] Replace strongest-pixel assurance with a substantial connected annular coverage or percentile
  threshold at 3:1 on bright and dark production shutter fixtures.
- [x] Keep antialiasing tolerance while rejecting one-pixel, short-arc, clipped, and mostly
  low-contrast mutations.

## P8 — keep an enabled recording Stop visually actionable (AGG45-08)

- [x] Give active Stop/Cancel ownership full-strength shutter presentation independently of new
  capture readiness; retain disabled paint only for unavailable start/capture actions.
- [x] Correct the stale source comment and test contract that currently pin recording Stop to 0.35
  alpha.
- [x] Add rendered Ready/recovery/terminal and keyboard-focus coverage for the recording Stop.

## P9 — localize the converter focal OSD (AGG45-09)

- [x] Reuse `R.string.osd_tele` or a bilingual formatted focal resource in the active converter
  readout without changing camera-standard abbreviations.
- [x] Extend bilingual Compose coverage for converter-active English and Korean focal strings.

## P10 — make dropdown identity responsive at large text (AGG45-10)

- [x] Share `LabelValueRow`'s narrow/high-font stacked trigger policy with `DropdownRow` while
  preserving one complete accessible action and radio-selection semantics.
- [x] Let popup choices expose distinguishing suffixes with bounded wrapping and no window overflow.
- [x] Add 212 dp/fontScale 2.0 EN/KO and RTL layout/semantics coverage for Phone and Converter
  values/options.

## Quality gates and delivery

- [x] Run focused tests for each owner, policy, scheduler, lifecycle, paint, localization, and
  responsive-layout seam plus `git diff --check`.
- [x] Run `./gradlew :app:assembleDebug :app:testDebugUnitTest :app:lintDebug` against the whole repo.
- [x] Run `./gradlew :app:lintRelease :app:assembleRelease :app:bundleRelease` against the whole repo.
- [x] Root-fix every gate error and warning without suppression.
- [x] Commit and push each independent review, plan, implementation, test, documentation, and
  closeout change with signed Conventional Commits plus gitmoji, pulling with rebase before push.
- [x] Mark this plan complete only after exact evidence is delivered.

## Deferred findings

None. All ten cycle-45 findings are scheduled for this cycle.

## Completion evidence

- All ten deduplicated findings were implemented; none was deferred. Review presentation now has a
  ViewModel-owned exact identity that survives policy replacement, and Ready/progress status
  retirement is serialized and generation-owned.
- Latest-capture restore owns one active provider query plus one conflated pending intent, with an
  application-context query closure, weak ViewModel return path, exact-token main publication, and
  teardown rejection. Hardware shutter execution follows primary-action admission without losing
  key ownership or active recording/timelapse stop behavior.
- A running timelapse rereads the accepted interval at every retry/save scheduling boundary. Real
  MainActivity create/save/destroy/recreate coverage now proves both microphone actions and both
  rationale/system phases retain and consume their exact owner once.
- Production focus paint must cover at least 58/72 contrast-qualified angular bins. Recording Stop
  stays full-strength through Not-Ready; the converter focal OSD uses EN/KO `osd_tele`; compact
  dropdown triggers/options reflow at large text in LTR and RTL without collapsing their text slot.
- Focused validation covered every new owner, lifecycle, scheduler, hardware policy, raster,
  localization, and responsive-layout seam. The final responsive test initially exposed two failed
  selected/option text cases; filling the weighted trigger and popup slots fixed both, and the
  complete 38-test P8-P10 slice passed.
- Configured debug gate passed: debug assembly, 2,067 JVM/Robolectric/Compose tests across 247
  suites, and debug lint were green. Configured release gate passed: release lint, signed APK, and
  signed AAB were green using read-only links to existing ignored signing inputs. The prebuilt
  native strip notice is expected packaging diagnostics, not a compiler or Android-lint warning.
- `python3 tools/verify_host.py` passed after the final source fix: Partition A 8,196/8,211 lines
  (99.82%), 110 tooling tests, nine coverage-tool tests, 184 device-harness self-tests, 140
  documentation checks, Python compilation, Android debug/androidTest assembly, tests/lint, and
  `git diff --check` were all green.
- Six gate issues encountered during implementation were root-fixed: two P1 test-compilation
  adjustments, the P3 constructor signature clash, one Kotlin warning, and two responsive Compose
  failures. No suppression, skipped assertion, or threshold weakening was used.
- Thirteen fine-grained GPG-signed Conventional Commits with gitmoji were pushed before this
  closeout; this completion record is the fourteenth. Deployment was not run (`DEPLOY_MODE=none`),
  and no device action was taken.
- Work remained in `/tmp/find-x9-ultra-cycle45.4l2DdI/repo`. The shared dirty `main` checkout was
  never edited, staged, stashed, reset, checked out, cleaned, or used for a build; its pre-existing
  uncommitted changes remain untouched.

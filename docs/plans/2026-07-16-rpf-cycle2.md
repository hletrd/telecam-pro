# Review-Plan-Fix Cycle 2 Plan — 2026-07-16

Derived from `.context/reviews/_aggregate.md` (13 current-HEAD specialist
reports, 77 raw sections, 16 deduplicated findings). Every aggregate finding is
scheduled below; there are no newly deferred cycle-2 findings.

Run constraints:

- Deployment mode is `none`: do not connect ADB, install, launch, publish,
  upload, or otherwise modify a device or production environment.
- The nested `ralph` skill named by the workflow is not installed. Implement
  this plan directly with the same fine-grained verify/commit/pull-rebase/push
  loop.
- Commits must be GPG-signed (`-S`), Conventional Commits with gitmoji, have no
  AI co-author trailer, and be pushed after `git pull --rebase`.
- Configured gates are `:app:assembleDebug`, `:app:testDebugUnitTest`,
  `:app:lintDebug`, `:app:lintRelease`, `:app:assembleRelease`, and
  `:app:bundleRelease` under JDK 21.
- Camera2/GL/HAL, predictive-Back, touch-target, and ANR behavior changed here
  must be reported as statically/host-gate verified with PMA110 verification
  pending. `CLAUDE.md` forbids claiming device behavior from host gates alone.

Status: ☐ todo · ◐ in progress · ☑ done · ⏸ deferred

## Phase 1 — Durable renderer and atomic optics state

- ☑ **P1.1 / AGG-1 (High/High)** — Introduce an immutable renderer-config
  snapshot/store owned by `CameraEngine`, synchronously update it from every
  peaking/level/color, zebra/level, false-color, analysis, and punch-in setter,
  and replay the complete snapshot in every `gl.start` callback after the
  handler/callback exist. Add a platform-neutral restore-before-start/restart
  store test. `CameraEngine.kt:222-237,460-473,1583`,
  `GlPipeline.kt:123-133,600-602`, `CameraViewModel.kt:291-315`.
- ☑ **P1.2 / AGG-2 (High/High)** — Extract one pure Photo↔Video optics reducer
  for unified/lens-local zoom. Change `CameraEngine.setVideoMode` to accept the
  already-resolved destination lens and complete controls atomically instead of
  reinterpreting mutable controls later. In `CameraViewModel.onModeChange`,
  remove the scheduled apply, clear/rebase `pendingControls`, apply the reducer,
  and send that exact resolved packet. Cover 10× Photo→TELE10X/1× Video,
  Video→Photo, TELE, and stale-packet orderings. `CameraViewModel.kt:74-85,
  846-879,1366-1382`, `CameraEngine.kt:375-428`, `ZoomMath.kt`.
- ☑ **P1.3 / AGG-5 (Medium/High)** — Split the 20× photo-unified ceiling from
  the 10× video-local ceiling, normalize non-finite persisted values, and add a
  pure live-cap reconciliation helper. On `onCapsReady`, clamp UI/engine/GL to
  the selected camera range and update the Photo lens band if needed. Test
  VIDEO 20→10, non-finite input, TELE display bounds, and narrower live caps.
  `ZoomMath.kt:65-89`, `ZoomMathTest.kt`, `CameraViewModel.kt:145-147,282-301`.

## Phase 2 — Analysis field and camera readiness transactions

- ☑ **P2.1 / AGG-6+7 (Medium/High)** — Derive a bounded analysis target from
  the live preview surface aspect (long side at most 256), resize both FBO
  texture and CPU buffers when it changes, and redraw the capture/EIS crop at
  frame center rather than preview punch-in crop/center. Add pure portrait,
  landscape, square, invalid-size, and loupe-invariance planning tests.
  `GlPipeline.kt:344-468,647-649`, `FlipRenderer.kt:139-151,235-249`.
- ☑ **P2.2 / AGG-8 (Medium/High)** — Make regular/high-speed repeating-start
  paths return success. Invoke controller `onReady` only after a successful
  repeating submit; send failure through the existing engine error/recovery
  callback. Also reject a deferred-session post that cannot reach its handler
  instead of silently retaining callbacks. Add a pure ready-vs-error delivery
  test seam. `CameraController.kt:230-240,439-445,490-496,521-661`,
  `CameraEngine.kt:350-365`.
- ☑ **P2.3 / AGG-9 (Medium/High)** — Add generation-owned reconfiguration
  snapshots and rollback delivery. Mode/lens/override intents retain the last
  accepted mode/lens/TELE/zoom/override state; every current-generation
  pre-close ID/caps/selection abort restores engine fields, pin-FPS, UI optics,
  and Ready only when the old controller is still viable. Superseded generations
  must never roll back a newer intent. Add pure snapshot/generation tests.
  `CameraEngine.kt:375-428,676-815`, `CameraViewModel.kt:140-170,920-1000,
  1245-1270`.
- ☑ **P2.4 / AGG-13 (Medium/Medium)** — Reorder TELE session plans so a
  regular full/degraded still-capable plan is attempted before vendor
  preview-only; preserve vendor-first full/degraded attempts and regular/vendor
  preview-only as the terminal options. Update the pure ladder tests and comments;
  PMA110 behavior remains pending. `CameraController.kt:419-466,1019-1071`,
  `SessionFallbackLadderTest.kt`.

## Phase 3 — Pro controls, capability truth, and modal behavior

- ☑ **P3.1 / AGG-4 (High/High)** — Gate Focus/SS/ISO quick chips by actual
  manual-focus/exposure/ISO capabilities, not by already owning the destination
  mode. Keep existing tap reducers for AF→MF, P/ISO→S, and P/S→ISO. Extract and
  test the enablement matrix so default P/AF controls remain actionable only
  when hardware supports them. `ManualDials.kt:153-176,261-297`.
- ☑ **P3.2 / AGG-12 (Medium/Medium)** — Remove fabricated 4K/1080p fallback
  options. Show an explicit Resolution unavailable state while selected-camera
  caps are absent/empty and disable selection. In `CameraEngine`, reject a
  nonmember before mutating requested/current size or reopening, while still
  allowing pre-cap settings restoration for later validation. `ProSheet.kt:
  818-831`, `CameraViewModel.kt:1016-1023`, `CameraEngine.kt:634-655`.
- ☑ **P3.3 / AGG-10+14 (Medium/High)** — Add topmost `BackHandler`s to Fn and
  review overlays, with the delete dialog consuming Back before the review.
  Give each Photo/Video `ModeLabel` a minimum 48×48 dp clickable region while
  preserving compact visual content. Host compilation verifies wiring;
  predictive-Back/glove behavior remains device-pending. `CameraScreen.kt:
  1138-1230,1373-1407`, `MediaReview.kt:249-662`.

## Phase 4 — Media and lifecycle ownership

- ☑ **P4.1 / AGG-11 (Medium/High)** — Calculate bounded video-frame dimensions
  from retriever metadata and use `getScaledFrameAtTime`; retain a fallback that
  recycles the full source when scaling creates a distinct bitmap. Add pure
  target-size tests for 4K landscape/portrait, below-limit, invalid, and maxDim
  inputs. `MediaReview.kt:93-124`.
- ☑ **P4.2 / AGG-15 (Low/Medium)** — Retain the caller-created video playback
  `Surface` beside the identity-guarded player and release/clear it on prepare
  failure, texture destruction, and composition disposal without double-releasing
  a newer generation. `MediaReview.kt:410-491`.
- ☑ **P4.3 / AGG-3 (High/High)** — Keep ViewModel teardown non-blocking by
  moving `CameraEngine.release`'s bounded recorder/camera/GL waits to a named
  background owner after callbacks/receiver work is detached. Preserve the
  existing recorder→camera→GL→executor order and rejection fallback; host tests
  can validate dispatch, while ANR timing remains PMA110-pending.
  `CameraViewModel.kt:1466-1471`, `CameraEngine.kt:1585-1614`.

## Phase 5 — Documentation and verification

- ☑ **P5.1 / AGG-16 (Medium/High)** — Correct active source KDoc/comments to
  describe still-or-video review, `lastMediaUri` as displayable media,
  TextureView preview, conditional logical YUV/standalone JPEG/RAW capture,
  HEIF+JPEG aspect crop, and offscreen analysis redraw. Update `CLAUDE.md` and
  `docs/ARCHITECTURE.md` to say the analysis target is aspect-matched and the
  complete renderer snapshot is replayed. Do not edit historical snapshots.
  `MediaReview.kt:72-76`, `CameraState.kt:542-543`, `EglCore.kt:11-17`,
  `CameraController.kt:28-34`, `CameraEngine.kt:627-628`,
  `GlPipeline.kt:398-402`, `CLAUDE.md:142-146,287-292`,
  `docs/ARCHITECTURE.md:334-336`.
- ☑ **P5.2** — Run the complete configured gate set in one invocation under
  JDK 21 with existing encrypted release-signing material loaded only into
  process environment variables. Fix every error/warning introduced by this
  cycle, update this plan's progress/evidence and device-pending language, then
  commit/pull-rebase/push each fine-grained slice. No deployment or ADB.

## Deferred findings

None newly deferred. Existing cycle-1/BACKLOG structural, localization,
instrumentation, device-measurement, DNG-threading, dual-encode, asynchronous-REC,
and vendor-audio items were explicitly excluded from the cycle-2 reviewer count
and retain their prior exit criteria. The device confirmations named above are
verification status for implemented fixes, not deferred code fixes.

## Aggregate coverage map

- Scheduled: AGG-1 through AGG-16, exactly once or in an explicitly combined
  plan entry above.
- Deferred: none from this cycle.
- No plan is newly archived: cycle 1 retains live deferred records and is not
  fully done; existing archived plans already carry their own completion banners.

## Progress log

- 2026-07-16 — Prompt 1 complete: 13/13 reports returned; 77 raw sections
  deduped to 16 aggregate findings; assemble/JVM baseline passed; security found
  zero new issues; no device/deployment work.
- 2026-07-16 — Prompt 2 complete: all 16 findings scheduled above with no new
  deferrals; implementation has not started.
- 2026-07-16 — Prompt 3 implementation complete for P1.1–P5.1. Targeted debug
  compilation and JVM suites pass; the final source-only integration review
  found no remaining correctness blocker. Camera2/GL, Back, touch-target, and
  ANR behavior remain PMA110-pending; no deployment or ADB work was performed.
- 2026-07-16 — P5.2 complete under JDK 21: `assembleDebug`,
  `testDebugUnitTest`, `lintDebug`, `lintRelease`, `assembleRelease`, and
  `bundleRelease` passed together in 3m24s (113 tasks; no gate warnings or
  failures). Release signing material was process-only. Deployment remained
  `none`; PMA110 behavior checks remain pending.

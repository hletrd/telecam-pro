# Review-plan-fix cycle 10 — implementation plan

Date: 2026-08-23  
Source: `.context/reviews/_aggregate.md`, cycle 10  
Status: complete; implementation, configured gates, signed commits, and pushes finished

## Repository rules applied

- Preserve PMA110 Camera2/HAL behavior byte-for-byte; generic/removable-camera behavior must derive
  only from enumerated Camera2 capability and remains unvalidated until measured.
- Correctness, privacy/data-loss, lifecycle, and evidence-integrity findings are mandatory and are
  not deferred.
- New user-facing text needs English and Korean resources. This plan does not expect new copy.
- Preserve the quiet Sony Alpha/Xperia Pro viewfinder and accessibility contracts.
- Deployment and device work are disabled for this invocation; no current device serial is supplied.
- Completed plan files remain in place. Moving or deleting them requires explicit destructive-action
  confirmation, so prior complete plans are represented by their existing complete status rather
  than filesystem archival.
- Preserve dependency verification, immutable-release boundaries, exact coverage, GPG signing,
  Conventional Commits with gitmoji, pull-with-rebase before push, and no force push.

No finding is deferred. Every aggregate item AGG10-01 through AGG10-07 is scheduled below.

## Phase 1 — make topology convergence generation- and recorder-owned

- [x] **P1 / AGG10-01:** update route inventory and invalidate caches immediately during REC, but
  defer optics convergence without clearing accepted readiness while a recorder owns the session.
  Replay exactly one latest-wins topology action after recorder teardown. Preserve the existing
  active-route-loss recovery owner.
- [x] Add deterministic policy/production-composition coverage for harmless attach during REC,
  active external removal/replacement, Starting/Recording/teardown windows, and final Ready recovery.
- [x] **P2 / AGG10-02:** carry a definite removal/identity epoch per external ID through the ordered
  availability callback. A removed→available same-ID replacement must invalidate selection,
  characteristics/capabilities, EXIF, size, and route caches even when final ID membership matches.
- [x] Add injected same-ID A→B lifecycle/coalescing tests with different characteristics, plus
  release-during-callback and incomplete-inventory retry coverage.

Acceptance: ordinary availability noise does not reopen the camera, active REC stays Ready and
stoppable, exactly one deferred convergence runs after Stop, and a physically replaced same-ID
external camera cannot inherit stale facts.

## Phase 2 — make deleted-family intent durable, bounded, and teardown-safe

- [x] **P3 / AGG10-03:** persist family deletion intent before acknowledgement in a recovery-visible
  journal. Every late JPEG/HEIF/DNG output and launch recovery must consult it before publication or
  adoption, including when row DISCARD commit and provider deletion both fail.
- [x] Keep unresolved retry ownership process-wide or close Engine teardown behind a producer
  barrier so a late output cannot arrive after the last retry. Enforce a bounded fail-closed
  admission state when unresolved ownership reaches capacity rather than growing memory without a
  ceiling.
- [x] Add persistent-failure tests beyond 32 families and the full release → late output → failed
  discard/delete → replacement Engine recovery trace; prove deleted media remains unpublished.

Acceptance: deletion acknowledgement has a durable veto, unresolved ownership stays bounded, and
process/Engine replacement cannot resurrect a deleted family.

## Phase 3 — impose a process-wide post-native recording-storage bound

- [x] **P4 / AGG10-04:** replace per-Engine independent worker capacity with a process-lifetime
  bounded dispatcher or shared worker-permit owner. Preserve per-Engine callbacks, first-failure
  reporting, durable pending-row overflow, and non-interrupting accepted task completion.
- [x] Add a two-Engine barrier test that blocks all old-generation tasks, releases that Engine,
  constructs a new Engine/dispatcher, and proves total active work never exceeds the documented
  process bound while overflow remains recovery-owned.
- [x] Reconcile `docs/ARCHITECTURE.md` with the exact implemented ownership boundary.

Acceptance: repeated Engine recreation cannot multiply active provider/extractor workers or queued
tails beyond one documented process-wide ceiling.

## Phase 4 — bind harness imports to attested immutable bytes

- [x] **P5 / AGG10-05:** reject symlinks and non-regular executable harness inputs, copy accepted
  harness bytes into a private digest-qualified snapshot, and import/register/execute cases only
  from that snapshot. Attestation must name the snapshot manifest actually executed.
- [x] Add subprocess tests paused between bootstrap and import, source restoration before dispatch,
  symlinked module/package inputs, and snapshot cleanup/failure behavior. Update harness docs to the
  exact guarantee without claiming mutable source bytes were directly executed.

Acceptance: source mutation after manifest capture cannot alter resident executable modules, and no
symlinked or special file can escape the attested manifest.

## Phase 5 — make external EXIF identity truthful

- [x] **P6 / AGG10-06:** carry `CameraRoute` through still ownership and EXIF composition. For
  EXTERNAL, omit host handset Make/Model and host-prefixed LensModel unless trustworthy external
  identity is enumerated; retain only advertised focal/aperture facts. BACK/FRONT and PMA110 bytes
  stay unchanged.
- [x] Add JPEG and HEIF EXIF matrices for BACK, FRONT, and EXTERNAL routes, including blank/partial
  optical capability.

Acceptance: an external-camera image never claims the host handset captured it, while all built-in
camera metadata remains byte-identical.

## Phase 6 — make immutable release evidence match its authorities

- [x] **P7 / AGG10-07:** export lint reports into `$release_root/logs/` from the immutable snapshot
  output and add an output-layout test; keep README, backlog, and operator sheet aligned with that
  real artifact boundary.

Acceptance: the documented immutable logs directory exists after a release build and contains the
lint evidence produced from the snapshotted committed bytes.

## Phase 7 — gates, delivery, and evidence

- [x] Run `./gradlew :app:assembleDebug :app:testDebugUnitTest :app:lintDebug` against the whole repo.
- [x] Run `./gradlew :app:lintRelease :app:assembleRelease :app:bundleRelease` through the
  repository-required immutable release entry point if direct tasks fail closed by design.
- [x] Run repository host/tool/harness/docs verification and `git diff --check` where required by
  project policy.
- [x] Root-fix every gate error and cleanly actionable warning; do not add suppressions.
- [x] Commit independent source/test/docs concerns with fine-grained GPG-signed Conventional Commit
  + gitmoji messages; pull with rebase before every push, then push immediately. Do not deploy.
- [x] Mark every task complete and record exact gate/commit evidence here.

## Planned commit sequence

`fix(camera)`, `test(camera)`, `fix(storage)`, `test(storage)`, `fix(video)`, `test(video)`,
`fix(attestation)`, `test(attestation)`, `fix(metadata)`, `test(metadata)`, `fix(release)`,
`test(release)`, and `docs(plan)`, split further where a commit would mix independent concerns.

## Deferred findings

None.

## Residual warnings

None newly accepted. The AGP experimental-option notice and native-library strip notice are the
existing reviewed toolchain outputs; no cycle-10 code warning was introduced.

## Completion evidence

- Topology inventory and cache truth now update immediately while one latest-wins optics action is
  leased across REC admission, recording, and native teardown. Same-ID removal epochs invalidate
  removable-camera facts, and late callbacks after release are inert. Forty focused topology and
  recording-admission tests passed before the integrated suite.
- Deleted capture families now have a recovery-visible durable journal. Late output publication and
  launch recovery consult it, unresolved URI ownership is capped at 32, and the shutter fails closed
  at saturation. Thirty-five focused deletion/publication/recovery tests passed.
- Post-native recording storage uses one process-lifetime two-worker/eight-backlog owner shared by
  every Engine facade. A two-Engine barrier test proves the cap, callback identity, overflow, and
  non-interrupting accepted completion.
- Device-harness CLI runs from a private digest-qualified regular-file snapshot; symlinks/special
  inputs are rejected and the executed manifest is attested. All 150 harness tests pass.
- EXTERNAL stills omit host Make/Model and host-prefixed LensModel while preserving advertised
  optics; built-in route metadata remains unchanged.
- Immutable release output now copies `lint-results-*` under the documented `logs/` directory.
  Three immutable-wrapper contract tests pass, including lint-only output.
- The first consolidated verifier found six new exact coverage-manifest lines. Four targeted tests
  exercised their real branches without changing thresholds, partitions, suppressions, or the
  reviewed residual manifest. Final Partition A is **99.78% (5,774/5,787)** with the exact existing
  13-line/9-class residual manifest. `GATE_FIXES = 6`.
- Final `python3 tools/verify_host.py` passed from `f4620a6`: debug assembly, the whole JVM/
  Robolectric/Compose suite, debug lint, exact coverage, 23 release/tool tests, nine coverage-tool
  tests, 150 harness tests, 86 documentation checks, Python compilation, and `git diff --check`.
- The repository's direct release gate failed closed as designed, then the exact configured release
  tasks passed through `tools/build_immutable_release.py` at final tested commit `f4620a6`, producing
  signed APK/AAB outputs and immutable lint reports under
  `app/build/immutable-release/f4620a60c33d-c679a24d/`.
- Eight implementation/planning/gate-fix commits through `f4620a6` were GPG-signed, pull-rebased,
  and pushed to `origin/main`. Deployment remained disabled; no physical-device behavior is newly
  claimed.

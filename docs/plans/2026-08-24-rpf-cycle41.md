# Review-plan-fix cycle 41 — implementation plan

Date: 2026-08-24
Source: `.context/reviews/_aggregate.md`, cycle 41
Status: complete; all five findings fixed and every configured gate green

## Repository rules applied

- Preserve measured PMA110 behavior, capability-enumerated multi-device paths, the quiet Sony-style
  UI, EN/KO parity, fail-closed storage durability, and clean-clone authority.
- Schedule every aggregate finding below. Nothing is silently dropped, deferred,
  severity-downgraded, or relabeled as optional.
- Keep completed historical plans as non-destructive archives; do not rewrite their evidence.
- Work only in `/tmp/find-x9-ultra-cycle41.nWoiMj`; never modify, stash, reset, check out, or build
  from the shared dirty `main` checkout.
- Use fine-grained GPG-signed Conventional Commits with gitmoji, pull with rebase before every push,
  and do not deploy because `DEPLOY_MODE=none`.

## P1 — give immediate commands click-only semantics (AGG41-01)

- [x] Replace MR Save/Update's selectable FilterChip contract with a click-only button/chip
  primitive while preserving the compact visual treatment and enabled state.
- [x] Apply the same truthful interaction model to Custom WB measurement; keep any active-mode
  color independent from semantic selection.
- [x] Extend composed-tree regressions to require Button role and click action, reject Selected and
  toggle state, exercise Save/Update and Custom WB, and cover disabled behavior.

## P2 — remove the retired zoom-throttle model (AGG41-02)

- [x] Remove unused time/throttle arguments and write-only state from the moving-gesture decision.
- [x] Align live Engine/ViewModel comments and test names with start edge, zero moving submits,
  quiet landing, and end-edge ownership.
- [x] Retain a direct elapsed-time-independent regression that moving ticks never submit to Camera2.

## P3 — own intentional duplicate-ZIP warnings (AGG41-03)

- [x] Assert each deliberate `zipfile` duplicate-member warning at its fixture construction site,
  without adding a global warning suppression.
- [x] Run the complete tooling suite with warnings promoted to errors and confirm no unowned warning.

## P4 — cover Java production owners in architecture inventory (AGG41-04)

- [x] Add `SharedPreferencesDurableEdit.java` to the storage Module Map with its Boolean durability
  responsibility.
- [x] Make the Module Map completeness check cover Kotlin and Java production source roots.
- [x] Add a negative fixture proving an omitted Java production module fails the documentation gate.

## P5 — correct current guide-alpha authority (AGG41-05)

- [x] Update every live comparison from the retired 0.55 GuideLine alpha to the current 0.40 value
  and correct the 0.30-to-0.35 arithmetic description.
- [x] Add a narrow source/document contract and negative fixture rejecting stale 0.55 GuideLine
  guidance while the executable token remains 0.40.

## Quality gates and delivery

- [x] Run focused Compose semantics, zoom, tooling-warning, architecture, and guide-authority tests
  plus `git diff --check`.
- [x] Run `./gradlew :app:assembleDebug :app:testDebugUnitTest :app:lintDebug` against the whole repo.
- [x] Run `./gradlew :app:lintRelease :app:assembleRelease :app:bundleRelease` against the whole repo.
- [x] Root-fix every gate error and warning without suppression.
- [x] Commit and push each independent review, plan, implementation, test, documentation, and
  closeout change with signed Conventional Commits plus gitmoji, pulling with rebase before push.
- [x] Mark this plan complete only after exact evidence is delivered.

## Deferred findings

None. All five cycle-41 findings are scheduled for this cycle.

## Completion evidence

- All five deduplicated findings were implemented; nothing was deferred. MR writes and Custom WB
  capture now use click-only button semantics with no selected/toggle state, including enabled,
  disabled, action, and active-paint regressions.
- Moving zoom ticks have no time/throttle inputs or write-only timestamp state. Live comments and
  tests now describe the actual start edge, HAL-suppressed moving ticks, quiet landing, and end edge.
- Intentional duplicate-ZIP warnings are locally asserted; all 106 tooling tests passed under
  `python3 -W error` with no unowned warning.
- The architecture map and checker cover Kotlin and Java production owners, with a committed-export
  negative fixture for an omitted Java file. Current GuideLine rationale consistently names 0.40,
  with three negative drift fixtures. `tools/check_docs.py` passed 126 checks with zero failures and
  24 optional-private skips.
- The configured debug gate passed against the whole repository. The configured release lint, APK,
  and AAB gate passed on clean signed source using read-only links to the existing gitignored local
  signing inputs; Android lint reported zero issues. The prebuilt native strip notice is expected
  packaging diagnostics, not an Android-lint warning.
- `python3 tools/verify_host.py` passed after root-fixing one stale coverage partition entry exposed
  by the new composable shape. Partition A remained 8,035/8,050 lines (99.81%) with its exact
  reviewed 15-line residual manifest; tooling, coverage-tool, device-harness, docs, compilation,
  Android build/test/lint, and diff checks were all green.
- Eight fine-grained GPG-signed Conventional Commits with gitmoji were pushed after pull-with-rebase
  before this closeout; this completion record is the ninth. Deployment was not run
  (`DEPLOY_MODE=none`), and no device action was taken.
- Work remained in `/tmp/find-x9-ultra-cycle41.nWoiMj`. The shared dirty `main` checkout was never
  edited, staged, stashed, reset, checked out, or used for a build; its pre-existing CameraEngine,
  MediaStore, UI, test, and tooling deltas remain untouched.

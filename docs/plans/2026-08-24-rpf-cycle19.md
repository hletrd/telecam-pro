# Review-plan-fix cycle 19 — implementation plan

Date: 2026-08-24
Source: `.context/reviews/_aggregate.md`, cycle 19
Status: planned; implementation not started

## Repository rules applied

- Preserve PMA110 Camera2/GL/capture behavior and every measured device workaround. This cycle is
  limited to durable DISCARD ownership/recovery, review-player preparation ownership, release
  evidence validation, tests, and active documentation.
- The correctness/privacy, release-integrity, liveness, performance, and documentation findings are
  mandatory. None is deferred, severity-downgraded, or silently dropped.
- Preserve provider-safe pending rows, exact URI delete authority, process-finite recovery, exact
  native-resource release, and wrapper-only immutable release evidence.
- Completed prior plans remain non-destructive archives in `docs/plans/`; cycle 18 is complete and
  requires no move or deletion.
- Use fine-grained GPG-signed Conventional Commits with gitmoji, pull with rebase before every push,
  never force-push, and never bypass configured gates.

Every aggregate finding AGG19-01 through AGG19-06 is scheduled below. Deferred findings: none.

## Phase 1 — fail closed when DISCARD authority is unavailable

- [ ] **P1 / AGG19-01:** replace `PendingDiscardJournal.contains()`'s Boolean with an explicit lookup
  result that distinguishes PRESENT, ABSENT, and UNAVAILABLE. A database open/query/schema failure
  must never be interpreted as absence.
- [ ] Propagate UNAVAILABLE through `MediaStoreWriter` as a typed retryable recovery failure before
  structural adoption or publication. Preserve legacy SharedPreferences fallback only after an
  authoritative SQLite ABSENT result.
- [ ] Ensure publication cannot report success while an exact DISCARD owner is present or
  unavailable; keep bytes pending and provider-safe until authority is resolved.
- [ ] Replace the fail-open database-upgrade oracle and add an integration test with a SQLite-only
  DISCARD marker, injected lookup failure, structurally valid media, and working provider. Assert no
  `IS_PENDING=0` update, no capture publication, and a retry terminal.

Acceptance: a durable rejection/deletion marker can never be bypassed by journal unavailability,
and recovery cannot resurrect an exact delete-owned row.

## Phase 2 — keep legacy migration work bounded across cleanup failures

- [ ] **P2 / AGG19-02:** split durable legacy-import completion from best-effort removal of migrated
  SharedPreferences keys. Record the import completion in the same SQLite transaction as the
  inserts so cleanup failure cannot trigger re-import on later pages or launches.
- [ ] Make cleanup independently idempotent/retryable without weakening marker durability or
  deleting preference authority before the SQL transaction is durable.
- [ ] Add a persistent-cleanup-failure fixture spanning multiple `page()` calls and assert that the
  full legacy set is imported at most once while each SQL page reads no more than
  `batchLimit + 1` rows.
- [ ] Update `docs/ARCHITECTURE.md` only after the implementation makes the existing bounded-input
  claim true under failure.

Acceptance: journal size cannot multiply per-page input work even when preference cleanup remains
unavailable, and the 120-second recovery deadline is not consumed by repeated import work.

## Phase 3 — bound asynchronous video preparation

- [ ] **P3 / AGG19-03:** add a lifecycle/composition-owned prepare-deadline owner tied to the exact
  `VideoPlaybackHandle`. Arm it immediately before `prepareAsync()` and cancel/retire it on matching
  prepared, error, release, Back, or replacement.
- [ ] On timeout, identity-check the active handle, release player/surface exactly once, clear its
  publication owner, and expose the existing localized retryable timeout state without falsely
  claiming process capacity exhaustion.
- [ ] Add deterministic pure/ownership tests for success-before-deadline, emitted error,
  never-callback timeout, Back-before-timeout, and stale timeout after replacement.

Acceptance: video review cannot remain PREPARING indefinitely, stale timers cannot affect a newer
clip, and every native owner is released exactly once.

## Phase 4 — bind release verification to exact immutable inputs

- [ ] **P4 / AGG19-04:** no-follow snapshot the attestation and checksum sidecar once, hash and parse
  only those captured bytes, and revalidate both source identities before upload-ready success.
- [ ] Add deterministic rename/path-swap and in-place A→B→A mutation tests at the attestation
  hash/parse boundary, matching the AAB/evidence mutation posture.
- [ ] **P5 / AGG19-05:** make `build_immutable_release.py` accept exactly one non-empty direct child
  of `app/build/immutable-release`, matching the upload verifier and documented namespace grammar.
- [ ] Add direct-child success and grandchild rejection tests while preserving the wrapper's
  default unique commit/nonce output.

Acceptance: the attestation bytes authenticated by the sidecar are exactly those parsed and checked,
and every wrapper-success output is structurally admissible to the upload verifier.

## Phase 5 — remove stale release-board recency metadata

- [ ] **P6 / AGG19-06:** remove the manually maintained `Last synced ... cycle` marker from
  `docs/BACKLOG.md` and retain the adjacent `git log` guidance as the durable recency authority.
- [ ] Update architecture/release documentation only where behavior changed, and run the complete
  documentation checker so current claims remain mechanically consistent.

Acceptance: the active release board no longer advertises a stale cycle while current work is
present, and no replacement manual recency marker can drift silently.

## Phase 6 — gates, commits, push, and closeout

- [ ] Run `./gradlew :app:assembleDebug :app:testDebugUnitTest :app:lintDebug` across the repository.
- [ ] Run `./gradlew :app:lintRelease :app:assembleRelease :app:bundleRelease` on committed source.
- [ ] Run focused storage/review/release tests, `python3 tools/verify_host.py`, Python compilation,
  exact Partition-A coverage verification, documentation checks, and diff/whitespace checks.
- [ ] Root-fix every error and actionable warning without suppression. Record any genuinely
  unfixable new warning under the strict deferred format before commit.
- [ ] Commit and push each independent fix with GPG-signed Conventional Commit + gitmoji messages,
  pulling with rebase before every push. Verify signatures and upstream synchronization, mark this
  plan complete, and do not deploy or perform physical-device actions.

## Planned commit sequence

`docs(plan)`, `docs(review)`, `fix(storage)`, `perf(storage)`, `fix(review)`, `fix(provenance)`,
`fix(release)`, `docs(backlog)`, `docs(architecture)`, `test(coverage)`, and `docs(plan)`, split
further when one commit would combine unrelated behavior.

## Deferred findings

None. Existing owner-approved field checks, upstream warnings, and product/design deferrals in
`docs/BACKLOG.md` remain unchanged and are not cycle-19 review findings.

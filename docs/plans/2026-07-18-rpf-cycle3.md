# RPF cycle 3 — implementation plan (2026-07-18)

Source: `.context/reviews/_aggregate.md` (55 deduped entries AGG3-1..55 from 13 lanes, HEAD
c928eac). Every AGG3 entry is scheduled below or recorded in § Deferrals with severity/confidence
preserved. Adjudicated disputes D1 (tap-at-zoom: AOSP post-zoomRatio-FOV semantics — no HAL zoom
term needed; GL-only transforms ARE a bug) and D2 (contrast floor misses are valid) are binding.
Rules honored: GPG-signed fine-grained Conventional Commits with gitmoji, no co-author trailer,
pull-rebase before push, gates green before commit, device verification before claiming camera/GL
fixes done. Deploy serial this session: `127.0.0.1:5599` (loopback proxy — never `adb kill-server`).

Status legend: ☐ todo · ◐ in progress · ☑ done · ⏸ deferred

## P1 — Data loss & hard-crash class (NON-DEFERRABLE, land first)

- ◐ **P1.1 (AGG3-1, Critical, device-reproduced 3/3)** [code landed c40d16c; device verification in progress] Long-exposure still on standalone TELE:
  investigate with request tracing, then fix so the REPEATING preview request never carries the
  full long exposure (bound preview exposure to a cadence-safe cap; the STILL request alone
  carries the user's exposure). Verify on device at 6.3 s S-mode AND a P-mode dark scene: shot
  must start (`ShutterLag: started`), complete, and write a file with the requested exposure in
  EXIF; no `CAMERA_ERROR(3)`. If the HAL also rejects the long STILL request, bisect the real
  ceiling and clamp the shutter ruler truthfully to the device-verified range.
- ☐ **P1.2 (AGG3-2 High data-loss + AGG3-53)** Mid-REC terminal audio failure must degrade to
  video-only, not delete the clip: stop feeding audio, mark track count down, keep draining
  video, finalize + PUBLISH. Extract `shouldStartMuxer(...)` predicate + host tests (the AGG2-6
  case, idempotency, mid-REC degrade case).
- ☐ **P1.3 (AGG3-24 part, data-loss-adjacent)** `VideoRecorder.stop()` idempotency: clear `uri`
  after ownership-checked finalize (second stop must be a no-op, never a delete of the published
  clip); on transient publish failure RETAIN the pending file for the orphan sweep instead of
  deleting a complete recording.
- ☐ **P1.4 (AGG3-29 + AGG3-30)** EGL init failure must land in the preview-health path (no
  uncaught crash on the GL HandlerThread, no leaked EGLDisplay); `GlPipeline.start()` idempotent
  under double-call (guard thread/EGL/analysis-executor creation).

## P2 — Zoom & focus interaction correctness (user focus area 1)

- ☐ **P2.1 (AGG3-10 + AGG3-25 + AGG3-26 + AGG3-51)** One `invalidateZoomGlide()` (pending ratio,
  ease target, ease ticker, quiet-landing, interaction-end, 16 ms flush) called from ALL optics
  remap doors (mode/lens/TC/MR-recall/rollback) AND `onStop`; ViewModel-level wiring tests for
  every door (closes the 10-site duplication).
- ☐ **P2.2 (AGG3-9)** Zoom-OUT first tick submits on the leading edge (no swallow) so OUT matches
  IN responsiveness; keep wide-aim for subsequent mid-gesture ticks.
- ☑ **P2.3 (AGG3-27 + AGG3-52)** [698f699] Still capture snapshots the REQUESTED (coalesced/pending) ratio
  mid-gesture; extend `ZoomSubmitPlan` with `controlsZoomRatio` and pin exact-vs-wide-aim per case.
- ☑ **P2.4 (AGG3-28)** [c11d2c3] Generation-gate `onZoomResult → gl.setHalZoom` (same pattern as other
  per-generation callbacks) so a closing controller's late result cannot clobber the new GL zoom.
- ☐ **P2.5 (AGG3-8)** Sensor fast path carries the live AF-override keys from the cached builder
  instead of refusing wholesale — preserves the tap-AF hold (device-verified in cycle 2) while
  restoring fast-path cadence under held AF-lock + app-side AE. Device-verify BOTH: tapped focus
  holds across ISO/shutter changes AND preview cadence stays smooth in dim light with tap-AF held.
- ☐ **P2.6 (AGG3-21)** `RulerSlider` drag-CANCEL lands the exact final value via the same path as
  drag END.
- ☐ **P2.7 (AGG3-5)** Loupe lifetime: a user-positioned loupe must NOT snap back to center on the
  2 s tap-clear; recompute/reset the stored center when a TC/lens flip changes preview rotation.
- ☐ **P2.8 (AGG3-4, dispute D1 disposition)** Tap mapping accounts for GL-only display
  transforms: map view taps through the ACTIVE loupe crop (center + magnification) before the
  rotation math, for both the AF/metering point and the loupe recenter target. (No HAL-zoom term
  — AOSP post-zoomRatio-FOV semantics per D1.) Unit-test the composed mapping; note the
  zoomComp≠1 transient as accepted (sub-frame, documented).

## P3 — Framing / WYSIWYG (user focus area 2)

- ☐ **P3.1 (AGG3-3)** Unify preview/encoder cover-crop math for the 90° sensor; device-verify by
  frame-extracting a portrait 4K clip vs a same-scene screenshot (framing must match).
- ☐ **P3.2 (AGG-19 closure via CRIT-4 evidence)** Tele-finder gate axis: draw the PIP when
  `punchIn || zoomComp > k || zoomTarget > FINDER_MIN_ZOOM` (k device-tuned), keep default-OFF;
  update the over-selling copy at `CameraState.kt:242`; sync the predicate test.
- ☐ **P3.3 (AGG3-23)** Codec switch normalizes the transfer (HLG/O-Log → a codec-supported value)
  and the OSD reads the normalized value — no WYSIWYG lie in OSD or standby preview.
- ☐ **P3.4 (AGG3-6)** Scopes/zebra/false-color and app-side AE meter scene-referred (pre-LOG,
  pre-assist) luma in the analysis FBO; pin with a host test that toggling LOG/assist does not
  move the metered mean.
- ☑ **P3.5 (AGG3-22, D-17 re-triage)** [aa6cab9] Wrap-aware GyroEis roll smoothing (shortest angular
  distance) so a ±180° boundary sample cannot swing `snapToQuadrant` into a wrong saved-file
  rotation; unit tests at the wrap boundary.

## P4 — Recording / session robustness

- ☐ **P4.1 (AGG3-11)** Scope the REC-admission catch to pre-claim phases; a post-publication
  callback throw must fail through the recorder's own exactly-once path (no phantom recorder, no
  standby-mic restart against a live owner). Host test for the post-publication-throw ordering.
- ☐ **P4.2 (AGG3-12 / QA-2)** Classify the `-38 Function not implemented` stopRepeating failure
  during teardown as benign (contained, logged, never surfaces the user-visible camera-error
  path); device-verify photo↔video flips no longer toast.
- ☐ **P4.3 (AGG3-32)** Monotonic-PTS guard for the first encoder frame (no two frames at PTS 0).
- ◐ **P4.4 (AGG3-39 + AGG3-40)** [id-clear landed 8b54555; ViewModel Thread→io part pending C's
  file] Clear `activeRecordingCaptureId` with `activeRecordingUri`;
  route MediaStore delete/restore Threads to the io executor.
- ☐ **P4.5 (AGG3-18)** MR recall / settings restore applies one packet + at most ONE optics
  transaction (eliminate the 2-3 redundant close/open cycles); keep full sub-owner extraction
  with the AGG2-36 deferral.
- ☐ **P4.6 (AGG3-20, minimal fix for AGG3-19's window)** Controls delivery passes the packet
  snapshot taken UNDER the engine monitor (value, not re-read), closing the ordering-inversion
  window; structural unification stays deferred (see § Deferrals).
- ☐ **P4.7 (AGG2-38 closing test)** Host test: startRecording immediately followed by
  stopRecording on the same executor tick asserts delete-not-publish (retires the deferral per
  the 3-lane resolved-in-code ruling).

## P5 — UI/UX batch

- ☐ **P5.1 (AGG3-13, D2 disposition)** Extend the tested contrast floor to: ChromeIconButton
  top-bar glyphs, review metadata panel, review close ✕, delete icon, zoom badge, center toast,
  histogram/waveform/audio-meter panels — same scrim treatment + host-test pins as 05486cb.
- ☐ **P5.2 (AGG3-14)** 48 dp min touch target for every settings-sheet FilterChip surface
  (SegmentedSelector 30 sites, TransferSelector, PhotoFormatToggles).
- ☐ **P5.3 (AGG3-15)** Fn overlay tiles counter-rotate like every sibling compact-glyph surface.
- ☐ **P5.4 (AGG3-7)** ISO ruler uses the standard 1/3-stop ISO table clamped to device range.
- ☐ **P5.5 (AGG3-33)** AudioInputInspector: empty-capture-device guard + "Auto · Auto" dedupe.
- ☐ **P5.6 (AGG3-34)** Ruler exit animation keeps content composed during shrink.
- ☐ **P5.7 (AGG3-35)** Defensive clamp ordering in the Shoot-tab zoom slider (ProSheet).
- ☐ **P5.8 (AGG3-36)** Mid-REC guard consistency: dim/guard Lens-tab TC+lens rows during REC and
  align `quickFnEnabled` coverage.
- ☐ **P5.9 (AGG3-55 cosmetics)** `videoResolutionLabel` 4:3 heights labeled by real height; unify
  "×" glyph; TC chip "300mm"; MIN-1 comment fix.

## P6 — Performance batch

- ☐ **P6.1 (AGG3-41)** Key the capability projection on the capability-relevant subset only;
  memoize the 4 un-memoized ProSheet sites.
- ☐ **P6.2 (AGG3-42)** infoTicker samples StatFs/BatteryManager off main (io executor → state).
- ☐ **P6.3 (AGG3-43)** Waveform draw uses primitive arrays / batched drawPoints (no boxed
  Offsets per redraw).
- ☑ **P6.4 (AGG3-44)** [0718499] Accelerometer at UI-rate 60 ms period (consumers read ≤10 Hz).
- ☐ **P6.5 (AGG3-45)** Gate the onAnalysis→main AE post to app-side-AE modes only.
- ☐ **P6.6 (AGG3-16)** HEIF+JPEG share one decode/crop/rotate pass per shot (encode twice from
  one intermediate); verify identical outputs vs today on device.
- ☐ **P6.7 (AGG3-31)** Derive the O-Log2 forward/inverse segment boundary from ONE constant
  (fix the forward shadow discontinuity); keep LUT anchors pinned.
- ☐ **P6.8 (AGG3-38 part)** Rename `timelapseScheduler` → `delayScheduler` + document the four
  shared users (capacity note).

## P7 — Tests & documentation

- ☐ **P7.1 (AGG3-54)** Tighten `correction_isClampedToOneStop` to the real 0.30-stop bound.
- ☐ **P7.2 (AGG3-47)** Update the four stale sensor-fast-path doc citations to
  `sensorFastPathAdmitted` + the AF refusal clause (amended by P2.5's change).
- ☐ **P7.3 (AGG3-48)** Add BACKLOG "Deferred from review-plan-fix cycle 2" durable section, fold
  in this cycle's deferral-status updates (AGG-19 closed-by-P3.2, AGG2-38 closed-by-P4.7,
  AGG2-36 escalated, D-17 re-triaged/closed-by-P3.5).
- ☐ **P7.4 (AGG3-49)** ARCHITECTURE module-map "(app root)" split for the 4 mis-placed files.
- ☐ **P7.5 (AGG3-50)** Document the two cycle-2 audio/REC invariants (as amended by P1.2/P4.1).
- ☐ **P7.6 (AGG3-26 doc part / DOC-5)** Sync the door-reset claims with the post-P2.1 truth.
- ☐ **P7.7 (AGG3-55 phrasing)** "530 unit tests in 71 files (host-JVM)" wording; MIN-3/MIN-4
  comment call-outs.
- ☐ **P7.8** Final docs pass: CLAUDE.md hard-won-facts updates for P1.1 (long-exposure preview
  cap), P3.4 (scene-referred metering), QA-3/QA-4 residual field checks recorded in BACKLOG.

## P8 — Security & hygiene

- ☑ **P8.1 (AGG3-24 remainder)** VERIFIED 2026-07-18: only `keystore.properties.example` is
  tracked; `telecampro-upload.jks`, `keystore.properties`, and the GPG-encrypted (mode 600)
  passwords file are all gitignored; git history contains no key material (diff-filter=A scan).
  No user key material touched. Advisory recorded for the cycle report: the keystore lives in a
  synced "flash-shared" directory — consider relocating/encrypting at rest (user decision).
- ☑ **P8.2 (AGG3-46)** [ce77e31] Move the verified-public read-only OPPO Maven credentials to
  `gradle.properties` with a provenance comment (kills secret-scanner noise).

## P9 — Gates, deploy, device verification (per-cycle mandate)

- ☐ **P9.1** Gates green repo-wide: `:app:assembleDebug`, `:app:testDebugUnitTest`,
  `:app:lintDebug`; errors blocking, warnings fixed or recorded as deferrals.
- ☐ **P9.2** Deploy `adb -s 127.0.0.1:5599 install -r ...` + launch; on-device verification:
  real preview, zoom in/out (slider path), TC toggle, tap-to-focus then ISO/shutter change (AF
  HOLDS — P2.5 regression check), photo↔video switches (P4.2: no error toast), still capture,
  **long-shutter still at 6.3 s completes and saves (P1.1 exit check)**, logcat scan for
  FATAL/ANR/CAMERA_ERROR/CAMERA_DISCONNECTED.

## Deferrals (severity/confidence preserved; none are security/correctness/data-loss)

- **AGG3-17 (Medium structural, High confidence — ARCH-1, AGG2-36 escalated)** CameraEngine
  sub-owner extraction (3916-line god object; cycle-2 added state machines INTO it). Reason:
  dedicated large refactor with regression risk across every hot path; not schedulable alongside
  40 fixes in one cycle; no correctness defect identified — the atomicity invariants verified
  clean this cycle. Exit criterion (escalated): schedule a dedicated extraction cycle no later
  than the next cycle that touches engine ownership, starting with `ControlsState` +
  `RecordingAdmission` sub-owners. Repo-policy-bound when picked up.
- **AGG3-19 remainder (Medium, Medium confidence — ARCH-2)** Dual zoom source of truth
  unification (engine field vs controller `controls`). Reason: P4.6 closes the identified
  ordering window; full unification is the same structural surface as AGG3-17. Exit criterion:
  folded into the AGG3-17 extraction cycle (zoom owner extracted first).
- **AGG3-37 (Low-Medium, Medium confidence, risk-needs-manual-validation — DES-9)** RTL locale
  handling of the shooting chrome. Reason: needs a product decision (pin the whole shooting
  chrome LTR like a camera body vs full RTL support) + an RTL-locale device session; no
  functional defect for the shipped LTR locales. Exit criterion: RTL device session with a
  decision record; then either `LocalLayoutDirection` pin or full mirroring audit.
- **AGG2-12(b)(c) (Medium structural, carried)** UI parameter-narrowing + telemetry StateFlow
  split. No new evidence beyond AGG3-41 (scheduled as P6.1). Exit criterion unchanged (device
  recomposition trace after P6.1).
- **AGG2-21 (Low, carried)** Reduced-motion accommodation. No new evidence. Exit criterion
  unchanged (accessibility session/owner decision).
- **AGG2-24 (Low, carried)** Surface.release vs GL detach race. Re-read this cycle: release is
  already gated behind the checked detach signal; no field evidence. Exit criterion unchanged
  (device logs showing Preview EGL failure during rapid detach/reattach).
- **SEC-03 remainder (Low, carried)** OSV/dependency CVE audit. Zero dependency changes this
  range. Exit criterion unchanged: osv-scanner at the next Play-release gate.
- **QA-3 / QA-4 (informational, needs-human)** P-mode brightness-target judgment in a lit room;
  EIS warp-band re-confirmation per the established lit-scene method. Recorded as BACKLOG
  residual field checks (P7.8), not code defects.
- Closed by scheduling this cycle: **AGG-19 → P3.2**, **AGG2-38 → P4.7**, **D-17 → P3.5**.
- Deferred work remains repo-policy-bound when picked up (GPG signing, Conventional Commits +
  gitmoji, no co-author, pull-rebase, latest-stable toolchain, device verification).

## Coverage map (every AGG3 entry disposed exactly once)

AGG3-1→P1.1 · 2→P1.2 · 3→P3.1 · 4→P2.8 · 5→P2.7 · 6→P3.4 · 7→P5.4 · 8→P2.5 · 9→P2.2 ·
10→P2.1 · 11→P4.1 · 12→P4.2 · 13→P5.1 · 14→P5.2 · 15→P5.3 · 16→P6.6 · 17→deferred ·
18→P4.5 · 19→P4.6+deferred · 20→P4.6 · 21→P2.6 · 22→P3.5 · 23→P3.3 · 24→P1.3+P8.1 ·
25→P2.1 · 26→P2.1+P7.6 · 27→P2.3 · 28→P2.4 · 29→P1.4 · 30→P1.4 · 31→P6.7 · 32→P4.3 ·
33→P5.5 · 34→P5.6 · 35→P5.7 · 36→P5.8 · 37→deferred · 38→P6.8 · 39→P4.4 · 40→P4.4 ·
41→P6.1 · 42→P6.2 · 43→P6.3 · 44→P6.4 · 45→P6.5 · 46→P8.2 · 47→P7.2 · 48→P7.3 · 49→P7.4 ·
50→P7.5 · 51→P2.1 · 52→P2.3 · 53→P1.2 · 54→P7.1 · 55→P5.9+P7.7.
Standing-deferral dispositions: AGG-19 closed via P3.2; AGG2-38 closed via P4.7; AGG2-36
escalated-deferred; D-17 closed via P3.5; others carried unchanged.

## Plan archiving

Per repo convention (2026-07-16 cycle-9 plan § Deferral; restated since): completed dated plans
under `docs/plans/` ARE the durable archive and are never moved or deleted. All plans through
`2026-07-17-rpf-cycle2.md` are fully implemented (its P7.3 device record closed cycle-1's P6) and
stand as archived history. This file is the only active plan.

## Progress

- Prompt 1: complete — 13 lanes + `_aggregate.md` (55 deduped findings, 2 disputes adjudicated
  against AOSP canonical docs, QA device session with 2 real defects).
- Prompt 2: complete — this plan; 46 scheduled items across 9 phases, 8 deferral records, 3
  standing deferrals closed by scheduling.
- Prompt 3: pending.

# Review-Plan-Fix Cycle 4 Plan — 2026-07-16

Derived from `.context/reviews/_aggregate.md` (13 current-HEAD specialist
reports; exactly 5 deduplicated findings: 2 High and 3 Medium, all High
confidence). Every aggregate finding is scheduled exactly once below; there are
no newly deferred cycle-4 findings.

Run constraints:

- Deployment mode is `none`: do not connect ADB, install, launch, publish,
  upload, or otherwise modify a device or production environment.
- The nested `ralph` skill named by the workflow is not installed. Implement
  this plan directly with the same fine-grained verify/commit/pull-rebase/push
  loop.
- Commits must be GPG-signed (`-S`), Conventional Commits with gitmoji, have no
  AI co-author trailer, and be pushed after `git pull --rebase`.
- Configured gates are `:app:assembleDebug`, `:app:testDebugUnitTest`,
  `:app:lintDebug`, `:app:lintRelease`, `:app:assembleRelease`, and
  `:app:bundleRelease` under JDK 21.
- No dependency change is required. The version catalog was verified against
  current official stable releases during Prompt 1.
- Camera2/HAL route, first-frame, blackout, REC admission, and microphone
  handoff behavior must be reported as statically/host-gate verified with
  PMA110 verification pending. `CLAUDE.md` forbids claiming device behavior
  from host gates alone.

Status: ☐ todo · ◐ in progress · ☑ done · ⏸ deferred

## Phase 1 — Transaction-owned camera lifecycle

- ☑ **P1.1 / AGG-1 (High/High)** — Replace split ownership-check → acceptance
  → Ready sequences with one synchronized terminal compare-and-commit under the
  same monitor as `beginOpticsTransaction`. At the exact commit boundary,
  validate expected generation, expected controller, and pause state; apply
  owned terminal zoom/override mutations; clear the rollback baseline; install
  ready-controller/Ready state; and capture the exact callback generation.
  Notify external callbacks only after leaving the monitor. Route `openCamera`,
  dual-open, same-camera mode, settings/MR, and lens/TELE completions through
  this primitive. Add a production-used pure/JVM ownership coordinator or state
  seam with deterministic old-g → begin-g+1 → old-commit rejection and
  controller-identity cases. `CameraEngine.kt:101-105,159-175,486-517,
  579-617,648-675,1013-1060,1172-1176`;
  `ReconfigurationGenerationTest.kt`.
- ☑ **P1.2 / AGG-2 (High/High)** — Make cold setup an immutable desired-route
  transaction. Capture the complete desired optics and ownership token, resolve
  selection/caps/sizes into locals, recheck ownership after every blocking
  preflight boundary, publish route fields only as an owned commit, and pass the
  transaction into `openCamera`. When superseded before GL input, retain and
  converge on the latest desired route instead of rolling it back or accepting
  stale selection. Treat a live preview surface with `started == false` as a
  retryable state: resume must re-enter startup, and transient selection/caps
  failure must receive bounded recovery without waiting for surface recreation.
  Add deterministic startup supersession and fail-once/retry reducer/state tests.
  `CameraEngine.kt:327-407,486-517,550-575,625-648,990-1016,1095-1104,
  1935-1955`.

## Phase 2 — Operator truth, route efficiency, and audio ownership

- ☑ **P2.1 / AGG-3 (Medium/High)** — Use `evCompStops(state)` as the final
  signed stop amount in `ExposureMeter`, clamping it once to the displayed
  ±3 EV scale. Put meter composition behind a named pure helper and assert
  positive, negative, zero, and non-third-step inputs so a second raw-index
  multiplication cannot return. `CameraScreen.kt:1292-1304`;
  `ui/controls/ControlCycles.kt:111-120`; `ControlCyclesTest.kt`.
- ☑ **P2.2 / AGG-4 (Medium/High)** — Factor a resolved-optics structural-route
  predicate from route/session facts rather than lens enum identity. A
  non-TELE Photo lens-band recall that retains the same logical camera, mode,
  stream/session contract, live controller, and Ready owner must atomically
  apply recalled controls through the request/zoom fast path. Mode, TELE,
  different-id, missing-controller, and unready cases must still reconfigure.
  Add a pure transition matrix and retain current interactive lens behavior.
  `CameraEngine.kt:625-676,983-1063`;
  `OpticsTransitionPolicyTest.kt`.
- ☑ **P2.3 / AGG-5 (Medium/High)** — Serialize standby microphone ownership.
  Guard wanted/thread/release-latch reservation with one lock and immutable owner
  identity/generation; reserve exactly one owner before starting its thread,
  clear shared state only when that owner exits, and snapshot the owned release
  latch under the same lock before REC handoff. Do not null a potentially live
  owner after a bounded wait. Keep AudioRecord creation/read work outside the
  lock. Add a pure/JVM concurrency seam covering simultaneous admissions,
  identity-safe completion, disable/re-enable, and one visible release owner.
  `CameraEngine.kt:1721-1745,1829-1894,2322-2387`;
  `CameraViewModel.kt:535-539`.

## Phase 3 — Durable documentation and verification

- ☑ **P3.1** — Update active architecture/QA documentation with the accepted
  invariants: optics Ready binds one controller plus generation atomically; cold
  startup is supersedable and retryable; standby metering has exactly one mic
  owner. Record host regression evidence separately from PMA110 checks pending
  under deployment mode `none`. Do not edit dated historical review/spec
  snapshots.
- ☑ **P3.2** — Run targeted JVM tests after each implementation slice, then the
  complete configured gate set together under JDK 21. Fix every introduced
  error/warning, update this plan's progress/evidence, and use fine-grained
  signed commit → `git pull --rebase` → push iterations. Do not deploy or use
  ADB.

## Deferred findings

None newly deferred. Existing cycle-1/BACKLOG structural, localization,
instrumentation, device-measurement, DNG-threading, dual-encode,
asynchronous-REC, signing-environment, and vendor-audio items retain their prior
exit criteria. PMA110 confirmations named above are verification status for the
implemented fixes, not deferred code work.

## Aggregate coverage map

- Scheduled exactly once: AGG-1 through AGG-5.
- Deferred from this cycle: none.
- No plan file is moved: completed dated plans are durable historical records,
  and this repository has no plan-archive convention.

## Progress log

- 2026-07-16 — Prompt 1 complete: 13/13 reports returned; exactly 5 independent
  findings aggregated; debug assembly, 304 JVM tests, and lintDebug passed;
  security/docs found no additional independent issue; no deployment/device
  work.
- 2026-07-16 — Prompt 2 complete: all five findings scheduled above with no new
  deferrals; implementation has not started.
- 2026-07-16 — Prompt 3 implementation complete in six signed fix commits:
  `370e81b` + `c0283dd` (atomic optics readiness/invalidation), `d8f9973`
  (latest-route cold startup), `37a7660` (single-scale exposure display),
  `1044f42` (same-route MR recall fast path), and `c7e8b38` (single-owner
  standby microphone admission). Each slice passed targeted checks and was
  pull-rebased and pushed before the next slice.
- 2026-07-16 — Final JDK 21 host gate passed all six configured tasks together:
  debug/release assembly, 326 JVM tests with zero failures, debug/release lint,
  and release bundle. The combined gate needed no corrective iteration.
  Deployment remained `none`: no ADB, install, launch, upload, or device access
  occurred. Camera2/HAL route, blackout, first-frame, and microphone behavior
  are host/static-regression verified only; PMA110 confirmation remains pending.

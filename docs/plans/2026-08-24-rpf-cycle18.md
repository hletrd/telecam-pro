# Review-plan-fix cycle 18 — implementation plan

Date: 2026-08-24
Source: `.context/reviews/_aggregate.md`, cycle 18
Status: complete; implementation, configured gates, signed commits, and pushes finished

## Repository rules applied

- Preserve PMA110 Camera2/GL/capture behavior and every measured device workaround. This cycle is
  limited to release provenance, process-finite media ownership/recovery, review UI state, durable
  recovery indexing, tests, and active documentation.
- The release-integrity, correctness, liveness, resource-ownership, and accessibility findings are
  mandatory. None is deferred, severity-downgraded, or silently dropped.
- Preserve English/Korean parity, the quiet Sony-style review surface, exact bitmap/native ownership,
  the bounded process-worker topology, and the repository's approved coverage contract.
- Completed prior plans remain non-destructive archives in `docs/plans/`; cycle 17 is already marked
  complete and needs no move or deletion.
- Use fine-grained GPG-signed Conventional Commits with gitmoji, pull with rebase before each push,
  never force-push, and never bypass configured gates.

Every aggregate finding AGG18-01 through AGG18-07 is scheduled below. Deferred findings: none.

## Phase 1 — correct review deadline ownership and user-visible terminal states

- [x] **P1 / AGG18-02:** replace the post-timeout `isCompleted` snapshot in
  `ProgressiveLatestWorkLane` with one atomic terminal operation that either claims an already
  produced completion or wins retirement and exact disposal of any racing late result. Clear the
  latest owner on every terminal and never manufacture `Retired` from an ambiguous nullable await.
- [x] **P2 / AGG18-03:** record whether each request actually entered its blocking `work`. Return a
  distinct retryable active-timeout outcome when a started call exceeds the deadline; reserve
  capacity-exhausted/restart-required for a request that could not start because both lane consumers
  and the shared finite process pool are poisoned.
- [x] Map active timeout to a localized review error with Retry in full-screen review and truthful
  non-sticky thumbnail behavior. Preserve restart guidance only for real acquisition exhaustion.
- [x] Add deterministic tests for completion exactly at the timeout/classification boundary, exact
  publish-or-dispose ownership, `latest` clearing, a single slow active call with spare capacity,
  one blocked consumer plus a healthy replacement, and total lane/process saturation.

Acceptance: a current review request always reaches Completed, Retired by a real superseding owner,
retryable TimedOut, or genuine CapacityExhausted; no bitmap/player/surface is stranded and no active
call with spare capacity tells the operator to restart.

## Phase 2 — make video transport state follow a successful player start

- [x] **P3 / AGG18-06:** replace the optimistic `playing=true` Boolean with explicit Preparing,
  Playing, and Paused UI state. Initialize/reset video review to Preparing, enter Playing only after
  `MediaPlayer.start()` succeeds, and return to a terminal error when prepare/start fails.
- [x] Withhold tap and bottom transport actions while Preparing, expose localized polite preparing
  semantics without adding a viewfinder-style banner, and show Play/Pause state/action only when the
  corresponding operation is available.
- [x] Add pure/state and Compose/debug-host coverage proving Pause/Playing is absent before start,
  an unprepared tap cannot claim success, and successful start publishes state/action atomically.

Acceptance: visible and accessibility transport state never claims playback before a prepared player
has successfully started.

## Phase 3 — bound process-wide launch recovery and durable DISCARD input work

- [x] **P4 / AGG18-04:** add an independent process-owned launch-recovery deadline and explicit
  exhausted state. Deadline expiry must terminally fail all live subscribers, retain the one wedged
  daemon without spawning a replacement, ignore its late return, and give later subscribers the
  same bounded terminal until process restart.
- [x] Add deterministic never-return, Engine replacement, post-snapshot cancellation, late-return,
  subscriber-retention, and one-worker-only tests. Preserve the safe latest-published-family query
  after typed failure.
- [x] **P5 / AGG18-05:** replace SharedPreferences-wide DISCARD scans with a dedicated ordered,
  cursorable SQLite journal using `WHERE uri > ? ORDER BY uri LIMIT 65`. Keep REGISTERED/COMPLETE
  compatibility, migrate legacy DISCARD entries durably before removing their preference keys, and
  make delete success remove the exact database marker.
- [x] Add Robolectric/store tests for durable migration, crash-safe ordering, exact delete retention,
  and at least 10,000 markers while asserting each page reads only `batchLimit + 1` rows. Remove the
  old in-memory global-sort helper once no production or test path relies on it.

Acceptance: a provider call that never returns produces one bounded process terminal without thread
multiplication, and each DISCARD page performs bounded ordered input work regardless of journal size.

## Phase 4 — replace forgeable release self-attestation with an honest artifact boundary

- [x] **P6 / AGG18-01:** remove the unsigned caller-authored authority record and every claim that it
  authenticates same-user wrapper origin. Treat the outer immutable wrapper's private exported
  checkout, exact input seal, post-build identity verification, frozen allowlisted outputs, and
  wrapper-only immutable output namespace as the release-evidence boundary.
- [x] Ensure direct Gradle release outputs cannot be mistaken for immutable evidence: do not accept
  caller-supplied authority/provenance as proof, label direct entry points developer-only, and keep
  the authoritative wrapper as the only command that exports a candidate with commit/tree hashes
  after post-build verification. Preserve signing-input no-follow/seal checks and secret hygiene.
- [x] Replace the omission-only test with an adversarial fully schema-valid forged-record test that
  proves self-attestation grants no immutable evidence, retain mutation-during-wrapper tests, and
  align `CLAUDE.md`, `README.md`, `docs/ARCHITECTURE.md`, and release tooling comments with the real
  trust boundary.

Acceptance: official immutable outputs can only come from wrapper-verified sealed exported bytes;
no public/caller-authored record is described or accepted as process authentication.

## Phase 5 — complete the as-built architecture map

- [x] **P7 / AGG18-07:** add `camera/EngineCallbackSink.kt` and
  `gl/FrameNotificationCoalescer.kt` to the Architecture module map with their callback-drain,
  notification-coalescing, admission, and retirement contracts. Mention coalescing in the GL frame
  journey/threading path.
- [x] Extend `tools/check_docs.py` with a production-module-map completeness check backed by an
  explicit allowlist for intentionally grouped leaf files, then test the guard.
- [x] Update review/recovery/release Architecture wording only after the corresponding code is true.

Acceptance: the active as-built document names every central ownership module and mechanically
rejects omission of future production modules.

## Phase 6 — gates, commits, push, and closeout

- [x] Run `./gradlew :app:assembleDebug :app:testDebugUnitTest :app:lintDebug` across the repository.
- [x] Run the configured `./gradlew :app:lintRelease :app:assembleRelease :app:bundleRelease` gate.
  If the repository's hardened release provenance contract intentionally rejects direct release
  entry points, run the documented immutable wrapper for the same tasks and record both outcomes.
- [x] Run `python3 tools/verify_host.py`, focused tool/release/docs tests, Python compilation, exact
  Partition-A coverage verification, and HEAD-to-working-tree diff/whitespace checks.
- [x] Root-fix every error and actionable warning without suppression. Any genuinely unfixable new
  warning must be recorded under the strict deferred format before commit.
- [x] Commit and push each independent fix with GPG-signed Conventional Commit + gitmoji messages,
  pulling with rebase before each push. Verify signatures and upstream synchronization, then mark
  this plan complete. Do not deploy or perform physical-device actions.

## Planned commit sequence

`docs(plan)`, `docs(review)`, `fix(review)`, `fix(ui)`, `fix(recovery)`, `perf(storage)`,
`fix(provenance)`, `docs(architecture)`, `test(coverage)`, and `docs(plan)`, split further when one
commit would combine unrelated behavior.

## Deferred findings

None. Existing owner-approved field checks, upstream warnings, and product/design deferrals in
`docs/BACKLOG.md` remain unchanged and are not cycle-18 review findings.

## Completion evidence

- The authoritative host gate passed at exact source commit `96ac75f`: **1,795**
  JVM/Robolectric/Compose tests, debug assembly/lint, **56** tool/release tests, **9** coverage-tool
  tests, **182** device-harness self-tests, **110** documentation checks, Python compilation, and
  the complete HEAD-to-working-tree whitespace check.
- Coverage is **55.39% overall (13,719/24,770)** and **99.81% Partition A (6,883/6,896)**. The exact
  approved **13-line / 9-class** residual remains unchanged. The first authoritative run exposed
  five new reachable misses: four recovery deadline/error races and one SQLite upgrade edge. All
  five were covered without suppression or residual-manifest changes, so `GATE_FIXES = 5`.
- The configured clean-HEAD release command passed `:app:lintRelease`, `:app:assembleRelease`, and
  `:app:bundleRelease`. The authoritative immutable wrapper then passed the same tasks from commit
  `96ac75f8ddab`, tree `91a643b47b2a`, and atomically published its receipt-bound output set under
  `app/build/immutable-release/96ac75f8ddab-c37d6b40/`. The receipt records AAB SHA-256
  `d3b98335a498bbc05012fe65944e46933eae175eb0aa86df72b59452ebcf9c71`.
- Eight cycle commits through `96ac75f` were GPG-signed, pull-rebased, pushed, and synchronized with
  `origin/main`. This closeout is the ninth signed/pushed cycle commit. Deployment and physical-device
  actions remained disabled.

## Residual warnings

- **Low / High — `gradle.properties:25-29`:** AGP still labels
  `android.experimental.reportAggregationSupport=true` experimental. This existing warning remains
  necessary for unified coverage registration and is not suppressed. Reopen when AGP stabilizes the
  option or the project retires the aggregate task.
- **Low / High — transitive `libandroidx.graphics.path.so`:** release packaging cannot strip this
  upstream prebuilt. APK/AAB packaging succeeds and app source has no symbol owner to repair. Reopen
  when the stable dependency becomes strip-compatible.

# Review-plan-fix cycle 35 — implementation plan

Date: 2026-08-24
Source: `.context/reviews/_aggregate.md`, cycle 35
Status: in progress

## Repository rules applied

- Preserve measured PMA110 behavior, capability-enumerated multi-device paths, exact Camera2/native
  ownership, durable media truth, quiet Sony-style UI, EN/KO parity, and clean-clone authority.
- Schedule every one of the eight aggregate findings below or record it explicitly under Deferred
  findings. Nothing is silently dropped or severity-downgraded.
- Keep completed historical plans as non-destructive archives in `docs/plans/`; no prior plan is
  moved or deleted.
- Work only in `/tmp/find-x9-ultra-rpf35.0PSzsb`; never touch the shared dirty `main` checkout.
- Use fine-grained GPG-signed Conventional Commits with gitmoji, pull with rebase before every push,
  and do not deploy because `DEPLOY_MODE=none`.

## P1 — close dual-open outgoing ownership exactly (AGG35-01, bounded part of AGG35-08)

- [ ] Extract an Android-free typed cleanup decision/terminal for candidate-slot state × optics
  ownership so callback self-removal cannot erase responsibility for the outgoing controller.
- [ ] On supersession, restore the outgoing owner only when no newer controller occupies the slot;
  otherwise close it exactly. Never overwrite a newer generation's controller.
- [ ] Add a deterministic transition matrix covering candidate current, candidate callback-cleared,
  and newer-controller states across superseded/current paths, proving every outgoing owner is
  restored or released and every candidate is released when required.

## P2 — make capture refusal truthful (AGG35-05, bounded part of AGG35-08)

- [ ] Extract a pure admission-to-status policy so capacity refusal cannot reuse deletion identity.
- [ ] Emit `STILL_CAPTURE_UNAVAILABLE` for a rejected shutter request and preserve actual deletion
  failure statuses on deletion paths.
- [ ] Test the policy and production integration boundary.

## P3 — preserve audio overload truth without root-state churn (AGG35-03)

- [ ] Introduce a shared per-channel overload enum/classifier operating on unquantized producer
  peaks, with exact inclusive near-clipping and signed-PCM clipping thresholds.
- [ ] Keep RMS values quantized for bar geometry, but store only coarse overload states in
  `CameraUiState`; stable RMS/category input must return the existing root state.
- [ ] Update Compose semantics to consume the coarse states and add boundary tests for 31129/31130,
  32703/32704/32766/32767, and -32768 through the production ViewModel reducer.

## P4 — support all EXIF orientation transforms in review (AGG35-04)

- [ ] Extract a pure orientation-to-transform seam covering EXIF 1 through 8, including horizontal,
  vertical, transpose, and transverse cases.
- [ ] Apply the transform to the bounded review bitmap with exact recycle/failure fallback behavior.
- [ ] Add asymmetric geometry/pixel tests covering all orientations and swapped dimensions.

## P5 — restore and strengthen completed-plan evidence checks (AGG35-02, AGG35-06)

- [ ] Parse plan identity as `(date, numeric cycle)` rather than lexicographic path order and reject
  ambiguous completed identities without weakening incomplete-plan handling.
- [ ] Make the negative committed-export fixture discover and mutate the same newest completed plan
  selected by the checker, after proving the exported baseline succeeds.
- [ ] Cover same-date 9/10 and 99/100, later-date lower cycles, incomplete newer plans, and malformed
  or ambiguous completed names; run the full host gate after cycle-35 closeout exists.

## P6 — align governing facts and enforce their contracts (AGG35-07)

- [ ] Update active AGP references to catalog 9.3.2 and active pseudo-ZSL freshness references to the
  executable 400 ms contract, including the CameraEngine hot-path comment.
- [ ] Correct Architecture's nonexistent logical-camera field-check reference without inventing
  device evidence: keep the conservative unmeasured-route limitation explicit but stop claiming the
  exhaustive committed field ledger contains a runnable check that it does not.
- [ ] Extend `tools/check_docs.py` and negative fixtures so catalog AGP, executable ZSL age, and active
  `FIELD_CHECKS` references cannot drift under a green gate.

## Quality gates and delivery

- [ ] Run focused tests for every fix plus `git diff --check`.
- [ ] Run `python3 tools/verify_host.py` against the whole repository.
- [ ] Run `./gradlew :app:assembleDebug :app:testDebugUnitTest :app:lintDebug`.
- [ ] Run `./gradlew :app:lintRelease :app:assembleRelease :app:bundleRelease`.
- [ ] Root-fix every configured gate error and actionable warning without suppression.
- [ ] Commit and push each independent review, plan, implementation, test, documentation, and
  closeout change with signed Conventional Commits plus gitmoji, pulling with rebase before push.
- [ ] Mark this plan complete only after exact evidence is delivered.

## Deferred findings

### AGG35-08 — broad CameraEngine facade decomposition

- **Citation:** `app/src/main/kotlin/me/hletrd/telecampro/camera/CameraEngine.kt:1` (7,647-line facade;
  representative ownership surfaces at lines 846-1050, 1400-1900, 2180-3815, 3992-4780,
  4820-6305, and 6350-6700).
- **Original severity / confidence:** Medium / High.
- **Concrete reason:** the review's two demonstrated responsibility leaks are fully scheduled in P1
  and P2. Decomposing the remaining facade wholesale is a broad architectural rewrite, not a bounded
  corrective change, and would expand native Camera2/GL/recorder risk without a concrete failing
  invariant for each extraction. This defers only the residual structural risk, not AGG35-01 or
  AGG35-05.
- **Exit criterion:** reopen a focused extraction when a new concrete ownership/status defect is
  traced across two or more remaining CameraEngine responsibility regions, or when a planned feature
  must modify three or more of those regions. The future work must preserve all measured HAL/device
  contracts, use identity-owned terminals with exhaustive transition tests, pass the then-current
  host/debug/release gates, and follow signed Conventional Commit/pull-rebase/no-force-push policy.

## Completion evidence

Pending implementation and verification.

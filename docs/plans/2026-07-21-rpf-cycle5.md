# Review-Plan-Fix Cycle 5 Implementation Plan — 2026-07-21

## Scope and immutable boundaries

- Review target: `b142f7d`; aggregate: `.context/reviews/_aggregate.md` with **4 unique new
  findings** from all 13 required roles plus independent XML/PNG reconciliation. All four findings
  concern evidence, tests, or documentation; no current production-code defect was confirmed.
- Device target: OPPO PMA110 at `127.0.0.1:5602` only. Never address port 5600.
- Deployment mode: `none`. A debug APK reinstall/relaunch, HAL-free snapshot run, safe real-app Fn
  inspection, and exact font/orientation restoration are authorized. No capture, REC, media-output,
  true-1x/PIP, concurrent-camera, physical-route, or risky QTI probe is authorized.
- Preserve the current 148 dp held tray and 0.22 scrim, absolute bottom-reachable Fn entry/tray,
  physical 4x2 order under LTR and RTL, sticky quick actions, coherent >=48 dp targets,
  conditional manual rulers, exact Settings tab/close semantics, compact MR disclosure, truthful
  same-stream Loupe Overview, native admission/quarantine, one-shot muxer start, media recovery,
  and standby-microphone ownership.
- The retained Cycle 4 evidence was re-reviewed visually and structurally, not accepted from PASS
  labels alone. The current actual held panel is 518/1440 px (36.0%) at 90/270, Close is 168x168 px,
  and Loupe is a control-clear non-actionable 30% box with 3% side and 14% bottom clearances. The
  103232 real-app report proves only the safe portrait Fn round trip.
- Review files are ignored working evidence. Historical reports, review archives, and dated plans
  remain intact. No historical conclusion will be rewritten or deleted.
- Ralph is unavailable in this session. Prompt 3 uses the declared direct, regression-safe
  fallback: small patches, focused tests, exact full gates, device-safe evidence, and fine-grained
  signed commits.

## Phase 1 — Reconcile review provenance

- [ ] **P1.1 / AGG5-01 (Low/High): distinguish active and archived reviews.** Update
  `.context/README.md` so root `.context/reviews/*.md` files are identified as current-cycle working
  snapshots and `archive-cycle*/` directories as history. Preserve the instruction to revalidate
  any archived finding against current source.
- [ ] **P1.2 / AGG5-01: reconcile the authority marker.** Update `docs/BACKLOG.md` from its stale
  cycle-2 sync marker to Cycle 5 and link the current completed-cycle plan path. Do not alter the
  release board, residual checks, or true-wide deferral.

## Phase 2 — Make every device report self-identifying and self-checking

- [ ] **P2.1 / AGG5-02 (Medium/High): collect immutable run identity.** Extend the CLI runner to
  preserve schema/version/time, selected tiers/filter, approval flags, exact command-safe inputs,
  git HEAD and dirty state, serial/model/API/build identity, host and installed APK paths/hashes,
  and active display metrics. Use structured JSON; do not record secrets or mutable prose claims.
- [ ] **P2.2 / AGG5-02: attest entry and restoration state.** Record pre/post foreground component,
  `font_scale`, `accelerometer_rotation`, and `user_rotation`. A CLI-produced report is non-green if
  post-run state cannot be collected, MainActivity is not foreground, or the three settings differ
  from their entry values. The Cycle 5 device run additionally requires the exact approved final
  values `0.8`, `0`, and `0`.
- [ ] **P2.3 / AGG5-02: bind saved artifacts.** After Markdown/JUnit/evidence creation, generate a
  sorted SHA-256 manifest in `run-attestation.json` and a SHA-256 sidecar for the attestation itself.
  Exclude the attestation and its sidecar from the recursive manifest to avoid a cycle. Document
  the files and integrity boundary in `device-tests/README.md`; do not claim signature-backed
  provenance.
- [ ] **P2.4 tests.** Add host-only fixtures for stable artifact ordering/hashes, exact JSON and
  sidecar output, identity/state serialization, successful restoration, and non-green missing or
  mismatched restoration. Keep framework-only tests independent of a real ADB device and git
  network state.

## Phase 3 — Close both Fn oracle false-green paths

- [ ] **P3.1 / AGG5-03 (Low/High): validate the named Close action.** Apply the same in-screen and
  >=48 dp touch-bound checks used for tiles to `Close function menu`, while retaining its one-node,
  actionable/focusable, and non-full-screen-scrim contracts.
- [ ] **P3.2 / AGG5-04 (Low/High): measure the visible panel.** For held 90/270 layouts, select the
  unique enclosing `android.widget.ScrollView` whose bounds contain all tile and Close actions.
  Use that outer node for raw edge, physical-bottom, and <=40% short-edge depth checks. Report a
  missing or ambiguous panel rather than silently falling back to the child union.
- [ ] **P3.3 negative fixtures.** Add a 30 dp Close fixture that fails independently and an
  oversized parent/ScrollView fixture whose child controls remain compact. Keep passing compact,
  wrong-anchor, physical-order, RTL, overlap, and target-size fixtures. Assert both 90 and 270.
- [ ] **P3.4 production non-change.** Do not alter Compose dimensions, tray typography/padding,
  scrim, Fn ordering, Loupe geometry/copy, or camera behavior. The retained/current UI already
  satisfies the intended contracts; this phase strengthens only the regression evidence.

## Phase 4 — Verification, device evidence, publication, and restoration

- [ ] **V1 focused host tests:** attestation helpers plus Fn layout/oracle fixtures.
- [ ] **V2 exact Gradle gate:**
  `JAVA_HOME=/opt/homebrew/opt/openjdk@21/libexec/openjdk.jdk/Contents/Home ./gradlew :app:assembleDebug :app:testDebugUnitTest :app:lintDebug`.
- [ ] **V3 exact harness gate:** `python3 -m unittest discover -s device-tests/tests`.
- [ ] **V4 exact device entry:** verify only `127.0.0.1:5602`, PMA110/API 36, matching host/install
  APK SHA, MainActivity foreground, font scale `0.8`, accelerometer rotation `0`, and user rotation
  `0`. Reinstall only the rebuilt debug APK if needed.
- [ ] **V5 HAL-free evidence:** run only `debug_snapshot_ui_contract` with destructive approval.
  Require the new report attestation, actual enclosing-panel depth at 0/90/270 and RTL held
  scenarios, physical 4x2 order, bottom reach, >=48 dp controls, exact sticky Gamma, Settings/MR/
  manual-ruler isolation, and truthful control-clear Loupe. Do not open Camera2 in the fixture.
- [ ] **V6 safe real-app sanity:** run only `function_menu_roundtrip` with MainActivity already
  foreground. Confirm the attested portrait tray, sticky actions, explicit Close, and Back
  restoration. Do not press shutter, start REC, inspect output media, open a real Camera2 Loupe, or
  probe any physical route.
- [ ] **V7 exact restoration:** leave MainActivity foreground with `font_scale=0.8`,
  `accelerometer_rotation=0`, and `user_rotation=0`; verify both the report-side post-state and a
  final independent read.
- [ ] **V8 git/publication:** commit each coherent docs/test enhancement with GPG `-S` and a
  Conventional Commit + gitmoji message. Before every push run `git pull --rebase`, resolve without
  discarding user work, and push. Complete this plan in a final signed docs commit. Deployment stays
  `none`.

## Finding disposition

- **Scheduled in this cycle:** AGG5-01, AGG5-02, AGG5-03, and AGG5-04. No new finding is deferred.
- **Not reopened:** release signing/artifact refresh, real Android muxer/Camera2/MediaStore/TalkBack
  coverage, broad CameraEngine/UI extraction, and full operator-landscape redesign remain their
  already documented backlog/manual exits; Cycle 5 does not mislabel them as new findings.

## Mandatory safety deferral

- **D1 — true 1x/wide second-stream finder (High/High, unchanged).** This is not one of Cycle 5's
  four new findings. A truthful true-wide inset requires a second stream or verified vendor
  concurrent-camera capability. The user explicitly prohibits the crash-prone QTI/physical-route
  probe. Exit requires separate explicit authorization and an isolated PMA110 plan proving source
  identity and preview/capture/REC stability. Until then, ship and test only the labelled
  same-stream Loupe Overview; never claim true-1x/PIP evidence.

## Completion evidence

- Pending Prompt 3 implementation, exact gates, attested device-safe reruns, final restoration, and
  pushed signed commits.

# Review-Plan-Fix Cycle 1 Plan — 2026-07-16

Derived from `.context/reviews/_aggregate.md` (13 current-HEAD specialist reports,
127 raw sections, 48 deduplicated findings). Every aggregate finding is scheduled
below or has an explicit deferred record with its original severity/confidence,
exact citation, reason, repo-rule basis, and exit criterion.

Run constraints:

- Deployment mode is `none`: do not connect ADB, install, launch, publish, upload,
  or otherwise modify a device/production environment.
- The nested `ralph` skill named by the workflow is not installed. Implementation
  will therefore use the plan directly, with the same fine-grained verify/commit/
  pull-rebase/push loop.
- Commits must be GPG-signed (`-S`), Conventional Commits with gitmoji, have no
  AI co-author trailer, and be pushed after `git pull --rebase`.
- Configured gates: `:app:assembleDebug`, `:app:testDebugUnitTest`,
  `:app:lintDebug`, `:app:lintRelease`, `:app:assembleRelease`,
  `:app:bundleRelease`.
- Camera/GL/device behavior changed by this plan must be reported as static/gate
  verified with PMA110 verification pending; `CLAUDE.md` forbids claiming a
  camera/GL/orientation fix from host gates alone.

Status: ☐ todo · ◐ in progress · ☑ done · ⏸ deferred

## Phase 1 — Zoom, restore, and capture-state truth

- ☑ **P1.1 / AGG-13 (High/High)** — Restore exact mode/lens/TELE/zoom as one
  engine intent. Add a request-time restore zoom parameter/resolution helper so
  `CameraViewModel.applyLoaded` and MR publish the same clamped controls the engine
  will use. Cover Photo/Video × TELE/non-TELE mapping. Citations:
  `CameraViewModel.kt:230-320,1217-1228`, `CameraEngine.kt:643-701`.
- ☑ **P1.2 / AGG-14+15 (High/High)** — Extract effective TELE bounds and
  hysteretic snap math. Keep incremental input able to escape 30×, clamp hardware
  targets to the applied range, and stop easing on applied no-progress. Add pure
  entry/escape/reversal/60× saturation tests. `CameraViewModel.kt:651-765`.
- ☑ **P1.3 / AGG-16 (High/High)** — Enforce non-empty `PhotoFormats` below the
  composable; normalize an attempted empty set to a valid processed default and fix
  the controller error copy to include JPEG. Add invariant tests.
  `ProControls.kt:580-613`, `CameraViewModel.kt:811-814`,
  `CameraController.kt:753-765`.
- ☑ **P1.4 / AGG-17 (High/High)** — Make DNG state truthful: default/restore to
  off when the active logical session cannot provide RAW, gate the toggle outside
  TELE/eligible standalone capture, add persistent explanatory copy, and retain the
  engine's defensive guard. Update README/store copy. `CameraState.kt:398-418`,
  `ProControls.kt:580-613`, `CameraEngine.kt:901-925`, `README.md:32`,
  `docs/play-store-listing.md:56-58`.
- ☑ **P1.5 / AGG-18 (High/High)** — Couple reticle and controller lifetime: when
  the two-second reticle expires, call through the engine to clear metering/touch AF;
  expose an accessible center-focus/reset action. `CameraViewModel.kt:540-550`,
  `CameraController.kt:669-697,737-748`.
- ☑ **P1.6 / AGG-19 (Medium/High)** — Make MR a complete shooting setup by adding
  timer, drive/interval, peaking level/color, zebra level, false color, histogram,
  waveform, level, and punch-in to `ExtraSettings`, migration-safe save/load,
  `currentExtras`, engine application, and UI state. Add schema/completeness tests.
  `CameraState.kt:86-87,470-488`, `SettingsStore.kt:32-64`,
  `CameraViewModel.kt:328-358`.

## Phase 2 — Still request identity, immutable save state, and shot ownership

- ☑ **P2.1 / AGG-4 (High/High)** — Bind Camera2 callbacks to their own pending
  token and correlate start/result/images by sensor timestamp. Ignore/close stale
  callbacks after watchdog reuse and add pure timestamp/token regression seams.
  `CameraController.kt:753-945`.
- ☑ **P2.2 / AGG-5 (High/High)** — Introduce immutable request-time `ShotSpec`
  (controls, caps/selection/TELE, aspect, quality, orientation, request/shutter time).
  Use it for EXIF and both processed encoders; use capture-result zoom/EV only when
  it belongs to that shot. Pass AEB step controls explicitly.
  `CameraEngine.kt:943-1175,1489-1585`.
- ☑ **P2.3 / AGG-7 (High/High)** — Bound burst/AEB memory by chaining the next
  capture after the current processed save completes/admission is released instead
  of immediately after copying a full frame. `CameraEngine.kt:929-1044`.
- ☑ **P2.4 / AGG-8 (Medium/High)** — Convert ±2 EV through the advertised EV step,
  center on current compensation, clamp/dedupe, and replace the unit-mistake tests
  with 1/2-, 1/3-, 1/6-step, offset, and narrow-range cases.
  `CameraEngine.kt:943-974,1777-1783`, `AeCompAebStepsTest.kt`.
- ☑ **P2.5 / AGG-9 (Medium/High)** — Return the actual `Bitmap.compress` Boolean
  and delete/fail instead of publishing on false. `CameraEngine.kt:1109-1143`.
- ☑ **P2.6 / AGG-6 (High/High)** — Add a testable capture-output tracker that owns
  every displayable/raw URI per capture id and tombstones deleted ids. Delete by the
  frozen review URI/id, delete late arrivals immediately, return resolver results,
  and show success only if all known siblings delete. `CameraViewModel.kt:93-97,
  187-200,1244-1255`, `MediaStoreWriter.kt:88-90`, `CameraScreen.kt:608-620`.
- ☑ **P2.7 / AGG-47 (Medium/Medium)** — Remove the ambiguous stride-only NV21
  shortcut or route it through semantic U/V sampling; extract plain stride conversion
  math and add planar/NV12/NV21-shaped fixtures. Mark PMA110 performance/color
  verification pending. `StillSnapshot.kt:60-105`.

## Phase 3 — Camera generation and recorder lifetime

- ☑ **P3.1 / AGG-10+11 (High/High + Medium/High)** — After dual-open's blocking
  wait, validate `paused`, recorder state, and controller/attempt ownership before
  any preview publish or deferred-session start; close both local devices on abort.
  Treat open-probe refusal as local fallback and identity-guard scheduled recovery.
  `CameraEngine.kt:570-585,723-813,1297-1328`.
- ☑ **P3.2 / AGG-12 (Medium/High)** — Enter camera reconfiguration synchronously
  for mode/lens intent so REC cannot arm against the previous ready session. Recheck
  lifecycle/recorder in queued work and restore Ready on same-session/no-op paths.
  `CameraEngine.kt:366-406,643-813,1180-1250`.
- ☑ **P3.3 / AGG-1 (High/High)** — Give recorder finalization a dedicated executor
  and exactly-once completion/latch; dispatch through a rejection-safe helper, await
  bounded completion in release before GL/executor teardown, and re-evaluate desired
  standby metering after stop. `CameraEngine.kt:33-38,1253-1380,1674-1739`.
- ☑ **P3.4 / AGG-2 (High/High)** — Handle `Handler.post` false/exception in
  controller close and GL encoder/stop; deliver completion exactly once and wait for
  GL teardown before dropping thread ownership. `CameraController.kt:895-935`,
  `GlPipeline.kt:243-269,471-495`.
- ☑ **P3.5 / AGG-3 (High/High)** — Add an exactly-once asynchronous recorder
  failure callback, have the engine own cleanup/finalization, and surface recording
  termination/status to the ViewModel so REC/timer cannot remain phantom-active.
  `VideoRecorder.kt:343-349,505-509`.
- ☑ **P3.6 / AGG-22 (Medium/High)** — Add session operation mode to the fallback
  plan: after vendor `0x80b4` stream attempts, retry `SESSION_REGULAR`. Extend the
  pure fallback tests; PMA110 behavior remains pending. `CameraController.kt:419-442`,
  `SessionFallbackLadderTest.kt`.
- ☑ **P3.7 / AGG-23 (Medium/High)** — Stop manufacturing FPS_30 for empty/
  incompatible advertised caps; return only advertised normal rates or an explicit
  unavailable result and gate selection/recording. Test membership.
  `CameraState.kt:345-364`, `VideoCapabilitiesTest.kt`.
- ☑ **P3.8 / AGG-27+28 (Medium/High)** — Update GL `lastDrawMs` outside the DEBUG
  branch and actually call guarded `startPreview()` when the zoom fast-path submit
  fails. `GlPipeline.kt:272-305`, `CameraController.kt:305-327`.

## Phase 4 — ViewModel lifecycle, review state, and accessibility truth

- ☑ **P4.1 / AGG-24+25+26 (Medium/High, Low/High, Low/High)** — Remove ticker
  arming from init, make `onStart` idempotent/remove-before-post, cancel countdown on
  stop, and retain/unregister/restrict the debug receiver. `CameraViewModel.kt:
  109-163,202-216,1103-1131,1323-1356`.
- ☑ **P4.2 / AGG-20 (High/High)** — Replace nullable review loading with a sealed
  Loading/Ready/Error model for image and video; Error retains Retry, Close, and safe
  Delete. `MediaReview.kt:217-232,397-415,462-481`.
- ☑ **P4.3 / AGG-21 (Medium/High)** — Decode a bounded screen/zoom preview first
  rather than full-resolution then fallback; keep the existing review zoom contract
  while avoiding ~200 MB first allocations. `MediaReview.kt:111-125,213-229`.
- ☑ **P4.4 / AGG-35 (High/High)** — Make Settings/Fn/review accessibility-modal:
  pane titles/traversal groups, meaningful scrims, no dummy click semantic, background
  semantics suppression, and initial/restored focus where Compose supports it without
  changing camera behavior. `CameraScreen.kt:590-621,1102-1183`,
  `ProSheet.kt:148-168`, `MediaReview.kt:252-256`.
- ☑ **P4.5 / AGG-36 (High/High)** — Add polite/assertive live regions to transient
  status/help and stable AF/REC descriptions without announcing telemetry.
  `CameraScreen.kt:443-455`, `ProSheet.kt:210-217`.
- ☑ **P4.6 / AGG-37 (Medium/High)** — Add real value `stateDescription` and actions
  to custom sliders, semantic long-click/help actions, and real disabled semantics to
  no-op Fn chips. `ManualDials.kt:362-466,825-839`, `ProControls.kt:109-166,
  210-223,283-297`.
- ☑ **P4.7 / AGG-38 (High/High)** — Raise HUD scrim opacity/solid backing so small
  text meets 4.5:1 against a white frame; add a pure contrast calculation test and
  record bright-scene visual confirmation as pending. `CameraScreen.kt:1319-1339`,
  `Overlays.kt:346-356`.

## Phase 5 — Toolchain, privacy, and documentation

- ☑ **P5.1 / AGG-32 (Medium/High)** — Upgrade verified latest-stable AGP 9.3.0 and
  Kotlin/Compose plugin 2.4.10, update version tables/comments, and run every gate.
  `gradle/libs.versions.toml:2-4`, `README.md:53-58`,
  `docs/ARCHITECTURE.md:591-595`.
- ☑ **P5.2 / AGG-29 (High/High)** — Mirror accurate shared-storage deletion copy in
  `PRIVACY.md` and `privacy-policy/index.html`: uninstall removes private settings,
  not captures in `DCIM/X9Tele`; users delete captures in app/gallery/files. Update
  both dates. `PRIVACY.md:23-27`, `privacy-policy/index.html:138-143`.
- ☑ **P5.3 / AGG-30+33+34 (Medium/High, Medium/High, Low/High)** — Reconcile
  architecture still/session/thread/state/menu descriptions, require SDK Platform 37
  while distinguishing target/min 36, and replace mutable test counts with a
  source-of-truth statement. `docs/ARCHITECTURE.md:141-165,269-330,481-497,
  542-554,603`, `README.md:71,126`.
- ☑ **P5.4 / AGG-31 (High/High)** — Refresh the ignored local QA runbook to derive
  package/activity, require the current session serial, and use current logical-1×,
  MediaStore, PASM, and nine-tab expectations. This remains intentionally under
  ignored `.claude/`; record the durable behavior source in tracked docs too.
  `.claude/agents/qa-adversary.md:20-24,40-78`.
- ☑ **P5.5** — Update this plan's progress log, gate evidence, deferred cross-links,
  and static/device-verification language after implementation.

## Progress log and evidence

- **2026-07-16 — implementation complete:** all scheduled P1-P5 items were
  implemented and split into signed commits `c3cb291`, `5a3725d`, `df10814`,
  `577200f`, and `5489f33` after the signed planning commit `cd8cf63`; each
  completed commit was rebased against and pushed to `origin/main`. Pure JVM seams
  cover restore/zoom bounds, output invariants and ownership, capture token/timestamp
  correlation, AEB unit conversion, YUV plane packing, session fallback, recorder
  first-failure/completion dispatch, video capability membership, persistence
  completeness, and HUD contrast.
- **Configured host gates — PASS:**
  `:app:assembleDebug`, `:app:testDebugUnitTest`, `:app:lintDebug`,
  `:app:lintRelease`, `:app:assembleRelease`, and `:app:bundleRelease` completed in
  one Gradle invocation under JDK 21. Release passwords were read from the existing
  encrypted local signing material into process-only environment variables; no
  secret was printed or written. An initial bundle attempt without those variables
  failed fast at the repository's signing guard and passed on the credentialed rerun.
- **Deployment/device evidence:** none by explicit directive (`DEPLOY_MODE=none`). No
  ADB connection, install, launch, publication, upload, or production mutation was
  performed. Camera2/HAL/GL/orientation, bright-scene contrast, TalkBack, and PMA110
  behavior remain **PENDING DEVICE VERIFICATION**, not claimed fixed from host gates.
- **Deferred links:** D1-D9 below remain the explicit exit criteria for the aggregate
  findings intentionally outside this cycle; `docs/BACKLOG.md` remains the durable
  home for the structural/test/device follow-ups referenced there.

## Deferred findings

Repo basis: `CLAUDE.md` says device-only camera/GL/orientation checks must remain
pending when not run, and `docs/BACKLOG.md:127-155` is the project's durable
deferred-work mechanism. The items below are not security/data-loss fixes. All
severity/confidence values are preserved, and no destructive file removal is planned.

- ⏸ **D1 / AGG-39 — localization migration (Medium/High).** Citations:
  `app/src/main/res/values/strings.xml:1-4` and 130+ user-facing literals in UI Kotlin.
  Reason: this is a product-wide resource/plural/argument migration, not a localized
  defect in the reviewed camera correctness changes; doing it piecemeal would create
  inconsistent accessibility copy. Exit criterion: before adding any supported locale
  or claiming RTL support, inventory all UI strings, migrate them in one dedicated
  slice, and run pseudo-locale + RTL UI verification.
- ⏸ **D2 / AGG-40 — viewport/font-scale matrix (Low/Medium, manual).** Citation:
  `CameraScreen.kt:648-703`; screenshots cover only 1440×2560. Reason: the user's
  no-deployment directive blocks target-device 1.0×/1.3×/2.0× font/display checks,
  and `CLAUDE.md` requires device evidence for UI claims tied to the PMA110 viewport.
  Exit criterion: an authorized PMA110 session runs the matrix; any clipping produces
  a tracked overflow/layout fix.
- ⏸ **D3 / AGG-41 — Compose instrumentation suite (High/High test gap).** Citation:
  no `app/src/androidTest` tree; affected surfaces `ProSheet.kt:148-218`,
  `CameraScreen.kt:590-621,1102-1183`, `ManualDials.kt:825-839`. Reason: the runtime
  finding is test coverage, not a shipped security/data-loss defect; adding a suite
  that cannot be executed under the explicit no-deployment directive would create an
  unverified gate. Static semantics fixes are scheduled in P4.4-P4.7. Exit criterion:
  device/emulator execution is authorized; add and run modal isolation, focus,
  live-region, slider-value, disabled-action, and font-scale tests.
- ⏸ **D4 / AGG-42 remainder — full engine/ViewModel fake-port seam (High/High test gap).**
  Citations: `CameraViewModel.kt:230-320`, `CameraController.kt:753-945`,
  `CameraEngine.kt:1253-1380`. Reason: narrow pure seams for zoom, AEB, output tracking,
  capture correlation, and session fallback are scheduled now; the remaining injected
  engine/clock/handler rewrite is the already-recorded BACKLOG D-4 structural work and
  would be a big-bang hot-path rewrite. Exit criterion: before CameraUnit/v2 work,
  implement the D-4 coordinator split and fake-clock lifecycle suite.
- ⏸ **D5 / AGG-43 — god object/state/telemetry split (Medium/High).** Citations:
  `CameraEngine.kt:29-1767`, `CameraViewModel.kt:55-1356`, `CameraState.kt:424-540`.
  Reason: already recorded at `docs/BACKLOG.md:144-146` as D-4; concrete invariant
  bugs are fixed in P1-P4 without a risky rewrite. Exit criterion: MUST land before
  CameraUnit/BACKLOG v2 work, exactly as the existing ledger requires.
- ⏸ **D6 / AGG-44 — asynchronous REC arming (Medium/High performance).** Citations:
  `CameraEngine.kt:1180-1250`, `VideoRecorder.kt:91-171`. Reason: existing BACKLOG D-6
  requires rapid REC/hardware-key device verification; current cycle cannot deploy.
  Exit criterion: authorized PMA110 rapid arm/start/stop session, then implement and
  verify explicit `Arming` state off main.
- ⏸ **D7 / AGG-45 — DNG write off Camera2 thread (Medium/High performance).** Citations:
  `CameraEngine.kt:999-1043,1160-1175`, `DngCapture.kt:21-34`. Reason: existing
  BACKLOG D-7 requires owned RAW lifetime/backpressure and BURST+RAW device checks.
  Exit criterion: authorized PMA110 rapid double-shutter/BURST+RAW verification after
  an owned-buffer implementation.
- ⏸ **D8 / AGG-46 — single-transform HEIF+JPEG (Medium/High performance).** Citations:
  `CameraEngine.kt:1016-1028,1052-1157`. Reason: existing BACKLOG D-8 requires both
  container outputs to be visually/file verified on device. P2.3 bounds queue growth
  now. Exit criterion: authorized HEIF+JPEG capture comparison, then share one owned
  transform with independent encoder failure handling.
- ⏸ **D9 / AGG-48 — STANDARD vendor-audio reset (Medium/Medium, manual).** Citation:
  `VideoRecorder.kt:361-386`. Reason: the correct OPPO reset value/scope is not in a
  public contract; inventing a vendor parameter could destabilize verified recording.
  `CLAUDE.md`/BACKLOG require human-ear/HAL evidence for Sound Focus/Stage claims, and
  device work is prohibited this cycle. Exit criterion: obtain/observe the standard
  reset value, verify HAL echo plus Focus→STANDARD off-axis A/B, then send it for every
  recording.

## Aggregate coverage map

- Scheduled: AGG-1 through AGG-38, AGG-42 (narrow seams), and AGG-47.
- Deferred with records above: AGG-39, AGG-40, AGG-41, AGG-42 remainder,
  AGG-43, AGG-44, AGG-45, AGG-46, AGG-48.
- No existing plan is newly archived: older archived plans already carry their own
  banners, while `2026-07-10-rpf-cycle2.md` and BACKLOG retain live deferrals and are
  therefore not fully done.

## Progress log

- 2026-07-16 — Prompt 1 complete: 13/13 reports returned; 127 raw sections deduped
  to 48 aggregate findings; debug baseline green; no deployment/device work.
- 2026-07-16 — Prompt 2 complete: every aggregate finding scheduled or explicitly
  deferred above; implementation not yet started.
- 2026-07-16 — Prompt 3 complete: all scheduled items implemented; configured host
  gates passed; signed implementation commits were rebased and pushed. Device-only
  acceptance remains pending because deployment was explicitly disabled.

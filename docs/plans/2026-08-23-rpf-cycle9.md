# Review-plan-fix cycle 9 — implementation plan

Date: 2026-08-23  
Source: `.context/reviews/_aggregate.md`, cycle-9 current section  
Status: complete; implementation, configured gates, signed commits, and pushes finished

## Repository rules applied

- Preserve PMA110 Camera2/HAL request behavior byte-for-byte; generic-device behavior must derive
  only from enumerated Camera2 capabilities and remains unvalidated until measured.
- Correctness, privacy/data-loss, lifecycle, and evidence-integrity findings are mandatory and are
  not deferred.
- New user-facing text requires English and Korean resources; camera-standard abbreviations remain
  under the documented translation exception.
- Preserve the quiet Sony Alpha/Xperia Pro viewfinder policy and 48 dp interaction targets.
- Deployment and device work are disabled for this run; no current `ANDROID_SERIAL` was supplied.
- Completed plan files remain in place. Moving or deleting them requires explicit destructive-action
  confirmation, so completion is represented by status/check marks instead of file archival.
- Preserve dependency verification, immutable release boundaries, exact coverage policy, GPG
  signing, Conventional Commits with gitmoji, pull-with-rebase before push, and no force push.

No finding is deferred. Every aggregate item AGG9-01 through AGG9-10 is scheduled below.

## Phase 1 — make camera route identity and lifecycle first-class

- [x] **P1 / AGG9-01:** publish explicit active `CameraRoute` through Engine, ViewModel, and UI.
  Keep EXTERNAL zoom lens-local; add truthful English/Korean external identity semantics and compact
  OSD state. Define external capture orientation without coercing it to rear-camera gravity math,
  while preserving PMA110 BACK/FRONT behavior exactly.
- [x] **P2 / AGG9-02:** register an ordered Engine-owned `CameraManager.AvailabilityCallback`,
  invalidate external route/selection/capability caches when the connected ID set changes, and
  converge active/switch routes through generation-owned setup. Unregister on release.
- [x] **P3 / AGG9-03:** resolve settings and MR banks against current route inventory before optics
  transaction. Normalize rear-only axes or reject incompatible banks synchronously; make external-
  only and front-only store/recall truthful in Engine, state, and controls.
- [x] **P4 / AGG9-07:** classify every advertised camera ID as recognized, unknown, or read-failed;
  make unknown/failure incomplete and give inventory a bounded retry independent of open success.
  Publish resolving/proved availability instead of leaving false UNKNOWN truth latched.
- [x] Add pure, Engine/ViewModel, Robolectric, and Compose matrices for external orientation/zoom/
  labels, attach/detach/replacement, incomplete-to-complete convergence, null/unknown facing, hidden
  TELE restoration, and front-only MR store/recall.

Acceptance: active route identity is never inferred from two-value facing; removable routes converge
without process restart; incomplete inventory heals; persisted optics cannot install a hidden or
impossible rear state; PMA110 paths remain unchanged.

## Phase 2 — make deleted-family publication ownership complete

- [x] **P5 / AGG9-04:** move the deleted-family decision to an Engine-owned pre-publication gate used
  by every HEIF/JPEG/DNG outcome. Successful late saves, retained saves, and rejected DNG dispatch
  must all consult the same tombstone before `IS_PENDING` is cleared.
- [x] Preserve a typed durable-discard result. When journal commit and immediate provider deletion
  both fail, retain bounded retry/recovery ownership and surface non-terminal truth instead of
  reporting a completed discard.
- [x] Add lifecycle/provider barrier tests: delete family, detach ViewModel, then resume each sibling
  at publish success, dispatch rejection, and journal+provider failure; prove none can publish or be
  adopted after restart.

Acceptance: every terminal still outcome is owned after UI teardown and a deleted family cannot
resurrect through successful publication, pending-row recovery, or erased discard failure.

## Phase 3 — bind device evidence to one physical target and exact executed harness bytes

- [x] **P6 / AGG9-05:** derive and attest a stable physical-device key after minimal read-only
  reachability, then acquire a nonblocking host-global lock before any device mutation. Keep report
  outputs checkout-local and record both connection alias and canonical identity.
- [x] Test direct+loopback aliases for one device, identical serial from independent report roots,
  and genuinely distinct devices. Reconcile README and completed-plan claims to the actual identity.
- [x] **P7 / AGG9-06:** execute the device harness from an immutable digest-qualified snapshot, or
  bind import-time and pre/post-run manifests so source drift produces a non-green result. Add a
  paused subprocess edit-at-boundary test and make documentation describe the exact guarantee.

Acceptance: no two invocations can mutate one handset through alias/worktree variation, and every
green report cryptographically identifies the harness bytes that actually registered/executed cases.

## Phase 4 — close production-composition test gaps and documentation drift

- [x] **P8 / AGG9-08:** introduce a narrow injectable pending-video allocation/admission seam and
  drive the real `CameraEngine.startRecording` entry through blocked provider, Stop, timeout, pause,
  release, executor rejection, and stale late return. Retain helper tests as the unit layer.
- [x] **P9 / AGG9-09:** render the real `CameraScreen` through a lightweight preview host in Compose
  tests, click the shipping Settings and Fn semantics nodes, and prove an armed timer cannot fire
  behind either modal. Retain direct ViewModel tests separately.
- [x] **P10 / AGG9-10:** narrow the top-level afocal invariant in `CLAUDE.md` and
  `docs/ARCHITECTURE.md` to the main viewfinder and saved media, link the Loupe Overview exception,
  and add a documentation contract rejecting an unqualified all-preview claim.

Acceptance: the cycle-8 recorder and modal fixes are protected through production wiring, and every
active authority states the owner-approved Loupe exception without ambiguity.

## Phase 5 — gates, delivery, and evidence

- [x] Run `./gradlew :app:assembleDebug :app:testDebugUnitTest :app:lintDebug` against the whole repo.
- [x] Run `./gradlew :app:lintRelease :app:assembleRelease :app:bundleRelease` through the repository's
  immutable release wrapper/authoritative release verifier as required by current policy.
- [x] Run the consolidated host/tool/harness/docs checks and `git diff --check`.
- [x] Fix every gate error and every cleanly actionable warning; record only policy-compliant warning
  residuals with severity, evidence, reason, and reopen criterion.
- [x] Commit each independent source/test/docs concern with a fine-grained GPG-signed Conventional
  Commit + gitmoji; `git pull --rebase` before every push and push immediately. Do not deploy.
- [x] Mark every task complete and add exact gate/commit evidence here.

## Planned commit sequence

`fix(camera)`, `test(camera)`, `fix(lifecycle)`, `test(lifecycle)`, `fix(attestation)`,
`test(attestation)`, `test(video)`, `test(ui)`, `docs(contracts)`, and `test(docs)`, split further
when a commit would mix independent concerns.

## Residual warnings

None newly accepted. Existing reviewed Gradle/lint warning dispositions remain governed by the
active backlog and prior completed plan; this cycle must not silently add another warning.

## Completion evidence

- First-class active `CameraRoute` now survives Engine snapshots/rollback, capture and REC ownership,
  ViewModel state, zoom/Fn/OSD, and accessibility. EXTERNAL stays GENERIC and lens-local, uses
  gravity-neutral saved orientation, and has English/Korean identity. Availability events invalidate
  removable-camera caches and converge topology; incomplete inventory retries independently; MR/
  settings restore cannot install hidden TELE or an impossible rear route.
- Deleted capture families now have an Engine-owned check/publish/recheck boundary for every still
  output. Successful late publication, rejected DNG dispatch, callback detachment, and typed durable-
  discard failure retain bounded ownership and cannot silently resurrect deleted media.
- Device runs now lock one canonical physical identity in a host-global namespace across aliases,
  worktrees, and report roots without persisting the raw hardware serial. Green attestation binds the
  harness bytes captured before imports to the unchanged bytes immediately before dispatch and after
  restoration; 147 harness self-tests pass.
- Real production-composition tests exercise CameraScreen Settings/Fn timer cancellation and eight
  CameraEngine recording admission/lifecycle shapes. Retired pre-native rows take durable DISCARD/
  recovery ownership.
- The Loupe Overview exception is explicit in `CLAUDE.md`, `docs/ARCHITECTURE.md`, and the anchored
  field criterion; `tools/check_docs.py` prevents an unqualified all-preview claim.
- The configured debug gate passed in 3m03s. The repository-mandated immutable equivalent of the
  release Gradle gate passed at `bb59761`, then the final consolidated release verifier passed from
  `6243c3e`: **1,641 JVM/Robolectric/Compose tests**, lint/build, **51.02% overall coverage
  (11,539/22,616)**, **99.77% Partition A (5,655/5,668)** with the exact reviewed 13-line/9-class
  residual manifest, 22 tool tests, nine coverage-tool tests, 147 harness tests, 86 documentation
  checks, Python compilation, `git diff --check`, and immutable signed APK/AAB builds.
- The first consolidated run found one exact coverage-manifest error across five newly uncovered
  lines. Five targeted branch/default tests root-fixed it without changing coverage configuration,
  thresholds, or the reviewed residual manifest. `GATE_FIXES = 5`.
- Eleven implementation/gate-fix commits (`e97bf36`, `1fb4059`, `52de1b8`, `5ceeded`, `6a77a47`,
  `94e1b0f`, `3fe1af6`, `070ff98`, `1934644`, `bb59761`, `6243c3e`) are GPG-signed, pull-rebased,
  and pushed to `origin/main`. Deployment remained disabled and no physical-device behavior is newly
  claimed.

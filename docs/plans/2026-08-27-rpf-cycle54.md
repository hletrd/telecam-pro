# Review-plan-fix cycle 54 — implementation plan

Date: 2026-08-27
Source: `.context/reviews/_aggregate.md`, cycle 54
Status: active

## Repository rules applied

- Preserve measured PMA110 behavior, capability-enumerated generic routes, exact native/resource
  ownership, durable MediaStore truth, bounded provider work, the quiet Sony-style UI, EN/KO parity,
  and explicit device-evidence limits.
- Schedule every one of the five aggregate findings below. Nothing is silently dropped,
  severity-downgraded, deferred, relabeled optional, or treated as fixed by documentation alone.
- Keep completed historical plans in `docs/plans/` as the repository's non-destructive archive.
  Existing plans are complete; none needs moving, deleting, rewriting, or reopening.
- Work only in `/tmp/find-x9-ultra-cycle54.7ZqPtj/repo`; never modify, stash, reset, check out, clean,
  or build from the shared dirty `main` checkout.
- Use fine-grained GPG-signed Conventional Commits with gitmoji, pull with rebase before every push,
  and do not deploy because `DEPLOY_MODE=none`.

## P1 — quarantine every uncertain standby AudioRecord terminal (AGG54-01, High/High)

- [ ] Register exact process ownership for every successfully constructed input before validation or
  cleanup, including an uninitialized recorder.
- [ ] Propagate stop/release success as typed truth through the native publication boundary. A
  thrown or otherwise unproven cleanup must atomically quarantine and strongly retain the exact
  input before logical release/process admission can reopen.
- [ ] Add deterministic uninitialized-release-throws and ordinary terminal-release-throws tests;
  prove no second standby/REC native acquisition and no forgotten exact owner.

## P2 — preallocate DNG identity before live-Image Camera2 work (AGG54-02, Medium/High)

- [ ] Add/reuse a process-finite, cancellable pre-capture provider lane for DNG row insert,
  REGISTERED durability, and exact `PendingOutputAllocation` identity capture.
- [ ] Dispatch Camera2 only after the allocation is claimed, carry that immutable packet through the
  request/callback, and keep the live RAW Image interval limited to DNG byte write plus the required
  COMPLETE-marker attempt.
- [ ] Route cancelled, superseded, rejected, late, and allocation-failure results to exact bounded
  cleanup/recovery without inline provider fallback; block identity acquisition in a deterministic
  test and prove Camera2/Image ownership remains prompt and finite.

## P3 — enforce one process-wide debug log budget (AGG54-03, Medium/High)

- [ ] Replace the two-second stable FocusConfidence heartbeat with a quota-safe policy that still
  makes refusal inputs diagnosable during field checks.
- [ ] Change-gate/pace hardware button diagnostics so the approximately-20-Hz zoom slide emits a
  bounded start/change/terminal summary rather than one row per repeat.
- [ ] Extend the ten-minute budget contract across 3A, focus, motion, hardware input, ZSL, startup,
  frame-gap, recovery, and fault reserve. Mutations restoring the old heartbeat/repeat logging must
  fail.

## P4 — bind review metadata and pixels to one frozen source (AGG54-04, Medium/High)

- [ ] Acquire one provenance-bounded immutable compressed still source per review request and derive
  displayed EXIF, orientation, bounds, and pixels from that same identity.
- [ ] Keep MediaStore columns separate but bind them to the request identity; metadata failure must
  omit only unavailable capture facts without reopening mutable bytes or losing a valid review.
- [ ] Add alternating-provider, over-ceiling owner-unverified EXIF, one-content-open, timeout,
  replacement, and disposal tests covering the production descriptor + decode path.

## P5 — retain review-spool capacity until file absence is proven (AGG54-05, Medium/High)

- [ ] Make full and partial spool deletion return typed terminal truth. Release byte accounting only
  after authoritative absence.
- [ ] Retain exact orphan/accounting ownership in a finite process cleanup owner and close new spool
  admission fail-closed if unresolved bytes reach capacity; preserve no-follow private-file rules and
  bounded restart cleanup.
- [ ] Inject delete false/throw, retry success, partial-spool cleanup failure, and repeated failed
  closes to prove on-disk bytes never exceed accounted capacity.

## Quality gates and delivery

- [ ] Run focused tests for standby cleanup quarantine, DNG preallocation/cancellation, process-wide
  diagnostic quota, immutable review metadata, and spool deletion accounting.
- [ ] Run `./gradlew :app:assembleDebug :app:testDebugUnitTest :app:lintDebug` across the whole repo.
- [ ] Run `./gradlew :app:lintRelease :app:assembleRelease :app:bundleRelease` across the whole repo.
- [ ] Run `python3 tools/verify_host.py` because repository authority requires the complete
  non-device host suite for delivery.
- [ ] Root-fix every gate error and warning without suppression; record any authorized unavoidable
  warning under the strict deferred-finding rules.
- [ ] Commit and push each independent implementation, test, documentation, and closeout change with
  signed Conventional Commits plus gitmoji, pulling with rebase before push.
- [ ] Mark this plan complete only after exact evidence is recorded.

## Deferred findings

None. All five cycle-54 findings are scheduled for implementation in this cycle.

# Review-plan-fix cycle 54 — implementation plan

Date: 2026-08-27
Source: `.context/reviews/_aggregate.md`, cycle 54
Status: complete; all five findings fixed and every configured quality gate green

## Repository rules applied

- Preserve measured PMA110 behavior, capability-enumerated generic routes, exact native/resource
  ownership, durable MediaStore truth, bounded provider work, the quiet Sony-style UI, EN/KO parity,
  and explicit device-evidence limits.
- Schedule every one of the five aggregate findings below. Nothing is silently dropped,
  severity-downgraded, deferred, relabeled optional, or treated as fixed by documentation alone.
- Keep completed historical plans in `docs/plans/` as the repository's non-destructive archive.
  Existing plans are complete; none needs moving, deleting, rewriting, or reopening.
- Work only in `/tmp/find-x9-ultra-cycle54.7ZqPtj/repo`; never modify, stash, reset, check out, clean,
  or build from the shared dirty `main` checkout.
- Use fine-grained GPG-signed Conventional Commits with gitmoji, pull with rebase before every push,
  and do not deploy because `DEPLOY_MODE=none`.

## P1 — quarantine every uncertain standby AudioRecord terminal (AGG54-01, High/High)

- [x] Register exact process ownership for every successfully constructed input before validation or
  cleanup, including an uninitialized recorder.
- [x] Propagate stop/release success as typed truth through the native publication boundary. A
  thrown or otherwise unproven cleanup must atomically quarantine and strongly retain the exact
  input before logical release/process admission can reopen.
- [x] Add deterministic uninitialized-release-throws and ordinary terminal-release-throws tests;
  prove no second standby/REC native acquisition and no forgotten exact owner.

## P2 — preallocate DNG identity before live-Image Camera2 work (AGG54-02, Medium/High)

- [x] Add/reuse a process-finite, cancellable pre-capture provider lane for DNG row insert,
  REGISTERED durability, and exact `PendingOutputAllocation` identity capture.
- [x] Dispatch Camera2 only after the allocation is claimed, carry that immutable packet through the
  request/callback, and keep the live RAW Image interval limited to DNG byte write plus the required
  COMPLETE-marker attempt.
- [x] Route cancelled, superseded, rejected, late, and allocation-failure results to exact bounded
  cleanup/recovery without inline provider fallback; block identity acquisition in a deterministic
  test and prove Camera2/Image ownership remains prompt and finite.

## P3 — enforce one process-wide debug log budget (AGG54-03, Medium/High)

- [x] Replace the two-second stable FocusConfidence heartbeat with a quota-safe policy that still
  makes refusal inputs diagnosable during field checks.
- [x] Change-gate/pace hardware button diagnostics so the approximately-20-Hz zoom slide emits a
  bounded start/change/terminal summary rather than one row per repeat.
- [x] Extend the ten-minute budget contract across 3A, focus, motion, hardware input, ZSL, startup,
  frame-gap, recovery, and fault reserve. Mutations restoring the old heartbeat/repeat logging must
  fail.

## P4 — bind review metadata and pixels to one frozen source (AGG54-04, Medium/High)

- [x] Acquire one provenance-bounded immutable compressed still source per review request and derive
  displayed EXIF, orientation, bounds, and pixels from that same identity.
- [x] Keep MediaStore columns separate but bind them to the request identity; metadata failure must
  omit only unavailable capture facts without reopening mutable bytes or losing a valid review.
- [x] Add alternating-provider, over-ceiling owner-unverified EXIF, one-content-open, timeout,
  replacement, and disposal tests covering the production descriptor + decode path.

## P5 — retain review-spool capacity until file absence is proven (AGG54-05, Medium/High)

- [x] Make full and partial spool deletion return typed terminal truth. Release byte accounting only
  after authoritative absence.
- [x] Retain exact orphan/accounting ownership in a finite process cleanup owner and close new spool
  admission fail-closed if unresolved bytes reach capacity; preserve no-follow private-file rules and
  bounded restart cleanup.
- [x] Inject delete false/throw, retry success, partial-spool cleanup failure, and repeated failed
  closes to prove on-disk bytes never exceed accounted capacity.

## Quality gates and delivery

- [x] Run focused tests for standby cleanup quarantine, DNG preallocation/cancellation, process-wide
  diagnostic quota, immutable review metadata, and spool deletion accounting.
- [x] Run `./gradlew :app:assembleDebug :app:testDebugUnitTest :app:lintDebug` across the whole repo.
- [x] Run `./gradlew :app:lintRelease :app:assembleRelease :app:bundleRelease` across the whole repo.
- [x] Run `python3 tools/verify_host.py` because repository authority requires the complete
  non-device host suite for delivery.
- [x] Root-fix every gate error and warning without suppression; record any authorized unavoidable
  warning under the strict deferred-finding rules.
- [x] Commit and push each independent implementation, test, documentation, and closeout change with
  signed Conventional Commits plus gitmoji, pulling with rebase before push.
- [x] Mark this plan complete only after exact evidence is recorded.

## Deferred findings

No review finding is deferred; all five were implemented in this cycle.

One pre-existing gate warning remains explicitly recorded:

- **Low / High — `gradle/verification-metadata.xml:905-909`:** release packaging reports that the
  transitive upstream prebuilt `libandroidx.graphics.path.so` cannot be stripped. APK/AAB packaging
  succeeds, and the repository has no source/symbol owner that can root-fix the binary. Reopen when
  the latest stable `androidx.graphics:graphics-path` dependency becomes strip-compatible. This does
  not defer any cycle-54 security, correctness, or data-loss finding.

## Completion evidence

- All five findings were fixed with no review deferral: uncertain standby mic cleanup now
  quarantines exact ownership; DNG identity is allocated on the finite pre-native provider lane;
  recurring diagnostics share a 180-row process budget with 120 rows reserved for faults; review
  pixels/orientation/displayed EXIF share one bounded source; and failed spool deletion retains exact
  file plus byte accounting until no-follow absence is proven.
- Focused standby/quarantine, DNG blocked-allocation/cancellation, diagnostics, immutable review,
  spool cleanup, and review ownership suites passed. The exact debug gate passed assembly, all 2,198
  JVM/Robolectric/Compose tests, and lint. The exact release gate passed release lint, R8 APK, and
  signed AAB using ignored read-only maintainer signing links; no credential value was printed,
  copied into source, or committed.
- `python3 tools/verify_host.py` passed: debug and androidTest assembly, the complete unit/lint gate,
  Partition A at 9,205/9,221 lines (99.83%) with the existing exact 16-line residual manifest, 136
  tool tests, nine coverage-tool tests, 195 device-harness self-tests, 155 documentation checks (24
  optional-private skips), Python compilation, and `git diff --check`.
- Three gate errors were root-fixed: new executable ownership branches initially drifted the exact
  coverage manifest; the extracted capture settlement invalidated a stale documentation-test shape;
  and one Compose focus test raced the finite review IO worker because Compose idleness does not own
  that dispatcher. No suppression or residual exception was added.
- Ten fine-grained GPG-signed Conventional Commits with gitmoji were pushed before this closeout;
  this completion record is the eleventh cycle commit. Deployment and device action were not run
  (`DEPLOY_MODE=none`). Open field checks remain evidence obligations, not deferred code findings.
- All work ran in `/tmp/find-x9-ultra-cycle54.7ZqPtj/repo`. The shared dirty `main` checkout was never
  edited, staged, stashed, reset, checked out, cleaned, or used for a build; its deltas remain intact.

## 2026-08-27 cycle-55 evidence correction

Cycle 55 found that P2's phrase “prompt and finite” overstated attempt liveness: DNG allocation was
process-finite and lifecycle-cancellable but had no same-route foreground deadline. It also exposed
two integration terminals outside the cycle-54 focused seam: DNG lease release did not always
republish shutter availability, and Camera2 failure/raw-null canceled rather than transferred the
already-reserved cleanup for the preallocated row. Cycle 55 added the exact 8 s deadline, complete
admission publication, and incomplete-row cleanup ownership. This dated note corrects the historical
evidence without rewriting its original record.

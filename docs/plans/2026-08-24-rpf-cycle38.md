# Review-plan-fix cycle 38 — implementation plan

Date: 2026-08-24
Source: `.context/reviews/_aggregate.md`, cycle 38
Status: complete; all four findings fixed and every configured gate green

## Repository rules applied

- Preserve measured PMA110 behavior, capability-enumerated multi-device paths, exact Camera2/native
  ownership, quiet Sony-style UI, EN/KO parity, and clean-clone authority.
- Schedule every one of the four aggregate findings below. Nothing is silently dropped, deferred,
  or severity-downgraded.
- Keep completed historical plans as non-destructive archives in `docs/plans/`; archive the prior
  aggregate as `.context/reviews/_aggregate-cycle37.md` and retain every specialist report.
- Work only in `/private/tmp/find-x9-cycle38.FKvYBP`; never modify, stash, reset, check out, or build
  from the shared dirty `main` checkout.
- Use fine-grained GPG-signed Conventional Commits with gitmoji, pull with rebase before every push,
  and do not deploy because `DEPLOY_MODE=none`.

## P1 — avoid stabilization reconfiguration when the effective HAL mode is unchanged (AGG38-01)

- [x] Resolve the effective Camera2 stabilization mode before and after a label update against the
  current capabilities, and update stored intent without request rebuild or session reopen when both
  labels map to the same HAL value.
- [x] Preserve full request/session reconfiguration for real OFF/ON/PREVIEW_STABILIZATION changes
  and preserve pre-capability restore behavior.
- [x] Add focused pure and Engine-facing regression coverage for same-effective and real-effective
  transitions, including capability-driven Enhanced→Standard and Enhanced→Off normalization.

## P2 — make the shared-pool exhaustion test deterministic (AGG38-02)

- [x] Saturate the four-thread pool with one blocking request on each of four independent lanes so
  no same-lane latest-wins replacement can retire work before its start latch.
- [x] Keep the healthy-lane bounded `CapacityExhausted` assertion and explicit cleanup, then run the
  test repeatedly under host load to prove the missing happens-before edge is closed.

## P3 — retain focal-rail contrast in selected-disabled states (AGG38-03)

- [x] Keep a dark `HudPlate` contrast foundation for selected-disabled focal chips while preserving
  the quiet disabled border/label vocabulary, semantics, touch target, and enabled 3:1 edge.
- [x] Render and measure all selected/enabled combinations over near-white and near-black preview
  fixtures in native Compose coverage.
- [x] Add a dated correction note to the cycle-37 plan's completion evidence clarifying that its
  selected-disabled bright-frame matrix claim was closed by cycle 38; do not rewrite or erase the
  original historical claim.

## P4 — remove the phantom finder bottom-margin seam (AGG38-04)

- [x] Remove `FINDER_BOTTOM_MARGIN` and the unused `finderRect.bottomMargin` parameter, and align
  KDoc/callers with the actual top-anchor plus measured/fractional bottom-clearance laws.
- [x] Replace inert-margin assertions with position-sensitive tests that independently pin side
  margin, top anchor, fractional floor, and measured clearance behavior.

## Quality gates and delivery

- [x] Gate fix: add exact first/second start handshakes to the analogous poisoned setup-lane test
  after the first whole-repo debug gate exposed its scheduler-dependent two-start latch.

- [x] Run focused tests for every fix plus repeated execution of the former flaky test and
  `git diff --check`.
- [x] Run `python3 tools/verify_host.py` against the whole repository.
- [x] Run `./gradlew :app:assembleDebug :app:testDebugUnitTest :app:lintDebug`.
- [x] Run `./gradlew :app:lintRelease :app:assembleRelease :app:bundleRelease`.
- [x] Root-fix every configured gate error and actionable warning without suppression.
- [x] Commit and push each independent review, plan, implementation, test, documentation, and
  closeout change with signed Conventional Commits plus gitmoji, pulling with rebase before push.
- [x] Mark this plan complete only after exact evidence is delivered.

## Deferred findings

None. Every cycle-38 finding was implemented in P1-P4.

## Completion evidence

- All four deduplicated findings were implemented; nothing was deferred. Capability-only
  stabilization normalization now stores the truthful label without rebuilding/reopening an
  unchanged HAL mode, the capacity test uses independent blockers, selected-disabled focal chips
  retain the live-frame contrast floor, and the obsolete finder margin seam is gone.
- Focused stabilization, capacity, native Compose, and finder-geometry tests passed. Both
  scheduler-sensitive capacity regressions passed eight forced reruns after their setup ownership
  was made deterministic.
- The first whole-repository debug gate exposed one analogous two-start race in
  `MediaReviewOwnershipTest`; exact first/second start handshakes root-fixed it without extending a
  timeout or changing production behavior (`GATE_FIXES=1`).
- `python3 tools/verify_host.py` passed debug assembly, androidTest packaging, all 2,015 JVM/
  Robolectric/Compose tests, lint, exact coverage, 99 tool tests, nine coverage-tool tests, 184
  harness tests, 120 documentation checks, Python compilation, and diff check. Partition A is
  8,029/8,044 lines (99.81%) with the exact existing 15-line reviewed residual set.
- `./gradlew :app:assembleDebug :app:testDebugUnitTest :app:lintDebug` passed.
- The clean signed-source `./gradlew :app:lintRelease :app:assembleRelease :app:bundleRelease` gate
  passed with R8. The isolated worktree referenced the repository's existing gitignored signing
  inputs through read-only symlinks; no secret value was copied or printed. The prebuilt native-strip
  notice remains expected packaging diagnostics, not a suppressed error.
- Work remained in `/private/tmp/find-x9-cycle38.FKvYBP`. The shared dirty `main` checkout was never
  edited, staged, stashed, reset, checked out, or used for a build. Deployment was not run
  (`DEPLOY_MODE=none`), and no device action was taken.

### Evidence correction — 2026-08-24, cycle 39

- The cycle-38 stabilization tests directly covered the pure effective-mode predicate, but did not
  exercise `CameraEngine.setVideoStabMode` as the “Engine-facing regression” wording above claimed.
  Cycle 39 closes that evidence gap with `CameraEngineStabilizationTest`, which drives the production
  Engine entry and observes stored intent plus exact stabilization-apply/session-reopen cardinality
  for same-effective and real-effective transitions. This note preserves the original historical
  claim while identifying the later evidence that makes it true.

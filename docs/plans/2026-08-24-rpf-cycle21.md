# Review-plan-fix cycle 21 — implementation plan

Date: 2026-08-24
Source: `.context/reviews/_aggregate.md`, cycle 21
Status: complete; implementation, configured gates, signed commits, and pushes finished

## Repository rules applied

- Preserve PMA110 Camera2/GL/capture behavior and every measured device workaround. This cycle is
  limited to capture-family deletion ownership, discard-journal lock scope, release-verifier source
  identity, their tests, and directly affected active documentation.
- Every aggregate finding AGG21-01 through AGG21-04 is mandatory. None is deferred,
  severity-downgraded, or silently dropped.
- Preserve provider-safe pending rows, exact deletion authority, finite process owners, recovery
  durability, and wrapper-only immutable release evidence.
- Completed prior plans remain non-destructive archives in `docs/plans/`; cycle 20 is complete and
  requires no move or deletion.
- Use fine-grained GPG-signed Conventional Commits with gitmoji, pull with rebase before every push,
  never force-push, and never bypass configured gates.

Every aggregate finding is scheduled below. Deferred findings: none.

## Phase 1 — isolate capture-family retirement by exact family

- [x] **P1 / AGG21-01:** replace the process-global monitor-spanning family retirement transaction
  with an exact-family or bounded-stripe ownership protocol. Provider absence queries and retry
  sleeps must run outside the global preference/capacity monitor.
- [x] Preserve same-family ordering: retirement and re-marking share one exact-family authority, so
  a re-mark arriving while an old absence query is in flight commits after that old retirement and
  survives. Preserve the 64-marker process-wide capacity contract without races or silent eviction.
- [x] Add deterministic tests for blocked family A with durable mark progress for family B and
  same-family re-mark ordering, while preserving the existing producer-active, provider-present,
  query-unavailable, capacity, and commit-retry coverage.
- [x] Update `docs/ARCHITECTURE.md` to describe exact-family synchronization and the rule that no
  provider call or sleep runs under the global family metadata monitor.

Acceptance: one non-returning family absence query cannot delay durable deletion authority for an
unrelated family, and stale retirement can never erase newer deletion intent for the same family.

## Phase 2 — move legacy preference cleanup outside discard database ownership

- [x] **P2 / AGG21-04:** freeze the bounded discard page and cleanup-key set while holding
  `PendingDiscardJournal.databaseLock`, release that monitor, and only then invoke best-effort legacy
  SharedPreferences cleanup.
- [x] Preserve one-time transactional migration, monotonic paging, at-most-65-row reads, retryable
  cleanup on later launches, and the exact-URI publish/delete authority introduced in cycle 20.
- [x] Add a latch-controlled test proving unrelated mark, lookup, remove, and publication lookup
  progress while preference cleanup is blocked, plus cleanup-failure/retry coverage.
- [x] Reconcile `docs/ARCHITECTURE.md` and documentation checks so the existing database-only lock
  claim is executable rather than aspirational.

Acceptance: synchronous legacy preference storage can block only its recovery caller; it cannot
freeze unrelated discard ownership or media publication.

## Phase 3 — make packageable ignored source part of upload identity

- [x] **P3 / AGG21-02:** add NUL-safe enumeration of ignored, untracked inputs under every Android
  packageable source root protected by the release build, at both initial and terminal repository
  boundaries. Prefer one shared root definition or a checker that fails if Gradle and Python drift.
- [x] Reject ignored Kotlin, Java, resources, manifests, native inputs, and other packageable files
  while continuing to allow ignored build outputs, credentials, and immutable artifact namespaces
  outside protected source roots.
- [x] Add initial/terminal checker regressions for ignored Kotlin, resource, and manifest inputs,
  plus a disposable-real-Git NUL-safe scope test. Cover filenames caught by the repository's broad
  secret/password/backup ignore patterns.

Acceptance: `upload-ready` cannot be returned while any ignored untracked input can influence the
release package.

## Phase 4 — make repository checks the terminal release-verifier boundary

- [x] **P4 / AGG21-03:** reorder terminal verification so all source-version, artifact, evidence,
  attestation, and sidecar identity reads complete before the last HEAD, exact porcelain status, and
  ignored-source observations. No external tool or long-running digest may follow those final Git
  observations.
- [x] Add exact late-boundary regressions for a clean HEAD change, dirty tracked/untracked status,
  and ignored packageable source mutation during the current final identity scan.
- [x] Keep the strong operator wording in `docs/play-console-submit.md`: source mutation invalidates
  the verdict. If tests reveal a genuinely unclosable atomic-at-return interval, record and resolve
  the ownership contract explicitly rather than weakening it silently.

Acceptance: every simulated repository mutation before the success return is detected by the final
repository boundary, and the unchanged valid fixture remains upload-ready.

## Phase 5 — gates, commits, push, and closeout

- [x] Run `./gradlew :app:assembleDebug :app:testDebugUnitTest :app:lintDebug` across the repository.
- [x] Run `./gradlew :app:lintRelease :app:assembleRelease :app:bundleRelease` on committed source.
- [x] Run focused storage and release-tool tests, `python3 tools/verify_host.py`, Python compilation,
  exact Partition-A coverage verification, documentation checks, and whitespace checks.
- [x] Root-fix every error and actionable warning without suppression. Record any genuinely
  unfixable new warning under the strict deferred format before commit.
- [x] Commit and push each independent fix with GPG-signed Conventional Commit + gitmoji messages,
  pulling with rebase before every push. Verify signatures and upstream synchronization, mark this
  plan complete, and do not deploy or perform physical-device actions.

## Planned commit sequence

`docs(review)`, `docs(plan)`, `fix(storage)`, `fix(provenance)`, `docs(architecture)`, `test(coverage)`,
and `docs(plan)`, split further when one commit would combine unrelated behavior.

## Deferred findings

None. Existing owner-approved field checks, upstream warnings, and product/design deferrals in
`docs/BACKLOG.md` remain unchanged and are not cycle-21 review findings.

## Completion evidence

- The authoritative host gate passed at exact source commit `139fbf3`: **1,817**
  JVM/Robolectric/Compose tests, debug assembly/lint, **69** tool/release tests, **9** coverage-tool
  tests, **182** device-harness self-tests, **111** documentation checks, Python compilation, and
  the complete HEAD-to-working-tree whitespace check.
- Coverage is **56.20% overall (14,059/25,015)** and **99.82% Partition A (7,028/7,041)**. The
  exact approved **13-line / 9-class** residual remains unchanged.
- The configured clean-HEAD release gate passed `:app:lintRelease`, `:app:assembleRelease`, and
  `:app:bundleRelease` at `cd7f0d2`, including R8, signed APK packaging, and signed AAB packaging.
  This cycle did not create an immutable release candidate or deploy.
- No gate errors or new actionable warnings were found or masked, so `GATE_FIXES = 0`.
- Eight cycle commits through `139fbf3` were GPG-signed, individually pull-rebased, pushed, and
  synchronized with `origin/main`. This closeout is the ninth signed/pushed cycle commit.
- Deployment and physical-device actions remained disabled (`DEPLOY_MODE=none`).

## Residual warnings

- **Low / High — `app/src/main/kotlin/me/hletrd/telecampro/storage/MediaStoreWriter.kt:333,398,444`:**
  the three existing `UseKtx` suggestions remain because these durability gates consume the Boolean
  result of synchronous `SharedPreferences.Editor.commit()`; the KTX `edit(commit = true)` extension
  returns `Unit` and would erase failure truth. Reopen when AndroidX exposes a result-bearing
  synchronous edit or an equally explicit, tested helper preserves the exact Boolean contract.
- **Low / High — `app/build.gradle.kts:566`:** `NotShrinkingResources` remains an existing,
  owner-recorded release-size recommendation. Code minification is enabled; resource shrinking is a
  separate product/release choice. Reopen when the owner elects to enable it and the resulting
  minified resource set receives the repository's release/device validation.
- **Low / High — `gradle.properties:25-29`:** AGP continues to label
  `android.experimental.reportAggregationSupport=true` experimental. It is required for the
  repository's unified coverage registration and is not suppressed. Reopen when AGP stabilizes the
  option or the project retires the aggregate coverage task.

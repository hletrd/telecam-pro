# Review-Plan-Fix Cycle 2 Plan — 2026-07-10

Derived from `.context/reviews/_aggregate.md` (cycle-2 13-agent fan-out + one forwarded
camera-cluster fork report). Rule: every aggregate finding is **scheduled** below or **deferred with
recorded reason, original severity/confidence, and an exit criterion** — nothing silently dropped.
Work follows repo policy (CLAUDE.md + global git rules): GPG-signed `-S` commits, Conventional
Commits + gitmoji, no `Co-Authored-By`, fine-grained one-concern commits, `git pull --rebase` before
every push.

Gate objective (all three must stay green after every commit):
`:app:assembleDebug` · `:app:testDebugUnitTest` · `:app:lintDebug` (cycle-2 baseline: all green,
109/109 tests, zero lint issues debug AND release — qa-adversary verified with fresh
`--rerun-tasks`, on device this time).

**Device constraint:** the PMA110 WAS reachable this session (proxied wireless ADB
`127.0.0.1:5599`) and qa-adversary completed a full on-device pass. Camera/GL/orientation-affecting
items below carry `[device-verify]`; if the proxy is still up at implementation time, re-verify the
touched flows on hardware (launch → preview, capture pair, record start/stop, resume); if it has
dropped, mark those items `[device-verify pending]` per CLAUDE.md and say so in the commit body.
Any code change invalidates the recorded release AAB hash — `docs/play-console-submit.md` already
mandates a fresh release gate before upload.

**Plan archive status:** `2026-07-02-review-fixes.md` and `2026-07-03-cycle1-fixes.md` are archived.
`2026-07-10-rpf-cycle1.md` is fully executed per its own Progress Log but was missing the archive
banner (aggregate AGG-27) — the banner is added as part of this cycle (P6.5). This file becomes the
active plan and gets archived the same way when done.

Status: ☐ todo · ◐ in progress · ☑ done (gates green) · ⏸ deferred
**→ See the Progress log at the bottom for the authoritative completion state.**

---

## Phase 1 — Correctness & input-safety (highest severity first)

- ☑ **P1.1 / AGG-1** (High/High, critic M1; + critic N10 fold-in) — Gate hardware keys on the review
  overlay: hoist `reviewOpen` into `CameraUiState` (set via a new `CameraActions.onReviewOpenChange`
  from `CameraScreen`), early-return in `MainActivity.onKeyDown/onKeyUp/dispatchKeyEvent` when
  review is open; freeze the reviewed `uri` for the overlay's lifetime so a background capture can't
  swap it; stop consuming shutter keys on the PermissionGate screen (act only when the camera
  surface is the foreground content). `MainActivity.kt:160-173`, `CameraScreen.kt:470,501-511`,
  `CameraViewModel.kt:876-888`. [device-verify: volume key during review]
- ☑ **P1.2 / AGG-9** (Med-High/High, debugger N2) — `CameraViewModel.onVideoStabMode`
  (`CameraViewModel.kt:776-781`): add the same `rejectIfRecording("Stop REC first")` guard every
  other session-reconfiguring control has.
- ☑ **P1.3 / AGG-20** (Medium/Medium, tracer NEW-2) — `VideoRecorder.stop()`
  (`VideoRecorder.kt:192`): move `runCatching { audioRecord?.release() }` inside the
  `if (!drainWedged)` block, accepting the same deliberate leak-on-wedge the codec/muxer already get.
- ☑ **P1.4 / AGG-37** (Low-Med/Medium, debugger N5) — `CameraController.close()`
  (`CameraController.kt:728-764`): wrap the teardown `handler.post {}` in `runCatching` (the file's
  own documented dead-thread quirk), and make the `closed` check-then-act atomic (e.g.
  `synchronized`/`AtomicBoolean.compareAndSet`).
- ☑ **P1.5 / AGG-39** (Low-Med/Medium, debugger N7) — zoom NaN loop: clamp `zoomRatio` at
  `SettingsStore` load (`SettingsStore.kt:154`, `.coerceIn(0.1f, 100f)` defensive) AND guard
  `zoomEaseTicker` (`CameraViewModel.kt:600-615`) against non-finite/non-positive `cur`/`next`.
- ☑ **P1.6 / AGG-21** (Medium/Medium, debugger F4 + code-reviewer L8) — `CameraEngine.kt:120-177`:
  in the `setupExecutor` continuation, re-validate the surface (`previewSurface !== surface →
  return@execute`) before `gl.start`/`gl.setPreviewOutput`, so a destroy+recreate during the startup
  window can't bind a released Surface. [device-verify: cold start + rapid background]
- ☑ **P1.7 / AGG-38** (Low-Med/High, debugger N6) — `ManualControls.kt:161-173,284-298`: restructure
  `applyFocus`/`applyFlash`'s AE-on branch so the `when` is expression-position (compiler-enforced
  exhaustiveness).
- ☑ **P1.8 / AGG-41(part)** (Low, code-reviewer) — `AutoExposure.kt:106,128`: guard
  `slowCapNs.coerceAtLeast(expMinNs)` against a degenerate exposure range (mirror
  `manualAebExposuresNs`'s guard); add a boundary test.
- ☑ **P1.9 / AGG-12** (Med-High/High dormant, test-engineer #4) — extract
  `meteringRect(activeL,activeT,activeR,activeB,cx,cy,fraction)` from
  `CameraController.applyMetering` (`CameraController.kt:557-586`) with a defensive fraction clamp;
  unit-test center/edge/fraction≈1.0.
- ☑ **P1.10 / AGG-22** (Medium/Medium, debugger N3 + code-reviewer L4/M2) — `MediaReview.kt`:
  identity-guard `playerRef` release (`onSurfaceTextureDestroyed` releases only its own player);
  merge the two sibling `pointerInput` handlers into one `awaitEachGesture` (mirroring
  `CameraScreen.kt:194-233`) so video tap-to-pause isn't starved; bound the video poster decode
  (`loadVideoFrame` maxDim) / skip it when `videoInfo` is known. [device-verify: review a clip, tap
  to pause, capture-during-review]
- ☑ **P1.11 / AGG-34** (Low-Med/High, tracer NEW-1) — `CameraViewModel.kt:547,557,565,578,579,580,
  584,633`: make all 8 persisted-field setters (antibanding, exposure step, AF spot, edge, NR, color
  effect, flash, JPEG quality) schedule the debounced save (persist-only path in `updateControls`).
- ☑ **P1.12 / AGG-18** (Medium/High, critic M2) — persist `videoResolution`: add to `ExtraSettings`
  (`"WxH"` string), save in `onVideoResolution`, restore in `applyLoaded` validated against
  `caps.availableVideoSizes`/`openGateVideoSizes` (fall back to `chooseVideoSize`), include in MR
  presets.

## Phase 2 — Threading & lifecycle

- ☑ **P2.1 / AGG-7** (Med-High/High, verifier #1 + architect A1/A2 + camera-cluster A8) — serialize
  the last unserialized open: dispatch `CameraEngine.resume()`'s body onto `setupExecutor`
  (`CameraEngine.kt:988-994`), matching the other three call sites. Also post `pause()`'s
  `controller?.close()` (`:983`) to `setupExecutor` (perf P-4) so onStop never blocks main up to
  1.5 s; setupExecutor ordering keeps close-before-reopen. [device-verify: background/resume cycle,
  lens switch + immediate Home]
- ☑ **P2.2 / AGG-8** (Med-High/Medium, debugger N1) — make encoder teardown ordered: give
  `GlPipeline.setEncoderOutput` a completion callback (posted on the GL thread; run inline if the
  thread is gone) and dispatch `finishRecording` from it in `stopRecording()`/`pause()`
  (`CameraEngine.kt:921-937,961-985`). [device-verify: record 4K with scope on, tap Stop]
- ☑ **P2.3 / AGG-35** (Low-Med/Medium, verifier #3 + tracer OPEN-2) — standby-meter stop
  confirmation: have the meter thread signal a latch AFTER `AudioRecord.release()`; `startRecording`
  awaits it (bounded ~400 ms) instead of the bare `join(150)`; proceed-on-timeout stays (documented).
- ☑ **P2.4 / AGG-36** (Low-Med/Medium, debugger N4) — `GyroEis.reset()`: stop zeroing
  `rollDegrees`/`stableOrientation` (hold-last-confident-value must survive pause/resume); keep
  zeroing only the integrator fields. [device-verify pending: flat-resume capture orientation]
- ☑ **P2.5 / AGG-40** (Low/High, debugger N8 + perf P-10) — pause `levelTicker`/`orientationTicker`/
  `infoTicker` in `onStop()`, re-arm in `onStart()`.
- ☑ **P2.6 / AGG-10** (Med-High/Medium, tracer OPEN-1) — `CameraController.kt:53`: on
  `handler.post` failure in the openCamera executor, run the callback inline
  (`runCatching { handler.post(cmd) }.onFailure { cmd.run() }`) — every StateCallback path checks
  `closed` first, so a late `onOpened` then closes the leaked device on the binder thread. Comment
  the reasoning. [device-verify pending: keyguard cold-open + swipe-kill repro is timing-dependent]
- ☑ **P2.7 / AGG-43(part)** (Low, perf P-14 + debugger + feature-dev, 3-agent) — mark
  `VideoRecorder.muxerStarted` `@Volatile` (`VideoRecorder.kt:60`).
- ☑ **P2.8 / architect N3** (Med-Low/High) — `CameraEngine.release()` (`:1012-1026`): document the
  accepted synchronous teardown (process-teardown context) with a comment per architect's option (a);
  no behavior change this cycle.

## Phase 3 — Performance quick wins (mechanism-confirmed, low-risk slices)

- ☑ **P3.1 / AGG-4 slice** (High mechanism, perf P-2 + code-reviewer, 3-agent) — publish
  `histogramData`/`waveformData` into `CameraUiState` only when a consumer is live
  (`histogram || waveform || exposureMode == MANUAL`); AE keeps reading the callback arg
  (`CameraViewModel.kt:143-148`).
- ☑ **P3.2 / AGG-3 slice** (High mechanism, perf P-1) — reorder `GlPipeline.drawFrame` so the
  analysis readback runs AFTER the encoder draw+swap (`GlPipeline.kt:300-317`). The downsampled-FBO
  readback is deferred (D-5). [device-verify: recording with scopes on]
- ☑ **P3.3 / AGG-23** (Medium/High, code-reviewer M1 + perf P-5) — resolve `overrideIdForFocal`
  inside the `setupExecutor` dispatch instead of on the caller's thread (`CameraEngine.kt:532-579`);
  cold-start restore and every lens tap stop paying main-thread Binder IPC bursts.
  [device-verify: lens taps still switch correctly]
- ☑ **P3.4 / AGG-41(part)** (Low/High) — unify RMS normalization on 32768 (`CameraEngine.kt:1164`).
- ☑ **P3.5 / AGG-41(part)** (Low) — `onVideoFrameRate` (and any other direct `engine.setControls`
  site) must clear/refresh `pendingControls` so a trailing throttled apply can't revert fps
  (`CameraViewModel.kt:744-753,1038-1056`).
- ☑ **P3.6 / AGG-41(part)** (Low) — bound the `runAudio` stop-path spin: when `!running` and no
  input buffer, use a blocking `dequeueInputBuffer(TIMEOUT_US)`/bounded retry before bailing
  (`VideoRecorder.kt:401-457`).
- ☑ **P3.7 / AGG-43(part)** (Low/High, perf P-13) — replace `LaunchedEffect(state.controls.zoomRatio)`
  with `snapshotFlow`+`collectLatest` for the zoom-HUD auto-hide (`CameraScreen.kt:176-184`).
- ☑ **P3.8 / AGG-41(part)** (Low, code-reviewer) — route Fn-slot editor Up/Down/Add/Remove and
  similar low-stakes toggles through `scheduleSettingsSave` instead of per-tap synchronous commit
  (`CameraViewModel.kt:969-985`); keep immediate commit for mode/lens/remember.
- ☑ **P3.9 / AGG-43(part)** (Low/High, perf P-11) — skip the RMS loop at unity gain when the level
  emit is throttled anyway (`VideoRecorder.kt:407-466`).
- ☑ **P3.10 / AGG-42 N3** (Low/Medium, critic) — orphan sweep: skip pending entries newer than
  process start so a launch-window capture can't be swept (`MediaStoreWriter.cleanupOrphanedPending`).

## Phase 4 — UX (designer findings; Sony-reference + UX_POLICY compliant)

- ☑ **P4.1 / AGG-5** (High/High, designer F3, carried D4) — surface `CONTROL_AF_STATE` from
  `CameraController.kt:495-518` through the engine into `CameraUiState` (searching/locked/failed);
  color `FocusReticle` accordingly (`Overlays.kt:214-237`). [device-verify: tap-AF converge + fail]
- ☑ **P4.2 / AGG-6** (High/Medium, designer F1) — `StatusBar` row: add
  `horizontalScroll(rememberScrollState())` + drop the debug camera-id tag from the row's budget
  (`Overlays.kt:300-406`).
- ☑ **P4.3 / AGG-16** (Med-High/High, designer F4) — 48 dp minimum hit targets: `DialChip`
  (`ManualDials.kt:408-454`), `MemoryRecallStrip` slots (`CameraScreen.kt:968-993`),
  `MiniTextButton` (`ProSheet.kt:1096-1109`) — TeleChip-style outer-box pattern to keep visuals.
- ☑ **P4.4 / AGG-32 F6** (Medium/High) — apply glyph counter-rotation to `ExposureMeter`
  (`CameraScreen.kt:1120-1165`), the half-press action label (`:312-326`), and `TimerCountdown`
  (`Overlays.kt:508-518`; use `rotateLayout` for the big digit).
- ☑ **P4.5 / AGG-32 F7** (Medium) — give `ModeLabel` the standard HUD scrim
  (`CameraScreen.kt:1214-1243`).
- ☑ **P4.6 / AGG-32 F5** (Medium/High) — add `cameraReady`/`cameraError` to `CameraUiState`; dim the
  shutter/record buttons while not ready; persistent compact OSD tag (not a banner) while camera is
  down; emit a status when `scheduleCameraRecovery` exhausts retries (`CameraEngine.kt:481-490`).
- ☑ **P4.7 / AGG-32 F11** (Medium/High) — cold-launch feedback: show "Starting camera…" via the
  existing status mechanism from `onPreviewSurfaceAvailable` until the session is configured
  (cleared on ready), policy-compliant (OSD status, no banner).
- ☑ **P4.8 / AGG-32 F8** (Medium/High) — restyle `PermissionGate` with the app palette (primary CTA
  weight > "Privacy") and tint the delete dialog's confirm with the app's delete-red
  (`MainActivity.kt:282-314`, `MediaReview.kt:506-521`).
- ☑ **P4.9 / AGG-45 F9** (Low/Medium) — labeled a11y long-click actions for `DialChip` Fn-menu and
  MR save (`ManualDials.kt:435`, `CameraScreen.kt:985-991`).
- ☑ **P4.10 / AGG-11** (Med-High/High, test-engineer #3) — `ExposureMeter` calls
  `ControlCycles.evCompStops(state)` instead of its inline copy (`CameraScreen.kt:1122-1124`).
- ☑ **P4.11 / AGG-42 N6** (Low/High, critic) — `videoSizeSummary`: drop the unreachable 8K branch,
  align labels with `ProControls.videoResolutionLabel` ("4K UHD") (`CameraViewModel.kt:450-456`).

## Phase 5 — Locale fix + test batch (extend the pure-seam house pattern)

- ☑ **P5.1 / AGG-2** (High/High, test-engineer #1) — pass `Locale.US` at all 17 UI `.format()` sites
  (or one `fmt()` helper) — `ProControls.kt:504,505,511,522`, `ManualDials.kt:246,276,284,512,679,
  701`, `CameraScreen.kt:907,1132`, `Overlays.kt:245,309`; add a `Locale.GERMANY` regression test
  around `formatShutterSpeed`/EV formatting.
- ☑ **P5.2 / AGG-28+13(part)** — cycle-function coverage: move `nextFlashMode`/`nextTimer`/
  `nextAspect` (`CameraScreen.kt:597-613`) into `ControlCycles.kt`; add closed-cycle +
  all-values-visited property tests for all 13 `next*` functions; pin `nextWbMode`'s documented
  skip-list.
- ☑ **P5.3 / AGG-15** (Med-High, test-engineer #7) — `SettingsStoreTest`: direct `enumOr`/
  `enumListOr` tests now; a ~50-line in-memory `SharedPreferences` fake; round-trip identity for
  non-default `ManualControls`+`ExtraSettings` (catches the P1.12 field too); legacy `fnSlots`
  migration; per-slot preset prefix isolation.
- ☑ **P5.4 / AGG-13** (Med-High, verifier #4 + test-engineer #5) — extract + test
  `unwrapRotationDelta`/`shortestAngleTarget` (`CameraScreen.kt:162-171`), `swapsDimensions`
  (`:520-532`), `levelDeviationDegrees` (`Overlays.kt:180-198`): quadrant boundaries both directions.
- ☑ **P5.5 / AGG-14** (Med-High, test-engineer #6) — extract + test `viewTapToSensorPoint`/
  `viewTapToLoupeCenter` (`CameraEngine.kt:373-406`): center invariance, corner quadrant, [0,1] clamp.
- ☑ **P5.6 / AGG-28(part)** — `computeHistogram`/`computeWaveform` → `internal` + synthetic-buffer
  tests (`GlPipeline.kt:371-422`).
- ☑ **P5.7 / AGG-28(part)** — extract + test `coverScale(...)` from `FlipRenderer.draw`
  (`FlipRenderer.kt:137-143`).
- ☑ **P5.8 / AGG-28(part)** — extract expression-position `hevcColorTagsFor(transfer)` used by BOTH
  `hevcFormat` and `apvFormat` (`ColorProfiles.kt:53-124`) + per-entry transfer-tag tests (kills the
  PQ-mistag regression class by construction).
- ☑ **P5.9 / AGG-28(part)** — plain-bounds extraction + tests for `clampFpsRange`/`autoFpsRange`
  (`CaptureCapabilities.kt:91-102`) — the documented low-light-AE bug zone.
- ☑ **P5.10 / AGG-28(part)** — `roundToSignificant` tests (power-of-ten boundary) + plain-bounds
  `isoStops`/`shutterStops` extraction + conventional-value tests (`ManualDials.kt:581-616`).
- ☑ **P5.11 / AGG-28(part)** — extract + test `shouldUpdateOrientation`/`snapToQuadrant` from
  `GyroEis.onSensorChanged` (`GyroEis.kt:87-129`) at exact thresholds and 45°-offset midpoints.
- ☑ **P5.12 / AGG-28(part)** — extract + test `aeCompAebSteps(evComp, lower, upper)`
  (`CameraEngine.kt:639-643`): clamp + dedupe on narrow EV ranges.
- ☑ **P5.13 / AGG-28(part) + architect N4** — extract `pickVideoSize(sizes, capWidth, wantW, wantH)`
  shared by `CameraCaps.read` and `chooseVideoSize`'s fallback (one selection rule, tested)
  (`CameraEngine.kt:1062-1081`, `CaptureCapabilities.kt:130-138`).
- ☑ **P5.14 / AGG-28(part)** — `audioPtsUs` + `resolveAudioChannelCount` extraction + tests
  (`VideoRecorder.kt:398-420,496-505`).
- ☑ **P5.15 / AGG-28(part)** — `autoShutterText`/`autoIsoText` tests + `evCompStops(caps=null)`
  fallback test (`ControlCycles.kt:95-118`).
- ☑ **P5.16 / AGG-28(part)** — extract + test `manualMeterEv(mode, luma)` (`CameraScreen.kt:
  1166-1174`) — also shaves a slice off architect N7.
- ☑ **P5.17 / AGG-46** (Low) — free wins: `typeLabel`/`isBluetoothInput` table tests
  (`AudioInputInspector.kt:41,56-66`); direct `correctionStops(0f)` assert; `driveIso`/
  `driveShutterNs` round-to-same→null tests; odd-byteCount `applyGainAndLevel` pin;
  `activeFnSlots` one-liner; focus-formatter boundary tests (`ProControls.kt:517-524`,
  `ManualDials.kt:465-469`).

## Phase 6 — Docs & comments

- ☑ **P6.1 / AGG-30** (Medium/High, 6-source) — "63 unit tests" → "109 tests across 13 files" in
  `README.md:126`, `docs/ARCHITECTURE.md:573`, `docs/BACKLOG.md:19`, `docs/play-console-submit.md:27`
  (word it so it can't silently drift: point at `app/src/test/`).
- ☑ **P6.2 / AGG-44** — `docs/ARCHITECTURE.md:528`: 4 → 6 dials (focus, shutter, ISO, WB, EV, zoom).
- ☑ **P6.3 / AGG-42 N4** — reconcile `VendorLogMode` KDoc (`CameraState.kt:218-227`) to the settled
  2026-07-09 INERT finding.
- ☑ **P6.4 / AGG-41(part)** — sweep stale `AspectRatio.FULL` comments (`CameraState.kt:346-349`,
  `CameraEngine.kt:499`, `Overlays.kt:144-151`) → `W4_3`.
- ☑ **P6.5 / AGG-27** — add the "Archived plan." banner to `docs/plans/2026-07-10-rpf-cycle1.md`
  (option (b) from the finding: banner pointing at the Progress Log; checkboxes stay as the queued
  ledger).
- ☑ **P6.6 / AGG-47** — `ColorProfiles.kt:60-69` comment: note the device-verified CICP-14 outcome
  (functionally BT.709; not the PQ mistag).
- ☑ **P6.7 / AGG-42 N7-F4** — add a commented R8 keep-rule stub for the name-persisted enums to
  `proguard-rules.pro` (inert while minify is off; prevents the documented future trap).
- ☑ **P6.8** — small doc residue: ARCHITECTURE thread-table mentions the ViewModel tickers
  (architect F7 residue); README video-fps list mentions 25 fps (critic N8 residue).

## Phase 7 — Security hardening

- ☑ **P7.1 / AGG-33** (Low/High, security S-1 + verifier #2) — `BuildConfig.DEBUG`-gate the 15
  remaining diagnostic log sites: `CameraController.kt:202,247,251,330,341,359,380,385,398,400,433,
  534`, `VideoRecorder.kt:240,325,342` (keep `CameraController.kt:338` `Log.e`).

---

## Deferred (recorded per the strict deferral rules; severities preserved, not downgraded)

Repo-rule basis for deferral mechanics: CLAUDE.md explicitly maintains a deferred-work ledger
("`docs/BACKLOG.md` — release status … and deferred work") and mandates device verification before
claiming camera/GL/orientation fixes ("Verify on device before claiming a camera/GL/orientation fix
is done… If the device is unreachable, say so and mark the item pending"). Items whose correctness
effect cannot be verified without a lit-scene/human device session are therefore parked as tracked
residuals rather than shipped blind — the same mechanism BACKLOG's "Residual Field Checks" already
uses.

- ⏸ **D-1 / AGG-19** — Video muxer orientation-hint SIGN unverified (Medium/Medium,
  Needs-manual-validation; 4-source). `RotationMath.kt:44-52`. Reason: requires a lit,
  deliberately-held landscape clip judged by a human — CLAUDE.md: "A deliberately held, lit
  portrait/landscape output check remains the final proof of visual uprightness," and
  `docs/BACKLOG.md` Residual Field Checks already tracks it. Exit criterion: the BACKLOG field check
  runs; if the sign is wrong, flip to `(360-deg)%360` in the one pure function + update its test.
- ⏸ **D-2 / AGG-31** — Play/privacy contact email `mnmnnmnnn@gmail.com` possibly a placeholder
  (Medium/Needs-validation; 2-agent). `docs/play-store-listing.md:15`, `docs/play-console-submit.md:
  42`, `PRIVACY.md:51`, `privacy-policy/index.html:166`. Reason: only the owner knows the real
  monitored mailbox; inventing one would be worse. Not a code defect. Exit criterion: owner confirms
  or replaces the address before Play submission (play-console-submit checklist).
- ⏸ **D-3 / AGG-29** — Shaders O-Log2 segment-boundary mismatch (Medium/High math,
  Needs-manual-validation perceptibility). `Shaders.kt:68-86`. Reason: reviewer conflict — verifier
  holds the constants come from OPPO's white paper and must not be hot-patched; changing the shipping
  OETF needs the white paper in hand plus a device A/B of log footage. Correctness-adjacent but
  bounded (~0.0006 absolute near-black error). Exit criterion: white-paper recheck of the 0.20856
  threshold; then either fix inverse+forward coherently with a Kotlin-mirror test, or document the
  constants as vendor-exact.
- ⏸ **D-4 / AGG-48 + AGG-17(structural remainder)** — Architectural refactors: A3 state
  triplication/`applyExtras`, A4 CameraEngine god-object split (`StillWriter`), A5 AE-loop move into
  the engine, A6 `CameraSession` seam + dormant native-log fencing, N1 caps projection out of
  `CameraUiState`, N2 telemetry-flow split, N6 typed engine events, N9 god-composables, N7/N8 full
  domain-logic extraction (beyond the P5.16 slice). Severities Med → Med-High preserved. Reason:
  cycle-1 plan already recorded these as conscious deferrals (D-1/D-8/D-9 there); architect re-assessed
  the deferral as defensible for the device-verified v1; large churn on hot paths contradicts the
  "production-bound, no risky rewrites pre-release" posture. Exit criterion: MUST land before the
  CameraUnit/BACKLOG-#4 v2 work begins (architect: that work adds a fourth open path and a second
  session type).
- ⏸ **D-5 / AGG-3 remainder** — Downsampled-FBO/PBO analysis readback (High/High mechanism). Reason:
  GL pipeline rework requires on-device frame-pacing measurement to prove the win and no regression;
  P3.2 lands the safe reorder now. Exit criterion: device perf session with recording-pacing
  measurements (perf review's magnitude validation).
- ⏸ **D-6 / AGG-24** — `startRecording` off the main thread (Medium/High). Reason: REC-start
  reordering interacts with `recorderTeardownInFlight`/standby-meter handoff; needs on-device
  verification of rapid arm/start/stop sequences; perf, not correctness (UI freeze, no data loss).
  Exit criterion: device session covering rapid REC toggles + hardware-key REC.
- ⏸ **D-7 / AGG-25** — DNG write off the camera thread via copied `writeByteBuffer` (Medium/Medium).
  Reason: changes the verified capture path (release output files were device-verified 2026-07-10);
  needs BURST+RAW device re-verification. Exit criterion: device session re-running the rapid
  double-shutter + BURST checks.
- ⏸ **D-8 / AGG-26** — Single-decode HEIF+JPEG save pipeline (Medium/High). Same reason/exit as D-7
  (verified save path; needs device capture re-verification of both containers).
- ⏸ **D-9 / AGG-43 remainder** — GL frame coalescing (P-8), cached repeating-request builder (P-9),
  waveform draw-list reuse (P-12) (Low). Reason: profiling-guided optimizations; mechanism cheap but
  benefit unproven; hot paths. Exit criterion: device profiling session shows GL thread falling
  behind / GC pressure.
- ⏸ **D-10 / AGG-32 F2** — MR management placement (Medium/High). Reason: information-architecture
  decision (duplicate into Video tab vs My tab vs global section) belongs to the owner; the
  viewfinder MR strip already covers both modes functionally. Exit criterion: owner picks a placement;
  then a small ProSheet change.
- ⏸ **D-11 / AGG-42 N2** — AE meters log-curve luma in video+LOG priority modes (Low/Medium).
  Reason: needs a colorimetric decision (de-log the meter vs transfer-aware target) and device
  validation in a lit scene; blind change risks mis-exposing verified modes. Exit criterion: device
  session comparing AE convergence SDR vs LOG; pick target policy.
- ⏸ **D-12 / AGG-42 N7-F1** — remove dead `android:fullBackupContent`/`backup_rules.xml` (Low/High).
  Reason: file deletion — global destructive-action rule requires explicit user confirmation
  ("Before performing ANY destructive action, ALWAYS stop and explicitly ask the user"). Inert config;
  zero runtime effect either way. Exit criterion: owner approves the deletion (one commit).
- ⏸ **D-13 / AGG-42 N7-F3** — `keystore.properties`-must-exist gate defeats pure-env CI signing
  (Low/Medium). Reason: touching release signing without a CI to test against risks breaking the
  release gate; owner's CI design decision. Exit criterion: when CI is set up, switch the gate to
  "file OR complete TELECAMPRO_* env set".
- ⏸ **D-14 / AGG-42 N9** — configurable keep-screen-on (Low). Reason: already an acknowledged
  BACKLOG deferred-optional item; product choice. Exit criterion: BACKLOG item picked up.
- ⏸ **D-15 / AGG-46 residue** — `ProSheet.fnSlotValue` 19-case table test (Low). Reason:
  test-engineer's own assessment — authoring overhead exceeds risk today. Exit criterion: when
  `FnSlot` next gains a member, add the table test in the same change.
- ⏸ **D-16 / AGG-42 S-2 (= critic N7-F2)** — committed public OPPO Maven credential (Low/High,
  accepted). Reason: security review verified it is OPPO's published public read-only credential,
  build-time only; previously accepted; folding into the CameraUnit AUTH_CODE work. Exit criterion:
  CameraUnit registration lands → move to env/gradle.properties.
- ⏸ **D-17 / feature-dev note** — `GyroEis` roll ±180° `atan2` wrap transient (~10-25 ms, cosmetic,
  Low/Low). Reason: below actionability threshold per two cycles of review; partially covered once
  P5.4's level tests land. Exit criterion: reappears as user-visible after P5.4's extraction.
- ⏸ **D-18 / AGG-42 N9-adjacent (qa NEEDS-HUMAN set)** — lit-scene uprightness, AF sharpness,
  stabilization shake check, scope visual parity, Sound Focus acoustics. Reason: need a lit scene /
  human ears (qa-adversary environmental limitation); already BACKLOG Residual Field Checks. Exit
  criterion: the BACKLOG field session.

Non-deferrability check: no Critical findings exist; the only correctness-class deferrals are D-1
(device-sign residual — repo rule quoted above explicitly makes the lit-scene check the final proof)
and D-3 (reviewer-conflicted vendor constants — hot-patching without the authoritative source would
itself be the risk). All security findings are scheduled (P7.1) except D-16, which the security
review itself classifies as accepted/benign public credential.

---

## Progress log

- 2026-07-10 — plan created from cycle-2 aggregate.
- **2026-07-10 — cycle 2 implementation complete.** Every scheduled item (P1.1-P7.1) landed across
  ~20 GPG-signed commits (02f5373..HEAD); gates green throughout (assembleDebug / testDebugUnitTest /
  lintDebug); the unit suite grew 109 → 200+ tests. Highlights and caveats:
  - ☑ Phase 1 (all): review-overlay key gating (P1.1 incl. the frozen review uri and the
    PermissionGate volume fix), stab-mid-record guard, audioRecord wedge guard, close() CAS,
    zoom NaN guard + load clamp, stale-surface bind guard, expression-position whens, driveProgram
    range guard, meteringRect, MediaReview gesture merge + player identity, the 8 persisted setters,
    video-resolution persistence (ExtraSettings `videoResolution` + engine requestedVideoSize).
  - ☑ Phase 2 (all): resume()/pause() on setupExecutor, ordered encoder teardown via the GL
    completion callback, standby-meter release latch, GyroEis holds orientation across resume,
    tickers paused on onStop, openCamera executor inline-run leak guard, muxerStarted @Volatile,
    release() teardown documented as accepted.
  - ☑ Phase 3 (all): scope-data publication gating, analysis-after-encoder reorder, lens-tap IPC off
    main, RMS 32768 unification, fps rebase of pending throttled applies, bounded audio-EOS wait,
    zoom snapshotFlow, debounced Fn-editor saves, throttled RMS skip, age-gated orphan sweep.
  - ☑ Phase 4 (all): AF-state reticle (green/red), StatusBar scroll, 48 dp DialChip/MR/MiniTextButton,
    timer/half-press/meter-adjacent glyph rotation, ModeLabel scrim, camera-health dim + statuses,
    PermissionGate restyle, labeled long-press actions, evCompStops dedup, videoSizeSummary cleanup.
  - ☑ Phase 5 (all): Locale.US everywhere + GERMANY regression test; cycle-fn single home + property
    tests; SettingsStore prefs-fake round-trip suite; rotation-unwrap/swap/level/tap-mapping/
    histogram/coverScale/fps-bounds/color-tags/audio-PTS/AEB/pickStreamSize seams all extracted and
    pinned; manualMeterEv pure.
  - ☑ Phase 6 (all) + ☑ Phase 7 (P7.1: 15 log sites gated).
  - ALSO this cycle (user report, task #6): the viewfinder now letterboxes the full capture field
    and PHOTO mode runs a 4:3 preview stream — the cover-filled 16:9 preview mis-framed every photo.
  - **[device-verify pending]**: the PMA110 re-locked behind a secure keyguard mid-cycle. Structural
    verification (build/tests/lint, session contracts) done; on-device checks queued for the next
    unlocked session: letterboxed preview geometry + mode-switch reopen, review-overlay key gating,
    resume/lens-tap serialization, record start/stop with the ordered teardown, tap-AF reticle
    colors, MediaReview tap-to-pause, GyroEis resume-orientation capture.
- Deferrals D-1..D-18 stand as recorded (none touched).

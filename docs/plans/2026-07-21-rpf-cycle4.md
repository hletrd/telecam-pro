# Review-Plan-Fix Cycle 4 Implementation Plan — 2026-07-21

## Scope and immutable boundaries

- Review target: `84d47c1`; aggregate: `.context/reviews/_aggregate.md` with **6 unique new
  findings** from all 14 required roles plus direct evidence synthesis.
- Device target: OPPO PMA110 at `127.0.0.1:5602` only. Never address port 5600.
- Deployment mode: `none`. A debug APK reinstall/relaunch, HAL-free snapshot capture, safe real-app
  Fn/Settings inspection, orientation/font changes, and exact restoration are authorized. No
  capture, REC, media-output, true-1x/PIP, concurrent-camera, physical-route, or risky QTI probe.
- Preserve sticky Fn quick actions, physical held 4x2 order, bottom-reachable Fn entry/tray, one
  coherent >=48 dp action per tile, exact Settings tab/close semantics, one compact active MR tag,
  conditional manual rulers, truthful same-stream Loupe Overview, and preview-first idle chrome.
- Review files are repo-ignored working evidence. Historical dated plans and 092217 evidence remain
  intact; Cycle 4 will supersede inaccurate claims with replacement evidence rather than rewriting
  history.
- Ralph is unavailable in this session. Prompt 3 uses the declared direct, regression-safe fallback:
  small patches, focused tests, full gates, device-safe evidence, and fine-grained signed commits.

## Phase 1 — Make muxer start a one-shot recording terminal

- [x] **P1.1 / AGG4-01 (High/High): one owner for `MediaMuxer.start()` failure.** Replace the boolean-
  only publication seam with an immutable start state (`WAITING`, successful, or terminal). A ready
  start is attempted at most once. Native refusal becomes terminal without cleanup; a thrown start
  exception is contained at the muxer boundary, recorded through the clip-level first-failure owner,
  and never reclassified as an audio-only fault or retried from `degradeAudioToVideoOnly()`.
- [x] **P1.2 tests.** Extend the Android-free native-start suite to prove not-ready is inert, success
  publishes once, refusal is terminal, a thrown start invokes exactly one failure observer, a second
  call cannot touch the native owner, and an audio-degrade re-entry cannot let a second throw escape.
  Retain genuine AudioRecord/AAC/read failures as video-only degradation.

## Phase 2 — Make Fn geometry absolute, compact, and preview-first

- [x] **P2.1 / AGG4-02 (Medium/High): RTL-safe raw edges and order.** Render compact entry alignment,
  detailed-row placement, held tray anchoring, raw grid order, and label/value raw axes in an absolute
  coordinate space. Keep text readable/localizable and preserve existing policy outputs for
  portrait/90/270, including -90/450 normalization.
- [x] **P2.2 RTL tests.** Add pure absolute-edge/order assertions and device-harness fixtures that
  model both layout directions through identical raw/physical coordinates. Existing 4x2 order and
  bottom-zone checks remain mandatory.
- [x] **P2.3 / AGG4-06 (Medium/High): compact only the held tray.** Reduce the held raw panel width
  from 232 dp to a balanced strip whose two raw columns remain >=48 dp after padding/gaps; lower the
  full-screen scrim modestly while retaining an opaque compact panel so focal labels cannot bleed
  through tiles. Do not change portrait layout, sticky-action behavior, Close semantics, or slot
  order.
- [x] **P2.4 depth oracle.** Extend `function_menu_layout_errors` with a maximum held physical tray
  depth (while retaining the minimum target floor), add passing compact and failing oversized XML
  fixtures, and require both 90/270 snapshots to pass.

## Phase 3 — Reset and strengthen HAL-free snapshot evidence

- [x] **P3.1 / AGG4-04 (Medium/High): fresh subtree per launch.** Give every snapshot launch a
  monotonically fresh generation and `key` the whole stateful production `CameraScreen` subtree.
  Equal scenario/orientation relaunches must reset too. Keep the fixture HAL-free and actions no-op
  except deterministic UI state updates.
- [x] **P3.2 / AGG4-03 (Medium/High): exact Gamma truth.** Add a nonvisual debug-only semantic probe
  for current Gamma state. Assert the initial `HLG` and exact post-tap `O-Log` values in addition to
  the sticky Fn modal and visual delta; a repaint with the wrong state must fail.
- [x] **P3.3 scoped Loupe wording.** Add a pure harness oracle that rejects PIP and phrases claiming a
  `1x overview/finder/feed/camera`, while allowing the legitimate `1x lens` focal-rail control. Add
  positive and negative unit fixtures. Never exercise or imply a real second stream.
- [x] **P3.4 isolation assertions.** Require Loupe snapshots to contain no `Close adjustment`, ISO
  ruler, or Fn modal. Capture replacement 0/90/270 XML/PNG only after these negatives pass.

## Phase 4 — Clear direct controls from the Loupe rectangle

- [x] **P4.1 / AGG4-05 (Medium/High): shared bottom clearance.** Split the finder side inset from a
  larger bottom inset in `finderRect`. Keep size at 30% and keep GL viewport/scissor, Compose border,
  RTL-independent bottom-left anchor, and hit exclusion derived from the same rectangle. Move the
  same-stream overview only far enough upward to clear persistent Fn/lens chrome.
- [x] **P4.2 geometry and device tests.** Update `FinderGeometryTest` for independent side/bottom
  margins, density scaling, unchanged aspect/size, and exact hit bounds. Add `loupe_layout_errors`
  requiring one non-interactive overview inside the preview that has zero overlap with named
  actionable chrome at 0/90/270.

## Phase 5 — Verification, evidence, publication, and restoration

- [x] **V1 focused host tests:** native-start, finder geometry, Fn policy, and Python UI-oracle tests.
- [x] **V2 full host gate:**
  `JAVA_HOME=/opt/homebrew/opt/openjdk@21/libexec/openjdk.jdk/Contents/Home ./gradlew :app:assembleDebug :app:testDebugUnitTest :app:lintDebug`.
- [x] **V3 harness gate:** `python3 -m unittest discover -s device-tests/tests`.
- [x] **V4 exact device entry state:** on `127.0.0.1:5602` only, record device identity, installed/base
  APK hash, foreground component, font scale, accelerometer rotation, and user rotation. Never use
  5600. Rebuild/reinstall only the debug APK.
- [x] **V5 replacement HAL-free evidence:** run only `debug_snapshot_ui_contract`; persist XML/PNG for
  idle, Fn, Settings, MR, adjustment, and Loupe at 0/90/270 plus physical-upright held PNGs. Prove
  exact Gamma, state isolation, no Loupe/control overlap, compact <=40% held tray depth, 4x2 order,
  bottom reach, >=48 dp actions, Settings semantics, and truthful wording.
- [x] **V6 safe real-app sanity:** run only the non-capture function-menu round trip on the rebuilt
  app; do not open Loupe through Camera2, press shutter, start REC, or inspect output media.
- [x] **V7 exact restoration:** leave `MainActivity` foreground with font scale `0.8`,
  `accelerometer_rotation=0`, and `user_rotation=0`; verify the component and values after cleanup.
- [x] **V8 git/publication:** commit each coherent fix with GPG `-S` and Conventional Commit +
  gitmoji; before every push run `git pull --rebase`, resolve without discarding user work, then push.
  Complete this plan in a final signed docs commit. Deployment stays `none`.

## Mandatory safety deferral

- **D1 — true 1x/wide second-stream finder (High/High, unchanged).** This is not one of Cycle 4's six
  new findings. A truthful true-wide inset requires a second stream or verified vendor concurrent-
  camera capability. The user explicitly prohibits the crash-prone QTI/physical-route probe. Exit
  requires separate explicit authorization and an isolated PMA110 plan proving source identity and
  preview/capture/REC stability. Until then, ship and test only the labelled same-stream Loupe
  Overview; never claim true 1x PIP evidence.

## Completion evidence

- Implementation was split into signed, pushed commits: `3705546` (terminal muxer start), `0c7a199`
  (absolute/compact held Fn), `147b6ee` (fresh exact snapshot evidence), and `5ca77cf` (shared Loupe
  clearance). Device-gate feedback produced two additional signed test fixes: `8c0bae9` keeps the RTL
  debug probes visible without changing production layout direction, and `c39a9ad` scopes the RTL
  oracle to the raw physical Fn geometry named by AGG4-02.
- The exact Gradle gate passed at final code HEAD: assemble, **792 JVM tests**, and lint are green.
  The exact harness gate passed **79/79 Python tests**. Focused native-start, finder-geometry, Fn,
  Gamma/copy/isolation, and layout-oracle tests also passed.
- Entry and final verification used only `127.0.0.1:5602`, an OPPO PMA110 on build
  `B.4819642-23907c2-239f6c4`. The rebuilt host APK and installed `base.apk` both have SHA-256
  `d94d50dea3cdb97f755998fdafc512a10f9f01e6be06f7f2f3f7a2b0a6dc9be6`.
- Replacement HAL-free evidence `device-tests/reports/20260721-102936` passed in **152.9 s**. Its 23
  XML files parse cleanly, its 0/90/270 contracts pass, and 14 reviewed held images are persisted in
  `physical-upright/`. Loupe has one non-actionable, preview-bounded, control-clear overview and zero
  retained adjustment/Fn state. The failed `101925` and `102505` reports are preserved as transparent
  gate history and superseded by `102936`; the old `092217` false-positive evidence remains intact.
- Safe real-app evidence `device-tests/reports/20260721-103232` passed in **17.6 s**: eight enabled Fn
  tiles and four sticky in-place actions retained coherent >=48 dp bounds, and Back restored camera
  chrome. No shutter, REC, media-output, real Loupe camera path, concurrent-camera, or physical-route
  probe was run.
- Final device state is verified exactly: `MainActivity` foreground, font scale `0.8`,
  `accelerometer_rotation=0`, and `user_rotation=0`. Deployment is `none`; D1 remains deferred.

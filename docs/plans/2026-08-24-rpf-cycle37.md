# Review-plan-fix cycle 37 — implementation plan

Date: 2026-08-24
Source: `.context/reviews/_aggregate.md`, cycle 37
Status: active; seven findings scheduled, none deferred

## Repository rules applied

- Preserve measured PMA110 behavior, capability-enumerated multi-device paths, exact Camera2/native
  ownership, quiet Sony-style UI, EN/KO parity, and clean-clone authority.
- Schedule every one of the seven aggregate findings below. Nothing is silently dropped, deferred,
  or severity-downgraded.
- Keep completed historical plans as non-destructive archives in `docs/plans/`; no completed plan
  needs moving or rewriting.
- Work only in `/private/tmp/find-x9-cycle37.AoQoKx`; never modify, stash, reset, check out, or build
  from the shared dirty `main` checkout.
- Use fine-grained GPG-signed Conventional Commits with gitmoji, pull with rebase before every push,
  and do not deploy because `DEPLOY_MODE=none`.

## P1 — make Python host/documentation verdicts optimization-safe (AGG37-01)

- [ ] Add an outer fail-closed optimization guard to `tools/verify_host.py` and
  `tools/check_docs.py`, before suite/check execution.
- [ ] Replace correctness-bearing operational assertions in `tools/check_docs.py` with explicit,
  always-on validation that preserves stable diagnostics.
- [ ] Add subprocess regressions for `python -O` and environment-only `PYTHONOPTIMIZE=1`, including
  a non-integral-millisecond ZSL fixture that can never false-green.

## P2 — project Gamma truth into every quick surface (AGG37-02)

- [ ] Single-source Gamma quick enablement and cycling from
  `availableTransfers(videoCodec, tenBitEncodeAvailable)` rather than global enum order.
- [ ] Disable Gamma while capability truth is unavailable or the projected set is singleton; keep
  ViewModel normalization as defense in depth.
- [ ] Cover Fn overlay, My Menu/Recent, and expanded DISP behavior for loading, SDR-only, and Main10
  capability states.

## P3 — expose only truthful stabilization choices and state (AGG37-03)

- [ ] Derive an exact advertised stabilization choice list from route capabilities for OFF-only,
  OFF+ON, and OFF+ON+PREVIEW hardware.
- [ ] Normalize restored/live stabilization state as capabilities change, and make menu, quick
  cycling, caption, and OSD consume that resolved truth.
- [ ] Add pure and ViewModel/UI tests for capability arrival, route changes, selection cycling, and
  every advertised-mode matrix.

## P4 — distinguish disabled focal-rail presentation (AGG37-04)

- [ ] Resolve a quiet disabled rail edge and coherent selected-disabled fill without weakening the
  enabled 3:1 affordance token or changing semantics.
- [ ] Add palette and native Compose coverage for enabled, selected, disabled, selected-disabled,
  bright-frame, and dark-frame rail states.

## P5 — align and enforce the inclusive ZSL freshness boundary (AGG37-05)

- [ ] Update `CLAUDE.md` and `docs/ARCHITECTURE.md` from strict `< 400 ms` to the production/test
  contract `<= 400 ms` / “up to 400 ms.”
- [ ] Extend `tools/check_docs.py` and its fixtures to validate inclusivity as well as the numeric
  maximum, so comparator-only drift fails.

## P6 — synchronize privacy-policy facts (AGG37-06)

- [ ] Add capture-metadata, explicit no-location/GPS, and broad on-device library-read/no-transmission
  disclosures to `PRIVACY.md`, preserving the current policy meaning and date.
- [ ] Add documentation parity checks that require those facts across Markdown, published HTML,
  and EN/KO in-app policy copies.

## P7 — correct the stale MR tint comment (AGG37-07)

- [ ] Reword the `ProSheet.kt` comment to describe the independent 0.18 amber active-row wash and
  its separation from the 0.36 white `AffordanceEdge`; do not change either rendered value.

## Quality gates and delivery

- [ ] Run focused tests for every fix plus `git diff --check`.
- [ ] Run `python3 tools/verify_host.py` against the whole repository.
- [ ] Run `./gradlew :app:assembleDebug :app:testDebugUnitTest :app:lintDebug`.
- [ ] Run `./gradlew :app:lintRelease :app:assembleRelease :app:bundleRelease`.
- [ ] Root-fix every configured gate error and actionable warning without suppression.
- [ ] Commit and push each independent review, plan, implementation, test, documentation, and
  closeout change with signed Conventional Commits plus gitmoji, pulling with rebase before push.
- [ ] Mark this plan complete only after exact evidence is delivered.

## Deferred findings

None. Every cycle-37 finding is scheduled in P1-P7.

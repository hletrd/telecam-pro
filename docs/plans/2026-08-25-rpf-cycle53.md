# Review-plan-fix cycle 53 — implementation plan

Date: 2026-08-25
Source: `.context/reviews/_aggregate.md`, cycle 53
Status: complete; all five findings fixed and every configured quality gate green

## Repository rules applied

- Preserve measured PMA110 behavior, capability-enumerated generic routes, exact native/resource
  ownership, durable MediaStore truth, bounded provider work, the quiet Sony-style UI, EN/KO parity,
  and explicit device-evidence limits.
- Schedule every one of the five aggregate findings below. Nothing is silently dropped, severity-
  downgraded, deferred, relabeled optional, or treated as fixed by documentation alone.
- Keep completed historical plans in `docs/plans/` as the repository's non-destructive archive.
  Every earlier plan is marked complete; none needs moving, deleting, rewriting, or reopening.
- Work only in `/tmp/find-x9-ultra-cycle53.cJwfCJ`; never modify, stash, reset, check out, clean, or
  build from the shared dirty `main` checkout.
- Use fine-grained GPG-signed Conventional Commits with gitmoji, pull with rebase before every push,
  and do not deploy because `DEPLOY_MODE=none`.

## P1 — linearize standby native publication with quarantine (AGG53-01, High/High)

- [x] Replace the momentary create/start result with an exact process-gate publication token or
  equivalent transaction that atomically binds the concrete input/termination owner while current,
  otherwise classifies and strongly retains it as revoked.
- [x] Close process native admission and install strong exact-input retention before a stop-timeout
  path exposes abandonment or releases logical waiters.
- [x] Add deterministic return-before-bind, return-before-start-publication, and
  abandon-before-quarantine competing-acquisition tests; prove no post-terminal stop/release and no
  second native acquisition.

Evidence: `d8e7ab09` adds exact native-publication tokens and strong process retention; `d671da0e`
closes every return/failure/stale-token branch. Deterministic create/start/timeout races and ordinary
cleanup retirement pass in the complete unit and coverage gates.

## P2 — bind immediate DISCARD to caller and committed row identity (AGG53-02, Medium/High)

- [x] Carry immutable allocation/family identity from pending-output creation into DISCARD intent so
  marker creation cannot bless a replacement row for a stale caller.
- [x] Return/use the exact committed identity record for the immediate provider delete, including
  provider-version stability and null-safe predicates (`IS NULL` for expected nullable columns).
- [x] Test URI reassignment before marker creation and after marker commit/before immediate delete,
  plus nullable-field remaps, stable exact-row success, and legacy/reset fail-closed behavior.

Evidence: `9f14994b` carries `PendingOutputAllocation` through still and video ownership packets,
requires caller identity before the v2 marker commits, revalidates the exact committed record and
provider version, and uses null-safe conditional deletion. Remap-before/after-marker and null-field
tests pass; URI-only unknowns retain durable family/REGISTERED recovery truth.

## P3 — move failed-DNG provider cleanup off the Camera2 callback (AGG53-03, Medium/High)

- [x] Add or reuse one process-wide finite rejected-output cleanup dispatcher whose capacity is
  reserved before a RAW request can create output requiring cleanup.
- [x] Keep only live-Image DNG byte writing and its required durable completion marker on the camera
  callback. Dispatch failed-row identity/SQLite/delete/retry work after byte work so the callback can
  close the RAW Image promptly; never fall back to inline provider work on saturation/shutdown.
- [x] Block a fake identity/provider read and prove callback/Image completion, bounded ownership,
  exact eventual DISCARD, overflow disposition, and old-Engine/replacement safety.

Evidence: `9f14994b` reserves a two-worker/eight-backlog process owner before RAW dispatch and returns
failed DNG allocation packets without provider work on the Camera2 callback. Blocked work, capacity,
shutdown, unresolved retry, and old-owner completion tests pass; `d671da0e` closes the remaining
dispatcher branches without inline fallback.

## P4 — decode every review from one immutable compressed source (AGG53-04, Medium/High)

- [x] Remove multi-open mutable provider semantics for `APP_OWNED` review. Bounds, sampled pixels,
  and EXIF must consume one frozen compressed-byte identity.
- [x] Preserve supported app-owned hi-res media above 64 MiB with a disk-aware trusted-source policy
  or truthful bounded fallback; retain owner-unverified strict limits, process-wide byte accounting,
  latest-wins disposal, decoded-pixel bounds, no-follow private files, and crash reclamation.
- [x] Add alternating small-bounds/large-pixels/changed-EXIF coverage and a valid above-64-MiB trusted
  fixture proving the cycle-52 regression does not return.

Evidence: `4a40431c` freezes both provenance classes into one no-follow private spool. Unverified
input stays at 64 MiB; trusted input is bounded by 512 MiB, one quarter of usable cache, and a
two-source process budget. Alternating identities, a valid 64 MiB+1 JPEG, cleanup, and disk policy
pass in the focused and complete suites.

## P5 — bound continuous debug telemetry below the device log quota (AGG53-05, Medium/High)

- [x] Change-gate a compact, bucketed 3A tuple and emit only a long heartbeat when stable; preserve
  enough exact detail to diagnose focus/exposure/request truth without per-frame churn.
- [x] Replace one-row-per-second sustained-YUV logging with bounded in-memory accumulation and one
  disable/end summary (or another fixed small output budget).
- [x] Add a pure ten-minute A5 cadence-budget test covering every continuous producer and reserving
  quota for startup, frame-gap, camera-error, recovery, and focus diagnostics.

Evidence: `e8ba2cab` adds the pure quota owner: stable 3A emits 41 rows over ten minutes and a tuple
changing every sample emits at most 201; ZSL cadence emits only enable plus terminal summary rows.
Bucket, heartbeat, worst-case quota, partial-window, and summary tests pass.

## Quality gates and delivery

- [x] Run focused tests for standby publication/timeout ownership, DISCARD pre/post-marker remap,
  DNG callback release and cleanup capacity, immutable review identity and >64 MiB trusted input, and
  ten-minute diagnostic budgets.
- [x] Run `./gradlew :app:assembleDebug :app:testDebugUnitTest :app:lintDebug` across the whole repo.
- [x] Run `./gradlew :app:lintRelease :app:assembleRelease :app:bundleRelease` across the whole repo.
- [x] Run `python3 tools/verify_host.py` because repository authority requires it for delivery.
- [x] Root-fix every gate error and warning without suppression; record any authorized unavoidable
  warning under the strict deferred-finding rules.
- [x] Commit and push each independent implementation, test, documentation, and closeout change with
  signed Conventional Commits plus gitmoji, pulling with rebase before push.
- [x] Mark this plan complete only after exact evidence is recorded.

## Deferred findings

None. All five cycle-53 findings are scheduled for implementation in this cycle.

## Completion evidence

- All five deduplicated findings were implemented; none was deferred. Standby native publication is
  process-gate linearizable, immediate DISCARD consumes creation-time exact identity, failed-DNG
  provider work is process-finite and off-camera, review decoding owns one immutable compressed
  identity, and continuous debug evidence stays below the measured ColorOS quota.
- The exact debug gate passed: APK assembly, the complete JVM/Robolectric/Compose suite, and debug
  lint. The exact release gate passed: release lint, R8 APK, and signed AAB. The disposable clone used
  ignored read-only links to existing maintainer signing inputs; no credential value was printed,
  copied into source, or committed.
- `python3 tools/verify_host.py` passed: androidTest assembly, the full unit/debug-lint gate,
  Partition A at 8990/9006 lines (99.82%) with an exact reviewed 16-line residual manifest, 136 tool
  tests, nine coverage-tool tests, 195 device-harness self-tests, 155 documentation checks (24
  optional-private skips), Python compilation, and `git diff --check`.
- Two cycle gate issues were root-fixed: new ownership branches initially fell below the 99.5%
  coverage contract, and the new production telemetry module was absent from the architecture map.
  `d671da0e` adds executable branch coverage and one framework-bounded class partition;
  `c1f6a23e` restores the architecture/document contract. No warning was silently ignored.
- Eight fine-grained GPG-signed Conventional Commits with gitmoji were pushed before this closeout;
  this completion record is the ninth cycle commit. Deployment and device action were not run
  (`DEPLOY_MODE=none`). Open field checks remain evidence obligations, not deferred code findings.
- All work ran in `/tmp/find-x9-ultra-cycle53.cJwfCJ`. The shared dirty `main` checkout was never
  edited, staged, stashed, reset, checked out, cleaned, or used for a build; its uncommitted deltas
  remain intact.

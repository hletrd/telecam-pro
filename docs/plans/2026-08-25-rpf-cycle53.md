# Review-plan-fix cycle 53 — implementation plan

Date: 2026-08-25
Source: `.context/reviews/_aggregate.md`, cycle 53
Status: active; all five findings scheduled, none deferred

## Repository rules applied

- Preserve measured PMA110 behavior, capability-enumerated generic routes, exact native/resource
  ownership, durable MediaStore truth, bounded provider work, the quiet Sony-style UI, EN/KO parity,
  and explicit device-evidence limits.
- Schedule every one of the five aggregate findings below. Nothing is silently dropped, severity-
  downgraded, deferred, relabeled optional, or treated as fixed by documentation alone.
- Keep completed historical plans in `docs/plans/` as the repository's non-destructive archive.
  Every earlier plan is marked complete; none needs moving, deleting, rewriting, or reopening.
- Work only in `/tmp/find-x9-ultra-cycle53.cJwfCJ`; never modify, stash, reset, check out, clean, or
  build from the shared dirty `main` checkout.
- Use fine-grained GPG-signed Conventional Commits with gitmoji, pull with rebase before every push,
  and do not deploy because `DEPLOY_MODE=none`.

## P1 — linearize standby native publication with quarantine (AGG53-01, High/High)

- [ ] Replace the momentary create/start result with an exact process-gate publication token or
  equivalent transaction that atomically binds the concrete input/termination owner while current,
  otherwise classifies and strongly retains it as revoked.
- [ ] Close process native admission and install strong exact-input retention before a stop-timeout
  path exposes abandonment or releases logical waiters.
- [ ] Add deterministic return-before-bind, return-before-start-publication, and
  abandon-before-quarantine competing-acquisition tests; prove no post-terminal stop/release and no
  second native acquisition.

## P2 — bind immediate DISCARD to caller and committed row identity (AGG53-02, Medium/High)

- [ ] Carry immutable allocation/family identity from pending-output creation into DISCARD intent so
  marker creation cannot bless a replacement row for a stale caller.
- [ ] Return/use the exact committed identity record for the immediate provider delete, including
  provider-version stability and null-safe predicates (`IS NULL` for expected nullable columns).
- [ ] Test URI reassignment before marker creation and after marker commit/before immediate delete,
  plus nullable-field remaps, stable exact-row success, and legacy/reset fail-closed behavior.

## P3 — move failed-DNG provider cleanup off the Camera2 callback (AGG53-03, Medium/High)

- [ ] Add or reuse one process-wide finite rejected-output cleanup dispatcher whose capacity is
  reserved before a RAW request can create output requiring cleanup.
- [ ] Keep only live-Image DNG byte writing and its required durable completion marker on the camera
  callback. Dispatch failed-row identity/SQLite/delete/retry work after byte work so the callback can
  close the RAW Image promptly; never fall back to inline provider work on saturation/shutdown.
- [ ] Block a fake identity/provider read and prove callback/Image completion, bounded ownership,
  exact eventual DISCARD, overflow disposition, and old-Engine/replacement safety.

## P4 — decode every review from one immutable compressed source (AGG53-04, Medium/High)

- [ ] Remove multi-open mutable provider semantics for `APP_OWNED` review. Bounds, sampled pixels,
  and EXIF must consume one frozen compressed-byte identity.
- [ ] Preserve supported app-owned hi-res media above 64 MiB with a disk-aware trusted-source policy
  or truthful bounded fallback; retain owner-unverified strict limits, process-wide byte accounting,
  latest-wins disposal, decoded-pixel bounds, no-follow private files, and crash reclamation.
- [ ] Add alternating small-bounds/large-pixels/changed-EXIF coverage and a valid above-64-MiB trusted
  fixture proving the cycle-52 regression does not return.

## P5 — bound continuous debug telemetry below the device log quota (AGG53-05, Medium/High)

- [ ] Change-gate a compact, bucketed 3A tuple and emit only a long heartbeat when stable; preserve
  enough exact detail to diagnose focus/exposure/request truth without per-frame churn.
- [ ] Replace one-row-per-second sustained-YUV logging with bounded in-memory accumulation and one
  disable/end summary (or another fixed small output budget).
- [ ] Add a pure ten-minute A5 cadence-budget test covering every continuous producer and reserving
  quota for startup, frame-gap, camera-error, recovery, and focus diagnostics.

## Quality gates and delivery

- [ ] Run focused tests for standby publication/timeout ownership, DISCARD pre/post-marker remap,
  DNG callback release and cleanup capacity, immutable review identity and >64 MiB trusted input, and
  ten-minute diagnostic budgets.
- [ ] Run `./gradlew :app:assembleDebug :app:testDebugUnitTest :app:lintDebug` across the whole repo.
- [ ] Run `./gradlew :app:lintRelease :app:assembleRelease :app:bundleRelease` across the whole repo.
- [ ] Run `python3 tools/verify_host.py` because repository authority requires it for delivery.
- [ ] Root-fix every gate error and warning without suppression; record any authorized unavoidable
  warning under the strict deferred-finding rules.
- [ ] Commit and push each independent implementation, test, documentation, and closeout change with
  signed Conventional Commits plus gitmoji, pulling with rebase before push.
- [ ] Mark this plan complete only after exact evidence is recorded.

## Deferred findings

None. All five cycle-53 findings are scheduled for implementation in this cycle.


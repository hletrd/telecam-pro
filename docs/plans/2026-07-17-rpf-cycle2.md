# Review-plan-fix cycle 2 implementation plan (2026-07-17)

Date: 2026-07-17
Source: `.context/reviews/_aggregate.md` at HEAD `1f0f0b1` (13/13 reviewer reports, clean tree).
Deployment mode: **per-cycle** (deploy to PMA110 at `127.0.0.1:5599` + on-device functional
verification after gates are green). Baseline gates at HEAD are green (assembleDebug PASS,
494/494 tests across 65 suites, lintDebug "No issues found" — qa-adversary Gate 1).

This plan disposes of all 40 aggregate findings (AGG2-1..AGG2-40) plus the 2 carried cycle-1
deferrals (AGG-19, AGG-20). Every finding is either scheduled below or explicitly deferred in
§ Deferrals with its original severity/confidence, citation, reason, and exit criterion. No
finding is silently dropped. Security/correctness/data-loss findings are all scheduled (none
deferred; no repo rule was needed to defer any).

Run constraints:

- JDK 21; gates: `:app:assembleDebug`, `:app:testDebugUnitTest`, `:app:lintDebug` (whole repo).
  Error-level failures block commits; warnings best-effort or recorded as deferrals.
- Every commit GPG-signed (`-S`), Conventional Commits + gitmoji, no AI co-author trailer,
  `git pull --rebase` before every push, one concern per commit, push after each slice.
- Device work IS in scope (per-cycle deploy + verification), only via serial `127.0.0.1:5599`.
  qa-adversary found that serial unreachable during its session — the deploy step must
  (re)establish the loopback proxy first or record `per-cycle-failed`.
- No dependency/toolchain/permission change.

Status: ☐ todo · ◐ in progress · ☑ done · ⏸ deferred

## Phase 1 — Zoom pipeline correctness (user focus area 1)

- ☐ **P1.1 / AGG2-1 (Medium-High, confirmed; 4 reviewers)** — Take the engine monitor for the
  `setControls` packet write (`CameraEngine.kt:878-882`): `synchronized(this) { controls =
  normalized }`, keeping `controller?.updateControls` outside the critical section. Add a short
  "why" comment naming the packet-write invariant (all wholesale `controls` writers hold the
  engine monitor: rollback `@Synchronized`, commit-gate, reconcile — and now the main-thread
  apply loop).
- ☐ **P1.2 / AGG2-2 (High, confirmed; 4 reviewers)** — Null `zoomEaseTarget` and remove
  `zoomEaseTicker` callbacks in BOTH missed remap doors: the `engine.onOpticsRollback` handler
  (`CameraViewModel.kt:~261`) and `applyLoaded` (`~407`); also the debug-only `onCameraOverride`
  (~1526) for completeness. Matches the belt-and-suspenders form `onStop` uses.
- ☐ **P1.3 / AGG2-30 (test gap, High)** — Extract the ease-ticker per-tick math (log-space
  approach `cur * (target/cur)^0.4`, convergence epsilon, no-progress stop, non-finite guard)
  into a pure function (e.g. `ui/ZoomMath.zoomEaseStep`) used by the ticker, and unit-test it:
  convergence from above/below, epsilon landing, degenerate inputs. Add a test pinning the
  "every optics-remap door nulls both coalescer fields" invariant as far as pure code allows.
- ☐ **P1.4 / AGG2-8 (Medium-High, confirmed code; perf PERF-5)** — Stop shipping the mid-gesture
  wide-aim in captures and shorten its tail:
  (a) `CameraController.capturePhoto` must build stills from the engine-exact zoom, not the
  wide-aimed `halTarget` — store the exact requested ratio separately (e.g.
  `setZoomRatio(halRatio, exactRatio)`) and use the exact value for still requests;
  (b) land the exact HAL value once the gesture has been quiet for one throttle window
  (~250 ms) instead of waiting the full 700 ms interaction tail, keeping the 700 ms fps-boost
  window itself unchanged (a quiet-window fast-path submit, not a boost flip).
- ☐ **P1.5 / AGG2-29 (test gap, High)** — Extract the HAL zoom submit decision into a pure plan
  (`resolveHalZoomSubmit(requested, interactionActive, nowMs, lastSubmitMs, margin, throttleMs,
  bounds) -> plan`) used by `CameraEngine.setZoomRatio`, and unit-test: throttle boundary
  (== exactly), wide-aim clamping at both range edges, interaction-end always submits, and the
  P1.4b quiet-window landing. Also cover the controller result-forwarding change-gate
  (`rz != lastForwardedResultZoom` + NaN reset semantics) with a small test.

## Phase 2 — Recording correctness

- ☐ **P2.1 / AGG2-5 (Medium, likely)** — Release the standby-mic claim on any unexpected
  admission throw: `runCatching { startRecordingBlocking(recordAudio) }.getOrElse {
  abortRecordingStart(); false }` at `CameraEngine.kt:2404-2410` (keep the executor-level
  swallow as last-resort net; log the throwable).
- ☐ **P2.2 / AGG2-6 (Medium, data-loss class)** — In `VideoRecorder.runAudio()`'s EOS-wedge bail
  (`VideoRecorder.kt:475-480`), set `expectedTracks = 1` and
  `synchronized(muxerLock) { maybeStartMuxer() }` before the return, so a wedged audio encoder
  degrades to video-only instead of discarding a good video track — matching every sibling
  early-exit.
- ☐ **P2.3 / AGG2-35 + AGG2-36 partial (test gap Medium + structural)** — Extract the REC
  admission double-tap latch (`recStartInFlight`/`recStopRequestedDuringStart`) into a small
  monitor-owning class (`video/RecordingAdmissionLatch` or `camera/`): `tryBeginAdmission()`,
  `requestStop(): Boolean` (latch when in flight), `completeAdmission(succeeded): Boolean`
  (returns whether a latched stop must run now). Wire `CameraEngine.startRecording`/
  `stopRecording` through it and unit-test the transitions plus a `CountDownLatch` race
  ("stop arrives while admission is completing"), following `StandbyMeterOwnershipTest`.
- ☐ **P2.4 / AGG2-25 (Low, confirmed benign)** — Document the dual-thread contract of the
  `startRecording` result callback at the `CameraViewModel` call site (may run on
  recorderExecutor OR main; body must stay thread-safe-primitives-only).
- ☐ **P2.5 / AGG2-22 (Low)** — Standby-meter loop: capture the ownership/wanted state observed
  AFTER the blocking `rec.read()` returns and pass it to `classifyAudioRead` instead of the
  literal `true` (`CameraEngine.kt:3270-3300`), mirroring `VideoRecorder.runAudio()`; prevents
  mis-charging the recreate budget on a REC-handoff race.
- ☐ **P2.6 / AGG2-27 (Low informational)** — Reset `standbyMeterFailureStreak` when the standby
  intent is explicitly (re)enabled (`setStandbyAudioMonitor(true)`), so a recovered mic isn't
  denied its recreate budget by stale history.

## Phase 3 — Finder + tap/loupe correctness (user focus area 2)

- ☐ **P3.1 / AGG2-3 (High, confirmed)** — Recenter the punch-in loupe when the tap clears:
  `CameraEngine.clearTapPoint()` also calls `gl.setPunchInCenter(0.5f, 0.5f)`
  (`CameraEngine.kt:1234-1236`), mirroring the metering reset one line above.
- ☐ **P3.2 / AGG2-7 (Medium, confirmed gap)** — Make the finder flag self-contained: call
  `pushTeleFinder()` at the end of `rollbackOptics` and inside `setResolvedOptics` (after the
  transaction fields land), removing the reliance on trailing-setter call order in the
  restore/MR path; also re-seed GL zoom (`seedGlZoom()`) on rollback so `zoomTarget` cannot
  stay stale with the PIP (fdcr's companion observation).
- ☐ **P3.3 / AGG2-28 (Low, confirmed logic)** — Resolve the finder's video-mode semantics: gate
  the PIP to PHOTO mode (`!videoMode` in `pushTeleFinder` and the Compose border condition),
  documenting it as a photo-composition aid — removes the "photo aspect setting toggles a video
  overlay" surprise. (Alternative — keying off video framing — recorded in the item; the
  narrow gate is chosen for v1.)
- ☐ **P3.4 / AGG2-32 + architect(D) (test gap Medium)** — Extract ONE pure predicate
  `teleFinderVisible(enabled, teleconverter, photoMode, aspect, zoom)` used by
  `pushTeleFinder` + the Compose border (GL keeps its resolved flag + zoom gate), and add an
  agreement matrix test so the three sites cannot drift (`ColorTagsTest` pattern).
- ☐ **P3.5 / AGG2-17 (Medium, likely)** — Add a compact `PIP` OSD tag in `StatusBar` whenever
  the Tele Finder toggle is ON (independent of current gating), alongside `LOUPE`/`STEADY` —
  the OSD is the UX_POLICY-sanctioned place for state, and this disambiguates
  "on-but-gated-off" from "off".

## Phase 4 — My Menu guards + UI/UX

- ☐ **P4.1 / AGG2-4 (High, confirmed)** — Guard `performQuickFn`'s unguarded branches
  (`ProSheet.kt:1260-1294`): TELECONVERTER requires `!state.isRecording`; TRANSFER requires
  `!state.isRecording && codec == HEVC` (same predicates as `quickFnEnabled`/`DialChip`).
  Give My Menu rows a real disabled state: an `enabled` parameter on `LabelValueRow` (or a
  MyMenu wrapper) driven by the same per-slot availability, dimmed + non-clickable when false.
- ☐ **P4.2 / AGG2-14 (High, confirmed via the app's own contrast test)** — Bring every HUD text
  surface up to the tested floor: route `StatusInfoPill` (0.45), `MemoryRecallStrip` empty-slot
  label (0.36), `ExposureMeter` label (0.48), `ZoomIndicator` (0.5), `RecordingIndicator` mm:ss
  (0.5), half-press action label (0.55) through `HUD_TEXT_SCRIM_ALPHA` (0.82), and give
  `RulerReadout` (`ManualDials.kt:574-584`) its own rounded scrim background. Extend
  `HudContrastTest` to pin the fixed alphas.
- ☐ **P4.3 / AGG2-15 (Medium-High, confirmed gap)** — Extract `DialChipRow`'s trailing-edge fade
  (`ManualDials.kt:260-272`) into a shared modifier and apply it to `SegmentedSelector`'s
  scrolling row (`ProControls.kt:138-155`) — same fix, same reason as the landed Fn-row hint.
- ☐ **P4.4 / AGG2-16 (Medium, confirmed policy deviation)** — Raise `MemorySlot` touch target
  `minWidth` 40 dp → 48 dp (`CameraScreen.kt:1159-1178`).
- ☐ **P4.5 / AGG2-19 (Medium, likely)** — Help-tip strip: `maxLines = 1`,
  `overflow = TextOverflow.Ellipsis` (`ProSheet.kt:243-253`) so the panel honors its own
  "doesn't jump" contract at large font scales.
- ☐ **P4.6 / AGG2-18 (Medium, needs-manual-validation)** — Complete the `GalleryThumb`
  empty-state pictograph (`MediaReview.kt:385-390`): frame outline + existing dot (photo glyph),
  consistent with the app's other multi-primitive hand-drawn icons.
- ☐ **P4.7 / AGG2-20 (Low-Medium, likely)** — Counter-rotate the review overlay's compact text
  (metadata block, `4×/8×` scale label) by threading the existing `overlayRotation` value into
  `MediaReviewOverlay` — same "compact glyphs rotate, wide elements stay fixed" rule as the
  shooting screen.
- ☐ **P4.8 / AGG2-26 (Low, confirmed minor)** — Back each settings tab's scroll state with
  `rememberSaveable(saver = ScrollState.Saver)` keyed by tab (`ProSheet.kt:218`), restoring the
  saveability the pre-cycle-1 single state had.

## Phase 5 — Preview-cadence and recomposition perf

- ☐ **P5.1 / AGG2-11 ⊃ AGG-20 (Medium-High mechanism, measured device fact)** — Give the
  controller a sensor-values fast path mirroring `submitZoomFastPath`: when an updateControls
  delta touches ONLY high-churn keys (LENS_FOCUS_DISTANCE; SENSOR_SENSITIVITY +
  SENSOR_EXPOSURE_TIME; leave zoom on its existing path) with the session, AE-mode, flash,
  metering, and every other key unchanged, mutate the cached repeating builder and submit,
  throttled to ≥200 ms mid-gesture with an exact landing at gesture end; anything else takes
  the full `startPreview` rebuild. Route the app-side AE loop's (iso, exposure) pair through
  one batched submit with the same ≥200 ms floor (the 0.30-stop/tick cap tolerates the coarser
  cadence). This retires cycle-1 deferral AGG-20 by implementation instead of measurement.
- ☐ **P5.2 / AGG2-13 (Medium, likely)** — Coalesce continuous-ruler input
  (focus/WB-kelvin/zoom-ruler/shutter-ANGLE) with the proven 16 ms leading+trailing pattern (a
  generic `coalesceControls {}` beside `applyZoomRatio`), so drags stop publishing full
  `CameraUiState` at ~120 Hz input rate.
- ☐ **P5.3 / AGG2-12 partial (Medium structural)** — `remember(state.caps, state.controls) {
  controlAvailability(...) }` in `TopBar` and `ManualDialCluster` so 5-10 Hz ticker emissions
  stop recomputing capability projections. (Parameter-narrowing and telemetry-flow-splitting
  deferred — see § Deferrals.)
- ☐ **P5.4 / AGG2-23 (Low)** — Route `ZoomIndicator`'s display range through the shared
  zoom-bounds clamp / defensive coerce (`CameraScreen.kt:566-582`) so a future caps profile
  cannot make the `Range` constructor throw per-recomposition.

## Phase 6 — Docs + test hygiene

- ☐ **P6.1 / AGG2-9 (High, confirmed)** — Restore the destroyed CLAUDE.md bullet header
  (`- **VIDEO stays on the STANDALONE lenses — the logical camera's EIS leaks its warp margin`)
  before the orphan `(2026-07-14).**` continuation at `CLAUDE.md:147`.
- ☐ **P6.2 / AGG2-10 (Medium-High, confirmed; 3 reviewers)** — Actually sync
  `docs/ARCHITECTURE.md`: Tele Finder PIP subsection (single-stream honesty, `finderRect` seam,
  `pushTeleFinder`, scissor isolation, RendererConfig replay, the new photo-only gate),
  module-map rows for `CameraPermissionPolicy`, `HardwareInputPolicy`, `OpticsConstraints`,
  `ZoomMath` (+ `RendererConfig`/`StillSnapshot`/`AudioReadPolicy` table entries), "Tele
  Finder" in the Assist-tab bullet (:734), and the tracker trim-eviction guard sentence in
  "Last-capture review ownership".
- ☐ **P6.3 / AGG2-31 (test gap Medium-High)** — Extract `MediaStoreWriter`'s orphan-sweep
  selection/args construction and process-start-epoch computation into pure functions; unit-test
  placeholder/arg-count agreement, the `DCIM/X9Tele/%` anchor rejecting `X9TeleOther`, and the
  epoch arithmetic (silent `runCatching` failure shape demands pinned tests).
- ☐ **P6.4 / AGG2-33 + AGG2-34 (confirmed weak tests)** — Remove the tautological second
  assertion in `CaptureOutputTrackerTest.partialFamilyDelete_...` (or rewrite it to model
  caller-layer partial-delete outcomes); drop `ProSheetTabTest`'s source-text grep assertions
  (keep its genuine selection-model logic test).
- ☐ **P6.5 / AGG2-39 (Low)** — Hedge the Play listing bitrate line to "target ~120 Mbps"
  (measured VBR overshoot ~134 Mbps documented in ARCHITECTURE) — truth-in-marketing nit.

## Phase 7 — Gates, security spot-checks, deploy, on-device verification

- ☐ **P7.1** — Whole-repo gates after all slices: `:app:assembleDebug :app:testDebugUnitTest
  :app:lintDebug` green; record counts in GATE_FIXES.
- ☐ **P7.2 / AGG2-37 (SEC-03, Low)** — On this build host: inspect the merged debug manifest for
  unexpected exported components / INTERNET-capable providers, and run `:app:dependencies` as a
  resolution sanity check (full OSV audit remains a release-gate item; record what was checked).
- ☐ **P7.3 / AGG2-40 + cycle-1 P6 closure** — Re-establish the ADB loopback proxy if needed, run
  DEPLOY_CMD once, then on-device verification: wake, launch, real-preview screencap statistics,
  zoom exercise (Shoot-tab slider path), TC toggle, photo↔video switches, still capture +
  output-file check, logcat scan for FATAL/ANR/CAMERA_ERROR/CAMERA_DISCONNECTED. While there,
  attempt AGG2-38's REC start/quick-stop probe (best-effort; timing may not be scriptable).
  Append the outcome to THIS plan and to cycle-1's plan Progress (closing its P6 record).

## Deferrals (each with severity/confidence preserved, reason, exit criterion)

- **AGG-19 (Low/Medium, carried from cycle 1)** — `FINDER_MIN_ZOOM = 1.15`
  (`camera/CameraState.kt:238`) possibly too low; critic sharpened: the gate AXIS is suspect —
  at rest the honest PIP duplicates the main view; it is only informative during punch-in /
  `zoomComp > 1`. Reason: pure on-device framing judgment; opt-in default-OFF feature; no code
  contradiction. NOT dropped — re-examined by all 13 reviewers this cycle. Exit criterion:
  on-device framing session with the converter mounted evaluating BOTH the threshold and a
  `punchIn || zoomComp > k` gate (BACKLOG AGG-19 updated to name the axis question).
- **AGG-20 (Low/Medium, carried from cycle 1)** — RESOLVED BY SCHEDULING: absorbed into AGG2-11
  and implemented as P5.1 (fast path + AE batching) instead of waiting for a measurement
  session. No longer a deferral once P5.1 lands.
- **AGG2-12 (b)(c) (Medium structural, perf PERF-3 remainder)** — parameter-narrowing of hot
  children and telemetry StateFlow splitting. Reason: broad UI-surface refactor with visual
  regression risk, magnitude unmeasured; the scheduled P5.3 removes the dominant recompute.
  Severity preserved Medium. Exit criterion: a device recomposition/GC trace quantifying the
  residual idle churn after P5.3; implement narrowing if a trace shows sustained >X%/frame cost.
- **AGG2-21 (Low, designer 9, needs-manual-validation)** — reduced-motion accommodation.
  Reason: all animations are functional state-reveals, not decorative; gating them is a
  judgment call needing an accessibility review decision. Exit criterion: explicit accessibility
  session (or owner decision) on honoring `ANIMATOR_DURATION_SCALE`/reduce-motion.
- **AGG2-24 (Low, debugger 4, needs-manual-validation)** — `Surface.release()` racing the GL
  thread's queued unbind. Reason: standard Android idiom (EGL window surface holds its own
  native ref); failure mode is already contained by the bounded preview-recovery path; no field
  evidence. Exit criterion: device logs showing `Preview EGL failure` during rapid
  detach/reattach cycles → then move the release behind the GL detach completion callback.
- **AGG2-36 remainder (Low structural, architect E)** — full `ControlsState` /
  `RecordingAdmission` sub-owner extraction from `CameraEngine`. Reason: large hot-file
  refactor; this cycle lands the two concrete first steps (P1.1 monitor funnel, P2.3 latch
  extraction). Exit criterion: a dedicated refactor cycle scheduled when `CameraEngine` next
  grows a new ownership gate, or on the next atomicity-gap finding.
- **AGG2-38 (Low, critic, needs-manual-validation, device-timing)** — does a stop latched
  during the ~100-700 ms admission window ever publish a 0-second/corrupt MP4? Reason: needs a
  human-timed double-tap or instrumented run; adb input timing is unreliable for a sub-second
  window (best-effort attempt scheduled in P7.3). Exit criterion: a device session reproducing
  stop-during-admission ≥10× confirming the pending-entry delete path holds.
- **SEC-03 remainder (Low, security)** — full OSV/dependency-check CVE audit of the resolved
  graph. Reason: release-gate scope; P7.2 covers the merged-manifest and resolution sanity
  checks this cycle. Exit criterion: run OSV scan as part of the next Play-release gate
  (BACKLOG release checklist).
- Standing BACKLOG items D-1..D-18 and residual field checks keep their recorded exit criteria;
  nothing in this cycle contradicts them (confirmed by all reviewers against the brief).
- Deferred work remains bound by repo policy when picked up: GPG-signed commits, Conventional
  Commits + gitmoji, no co-author trailer, pull-rebase before push, latest-stable toolchain,
  device verification before claiming camera/GL fixes done.

## Aggregate coverage map (each finding disposed exactly once)

AGG2-1→P1.1, AGG2-2→P1.2, AGG2-3→P3.1, AGG2-4→P4.1, AGG2-5→P2.1, AGG2-6→P2.2, AGG2-7→P3.2,
AGG2-8→P1.4, AGG2-9→P6.1, AGG2-10→P6.2, AGG2-11→P5.1, AGG2-12→P5.3+deferral, AGG2-13→P5.2,
AGG2-14→P4.2, AGG2-15→P4.3, AGG2-16→P4.4, AGG2-17→P3.5, AGG2-18→P4.6, AGG2-19→P4.5,
AGG2-20→P4.7, AGG2-21→deferred, AGG2-22→P2.5, AGG2-23→P5.4, AGG2-24→deferred, AGG2-25→P2.4,
AGG2-26→P4.8, AGG2-27→P2.6, AGG2-28→P3.3, AGG2-29→P1.5, AGG2-30→P1.3, AGG2-31→P6.3,
AGG2-32→P3.4, AGG2-33→P6.4, AGG2-34→P6.4, AGG2-35→P2.3, AGG2-36→P1.1+P2.3+deferral,
AGG2-37→P7.2+deferral, AGG2-38→P7.3+deferral, AGG2-39→P6.5, AGG2-40→P7.3.
Cycle-1 carries: AGG-19→deferred (sharpened), AGG-20→P5.1 (retired by implementation).

## Plan archiving

Per repo convention (2026-07-16 cycle-9 plan § Deferral; restated in the 2026-07-17 cycle-1
plan): completed dated plans under `docs/plans/` ARE the durable archive and are never moved or
deleted. All plans through `2026-07-17-rpf-cycle1.md` are fully implemented (cycle 1's
outstanding P6 on-device record is closed by this plan's P7.3) and stand as archived history.

## Progress

- Prompt 1: complete — 13/13 reviewer reports (qa-adversary + feature-dev-code-reviewer
  re-spawned after the session restart; both completed) + `_aggregate.md`. Baseline gates green
  at HEAD 1f0f0b1 (assembleDebug PASS, 494/494 tests, lintDebug clean).
- Prompt 2: complete — this plan; 38 findings scheduled across 7 phases, 8 deferral records
  above (none in the security/correctness/data-loss class); no implementation started before
  this plan was written.
- Prompt 3: not started at plan time. Implementation slices, per-slice gate results,
  the P7.2 spot-check outcomes, and the P7.3 deploy/on-device verification record are appended
  here as they actually execute.

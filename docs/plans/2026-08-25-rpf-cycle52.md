# Review-plan-fix cycle 52 — implementation plan

Date: 2026-08-25
Source: `.context/reviews/_aggregate.md`, cycle 52
Status: active; all ten findings scheduled, no deferrals

## Repository rules applied

- Preserve measured PMA110 Camera2 behavior, capability-enumerated generic-device paths, exact
  native/resource ownership, durable media truth, the quiet Sony-style viewfinder, EN/KO parity,
  and explicit device-evidence limits.
- Schedule every one of the ten aggregate findings below. Nothing is silently dropped,
  severity-downgraded, deferred, relabeled optional, or treated as fixed by documentation alone.
- Keep completed historical plans in `docs/plans/` as the repository's non-destructive archive.
  Every earlier plan is marked complete; none needs moving, deletion, rewriting, or reopening.
- Work only in `/tmp/find-x9-ultra-cycle52.868ovy/repo`; never modify, stash, reset, check out,
  clean, or build from the shared dirty `main` checkout.
- Use fine-grained GPG-signed Conventional Commits with gitmoji, pull with rebase before every push,
  and do not deploy because `DEPLOY_MODE=none`.

## P1 — make camera-policy StateFlow publication linearizable (AGG52-01, Medium/High)

- [x] Move exact sequence ownership into every `_state.update` retry or an equivalent serialized
  gate so an admitted older callback cannot repaint a newer terminal.
- [x] Preserve input-block and pinned-review state while policy truth changes.
- [x] Add a deterministic seq1-admitted → seq2-committed → seq1-resumed concurrency test and keep
  sequential stale-callback coverage.

Evidence: `4f7d5037` adds the exact serialized publication gate and deterministic overtaking test;
the focused ViewModel Robolectric suite passes with pinned review and independent input ownership
unchanged.

## P2 — review trusted hi-res stills without a 64 MiB compressed-source refusal (AGG52-02, Medium/Medium)

- [ ] Carry media provenance into thumbnail/full-review decode requests.
- [ ] For published app-owned media, decode bounds, pixels, and EXIF from fresh read-only provider
  handles without copying the entire compressed file; retain one bounded immutable spool for
  owner-unverified input where TOCTOU resistance is required.
- [ ] Keep decoded-pixel limits and latest-wins disposal unchanged; add valid above-64-MiB trusted
  source coverage plus owner-unverified bound/refusal coverage without claiming device output size.

## P3 — reclaim review spools after abnormal process termination (AGG52-03, Low/High)

- [ ] Move review sources into one dedicated private cache subdirectory.
- [ ] Before first process admission, perform bounded no-follow cleanup of stale regular spool files
  only; never traverse or delete unrelated cache content.
- [ ] Test stale restart fixtures, unexpected entry types, normal exact cleanup, and bounded failure.

## P4 — validate truecolor PNG transparency sample ranges (AGG52-04, Low/High)

- [x] Decode the three 16-bit `tRNS` samples and require each value to fit the IHDR bit depth.
- [x] Add CRC-correct 255-pass and 256/65535-fail mutations with clean no-traceback diagnostics.
- [x] Re-run the complete screenshot grammar/order suite.

Evidence: `6c5336a3` enforces the IHDR-derived sample ceiling; the focused palette/chunk/order tests
and all 154 committed documentation checks pass with clean mutation diagnostics.

## P5 — move optics rollback effects outside the Engine monitor (AGG52-05, Low/High)

- [x] Commit only the complete rollback state and immutable effect/publication packet under the
  Engine monitor.
- [x] After unlock, execute exact GL/controller commands and callbacks in the current documented
  order, with generation/controller identity checks where state could be superseded.
- [x] Add lock-state coverage for every rollback callback and preserve rollback, Ready, transfer,
  controls, caps, preview-aspect, and newer-pipeline supersession behavior.

Evidence: `75f51952` freezes one rollback effect packet, gates capture/REC until its post-monitor
effects finish, and identity-checks terminal publication. Lock-state coverage proves optics, caps,
size, aspect, Ready, and status callbacks run unlocked; the focused rollback suites plus the full
JVM and debug-lint gates pass.

## P6 — bind durable DISCARD replay to MediaStore identity (AGG52-06, Medium/Medium)

- [ ] Additively migrate the SQLite journal to a versioned record carrying URI, volume/provider
  version, and enough canonical row/family metadata to reject a replacement row.
- [ ] Capture identity before a marker authorizes future deletion; before replay, require provider
  version and current row identity to match. Treat changed, missing, unreadable, ambiguous, and
  legacy URI-only identity fail-closed without deleting a replacement.
- [ ] Add fake-provider URI-remap/version-change tests, schema-migration coverage, and stable-row
  recovery coverage. Preserve the real-device provider-reset question as a field obligation.

## P7 — distinguish standby native-acquisition refusal from revocation (AGG52-07, Medium/High)

- [x] Replace the Boolean standby create/start seam with a typed pre-entry / current-return /
  revoked-return result while preserving production process-gate semantics.
- [x] On revoked create or start, strongly retain the exact input/termination owner under process
  quarantine and prohibit publication, stop, or release.
- [x] Add deterministic create-return and start-return revocation tests proving exact generation
  completion, restart-required truth, no cleanup, and no second microphone.

Evidence: `fa30b3ca` adds the exact typed process-gate result and quarantine-retained standby owner;
`401870ef` covers pre-entry rejection, revoked create/start returns, exact owner identity, logical
completion, restart truth, and no second input/cleanup.

## P8 — bound a blocked standby `AudioRecord.stop()` (AGG52-08, High/High)

- [x] Give each exact standby termination owner one independent scheduled hard deadline.
- [x] If stop misses it, atomically abandon and quarantine-retain the input/owner, close all later
  native admission, release only logical handoff waiters, and make a late stop/worker return inert.
- [x] Surface the existing unsafe-native restart status and fault-test REC, pause, Engine replacement,
  timeout, and late return with no native release or second microphone.

Evidence: `fa30b3ca` reuses the first-wins native-operation deadline on each exact standby stop and
publishes Engine restart-required/Not-Ready truth on abandonment; `401870ef` blocks stop through the
deadline and proves bounded REC handoff, pause/re-enable and replacement refusal, one quarantine
terminal, late-return inertness, and zero native release.

## P9 — remove duplicate Compose focus owners (AGG52-09, Medium/Medium)

- [ ] Remove explicit `focusable()` from Settings Close/tabs, compact Fn, ruler Close, tap-focus
  reset, and Fn-modal Close where clickable/selectable already owns focus and keyboard activation.
- [ ] Preserve semantics, focus requester targets, enabled/disabled behavior, and touch bounds.
- [ ] Add production-composition Enter/DirectionCenter activation and single-edge traversal coverage
  across modal Close controls and all settings tabs.

## P10 — correct the v1.0.1 Korean localization record (AGG52-10, Low/High)

- [ ] Change the Play submission history from 131 to the reproducible 126 Korean string entries.
- [ ] Add a historical-source contract that checks the retained count/commit evidence cannot drift.

## Quality gates and delivery

- [ ] Run focused tests for policy publication, trusted/untrusted review input, crash-reclaimed spools,
  PNG sample bounds, rollback lock state, DISCARD identity migration/reassignment, standby native
  revocation/stop timeout, Compose focus activation/traversal, and localization history.
- [ ] Run `./gradlew :app:assembleDebug :app:testDebugUnitTest :app:lintDebug` across the whole repo.
- [ ] Run `./gradlew :app:lintRelease :app:assembleRelease :app:bundleRelease` across the whole repo.
- [ ] Run `python3 tools/verify_host.py` because repository authority requires it for delivery even
  though the orchestrator's configured gates are the two Gradle groups above.
- [ ] Root-fix every gate error and warning without suppression; record any authorized unavoidable
  warning under the strict deferred-finding rules.
- [ ] Commit and push each independent review, plan, implementation, test, documentation, and
  closeout change with signed Conventional Commits plus gitmoji, pulling with rebase before push.
- [ ] Mark this plan complete only after exact evidence is delivered.

## Deferred findings

None. All ten cycle-52 findings are scheduled for implementation in this cycle.

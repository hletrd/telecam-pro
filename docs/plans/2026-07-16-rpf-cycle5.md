# Review-Plan-Fix Cycle 5 Plan — 2026-07-16

Derived from `.context/reviews/_aggregate.md` (13 current-HEAD specialist
reports; exactly 6 deduplicated findings: 2 High, 2 Medium, and 2 Low, all High
confidence). Every aggregate finding is scheduled exactly once below; there are
no newly deferred cycle-5 findings.

Run constraints:

- Deployment mode is `none`: do not connect ADB, install, launch, publish,
  upload, or otherwise modify a device or production environment.
- The nested `ralph` skill named by the workflow is not installed. Implement
  this plan directly with the same fine-grained verify/commit/pull-rebase/push
  loop.
- Commits must be GPG-signed (`-S`), Conventional Commits with gitmoji, have no
  AI co-author trailer, and be pushed after `git pull --rebase`.
- Configured gates are `:app:assembleDebug`, `:app:testDebugUnitTest`,
  `:app:lintDebug`, `:app:lintRelease`, `:app:assembleRelease`, and
  `:app:bundleRelease` under JDK 21.
- No dependency or toolchain change is required.
- Camera2/HAL session fallbacks, first-shot latency, physical-camera metadata,
  and interaction behavior must be reported as statically/host-gate verified
  with PMA110 verification pending. `CLAUDE.md` forbids claiming device
  behavior from host gates alone.

Status: ☐ todo · ◐ in progress · ☑ done · ⏸ deferred

## Phase 1 — Convergent ownership and accepted-session truth

- ☐ **P1.1 / AGG-1 (High/High)** — Route all four same-route terminal paths
  through one convergence helper. Retain `commitOpticsReady` as the atomic
  ownership boundary; if it rejects because the session generation changed,
  reconfigure exactly when the optics transaction is still current, the engine
  is live, and no recorder prevents replacement. A superseded transaction must
  remain a no-op, and a successful commit must never schedule redundant work.
  Preserve monotonic session-generation semantics. Add deterministic JVM tests
  for successful commit, invalidated-owner recovery, paused/recording rejection,
  and superseded no-op behavior. `CameraEngine.kt:169-220,655-668,728-740,
  1145-1185,1280-1293,1335-1349`; `ReconfigurationGenerationTest.kt`.
- ☐ **P1.2 / AGG-2 (High/High)** — Give `CameraController` explicit processed
  and RAW reader availability, and publish an immutable active-output snapshot
  only when controller plus session generation still own Ready. Extend
  `CameraUiState` with actual processed/RAW availability; use those fields for
  format controls, shutter/snapshot admission, and RAW normalization instead of
  route capability heuristics. Normalize every capture request against the
  accepted session: retain available requested outputs, choose a deterministic
  available fallback only when necessary, and abort with an accurate status if
  no still target exists. Add full, processed-only, RAW-only, and preview-only
  format tests plus stale-generation output-publication tests.
  `CameraController.kt:355-398,789-803,1080-1110`;
  `CameraEngine.kt:1470-1499`; `CameraState.kt:400-426`;
  `CameraViewModel.kt:195-217,974-985`; `ProSheet.kt:509-524`.

## Phase 2 — Camera-thread latency and startup ordering

- ☐ **P2.1 / AGG-3 (Medium/High)** — Introduce immutable lightweight lens EXIF
  metadata (focal length, aperture, equivalent focal length). Prefetch selected
  logical-camera and physical-member metadata on `setupExecutor`; keep Camera2
  callbacks cache-only, falling back to already-selected route metadata without
  calling `CameraManager` or broad `CameraCaps.read`. Copy/close the processed
  image before ancillary EXIF composition. Add pure cache-hit/cache-miss tests
  demonstrating that callback resolution has no service-resolver seam.
  `CameraController.kt:417-432,926-978`;
  `CameraEngine.kt:296-306,1674-1689,2341-2350`;
  `CaptureCapabilities.kt:124-230`.
- ☐ **P2.2 / AGG-5 (Low/High)** — Queue the one-shot debug
  capability/vendor-tag scan behind the initial route/reconfigure task on the
  single setup executor. Preserve diagnostic coverage and update KDoc to state
  the real ordering guarantee rather than claiming a stronger first-frame
  property. `CameraEngine.kt:35,416-440,1218-1245`;
  `VendorTagInspector.kt:17-35`.

## Phase 3 — Interaction contract and source truth

- ☐ **P3.1 / AGG-4 (Medium/High)** — Give `CameraSlider`, the Pro-sheet close
  button, clickable `LabelValueRow`, Speed/Angle choices, and Fn close control a
  minimum 48 dp interaction target without enlarging their decorative content.
  Expose Speed/Angle choices with radio-button role and selected semantics.
  Retain existing control actions and visual hierarchy. `ProControls.kt:217-260,
  348-363`; `ProSheet.kt:245-254`; `ManualDials.kt:636-659`;
  `CameraScreen.kt:1204-1217`.
- ☐ **P3.2 / AGG-6 (Low/High)** — Describe `FPS_120` and its engine support as
  dormant compatibility code that the shipping picker intentionally excludes
  because of the target HAL abort. Remove the unreachable high-speed still-off
  UI branch and make `PhotoFormatToggles` documentation name HEIF, JPEG, and
  DNG. Do not expose, launch, or exercise high-speed recording.
  `CameraState.kt:299-306,329-364`; `CameraEngine.kt:882-905`;
  `ProSheet.kt:839-865`; `CameraViewModel.kt:355-360`;
  `ProControls.kt:605`.

## Phase 4 — Durable documentation and verification

- ☐ **P4.1** — Update active architecture/QA documentation with the accepted
  invariants: failed same-route terminal commits converge through reconfigure;
  format truth comes from accepted session outputs; first-shot EXIF lookup is
  CameraService-free; debug diagnostics queue behind initial open work. Record
  host regression evidence separately from PMA110 checks pending under
  deployment mode `none`. Do not edit dated historical review/spec snapshots.
- ☐ **P4.2** — Run targeted JVM tests after each implementation slice, then the
  complete configured gate set together under JDK 21. Fix every introduced
  error/warning, update this plan's progress/evidence, and use fine-grained
  signed commit → `git pull --rebase` → push iterations. Do not deploy or use
  ADB.

## Deferred findings

None newly deferred. Existing BACKLOG/prior-plan structural, localization,
instrumentation, device-measurement, DNG-threading, dual-encode, asynchronous
REC, signing-environment, codec, and vendor-audio items retain their prior exit
criteria. PMA110 confirmations named above are verification status for the
implemented fixes, not deferred code work.

## Aggregate coverage map

- Scheduled exactly once: AGG-1 through AGG-6.
- Deferred from this cycle: none.
- No plan file is moved: completed dated plans are durable historical records,
  and this repository has no plan-archive convention.

## Progress log

- 2026-07-16 — Prompt 1 complete: 13/13 reports returned; exactly 6 independent
  findings aggregated; debug assembly, 326 JVM tests, and lintDebug passed;
  security found no additional independent issue; no deployment/device work.
- 2026-07-16 — Prompt 2 complete: all six findings scheduled above with no new
  deferrals; implementation has not started.

# Review-plan-fix cycle 40 — implementation plan

Date: 2026-08-24
Source: `.context/reviews/_aggregate.md`, cycle 40
Status: complete; all eight findings fixed and every configured gate green

## Repository rules applied

- Preserve measured PMA110 behavior, capability-enumerated multi-device paths, exact native and
  MediaStore durability ownership, the quiet Sony-style UI, EN/KO parity, and clean-clone authority.
- Schedule every aggregate finding below. Nothing is silently dropped, deferred, severity-downgraded,
  or relabeled as optional.
- Keep completed historical plans as non-destructive archives; append truthful corrections rather
  than rewriting historical claims away.
- Work only in `/tmp/find-x9-ultra-cycle40.2BfF76`; never modify, stash, reset, check out, or build
  from the shared dirty `main` checkout.
- Use fine-grained GPG-signed Conventional Commits with gitmoji, pull with rebase before every push,
  and do not deploy because `DEPLOY_MODE=none`.

## P1 — restore authoritative host-gate truth (AGG40-01)

- [x] Append a dated correction to the cycle-39 completion record naming
  `python3 tools/verify_host.py` as the authoritative host gate.
- [x] Run the corrected committed-export documentation/tool contracts, then run the complete host
  gate after the closeout text is present and record only evidence that actually passed.
- [x] Keep the latest-completed-plan check intact and add no bypass or suppression.

## P2 — correct control interaction semantics (AGG40-02, AGG40-03)

- [x] Give every mutually exclusive segmented and transfer selector option explicit radio-button
  semantics while retaining genuine photo-format multi-select chips as checkboxes.
- [x] Give MR Save/Update explicit button semantics.
- [x] Make the visible manual-ruler readout decorative to accessibility so the slider is the one
  named, value-bearing adjustable node.
- [x] Add production-composition regressions covering radio, checkbox, button, and exactly-one
  manual-ruler value node.

## P3 — align snapshot system chrome with production (AGG40-04)

- [x] Share or apply the production explicit dark status/navigation bar styles in
  `UiSnapshotActivity`; never depend on the device night setting for this dark visual fixture.
- [x] Add a source/tool contract that rejects a bare snapshot-host `enableEdgeToEdge()` call.

## P4 — repair live and top-level authorities (AGG40-05, AGG40-06, AGG40-07)

- [x] Correct the CameraScreen Loupe Overview comment to the shared TELE-or-unified-zoom predicate,
  Photo-only 4:3 constraint, Video aspect independence, and absolute bottom-right RTL law.
- [x] Include the live Compose consumer in Loupe documentation contracts and negative fixtures.
- [x] Make rotation KDoc cite committed B1 as closed and front metering KDoc cite committed A4 as
  open; remove private-backlog-only operational guidance.
- [x] Correct the REC-border authority to the device-verified unscaled platform radius and add a
  narrow contract rejecting the retired 1.2 multiplier.

## P5 — eliminate every current lint warning without suppression (AGG40-08)

- [x] Put `AudioMeter`'s optional `Modifier` first and update callers without behavior change.
- [x] Replace the range wording that triggers `PluralsCandidate` with natural EN/KO percent-range
  copy while preserving both numeric bounds.
- [x] Enable release resource shrinking alongside R8 and verify release packaging.
- [x] Use the KTX bitmap constructor and a lint-clean Java durability bridge where callers must
  preserve `Editor.commit()`'s Boolean result; preserve exact bitmap config and fail-closed writes.
- [x] Run debug and release lint and confirm zero warnings rather than suppressing or deferring any.

## Quality gates and delivery

- [x] Run focused Compose semantics, rotation/Loupe, snapshot contract, storage, bitmap, and tooling
  tests plus `git diff --check`.
- [x] Run `python3 tools/verify_host.py` against the whole repository.
- [x] Run `./gradlew :app:assembleDebug :app:testDebugUnitTest :app:lintDebug` against the whole repo.
- [x] Run `./gradlew :app:lintRelease :app:assembleRelease :app:bundleRelease` against the whole repo.
- [x] Root-fix every gate error and warning without suppression.
- [x] Commit and push each independent review, plan, implementation, test, documentation, and
  closeout change with signed Conventional Commits plus gitmoji, pulling with rebase before push.
- [x] Mark this plan complete only after exact evidence is delivered.

## Deferred findings

None. All eight cycle-40 findings were implemented in this cycle.

## Completion evidence

- All eight deduplicated findings were implemented; nothing was deferred. Exclusive selectors now
  expose radio roles, genuine output multi-select retains checkbox roles, MR writes expose button
  roles, and manual rulers have one adjustable value-bearing accessibility stop.
- Production and the deterministic snapshot host share explicit light-on-dark system chrome.
  Loupe, video-rotation, front-metering, and REC-border source authorities match current executable
  behavior and committed field evidence, with negative documentation contracts for each drift.
- All seven former lint warnings were root-fixed without suppression. Debug and release lint reports
  contain zero errors and zero warnings; release resource shrinking completed successfully with R8.
- Focused Compose, storage-durability, EXIF bitmap, instrumentation-accessibility, and tooling
  regressions passed. `tools/check_docs.py` passed 125 checks with zero failures and 24 optional
  private-document skips; 104 tooling tests, 9 coverage-tool tests, and 184 device-harness unit tests
  passed.
- `python3 tools/verify_host.py` passed against the complete final tree. Partition A remained above
  threshold at 8,035/8,050 lines (99.81%) with the exact reviewed 15-line residual manifest.
- `./gradlew :app:assembleDebug :app:testDebugUnitTest :app:lintDebug` passed against the whole repo.
- `./gradlew :app:lintRelease :app:assembleRelease :app:bundleRelease` passed on clean signed source,
  producing the developer release APK and AAB. The isolated clone referenced existing gitignored
  signing inputs through read-only symlinks; no secret was copied or printed. The prebuilt native
  strip notice remains expected packaging diagnostics rather than a warning from Android lint.
- Eight fine-grained GPG-signed Conventional Commits with gitmoji were pushed after pull-with-rebase.
  Deployment was not run (`DEPLOY_MODE=none`), and no device action was taken.
- Work remained in `/tmp/find-x9-ultra-cycle40.2BfF76`. The shared dirty `main` checkout was never
  edited, staged, stashed, reset, checked out, or used for a build; its pre-existing CameraEngine,
  MediaStore, UI, test, and tooling deltas remain untouched.

# Review-plan-fix cycle 40 — implementation plan

Date: 2026-08-24
Source: `.context/reviews/_aggregate.md`, cycle 40
Status: ready for implementation; all eight findings scheduled, none deferred

## Repository rules applied

- Preserve measured PMA110 behavior, capability-enumerated multi-device paths, exact native and
  MediaStore durability ownership, the quiet Sony-style UI, EN/KO parity, and clean-clone authority.
- Schedule every aggregate finding below. Nothing is silently dropped, deferred, severity-downgraded,
  or relabeled as optional.
- Keep completed historical plans as non-destructive archives; append truthful corrections rather
  than rewriting historical claims away.
- Work only in `/tmp/find-x9-ultra-cycle40.2BfF76`; never modify, stash, reset, check out, or build
  from the shared dirty `main` checkout.
- Use fine-grained GPG-signed Conventional Commits with gitmoji, pull with rebase before every push,
  and do not deploy because `DEPLOY_MODE=none`.

## P1 — restore authoritative host-gate truth (AGG40-01)

- [ ] Append a dated correction to the cycle-39 completion record naming
  `python3 tools/verify_host.py` as the authoritative host gate.
- [ ] Run the corrected committed-export documentation/tool contracts, then run the complete host
  gate after the closeout text is present and record only evidence that actually passed.
- [ ] Keep the latest-completed-plan check intact and add no bypass or suppression.

## P2 — correct control interaction semantics (AGG40-02, AGG40-03)

- [ ] Give every mutually exclusive segmented and transfer selector option explicit radio-button
  semantics while retaining genuine photo-format multi-select chips as checkboxes.
- [ ] Give MR Save/Update explicit button semantics.
- [ ] Make the visible manual-ruler readout decorative to accessibility so the slider is the one
  named, value-bearing adjustable node.
- [ ] Add production-composition regressions covering radio, checkbox, button, and exactly-one
  manual-ruler value node.

## P3 — align snapshot system chrome with production (AGG40-04)

- [ ] Share or apply the production explicit dark status/navigation bar styles in
  `UiSnapshotActivity`; never depend on the device night setting for this dark visual fixture.
- [ ] Add a source/tool contract that rejects a bare snapshot-host `enableEdgeToEdge()` call.

## P4 — repair live and top-level authorities (AGG40-05, AGG40-06, AGG40-07)

- [ ] Correct the CameraScreen Loupe Overview comment to the shared TELE-or-unified-zoom predicate,
  Photo-only 4:3 constraint, Video aspect independence, and absolute bottom-right RTL law.
- [ ] Include the live Compose consumer in Loupe documentation contracts and negative fixtures.
- [ ] Make rotation KDoc cite committed B1 as closed and front metering KDoc cite committed A4 as
  open; remove private-backlog-only operational guidance.
- [ ] Correct the REC-border authority to the device-verified unscaled platform radius and add a
  narrow contract rejecting the retired 1.2 multiplier.

## P5 — eliminate every current lint warning without suppression (AGG40-08)

- [ ] Put `AudioMeter`'s optional `Modifier` first and update callers without behavior change.
- [ ] Replace the range wording that triggers `PluralsCandidate` with natural EN/KO percent-range
  copy while preserving both numeric bounds.
- [ ] Enable release resource shrinking alongside R8 and verify release packaging.
- [ ] Use the KTX bitmap constructor and synchronous `SharedPreferences.edit(commit = true)` forms,
  preserving exact bitmap config and durable commit results.
- [ ] Run debug and release lint and confirm zero warnings rather than suppressing or deferring any.

## Quality gates and delivery

- [ ] Run focused Compose semantics, rotation/Loupe, snapshot contract, storage, bitmap, and tooling
  tests plus `git diff --check`.
- [ ] Run `python3 tools/verify_host.py` against the whole repository.
- [ ] Run `./gradlew :app:assembleDebug :app:testDebugUnitTest :app:lintDebug` against the whole repo.
- [ ] Run `./gradlew :app:lintRelease :app:assembleRelease :app:bundleRelease` against the whole repo.
- [ ] Root-fix every gate error and warning without suppression.
- [ ] Commit and push each independent review, plan, implementation, test, documentation, and
  closeout change with signed Conventional Commits plus gitmoji, pulling with rebase before push.
- [ ] Mark this plan complete only after exact evidence is delivered.

## Deferred findings

None. All eight cycle-40 findings are scheduled for this cycle.

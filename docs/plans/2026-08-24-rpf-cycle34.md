# Review-plan-fix cycle 34 — implementation plan

Date: 2026-08-24  
Source: `.context/reviews/_aggregate.md`, cycle 34  
Status: complete; all eleven findings fixed, configured gates green, signed commits pushed

## Repository rules applied

- Preserve measured PMA110 behavior, capability-enumerated multi-device paths, Camera2 generation
  ownership, durable media truth, quiet Sony-style UI, EN/KO parity, and clean-clone authority.
- Schedule every one of the eleven aggregate findings below. Nothing is deferred, silently dropped,
  severity-downgraded, or introduced as unrelated scope.
- Keep completed historical plans as non-destructive archives in `docs/plans/`; no prior plan needs
  moving or deletion.
- Work only in `/tmp/find-x9-ultra-cycle34.EZe8ao`; never touch the shared dirty `main` checkout.
- Use fine-grained GPG-signed Conventional Commits with gitmoji, pull with rebase before every push,
  and do not deploy because `DEPLOY_MODE=none`.

## P1 — restore the authoritative exact-coverage boundary (AGG34-01, AGG34-05)

- [x] Add behavior-driven tests for all nine `ReviewStillGeometry.position` outcomes and boundaries,
  plus Compose directional/EN/KO/RTL-independent state-description coverage.
- [x] Exercise reachable `FamilyDeletionMarkerCapacityOwner`, `CameraScreenPolicy`, and
  `OwnerlessMediaDeleteOverrides` branches.
- [x] Review and exactly account only genuinely framework-bound/proven-unreachable leftovers without
  lowering the threshold or weakening residual-manifest strictness.
- [x] Regenerate coverage and pass the full authoritative host gate from current sources.

## P2 — unify lens-preservation migration defaults (AGG34-02)

- [x] Make the persisted absent-key fallback use the documented default-on policy.
- [x] Pin no-blob, legacy-blob, and current-blob parity with a saved non-main lens and visible toggle.
- [x] Update stale comments/authorities only where needed; preserve explicit user-authored false.

## P3 — cancel superseded dual-open waits promptly (AGG34-03)

- [x] Add a transaction-owned supersession/pause/release terminal without changing the two-second
  absolute HAL timeout or existing candidate/outgoing cleanup.
- [x] Prove a silent candidate A cannot hold the sole setup lane after transaction B supersedes it.
- [x] Prove A's late callback remains inert and all controller/session ownership stays exact.

## P4 — make audio clipping semantics producer-truthful (AGG34-04)

- [x] Carry per-channel peak/overload evidence alongside RMS through standby and recording seams.
- [x] Hold overload evidence at the coarse, non-live accessibility cadence while preserving the
  existing visual RMS meter behavior.
- [x] Test actual interleaved PCM including isolated and sustained full-scale samples, EN/KO state
  semantics, silence, high RMS, near clipping, and clipping.

## P5 — protect all exported debug activities (AGG34-06)

- [x] DUMP-protect Compose PreviewActivity and make the UI-test ComponentActivity non-exported in
  the debug manifest without breaking same-app debug/test use.
- [x] Extend merged-debug-manifest assertions to enumerate every exported component and permission.

## P6 — update the latest stable Android toolchain (AGG34-07)

- [x] Upgrade AGP 9.3.1 to latest stable 9.3.2, refresh reviewed dependency-verification entries,
  and update committed version authorities.
- [x] Confirm no newer stable supersedes 9.3.2 at implementation time using Google's metadata.

## P7 — remove build-warning noise (AGG34-08, AGG34-09)

- [x] Remove the unused experimental report-aggregation opt-in rather than suppressing its warning.
- [x] Replace the unused `Unit` queue-fixture expression with a warning-free intent-preserving body.
- [x] Force Kotlin test recompilation and audit all configured gate output for actionable warnings.

## P8 — correct release-minification commentary and guard it (AGG34-10)

- [x] Rewrite the debug preview comment to match full-mode R8 release configuration.
- [x] Extend the documentation/source checker and its negative fixture to reject equivalent stale
  affirmative claims outside historical archives.

## P9 — restore truthful completion evidence (AGG34-11)

- [x] Preserve cycle-33 history but append a concise superseding cycle-34 note after the gate is green.
- [x] Enforce that the newest repository-wide green claim names `python3 tools/verify_host.py`, not a
  narrower Gradle subset.
- [x] Record exact final host/debug/release evidence in this plan only after it actually passes.

## Quality gates and delivery

- [x] Run focused tests for every fix, manifest merges, tool/document contracts, and `git diff --check`.
- [x] Run `python3 tools/verify_host.py` against the whole repository.
- [x] Run `./gradlew :app:assembleDebug :app:testDebugUnitTest :app:lintDebug`.
- [x] Run `./gradlew :app:lintRelease :app:assembleRelease :app:bundleRelease`.
- [x] Root-fix every configured gate error and actionable warning without suppression.
- [x] Commit and push each independent review, plan, implementation, test, toolchain, and docs change
  with signed Conventional Commits plus gitmoji, pulling with rebase before every push.
- [x] Update this plan with exact evidence and mark complete only after all work is delivered.

## Deferred findings

None.

## Completion evidence

- All eleven aggregate findings were implemented with no review finding deferred or severity
  downgraded. The shared dirty main checkout was never edited, staged, stashed, reset, checked out,
  or used for a build; all work ran in `/tmp/find-x9-ultra-cycle34.EZe8ao`.
- Legacy saved blobs now inherit the documented default-on lens-preservation policy while explicit
  OFF remains durable. A migration test pins a remembered non-main lens and the visible policy value.
- Superseded dual-open attempts recheck optics/lifecycle ownership every 20 ms instead of blocking
  the sole setup lane for the whole two-second HAL timeout. Current silent attempts retain the exact
  absolute deadline, and focused tests cover signal, supersession, lifecycle retirement, timeout,
  default clock, and interruption.
- Audio meter frames carry RMS plus held per-channel post-gain peaks. Every standby/recording PCM
  buffer contributes peak evidence, isolated full-scale samples survive the UI throttle, and EN/KO
  non-live accessibility semantics claim clipping only from real peak truth.
- Debug-only transitive Compose activities are protected or non-exported; merged-manifest tests
  enumerate them. AGP is upgraded to the official latest stable 9.3.2 with verified artifacts.
  The unused experimental aggregation opt-in and Kotlin fixture warning are gone, and R8 commentary
  plus a negative documentation contract now agree with release full-mode minification.
- All nine accessible still-review positions, transition thresholds, physical D-pad directions, the
  family-capacity invariant, and the review-finalization default axis are host-tested. Partition A
  passes at 7,993/8,008 lines (99.81%) with an exact reviewed manifest of 15 lines across 10 classes;
  only the two direct Android MediaStore default-lambda calls added this cycle are classified as
  framework-bound, while their surrounding ownership/timeout effects are injected and tested.
- Cycle 33's historical gate claim has a non-destructive superseding note. The documentation gate
  now requires the newest completed plan to name `python3 tools/verify_host.py` and rejects a
  missing-authority fixture.
- Final authoritative host evidence passed on committed implementation source:
  `python3 tools/verify_host.py` ran the debug APK and androidTest packaging, all JVM/Robolectric/
  Compose tests, lint, exact coverage, 92 tool tests, nine coverage-tool tests, 183 device-harness
  tests, 107 documentation checks, Python compilation, and the repository diff check.
- The requested debug gate
  `./gradlew :app:assembleDebug :app:testDebugUnitTest :app:lintDebug` passed. The requested release
  gate `./gradlew :app:lintRelease :app:assembleRelease :app:bundleRelease` passed with R8 and local
  upload signing; these are developer outputs only, not immutable release evidence or deployment.
- Fourteen gate issues were root-fixed while integrating the plans: four stale exact-coverage
  classes, two pre-existing warning sources, one debug-test compile error, one newly required AGP
  artifact checksum, two review-position fixture failures, and four audio integration compile errors.
  The remaining manifest-merger no-network removal notices and prebuilt-native strip notice are
  expected diagnostics of deliberate fail-closed security/packaging policy, not suppressed errors.
- Every cycle review, plan, implementation, evidence, and closeout commit was GPG-signed,
  pull-rebased, and pushed immediately. Deployment was not run (`DEPLOY_MODE=none`).

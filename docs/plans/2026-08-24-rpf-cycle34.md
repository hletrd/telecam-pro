# Review-plan-fix cycle 34 — implementation plan

Date: 2026-08-24  
Source: `.context/reviews/_aggregate.md`, cycle 34  
Status: planned; implementation not started

## Repository rules applied

- Preserve measured PMA110 behavior, capability-enumerated multi-device paths, Camera2 generation
  ownership, durable media truth, quiet Sony-style UI, EN/KO parity, and clean-clone authority.
- Schedule every one of the eleven aggregate findings below. Nothing is deferred, silently dropped,
  severity-downgraded, or introduced as unrelated scope.
- Keep completed historical plans as non-destructive archives in `docs/plans/`; no prior plan needs
  moving or deletion.
- Work only in `/tmp/find-x9-ultra-cycle34.EZe8ao`; never touch the shared dirty `main` checkout.
- Use fine-grained GPG-signed Conventional Commits with gitmoji, pull with rebase before every push,
  and do not deploy because `DEPLOY_MODE=none`.

## P1 — restore the authoritative exact-coverage boundary (AGG34-01, AGG34-05)

- [ ] Add behavior-driven tests for all nine `ReviewStillGeometry.position` outcomes and boundaries,
  plus Compose directional/EN/KO/RTL-independent state-description coverage.
- [ ] Exercise reachable `FamilyDeletionMarkerCapacityOwner`, `CameraScreenPolicy`, and
  `OwnerlessMediaDeleteOverrides` branches.
- [ ] Review and exactly account only genuinely framework-bound/proven-unreachable leftovers without
  lowering the threshold or weakening residual-manifest strictness.
- [ ] Regenerate coverage and pass the full authoritative host gate from current sources.

## P2 — unify lens-preservation migration defaults (AGG34-02)

- [ ] Make the persisted absent-key fallback use the documented default-on policy.
- [ ] Pin no-blob, legacy-blob, and current-blob parity with a saved non-main lens and visible toggle.
- [ ] Update stale comments/authorities only where needed; preserve explicit user-authored false.

## P3 — cancel superseded dual-open waits promptly (AGG34-03)

- [ ] Add a transaction-owned supersession/pause/release terminal without changing the two-second
  absolute HAL timeout or existing candidate/outgoing cleanup.
- [ ] Prove a silent candidate A cannot hold the sole setup lane after transaction B supersedes it.
- [ ] Prove A's late callback remains inert and all controller/session ownership stays exact.

## P4 — make audio clipping semantics producer-truthful (AGG34-04)

- [ ] Carry per-channel peak/overload evidence alongside RMS through standby and recording seams.
- [ ] Hold overload evidence at the coarse, non-live accessibility cadence while preserving the
  existing visual RMS meter behavior.
- [ ] Test actual interleaved PCM including isolated and sustained full-scale samples, EN/KO state
  semantics, silence, high RMS, near clipping, and clipping.

## P5 — protect all exported debug activities (AGG34-06)

- [ ] DUMP-protect Compose PreviewActivity and make the UI-test ComponentActivity non-exported in
  the debug manifest without breaking same-app debug/test use.
- [ ] Extend merged-debug-manifest assertions to enumerate every exported component and permission.

## P6 — update the latest stable Android toolchain (AGG34-07)

- [ ] Upgrade AGP 9.3.1 to latest stable 9.3.2, refresh reviewed dependency-verification entries,
  and update committed version authorities.
- [ ] Confirm no newer stable supersedes 9.3.2 at implementation time using Google's metadata.

## P7 — remove build-warning noise (AGG34-08, AGG34-09)

- [ ] Remove the unused experimental report-aggregation opt-in rather than suppressing its warning.
- [ ] Replace the unused `Unit` queue-fixture expression with a warning-free intent-preserving body.
- [ ] Force Kotlin test recompilation and audit all configured gate output for actionable warnings.

## P8 — correct release-minification commentary and guard it (AGG34-10)

- [ ] Rewrite the debug preview comment to match full-mode R8 release configuration.
- [ ] Extend the documentation/source checker and its negative fixture to reject equivalent stale
  affirmative claims outside historical archives.

## P9 — restore truthful completion evidence (AGG34-11)

- [ ] Preserve cycle-33 history but append a concise superseding cycle-34 note after the gate is green.
- [ ] Enforce that the newest repository-wide green claim names `python3 tools/verify_host.py`, not a
  narrower Gradle subset.
- [ ] Record exact final host/debug/release evidence in this plan only after it actually passes.

## Quality gates and delivery

- [ ] Run focused tests for every fix, manifest merges, tool/document contracts, and `git diff --check`.
- [ ] Run `python3 tools/verify_host.py` against the whole repository.
- [ ] Run `./gradlew :app:assembleDebug :app:testDebugUnitTest :app:lintDebug`.
- [ ] Run `./gradlew :app:lintRelease :app:assembleRelease :app:bundleRelease`.
- [ ] Root-fix every configured gate error and actionable warning without suppression.
- [ ] Commit and push each independent review, plan, implementation, test, toolchain, and docs change
  with signed Conventional Commits plus gitmoji, pulling with rebase before every push.
- [ ] Update this plan with exact evidence and mark complete only after all work is delivered.

## Deferred findings

None.

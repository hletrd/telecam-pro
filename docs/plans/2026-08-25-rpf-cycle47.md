# Review-plan-fix cycle 47 — implementation plan

Date: 2026-08-25
Source: `.context/reviews/_aggregate.md`, cycle 47
Status: complete; all ten findings fixed and every configured and authoritative host gate green

## Repository rules applied

- Preserve measured PMA110 Camera2 behavior, capability-enumerated generic-device paths, exact
  lifecycle/native ownership, durable media truth, the quiet Sony-style viewfinder, EN/KO parity,
  and explicit device-evidence limits.
- Schedule every one of the ten aggregate findings below. Nothing is silently dropped, deferred,
  severity-downgraded, or relabeled as optional.
- Keep completed historical plans in `docs/plans/` as the repository's non-destructive archive; no
  completed plan is deleted, moved, rewritten, or reopened.
- Work only in `/tmp/find-x9-ultra-cycle47.3oSGJA`; never modify, stash, reset, check out, clean, or
  build from the shared dirty `main` checkout.
- Use fine-grained GPG-signed Conventional Commits with gitmoji, pull with rebase before every push,
  and do not deploy because `DEPLOY_MODE=none`.

## P1 — make transfer part of the complete optics transaction (AGG47-01)

- [x] Put normalized target transfer and accepted-before transfer in the same immutable,
  generation-owned packet as mode, route, and session requirements.
- [x] Make mode/recall changes perform at most one structural reconfiguration and make direct
  transfer changes capture rollback truth before mutation.
- [x] Add deterministic one-reopen and forced-failure tests covering Engine/GL transfer, accepted
  Camera2 truth, Ready restoration, and next-recorder configuration.

## P2 — bound capture-registration diagnostics (AGG47-02)

- [x] Admit the pre-kill capture-family registration trace only for the exact Single/diagnostic
  path that consumes it; keep BURST, AEB, and TIMELAPSE from emitting the new per-shot line.
- [x] Add host coverage for drive-mode admission and one-shot/bounded ownership without weakening
  the existing settled trace contract.

## P3 — retire standby microphone ownership under external surfaces (AGG47-03)

- [x] Include aggregate Activity/Compose input-obscuration truth in standby-meter admission and
  refresh ownership atomically on every owner edge.
- [x] Add lifecycle/owner tests proving release before media-permission launch and reacquisition only
  after the external owner retires while the detailed meter remains requested.

## P4 — cancel accepted gestures when obscuration starts (AGG47-04)

- [x] Track the admitted pointer stream and dispatch exactly one `ACTION_CANCEL` when it first
  becomes obscured; reject the rest until a fresh unobscured DOWN.
- [x] Add real-Activity and production Compose gesture sequences covering tap and drag/pinch
  terminals plus normal next-gesture recovery.

## P5 — classify ADB reconnect failures exactly (AGG47-05)

- [x] Match only canonical host-side transport/device-selection errors, reconnect only endpoint
  serials that support it, and report the latest retry stderr.
- [x] Add table-driven tests for offline→success, offline→unauthorized, remote command absence, USB
  serials, and reconnect timeout.

## P6 — validate screenshot geometry from PNG bytes (AGG47-06)

- [x] Parse PNG IHDR and compare actual width, height, bit depth, and color type to the committed
  phone/tablet asset contract.
- [x] Close the device/locale/orientation/crop schema and add wrong-dimension/orientation PNG
  mutations whose updated digest must still fail readiness.

## P7 — expose truthful thumbnail state to TalkBack (AGG47-07)

- [x] Add localized EN/KO loading, ready, and failed state descriptions while preserving the stable
  media-specific review action name and quiet visual surface.
- [x] Extend production Compose coverage to assert action and state for every still/video/RAW branch,
  including unknown-kind timeout/capacity failure.

## P8 — compile and link shipping GLSL in an authoritative gate (AGG47-08)

- [x] Add a pinned, deterministic GLES-compatible whole-program compile/link and required-interface
  validator for the exact interpolated shipping vertex/fragment sources, or establish an equivalent
  bounded executed GL-init release gate if host validation cannot model external-OES correctly.
- [x] Add syntax, vertex/fragment interface, and required-uniform mutation coverage and wire the
  validator into the authoritative host gate.

## P9 — verify packaged release permissions against privacy authority (AGG47-09)

- [x] Parse the effective packaged manifest permission set from the existing bundletool dump and
  require exact equality with one closed documented permission authority.
- [x] Explicitly reject network permissions and add packaged-manifest tests for the expected set,
  an extra permission, and reintroduced network access while keeping source checks as early feedback.

## P10 — enforce a closed translation-exception allow list (AGG47-10)

- [x] Make the XML documentation/tool gate own the complete approved `translatable="false"`
  resource-name set and require Korean peers for every other user-facing string/plural.
- [x] Add mutations for ordinary non-translatable prose and an unapproved default-only resource;
  retain runtime localization samples for presentation quality only.

## Quality gates and delivery

- [x] Run focused tests for every transaction, ownership, tooling, accessibility, release, and
  localization seam plus `git diff --check`.
- [x] Run `./gradlew :app:assembleDebug :app:testDebugUnitTest :app:lintDebug` across the whole repo.
- [x] Run `./gradlew :app:lintRelease :app:assembleRelease :app:bundleRelease` across the whole repo.
- [x] Root-fix every gate error and warning without suppression.
- [x] Commit and push each independent review, plan, implementation, test, documentation, and
  closeout change with signed Conventional Commits plus gitmoji, pulling with rebase before push.
- [x] Mark this plan complete only after exact evidence is delivered.

## Deferred findings

None. All ten cycle-47 findings are scheduled for this cycle.

## Completion evidence

- All ten deduplicated findings were implemented; none was deferred. Transfer now belongs to one
  complete generation-owned optics transaction for mode, recall, and direct changes; failed direct
  SDR↔HLG reopens restore the accepted curve, and Photo→non-SDR Video queues no trailing transfer
  reopen. Capture registration logs are bounded to ordinary Single captures.
- External permission ownership retires the standby microphone before the system surface and
  restores it only after unobscuration. Mid-gesture obscuration delivers exactly one `ACTION_CANCEL`,
  rejects the tainted remainder, and admits the next fresh clean gesture.
- ADB recovery now distinguishes canonical endpoint transport failures from remote command errors,
  refuses wireless reconnect for USB serials, and reports the latest retry evidence. The complete
  device-harness self-test suite passed 195 tests.
- Phone/tablet screenshot readiness parses actual PNG IHDR geometry/encoding and enforces closed
  capture schemas. The translation exception set is closed at XML level with full EN/KO string and
  plural parity. Documentation tooling passed 151 checks with 24 documented private-context skips.
- Upload verification enforces the exact packaged AAB permission set against one privacy authority,
  including explicit network-permission rejection. Release artifact tests passed as part of 120
  tooling tests.
- The exact interpolated shipping GLSL ES vertex/fragment sources compile and link under the Android
  Emulator's validator; every runtime attribute/uniform is audited and syntax, stage-interface, and
  renamed-uniform mutations are covered. The Android SDK preflight now names that stable package.
- Configured debug gate passed: debug assembly, the complete JVM/Robolectric/Compose suite, and
  debug lint were green. Configured release gate passed: release lint, signed APK, and signed AAB
  were green using read-only links to the existing ignored signing inputs.
- `python3 tools/verify_host.py` passed: Partition A 8,219/8,234 lines (99.82%), 120 tooling tests,
  nine coverage-tool tests, 195 device-harness self-tests, 151 documentation checks, Python
  compilation, Android debug/androidTest assembly, tests/lint, and `git diff --check` were green.
- No error-level gate issue or actionable warning surfaced, so no gate-driven source fix was needed.
  The native strip message for `libandroidx.graphics.path.so` is the repository's known packaging
  diagnostic, not an actionable compiler or lint warning.
- Twelve fine-grained GPG-signed Conventional Commits with gitmoji were pushed before this closeout.
  Deployment was not run (`DEPLOY_MODE=none`), and no device action was taken.
- All work ran in isolated `/tmp` clone `/tmp/find-x9-ultra-cycle47.3oSGJA`. The shared dirty `main`
  checkout was never edited, staged, stashed, reset, checked out, cleaned, or used for a build; its
  uncommitted changes remain intact.

### 2026-08-25 evidence correction (cycle 48)

The original gallery matrix covered video loading/failed and still/RAW ready, but omitted the
`Ready(VIDEO, bitmap)` cross-product despite the broader completion wording in P7. Cycle 48 added
that production-surface paint, action/state, disabled, EN/KO, and pixel evidence. This correction
preserves the original closeout while making its evidence boundary explicit.

The original obscured-stream closeout proved Activity dispatch against a generic `View`, not the
production Compose gesture loops claimed in P4. Cycle 48 added real Activity+`CameraScreen` tap and
two-pointer pinch plus production `CameraSlider` drag sequences under both obscuration flags. That
evidence also caught and fixed a cancel edge that the generic target could not expose: Compose
collapsed `ACTION_CANCEL` to a released pointer and the finder briefly treated it as a tap.

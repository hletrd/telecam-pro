# Review-plan-fix cycle 47 — implementation plan

Date: 2026-08-25
Source: `.context/reviews/_aggregate.md`, cycle 47
Status: in progress

## Repository rules applied

- Preserve measured PMA110 Camera2 behavior, capability-enumerated generic-device paths, exact
  lifecycle/native ownership, durable media truth, the quiet Sony-style viewfinder, EN/KO parity,
  and explicit device-evidence limits.
- Schedule every one of the ten aggregate findings below. Nothing is silently dropped, deferred,
  severity-downgraded, or relabeled as optional.
- Keep completed historical plans in `docs/plans/` as the repository's non-destructive archive; no
  completed plan is deleted, moved, rewritten, or reopened.
- Work only in `/tmp/find-x9-ultra-cycle47.3oSGJA`; never modify, stash, reset, check out, clean, or
  build from the shared dirty `main` checkout.
- Use fine-grained GPG-signed Conventional Commits with gitmoji, pull with rebase before every push,
  and do not deploy because `DEPLOY_MODE=none`.

## P1 — make transfer part of the complete optics transaction (AGG47-01)

- [ ] Put normalized target transfer and accepted-before transfer in the same immutable,
  generation-owned packet as mode, route, and session requirements.
- [ ] Make mode/recall changes perform at most one structural reconfiguration and make direct
  transfer changes capture rollback truth before mutation.
- [ ] Add deterministic one-reopen and forced-failure tests covering Engine/GL transfer, accepted
  Camera2 truth, Ready restoration, and next-recorder configuration.

## P2 — bound capture-registration diagnostics (AGG47-02)

- [ ] Admit the pre-kill capture-family registration trace only for the exact Single/diagnostic
  path that consumes it; keep BURST, AEB, and TIMELAPSE from emitting the new per-shot line.
- [ ] Add host coverage for drive-mode admission and one-shot/bounded ownership without weakening
  the existing settled trace contract.

## P3 — retire standby microphone ownership under external surfaces (AGG47-03)

- [ ] Include aggregate Activity/Compose input-obscuration truth in standby-meter admission and
  refresh ownership atomically on every owner edge.
- [ ] Add lifecycle/owner tests proving release before media-permission launch and reacquisition only
  after the external owner retires while the detailed meter remains requested.

## P4 — cancel accepted gestures when obscuration starts (AGG47-04)

- [ ] Track the admitted pointer stream and dispatch exactly one `ACTION_CANCEL` when it first
  becomes obscured; reject the rest until a fresh unobscured DOWN.
- [ ] Add real-Activity and production Compose gesture sequences covering tap and drag/pinch
  terminals plus normal next-gesture recovery.

## P5 — classify ADB reconnect failures exactly (AGG47-05)

- [ ] Match only canonical host-side transport/device-selection errors, reconnect only endpoint
  serials that support it, and report the latest retry stderr.
- [ ] Add table-driven tests for offline→success, offline→unauthorized, remote command absence, USB
  serials, and reconnect timeout.

## P6 — validate screenshot geometry from PNG bytes (AGG47-06)

- [ ] Parse PNG IHDR and compare actual width, height, bit depth, and color type to the committed
  phone/tablet asset contract.
- [ ] Close the device/locale/orientation/crop schema and add wrong-dimension/orientation PNG
  mutations whose updated digest must still fail readiness.

## P7 — expose truthful thumbnail state to TalkBack (AGG47-07)

- [ ] Add localized EN/KO loading, ready, and failed state descriptions while preserving the stable
  media-specific review action name and quiet visual surface.
- [ ] Extend production Compose coverage to assert action and state for every still/video/RAW branch,
  including unknown-kind timeout/capacity failure.

## P8 — compile and link shipping GLSL in an authoritative gate (AGG47-08)

- [ ] Add a pinned, deterministic GLES-compatible whole-program compile/link and required-interface
  validator for the exact interpolated shipping vertex/fragment sources, or establish an equivalent
  bounded executed GL-init release gate if host validation cannot model external-OES correctly.
- [ ] Add syntax, vertex/fragment interface, and required-uniform mutation coverage and wire the
  validator into the authoritative host gate.

## P9 — verify packaged release permissions against privacy authority (AGG47-09)

- [ ] Parse the effective packaged manifest permission set from the existing bundletool dump and
  require exact equality with one closed documented permission authority.
- [ ] Explicitly reject network permissions and add packaged-manifest tests for the expected set,
  an extra permission, and reintroduced network access while keeping source checks as early feedback.

## P10 — enforce a closed translation-exception allow list (AGG47-10)

- [ ] Make the XML documentation/tool gate own the complete approved `translatable="false"`
  resource-name set and require Korean peers for every other user-facing string/plural.
- [ ] Add mutations for ordinary non-translatable prose and an unapproved default-only resource;
  retain runtime localization samples for presentation quality only.

## Quality gates and delivery

- [ ] Run focused tests for every transaction, ownership, tooling, accessibility, release, and
  localization seam plus `git diff --check`.
- [ ] Run `./gradlew :app:assembleDebug :app:testDebugUnitTest :app:lintDebug` across the whole repo.
- [ ] Run `./gradlew :app:lintRelease :app:assembleRelease :app:bundleRelease` across the whole repo.
- [ ] Root-fix every gate error and warning without suppression.
- [ ] Commit and push each independent review, plan, implementation, test, documentation, and
  closeout change with signed Conventional Commits plus gitmoji, pulling with rebase before push.
- [ ] Mark this plan complete only after exact evidence is delivered.

## Deferred findings

None. All ten cycle-47 findings are scheduled for this cycle.

# Review-plan-fix cycle 14 — implementation plan

Date: 2026-08-24
Source: `.context/reviews/_aggregate.md`, cycle 14
Status: planned; implementation not started

## Repository rules applied

- Preserve PMA110 behavior byte-for-byte and do not infer or run device behavior; deployment is
  disabled for this invocation.
- Correctness, security, evidence-integrity, liveness, and data-loss findings are mandatory and are
  not deferred.
- Preserve English/Korean parity, the quiet Sony Alpha/Xperia Pro surface, exact coverage, immutable
  build boundaries, and existing owner-cleared deferrals.
- Completed plans remain as non-destructive archives. Cycle 13's aggregate is preserved as
  `.context/reviews/_aggregate-cycle13.md`; no historical plan or review is deleted.
- Use fine-grained GPG-signed Conventional Commits with gitmoji, pull with rebase before each push,
  never force-push, and never bypass a configured gate.

Every aggregate finding AGG14-01 through AGG14-14 is scheduled below. No finding is deferred,
severity-downgraded, or silently dropped.

## Phase 1 — make release and inspection inputs immutable

- [ ] **P1 / AGG14-01:** give immutable release builds a sealed compiler-input owner equivalent to
  the debug snapshot seal. Pre-create legitimate writable output/cache roots, make tracked build
  inputs read-only, verify device/inode/type/mode/size/mtime/ctime plus content identity before
  artifact export, and add deterministic A -> B -> A mutation coverage.
- [ ] **P2 / AGG14-02:** seal device APK and release AAB private inspection copies for their complete
  multi-reader lifetime. Verify the same exact private inode after each external inspector and before
  publication; test permanent and transient private-copy mutation for both tools.

Acceptance: no compiler or inspector can consume bytes different from the source/artifact identity
named by the output, even when the path returns to its original digest.

## Phase 2 — close device-evidence authority races

- [ ] **P3 / AGG14-03:** own one no-follow report-root descriptor and the exact reserved output
  inodes across freeze, publication, final verification, and rollback. Publish the checksum sidecar
  only as the terminal success marker and add root/leaf rename/replacement tests proving no valid
  pair survives a failed finalization.
- [ ] **P4 / AGG14-04:** freeze Git HEAD/ref, index, status, ignore/config inputs, and scoped source
  bytes under one checkout identity decision. Add deterministic HEAD advance, index replacement,
  dirty-state transition, and A -> B -> A tests with unchanged scoped source.
- [ ] **P5 / AGG14-05:** extract `CAPTURE_SUBDIR` from the same frozen descriptor-owned source set
  used for APK/source matching, return it as a typed proven contract, remove the later pathname
  reopen, and test mutation between identity proof and harness setup.

Acceptance: a device run, its media scope, and its final attestation have one immutable checkout,
artifact, and report-owner authority from preflight through publication.

## Phase 3 — repair runtime correctness and teardown/resource ownership

- [ ] **P6 / AGG14-06:** separate any visual-media access from Images+Video completeness, request
  exactly the missing broad permission(s), preserve Android 14 selected-media partial access, and
  cover every grant combination and exact request decision.
- [ ] **P7 / AGG14-07:** replace independently detached callbacks with an atomically retired,
  generation-aware sink or a drainable admitted-call owner. Make residual ViewModel executor
  submission rejection-safe and force an acquire -> clear -> resume interleaving test that proves
  no post-clear mutation, Handler post, or worker exception.
- [ ] **P8 / AGG14-08:** move launch media recovery behind one process-wide single-flight finite
  owner; coalesce Engine subscribers, bound row batches with durable continuation/rescan semantics,
  and test a blocked provider through repeated Engine replacement plus a large pending-row set.

Acceptance: permission restore cannot permanently hide one media collection, lifecycle teardown has
one callback terminal boundary, and Engine churn cannot multiply recovery workers or unbounded scans.

## Phase 4 — bound high-cost preview and review work

- [ ] **P9 / AGG14-09:** introduce an identity-owned finite review-decode lane that admits at most
  one heavy bitmap decode, coalesces the newest URI/request, and disposes any stale bitmap result.
  Add deterministic cancel A -> load B/C concurrency and stale-publication tests.
- [ ] **P10 / AGG14-10:** coalesce `SurfaceTexture` frame notifications behind one scheduled/dirty
  owner. Latch/draw the newest real frame once, re-arm when a notification arrives during draw, keep
  zoom self-redraw preview-only, and prove queued callbacks do not duplicate encoder/analysis work.

Acceptance: cancellation or producer backpressure cannot multiply tens-of-MiB decodes or redundant
full GL/encoder/readback passes.

## Phase 5 — align active documentation, screenshots, and release truth

- [ ] **P11 / AGG14-11:** update every Loupe action/Engine/test comment to enabled + punch-in +
  (TELE or unified zoom >= 3x), Photo 4:3 only, Video independent of still aspect. Add negative docs
  checks for the obsolete Photo/TELE-only phrases and scope Compose coverage to the behavior it owns.
- [ ] **P12 / AGG14-12:** change Architecture's device-evidence commands to capture and use the
  immutable wrapper's exact printed APK path for install and component inspection. Add a docs check
  that rejects mutable `app/build/outputs` paths in that evidence block.
- [ ] **P13 / AGG14-13:** fail-close the stale phone screenshot set: mark frames 02 and 06 as not
  submission-ready, record their obsolete semantic strings and the exact immutable device-recapture
  requirement, and add a checked asset-validity/provenance manifest that invalidates current status
  when expected copy/source identity changes. Do not deploy or fabricate replacement device frames.
- [ ] **P14 / AGG14-14:** update the active BACKLOG GitHub About completion record to the current
  any-RAW-lens route and live tagline, label the TELE-only text as superseded history, and add a docs
  guard rejecting the obsolete claim in active-board sections.

Acceptance: no active authority or submission checklist claims obsolete route behavior, mutable
evidence bytes, stale device frames, or a superseded external tagline.

## Phase 6 — gates, delivery, and evidence

- [ ] Run `./gradlew :app:assembleDebug :app:testDebugUnitTest :app:lintDebug` across the repository.
- [ ] Run release lint/assembly/bundle through `tools/build_immutable_release.py` because direct
  release tasks fail closed by repository design.
- [ ] Run `python3 tools/verify_host.py`, focused Python/tool/device-harness/docs suites, Python
  compilation, exact Partition-A coverage verification, and repository diff/whitespace checks.
- [ ] Root-fix every error and actionable warning without suppression; record any genuinely
  unfixable warning under the strict deferred format before committing.
- [ ] Commit each independent fix with a GPG-signed Conventional Commit + gitmoji message, pull with
  rebase before every push, push, then mark this plan complete with exact evidence. Do not deploy.

## Planned commit sequence

`fix(provenance)`, `fix(attestation)`, `fix(permissions)`, `fix(lifecycle)`, `perf(storage)`,
`perf(review)`, `perf(gl)`, `docs(loupe)`, `docs(evidence)`, `docs(play)`, and `docs(plan)`, split
further wherever one commit would combine unrelated concerns.

## Deferred findings

None. P13 resolves the repository correctness issue by making stale assets fail closed; collecting
new physical-device screenshots remains an explicit owner/device release action and is not performed
under this invocation's no-deploy directive.

## Residual warnings

None accepted at planning time. Any configured-gate warning that cannot be fixed cleanly must be
recorded here with file+line, original severity/confidence, concrete reason, and reopening criterion.

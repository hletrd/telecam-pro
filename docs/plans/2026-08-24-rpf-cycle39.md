# Review-plan-fix cycle 39 — implementation plan

Date: 2026-08-24
Source: `.context/reviews/_aggregate.md`, cycle 39
Status: active

## Repository rules applied

- Preserve measured PMA110 behavior, capability-enumerated multi-device paths, exact Camera2/native
  ownership, quiet Sony-style UI, EN/KO parity, and clean-clone authority.
- Schedule both aggregate findings below. Nothing is silently dropped, deferred, or
  severity-downgraded.
- Keep completed historical plans as non-destructive archives in `docs/plans/`; do not rewrite the
  cycle-38 completion record, but append a dated correction where its evidence claim was incomplete.
- Work only in `/private/tmp/find-x9-cycle39.feeBBZ`; never modify, stash, reset, check out, or build
  from the shared dirty `main` checkout.
- Use fine-grained GPG-signed Conventional Commits with gitmoji, pull with rebase before every push,
  and do not deploy because `DEPLOY_MODE=none`.

## P1 — cover stabilization at the Engine side-effect boundary (AGG39-01)

- [ ] Add the narrowest internal Engine test seam needed to observe stabilization apply and session
  reopen effects without constructing a live Camera2 session or changing production behavior.
- [ ] Drive same-effective Enhanced→Standard and Enhanced→Off transitions through
  `CameraEngine.setVideoStabMode`; assert that the requested label is stored and both effect counts
  stay at zero.
- [ ] Drive real Off→Standard and Standard→Enhanced transitions through the same production entry;
  assert stored intent and exactly one stabilization apply plus one session reopen per transition.
- [ ] Append a dated correction to the cycle-38 plan naming the new Engine-level evidence; preserve
  its historical pure-predicate claim rather than rewriting it away.

## P2 — make Loupe Overview corner authority match executable geometry (AGG39-02)

- [ ] Correct `CLAUDE.md`, `docs/ARCHITECTURE.md`, and the renderer comment to name the right-inset
  bottom-right corner that `finderRect` and Compose already enforce.
- [ ] Extend `tools/check_docs.py` and its contract tests so the current authorities cannot drift
  back to bottom-left while the executable geometry remains right-inset.

## Quality gates and delivery

- [ ] Run focused Engine stabilization, finder geometry, documentation-contract, and tooling tests,
  plus `git diff --check`.
- [ ] Run `./gradlew :app:assembleDebug :app:testDebugUnitTest :app:lintDebug` against the whole repo.
- [ ] Run `./gradlew :app:lintRelease :app:assembleRelease :app:bundleRelease` against the whole repo.
- [ ] Root-fix every configured gate error and actionable warning without suppression.
- [ ] Commit and push each independent review, plan, implementation, test, documentation, and
  closeout change with signed Conventional Commits plus gitmoji, pulling with rebase before push.
- [ ] Mark this plan complete only after exact evidence is delivered.

## Deferred findings

None. Both cycle-39 findings are scheduled in P1-P2.

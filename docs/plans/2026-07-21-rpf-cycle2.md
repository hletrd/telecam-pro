# Review-Plan-Fix Cycle 2 Implementation Plan — 2026-07-21

## Scope and immutable boundaries

- Review target: `c046ba8b3feb5f951243eba168b8257e730e340c`
- Aggregate source: `.context/reviews/_aggregate.md` (17 unique Cycle 2 findings)
- Device target: PMA110 at `127.0.0.1:5602` only; never use port 5600
- Deployment mode: `none`; debug APK install/relaunch and deterministic UI snapshots only
- Preserve the portrait-locked activity, single physical Camera2 output route, preview/mask/encoder
  framing, and current no-true-PIP safety boundary.
- Do not capture, record, route a physical-camera PIP output, or perform a concurrent-camera/HAL
  experiment. Do not release MediaCodec/native-window owners when EGL release is unproven.
- Preserve the existing numeric Fn ownership transitions: Focus→MF, SS from P/ISO→S, ISO from
  P/S→ISO, WB preset→Exposure sheet, and EV unavailable in M.

## Phase 1 — terminal ownership and media correctness

- [x] **P1.1 / AGG2-1 (High/High): bounded recording teardown coordinator.** Add an independent
  deadline around accepted encoder detach. Exactly one of strict finalization or quarantine may win;
  late callbacks are inert. Detach failure starts identity-owned GL recovery, while a hard deadline
  terminates the app-side recording state even if the setup/GL lane cannot answer.
- [x] **P1.2 / AGG2-1 (High/High): fail-closed unsafe-recorder quarantine.** When EGL/native release
  remains unproven, set recorder loops terminal, stop AudioRecord without releasing a native object
  under a live thread, retain unsafe wrappers process-long, keep the pending row private for next-
  process recovery, resolve the latch/mic claim once, invalidate Camera Ready, and refuse new
  capture/REC/preview acquisition with a concise restart status. An abandoned GL owner must not
  restart an interactive preview in this state.
- [x] **P1.3 / AGG2-1 tests.** Cover accepted-never-run detach, explicit failure, strict release,
  ABANDONED/no-release, hard-timeout, late success, and exactly-once terminal arbitration with pure
  seams. No native fault injection is required for the host gate.
- [x] **P1.4 / AGG2-6 (Medium/High): conservative HEIF recovery.** Parse bounded `meta` children and
  require a primary item plus an `iloc` extent that is structurally supported and wholly in bounds
  before markerless adoption. Missing/malformed/out-of-range required metadata is invalid; unknown
  versions/construction methods remain pending. Replace the boxed-garbage positive fixture with a
  minimal structurally valid HEIF and add negative primary/location/extent matrices.

## Phase 2 — launch reconciliation and persisted-domain boundaries

- [x] **P2.1 / AGG2-4 + AGG2-5 (Medium/High): one media reconciliation lane.** Move launch recovery
  off `setupExecutor` onto a dedicated media executor so Camera2 startup is never queued behind
  provider probes/retries. Return a typed completion to the ViewModel and only then query the latest
  published capture.
- [x] **P2.2 / AGG2-4 (Medium/High): generation-safe review restore.** Feed the post-recovery latest
  family through the existing `CaptureOutputTracker`/current-review guard so adopted prior-process
  media appears, but a launch result cannot displace a capture made during startup.
- [x] **P2.3 / AGG2-17 (Low/High): cumulative recovery truth.** Fold scanned/adopted/deleted/retained
  counts across attempts while using only the last attempt's unresolved failures for the retry
  decision. Log a sanitized cumulative terminal summary and unit-test partial-success→clean-retry.
- [x] **P2.4 / AGG2-7 (Medium/High): total persisted scalar normalization.** At the settings-to-state
  boundary normalize finite/range-valid audio gain, shutter angle, custom-WB gains, and the existing
  numeric fields; keep route-capability projection separate. Defensively normalize audio gain again
  at recorder admission/PCM arithmetic. Test NaN, infinities, and both range edges.
- [x] **P2.5 / AGG2-16 (Low/High): canonical Fn state.** Normalize Photo, Video, and My Menu slots to
  valid distinct max-eight lists with mode fallback before publication and persistence. Strip,
  editor, and overlay must consume the same canonical list; test duplicate/9+/all-invalid input.

## Phase 3 — microphone truth, privacy, and quiet-view cost

- [x] **P3.1 / AGG2-8 (Medium/High): surface standby terminal health.** Wire exhausted typed standby
  failures through CameraEngine to stable ViewModel route/status state and restore the selected-route
  label only after successful PCM or explicit input intent. Extend controller/wiring tests.
- [x] **P3.2 / AGG2-15 (Low/High): meter only when visible.** Make the DISP/details state explicitly
  own standby-level intent. Video standby with details hidden must not keep AudioRecord/RMS/10 Hz
  StateFlow telemetry alive; recording metering remains unchanged. Release on modal/lifecycle exit.
- [x] **P3.3 / AGG2-9 (Medium/High): accurate privacy copy.** Update permission rationale, Play data
  safety/reviewer text, `PRIVACY.md`, and published privacy HTML to describe local microphone
  processing while the visible armed-video level meter is enabled, plus recording audio. Preserve
  the no-network/not-collected/not-shared truth.

## Phase 4 — one coherent accessibility control per visual target

- [x] **P4.1 / AGG2-2 (High/High): merge main-control semantics.** Put click/select first and one
  merged label/state/role/enabled/selected semantic on TopBar buttons/TELE, compact Fn, focal presets,
  mode labels, focus reset, adjustment close, shutter/snapshot, and related actionable surfaces.
  Explicitly publish disabled state. Keep visible text descendants out of extra focus stops.
- [x] **P4.2 / AGG2-2 (High/High): isolate Fn modal semantics.** The touch-only scrim gets no platform
  button semantics; the explicit Close target is the sole named close action. Each tile exports its
  full label/value/enabled/click action on one merged node in portrait and both held directions.
- [x] **P4.3 / AGG2-12 (Medium/High): make the device harness test actions, not labels.** Parse
  focusability and require described controls to own their action/check/selected/enabled state.
  Detect equal-bounds split nodes. Model compact and detailed chrome separately; feed realistic
  current hierarchies into host tests. A safe debug-fixture quick action must be activatable.

## Phase 5 — physically correct and efficient Fn operation

- [x] **P5.1 / AGG2-10 (Medium/High): physical bottom 4x2 held tray.** Add a pure orientation policy:
  portrait raw 4x2/BottomCenter; held raw 2x4, Start at 90 and End at 270, with slot reordering that
  yields the same physical top-row/bottom-row order. Keep the panel opaque, scrim light, and every
  action at least 48 dp.
- [x] **P5.2 / AGG2-11 (Medium/High): text containment.** Preserve full accessibility strings while
  giving visual label/value an explicit one-line ellipsis/compact-alias policy. Keep the portrait
  panel vertically scrollable and make held tile sizing constraint-safe. Add longest-copy and
  physical transform tests; final device evidence remains scoped to its reported font scale.
- [x] **P5.3 / AGG2-14 (Medium/High): retain quick-action context.** Quick cycle/toggle tiles update
  in place and leave Fn open for consecutive setup. Only numeric/ruler paths and WB-preset sheet
  navigation dismiss. Extract and test the existing manual-dial transition plan so no Fn entry can
  bypass exposure/focus ownership or open EV in M.
- [x] **P5.4 / AGG2-13 (Medium/Medium): keep TELE reachable.** Reuse the shared trailing-scroll fade
  and auto-reveal the TELE chip when it becomes active, while Settings and DISP remain fixed. Add a
  pure/recomposition regression test for active reveal admission.

## Phase 6 — PIP truth and documentation

- [x] **P6.1 / AGG2-3 immediate truthful surface (High/High).** Rename visible `Tele Finder`/`PIP`
  presentation to a concise same-stream loupe-overview term, state the exact Photo+4:3+TELE+punch-in
  predicate beside the implementation, and keep it default off. Tests/docs must no longer claim this
  fulfills the requested 1x overview.
- [x] **P6.2 / AGG2-3 deferred implementation (High/High).** Defer genuine automatic 1x PIP because
  the user expressly prohibited the required physical-output/concurrent-camera HAL experiment.
  Exit criteria: separate explicit approval; isolated PMA110 experiment on port 5602; genuine 1x
  source identity at 3x/10x/TC in supported Photo/Video modes; capture/REC/preview stability; safe
  fallback that hides the inset; no physical-camera routed output in shipping code until proven.
- [x] **P6.3 documentation truth.** Update architecture/UX/backlog and this plan with final behavior,
  narrow Cycle 1's held/a11y claims to its actual 0.8-font evidence, and record every remaining
  manual/HAL boundary without claiming unexecuted verification.

## Verification and publication

- [x] **V1 host gate:**
  `JAVA_HOME=/opt/homebrew/opt/openjdk@21/libexec/openjdk.jdk/Contents/Home ./gradlew :app:assembleDebug :app:testDebugUnitTest :app:lintDebug`
- [x] **V2 harness gate:** `python3 -m unittest discover -s device-tests/tests`
- [x] **V3 PMA110 UI:** install the exact debug APK only on `127.0.0.1:5602`; capture production-
  composable portrait/90/270 Fn PNG+XML; rotate held copies into the physical view; prove physical
  bottom 4x2, in-bounds/non-overlap, one named action node per target, and no default-scale text
  overflow. Also inspect real MainActivity portrait Fn after camera readiness without capture/REC.
- [x] **V4 restoration:** restore original orientation settings and foreground `MainActivity`.
- [x] **V5 git:** fine-grained GPG-signed Conventional Commits with gitmoji; before every push run
  `git pull --rebase`, resolve without discarding user work, then push `main`. Deployment remains
  `none`.

## Completion evidence

- Host gate: debug APK assembled, **776/776** JVM tests passed with zero failures/skips, and debug
  lint passed. The Python device-harness suite passed **69/69**.
- Recorder teardown: ten deterministic coordinator tests cover the accepted-but-silent detach,
  explicit failure, strict release, ABANDONED recovery, both deadlines, both scheduler-rejection
  positions, synchronous recovery completion, late callbacks, and 100 release/timeout races.
- Post-fix concurrency audit remediation at that revision: process-global native acquisition
  linearizes EGL, Camera2, codec setup/start, muxer construction, and standby-microphone
  create/start with quarantine. The later Cycle-3 audit found and scheduled the recorder worker's
  asynchronous `MediaMuxer.start()` and `AudioRecord.startRecording()` leaves; an Engine-owned
  standby lease excludes foreign standby/REC across ViewModel overlap; and any live recorder drain
  returns a quarantine-required outcome that retains every native owner and never frees REC admission.
- Exact rebuilt debug APK SHA-256:
  `8452896c8e1a1f355260abb28da094c56a1c7827856545eea3ddc47e6a57abcc`.
- HAL-free production-composable evidence: `/tmp/rpf2-final2-fn0.png`,
  `/tmp/rpf2-final2-fn0-action.png`, `/tmp/rpf2-final2-fn90-physical.png`, and
  `/tmp/rpf2-final2-fn270-physical.png`, with matching raw XML/PNG inputs under `/tmp/rpf2-final2-*`.
  Portrait and both held directions returned zero harness errors; every Fn surface exposed eight
  coherent tile buttons plus one Close button. Raw 90° was Start-anchored, raw 270° End-anchored,
  and both physical views preserved AE/Focus/Shutter/ISO over WB/Gamma/Steady/Audio. HLG→O-Log
  updated in place without closing Fn.
- Real ready-camera portrait evidence: `/tmp/rpf2-final2-main-fn.png` and `.xml`; the same action-node
  check returned zero errors, and explicit Close restored idle MainActivity without capture or REC.
- Device restoration: `font_scale=0.8`, `accelerometer_rotation=0`, `user_rotation=0`, and foreground
  `me.hletrd.telecampro.debug/com.hletrd.findx9tele.MainActivity` on `127.0.0.1:5602`.

## Deferred items

- **D1 only — genuine 1x PIP (P6.2).** This is the sole Cycle 2 implementation deferral and is
  blocked by an explicit user safety boundary, not engineering convenience. P6.1 removes the false
  implication now; the exit criteria above are mandatory before D1 can resume.

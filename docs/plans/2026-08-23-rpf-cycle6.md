# Review-plan-fix cycle 6 — implementation plan

Date: 2026-08-23  
Source: `.context/reviews/_aggregate.md` current cycle-6 section  
Status: complete; implementation, configured gates, signed commits, and pushes finished

## Repository rules applied

- Preserve PMA110 Camera2/HAL behavior; this plan changes release integrity, recorder phase
  ownership, UI accessibility/layout, tests, and documentation rather than measured camera request
  policy.
- Security, correctness, release-integrity, and liveness findings are mandatory and are not
  deferred.
- Keep user-facing copy English/Korean and preserve the Sony Alpha/Xperia quiet-viewfinder policy.
- Do not infer device behavior from host tests. Deployment is disabled for this invocation.
- Existing completed plan files stay in place; moving or deleting them would require separate
  destructive-action confirmation.
- Preserve the 99.5% Partition-A coverage floor, dependency verification, release signing rules,
  and current warning dispositions.
- Use fine-grained GPG-signed Conventional Commits with gitmoji. Pull with rebase before each push.

No finding is deferred. Every aggregate item AGG6-01 through AGG6-18 is scheduled below.

## Phase 1 — immutable and exact release inputs

- [x] **P1 / AGG6-01:** make the generated release-provenance asset namespace exact. The generating
  task must fail closed on unexpected sibling files before/after generation, and the artifact checker
  must reject unexpected members under that namespace. Add a regression that seeds an ignored stale
  sibling and proves an ordinary non-clean release cannot package it.
- [x] **P2 / AGG6-02:** bind release compilation and packaging to an immutable Git source identity.
  Prefer a disposable commit-addressed snapshot/worktree or a complete content-addressed input set
  consumed by every source/resource/package task. Add a deterministic barrier test that mutates a
  real tracked input after provenance verification and proves no signed/upload-ready artifact can
  claim the original tree while consuming the changed bytes.

Acceptance: release artifacts contain exactly the intended generated provenance asset and all
compiled/resource bytes come from the immutable source identity recorded in that asset. Direct
release entry points fail closed; debug builds remain usable from a dirty development tree.

## Phase 2 — finish recorder native/storage phase separation

- [x] **P3 / AGG6-03:** split `VideoRecorder.stop()` into checked native/container finalization and
  a frozen post-native storage/publication continuation on a separate owned executor. At successful
  native classification, free the same-Engine REC lane, teardown flag, and microphone handoff while
  preserving capture-specific publication/status identity. A stalled extractor/provider call must
  not block a second recording and must remain recoverable through the durable pending-row protocol.
- [x] **P4 / AGG6-07:** replace label-only native-owner cleanup coverage with injected throwing
  operations or a structural production-owner enumeration that reaches every actual release owner
  and proves owner retention, quarantine, and no later native phase on failure.
- [x] **P5 / AGG6-08:** replace the inert blocked-storage latch test with an executor-level
  integration test that genuinely blocks the post-native tail, exercises Engine finalization/release,
  admits and completes a second recording, and verifies independent terminal outcomes.
- [x] **P6 / AGG6-09:** update the architecture to describe native classification separately from
  the storage tail. Remove `RecorderFinalizationOwner.taskCompleted` if the new execution boundary
  leaves it without an observable owner, and add a docs contract for the split.

Acceptance: after all native recorder owners are proven released, a blocked first-clip storage tail
cannot delay same-Engine REC admission, overlap native owners, corrupt callbacks, or trigger false
quarantine; true native hangs still fail closed.

## Phase 3 — coherent keyboard focus and quantized controls

- [x] **P7 / AGG6-04:** bind every explicit focus target in shared camera chrome/Fn/dial/shutter
  primitives to the exact action enablement predicate while retaining disabled accessibility
  semantics. Add parameterized Compose traversal/activation tests over mixed enabled/disabled states.
- [x] **P8 / AGG6-05:** make each focal-rail chip one focus/semantics/activation owner. Preserve the
  48 dp target and clipped visual treatment without a nested selectable focus node. Test Tab and
  D-pad activation for enabled and disabled chips.
- [x] **P9 / AGG6-06:** give `LabeledSlider` an explicit domain step/quantizer so a consumed fine
  arrow always reaches the next representable production value or a true endpoint. Test the real
  integer Timelapse Interval row at several values, repeated arrows, page/home/end, and endpoints.

Acceptance: unavailable controls are not dead keyboard stops, each visible focal preset has one
focus action, and keyboard users can reach every timelapse interval value without touch.

## Phase 4 — readable and responsive camera UI

- [x] **P10 / AGG6-15:** apply bounded residual rotation to the central camera status plate and
  critical MediaReview RAW/loading/error/retry copy. Keep intentionally wide metadata screen-fixed.
  Add 0/90/270-degree and large-font bounds tests.
- [x] **P11 / AGG6-16:** give the horizontally scrollable OSD the existing direction-aware overflow
  affordance and an explicit content-change priority/reset policy so focal/encoder truth cannot stay
  hidden. Add maximal-tag LTR/RTL tests at 1x and 2x font scale.
- [x] **P12 / AGG6-17:** choose ProSheet side layout from resulting usable content width, not aspect
  alone, and make Fn slot editor rows adaptive below their minimum label/action width. Add composed
  non-overlap tests for 320/340/411 dp, LTR/RTL, English/Korean, and 2x font scale.
- [x] **P13 / AGG6-18:** extend deterministic debug UI fixtures to Settings tabs, Fn, RAW/review
  loading/error/delete, permission/rationale, maximal OSD, compact/freeform shapes, orientation,
  font scale, locale, and RTL. Add bounds assertions or approved deterministic visual checks for the
  highest-risk states; do not introduce production-only debug affordances.

Acceptance: critical state copy is readable in held orientation, important OSD truth is discoverable
and returns to priority, compact settings never overlap, and regressions can be reproduced through a
deterministic harness.

## Phase 5 — reconcile current documentation and public copy

- [x] **P14 / AGG6-10:** rewrite `docs/FIELD_CHECKS.md` Loupe Overview orientation criteria around
  the current per-draw override and mark the contradictory 2026-07-28 backlog conclusion superseded,
  preserving historical evidence. Add a docs check tying field criteria to the CLAUDE authority.
- [x] **P15 / AGG6-11:** replace the stale `MainActivity` half-press comment with the measured 767
  mapping and retain clear separation from standard zoom, speculative aliases, and the quick button.
- [x] **P16 / AGG6-12:** correct the MediaReview EXIF comment to state expected HEIF/JPEG parity and
  distinguish malformed, legacy, and provider-inaccessible cases.
- [x] **P17 / AGG6-13:** replace `docs/TESTING.md`'s single-device framing with hardware-bound,
  PMA110-first, multi-device-installable wording.
- [x] **P18 / AGG6-14:** make the published privacy HTML cover Android phones and tablets exactly as
  `PRIVACY.md` does, and add Markdown/HTML scope-parity validation.

Acceptance: active authority, field criteria, source comments, testing scope, and the public privacy
page all describe current behavior without erasing explicitly historical records.

## Phase 6 — gates, commits, and delivery

- [x] Run `./gradlew :app:assembleDebug :app:testDebugUnitTest :app:lintDebug` against the whole repo.
- [x] Because the release gate must start from committed immutable source, finish and push every
  fine-grained source/test/docs concern before release verification.
- [x] Run the immutable-source equivalent of
  `./gradlew :app:lintRelease :app:assembleRelease :app:bundleRelease` from clean committed
  HEAD.
- [x] Run `python3 tools/verify_host.py --release` as the consolidated cross-check.
- [x] Fix every error and every cleanly actionable warning. Any warning left unresolved must be
  recorded here with file/line, original severity/confidence, concrete reason, and reopen criterion.
- [x] Update this plan with implementation evidence, GPG-sign each fine-grained Conventional Commit
  with gitmoji, `git pull --rebase`, and push immediately after each concern.

## Planned commit sequence

`fix(release)`, `test(release)`, `refactor(video)`, `test(video)`, `fix(a11y)`, `fix(controls)`,
`fix(ui)`, `test(ui)`, and `docs(contracts)`, split further whenever one commit would mix independent
concerns.

## Completion evidence

- Release provenance now builds only through `tools/build_immutable_release.py` from a private,
  clean, commit-addressed clone; direct release Gradle entry points fail closed. The clone identity
  and bytes are revalidated before outputs are copied back, the generated provenance namespace is
  exact, and artifact checking rejects unexpected namespace members. Twenty-two release/tool tests,
  including both deterministic mutation barriers, passed.
- Native recorder finalization now returns a frozen storage continuation to an independent storage
  worker. Proven native release immediately frees same-Engine REC/microphone admission; late
  publication owns only its frozen capture identity. Structural tests cover every production native
  owner, and an executor integration test blocks the first publication while a second recording
  completes independently.
- Shared camera controls now have one enabled-aware action/focus owner; disabled controls retain
  disabled semantics without dead D-pad stops. The focal rail has one owner, and the production
  timelapse interval uses a one-second keyboard quantizer. Integrated Compose tests cover all shared
  primitives and production interval behavior.
- Critical camera/review status copy rotates within bounded layouts; OSD overflow has a
  direction-aware hint and priority reset key; compact ProSheet/Fn layouts resolve from usable width;
  debug fixtures now expose the high-risk review/settings/Fn/permission/locale/direction/font-scale
  states. No new user-facing prose was introduced.
- Current Loupe orientation, half-press, HEIF EXIF, multi-device testing, and phone/tablet privacy
  contracts are reconciled. `tools/check_docs.py` passes 75 checks.
- A fresh isolated-clone configured debug gate passed all 1,565 JVM tests plus debug assembly and
  lint. The consolidated release gate passed with overall coverage 45.92%, Partition A
  **99.75% (5,201/5,214)**, exact reviewed residuals (13 lines / 9 classes), 22 release/tool tests,
  9 coverage-tool tests, 128 device-harness tests, 75 documentation checks, Python compilation,
  diff checking, and a second immutable signed release build (`lintRelease`, `assembleRelease`,
  `bundleRelease`).
- Six fine-grained commits were GPG-signed, pull-rebased, and pushed. Deployment remained disabled;
  no physical-device behavior is claimed.

## Residual gate warnings

| ID | Severity / confidence | Evidence | Reason | Reopen criterion |
|---|---|---|---|---|
| WARN6-01 | Warning / High | `gradle.properties`: `android.experimental.reportAggregationSupport=true` | Existing reviewed warning: the exact host/instrumented coverage aggregation contract still requires AGP's experimental option. Suppressing it is prohibited and removing it would weaken coverage. | AGP stabilizes report aggregation or the repository adopts an equally exact supported workflow. |
| WARN6-02 | Low / High | `app/build.gradle.kts:334`, lint `NotShrinkingResources` | `docs/BACKLOG.md` explicitly keeps resource shrinking off as separate product work after R8; enabling it requires its own resource/device release audit. This is not a new cycle-6 warning. | The recorded shrink-resources audit/device validation is scheduled and completed. |
| WARN6-03 | Low / High | `storage/MediaStoreWriter.kt:270`, lint `UseKtx` | This completion journal must observe the Boolean return of synchronous `SharedPreferences.Editor.commit()`. The KTX `edit(commit = true)` extension returns `Unit`, so applying the lint suggestion would discard the durable-success signal used by bounded retry. | AndroidX exposes an equivalent result-preserving extension, or this journal moves to another durable API with an explicit success result. |

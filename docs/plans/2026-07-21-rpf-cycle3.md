# Review-Plan-Fix Cycle 3 Implementation Plan — 2026-07-21

## Scope and immutable boundaries

- Review target: `43f8377dd7c9`; aggregate: `.context/reviews/_aggregate.md` with **10 unique
  findings** from 13 completed role reports plus root synthesis.
- Device target: OPPO PMA110 at `127.0.0.1:5602` only. Never address port 5600.
- Deployment mode: `none`; a debug APK reinstall/relaunch and deterministic UI evidence are the
  only authorized device mutations. Save and restore orientation/font/foreground app state.
- Do not capture, record, route a second or physical-camera output, query a risky concurrent-camera
  combination, or perform a true-1× PIP HAL experiment. Preserve the portrait-locked activity and
  current single-stream framing.
- Preserve Cycle-1/2 behavior: sticky Fn quick actions, physical bottom 4×2 Fn order, one coherent
  accessible action per tile, numeric-dial ownership, process-wide native admission/quarantine,
  conservative media recovery, and visibility-owned standby microphone lifecycle.
- Historical completed dated plans stay in `docs/plans/` as the repository's archival record. No
  completed plan is deleted or rewritten to hide old evidence; the inaccurate Cycle-2 native-start
  sentence is narrowed in place and superseded by this plan's final evidence.
- The `ralph` implementation agent is unavailable in this environment. Prompt 3 therefore uses the
  workflow's required direct, regression-safe implementation fallback.

## Phase 1 — Close recorder and standby lifecycle gaps

- [x] **P1.1 / AGG3-01 (High/High): dominate worker-thread native starts.** Route the actual
  `AudioRecord.startRecording()` and `MediaMuxer.start()` leaves through
  `UnsafeRecorderQuarantine.runNativeAcquisition`. Recheck the recorder's terminal flag inside the
  process lock. A refusal must stop Java-side progression without starting, stopping, or releasing a
  quarantined native owner; ordinary start exceptions retain their existing audio-degrade or
  clip-failure ownership.
- [x] **P1.2 / AGG3-01 tests.** Extract the smallest typed start seam needed for host tests. Cover
  admitted completion before close, close-before-start refusal, terminal-local refusal inside an
  admitted process block, start exception preservation, and muxer-start publication only after a
  successful native call. Keep the existing gate race test and add leaf-level assertions so green
  gate tests cannot coexist with an escaped native start.
- [x] **P1.3 / AGG3-02 (Low/High): recover from rejected process-busy retry scheduling.** Contain a
  false return or throw from `StandbyRetryScheduler` and select an explicit ownership-safe fallback.
  Preserve the quiet, unbounded wait for a foreign process lease and do not charge process
  contention to the finite AudioRecord failure budget. The fallback must recheck pause, wanted
  intent, and REC ownership without recursive stack growth.
- [x] **P1.4 / AGG3-02 tests.** Make the test fixture's scheduler injectable; cover false, throw,
  later acceptance/lease release, disable, pause, and REC handoff while the fallback is pending.

## Phase 2 — Put Fn entry and tray in one physical reach zone

- [x] **P2.1 / AGG3-03 (Medium/High): orientation-aware Fn entry policy.** Add a pure normalized-
  orientation policy used by both compact and detailed chrome: raw Start at portrait/90°, raw End
  at 270°. Keep the existing tray mapping unchanged (portrait bottom 4×2; held raw 2×4 Start at 90°,
  End at 270°) so entry and panel remain near the operator's physical bottom edge in either grip.
- [x] **P2.2 / AGG3-03 tests.** Pin normalization (including -90°/450°), compact alignment, and
  detailed-row ordering. Extend machine-readable chrome checks to require an in-bounds coherent
  48 dp Fn action in the expected raw edge zone for 0°/90°/270°.
- [x] **P2.3 / AGG3-08 (Medium/High): automate sticky quick action behavior.** In the HAL-free
  `UiSnapshotActivity`, activate a deterministic Gamma quick tile, assert HLG→O-Log, require the
  Fn modal and Close action to remain, then dismiss. Do not use the physical camera.
- [x] **P2.4 / AGG3-09 (Medium/High): verify physical held geometry.** Parameterize the device
  checker with orientation and configured slot order. Assert raw edge anchoring, transform raw node
  centers into the physical held coordinate system, require exactly two rows of four, and compare
  row-major labels. Add portrait/90°/270° synthetic XML unit cases.

## Phase 3 — Unify modal accessibility

- [x] **P3.1 / AGG3-04 (Medium/High): one Settings close action.** Replace the semantic full-screen
  scrim Button with a touch-only dismiss surface, retaining Back and the visible 48 dp X as the sole
  named/focusable/clickable `Close settings` action. Keep panel taps consumed without a dummy node.
- [x] **P3.2 / AGG3-04 tests and harness.** Add `settings_modal_layout_errors` parallel to the Fn
  modal contract: exactly one close action, at least 48 dp, in bounds, not full screen, and no split
  duplicate. Rewrite setup helpers to use the explicit X/Back rather than select the largest bad
  semantics node. Cover portrait and both held raw hierarchies with unit fixtures.

## Phase 4 — Make Loupe, MR, and adjustment evidence explicit

- [x] **P4.1 / AGG3-10 (Medium/High): HAL-free Loupe Overview scenario.** Extend the debug snapshot
  host with a deterministic TELE Photo 4:3 + punch-in + Loupe Overview state. Assert the visible
  `Loupe Overview`/`Loupe overview`/`OVERVIEW` contract, a non-interactive overview region, and the
  absence of user-facing `PIP` or `1x` claims. This is Compose-only and must never open Camera2.
- [x] **P4.2 / user UI acceptance: progressive disclosure.** Add deterministic snapshot scenarios
  for idle preview, an active MR bank, and one numeric adjustment ruler. Machine-check that idle
  does not expose dense MR1/MR2/MR3 or manual-adjustment controls; an active memory shows one compact
  truthful tag; the ruler owns its manual mode and explicit 48 dp close control; no scenario clips,
  overlaps, leaves the display, or obscures the central preview beyond its intentional control band.
- [x] **P4.3 / preserved ownership audit.** Keep and rerun the pure manual transition matrix:
  Focus→MF, shutter P/ISO→S, ISO P/S→ISO priority, WB preset→Exposure sheet, and EV blocked in M.
  Re-audit MR save/recall recording locks and active-slot truth without changing behavior absent a
  reproduced defect.

## Phase 5 — Restore one truthful documentation contract

- [x] **P5.1 / AGG3-05 (High/High): canonical Loupe Overview language.** Update `README.md` and
  `CLAUDE.md` to use the shipped name and `OVERVIEW` tag. State that it is a same-stream view of the
  full delivered frame while punch-in is active, never a true automatic 1× or second-camera feed.
- [x] **P5.2 / AGG3-06 (Medium/High): tap-AF QA truth.** Fix `.claude/agents/qa-adversary.md` so the
  two-second timer hides only the reticle; testing must prove the AF/AE hold remains visible through
  its reset affordance, then explicitly reset and verify prior AF mode/centered metering.
- [x] **P5.3 / AGG3-07 (Low/High): audio-failure architecture truth.** Correct
  `docs/ARCHITECTURE.md` to say a negative read while REC is active is audio-terminal but degrades to
  video-only, zeroes the meter, and does not enter the clip-level first-failure latch; a negative
  post-stop read is normal EOS.
- [x] **P5.4 / AGG3-01 evidence correction.** Narrow the Cycle-2 completion sentence that currently
  claims every muxer/microphone start was gated, then record the new leaf-level proof here after the
  implementation and gates pass.

## Phase 6 — Verification, publication, and state restoration

- [x] **V1 full host gate:**
  `JAVA_HOME=/opt/homebrew/opt/openjdk@21/libexec/openjdk.jdk/Contents/Home ./gradlew :app:assembleDebug :app:testDebugUnitTest :app:lintDebug`
- [x] **V2 harness gate:** `python3 -m unittest discover -s device-tests/tests`.
- [x] **V3 exact APK/device identity:** compare the rebuilt APK SHA-256 with the installed debug
  package pulled from `127.0.0.1:5602`; record the PMA110 model/build and original font/orientation
  state. Never address port 5600.
- [x] **V4 fresh production-composable UI evidence:** capture PNG plus UI XML from the HAL-free
  snapshot activity in portrait, 90°, and 270°. Produce physical-view copies for both held angles.
  Run the same machine checker on each XML to prove Fn entry reach zone, physical bottom 4×2 order,
  sticky action, one coherent action per target, 48 dp minimum, and no clipping/overlap/out-of-bounds.
- [x] **V5 disclosure evidence:** capture/check idle, active-MR, numeric-adjustment, Settings modal,
  and Loupe Overview snapshot states. Prove preview-first density, one settings close node, truthful
  Loupe copy, no `PIP`/`1x`, and intentional adjustment bounds. No capture/REC is permitted.
- [x] **V6 real-app non-capture sanity:** relaunch `MainActivity`, wait only for ready idle chrome,
  inspect portrait Fn/settings accessibility without shutter/REC or Loupe HAL probing, then restore
  original font/orientation settings and foreground `MainActivity`.
- [x] **V7 git:** make fine-grained GPG-signed Conventional Commits with gitmoji. Before every push,
  run `git pull --rebase`; resolve without discarding user work; push immediately. Deployment stays
  `none`.

## Deferred item

- **D1 only — genuine automatic 1×/second-camera PIP (High/High, carried safety boundary).** No
  shipping implementation or device probe is authorized because the user explicitly warned that a
  physical-camera true-1× PIP experiment can crash the QTI HAL. Exit requires a separate explicit
  approval and an isolated PMA110 validation plan proving source identity and capture/REC/preview
  stability. This deferral does not block truthful same-stream Loupe Overview coverage.

## Completion evidence

Completed. All 10 aggregate findings were implemented or truthfully documented; genuine automatic
1×/second-camera PIP remains the sole explicit safety deferral.

- The final required host gates passed: debug assemble/unit/lint completed **53 Gradle tasks** with
  **787/787 JVM tests**, and the device-harness suite passed **76/76 Python tests**.
- Worker-thread `AudioRecord.startRecording()` and `MediaMuxer.start()` now pass the process-native
  admission gate at the leaf, and rejected/throwing standby process-busy retries recover without
  consuming the finite microphone-failure budget or violating pause/REC ownership.
- Compact and detailed Fn entry placement now follows the same normalized physical-bottom policy.
  The HAL-free checker proves a sticky Gamma update, portrait 4×2 order, both held physical 4×2
  orders, correct raw edge anchoring, coherent 48 dp actions, and bounded modal/ruler geometry.
- Settings exposes one explicit Close action and nine merged, named `Role.Tab` targets with exactly
  one selected tab. Device validation also closed stale snapshot-intent and task-stack restore gaps;
  harness launch now proves the exact resumed `MainActivity`, not merely a live package PID.
- `README.md`, `CLAUDE.md`, and `docs/ARCHITECTURE.md` now describe the shipped same-stream Loupe
  Overview and video-only audio degradation accurately. The repo-ignored local
  `.claude/agents/qa-adversary.md` instruction was corrected in place for the tap-AF hold/reset
  contract and remains outside git under the existing `.gitignore` policy.
- Final APK SHA-256 is
  `91cea07ee2d1d1213fda3c5171a146cf11688f564bf771c1f46d571761109932`; the rebuilt host APK and
  installed PMA110 base APK matched exactly. Device build was `PMA110_16.0.9.402(CN01)`.
- Final HAL-free evidence is `device-tests/reports/20260721-092217/`: **18 PNG + 19 UI XML** files
  plus **12 upright physical-view held PNGs**. Portrait/90°/270° each passed idle, Fn, Settings,
  MR1, ISO adjustment, and truthful non-interactive Loupe checks without capture, REC, or Camera2.
- Same-APK real-app sanity passed Settings' nine-tab traversal in
  `device-tests/reports/20260721-091341/` and Fn open/dismiss in
  `device-tests/reports/20260721-092449/`. Final state was foreground `MainActivity`, font scale
  `0.8`, auto-rotate `0`, and user rotation `0`, matching entry state. Deployment remained `none`.

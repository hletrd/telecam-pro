# Review-Plan-Fix Cycle 5 — Implementation Plan (2026-07-18)

Status: implementation, final gates, per-cycle deploy, and PMA110 device verification complete.
Review and aggregation started at `115a6f5`; final implementation HEAD `a42a82a`.

## Evidence boundary

- Fresh review gate: 615/615 JVM tests at review start; 651/651 after the P-waves; `assembleDebug`
  and `lintDebug` PASS on `a42a82a` (re-confirmed at deploy time).
- DEPLOY_MODE was **per-cycle** (an earlier close-out draft wrongly recorded `none` after two
  session-limit interruptions lost the run context; corrected by the orchestrator). The exact
  DEPLOY_CMD ran against PMA110 at the session serial `127.0.0.1:5601` (the 5599 proxy was
  re-established on a new port): install `Success`, launch clean.
- PMA110 device verification: **RUN and PASSED** — full record in
  "Cycle-5 device verification record" below, including the user-mandated TELE capture-reliability
  scenarios (swipe-kill mid-save with IS_PENDING adoption proven, HOME mid-REC finalize+publish,
  kill-after-stop races, LOG transfer-tag probe, recorded-framing re-check).
- Release artifact and Play screenshots remain non-current evidence until regenerated (P5.4
  signing blocker unchanged).

## P1 — Completed-media survival and capture correctness

- [x] **P1.1 Durable pending lifecycle (AGG1-1).** Add synchronous per-URI `REGISTERED` /
  `COMPLETE` ownership in private preferences. Register every inserted pending row, mark complete
  only after encoder/muxer output is finalized, clear ownership only after publish/delete, and run
  launch recovery on the IO lane. Completed rows are adopted/published; failed adoption remains
  pending for the next retry; only proven incomplete rows are deleted. A conservative image/video
  conservative terminal-structure probes cover JPEG/DNG/video marker gaps and legacy rows. An
  unmarked HEIF remains pending because header dimensions do not prove its payload closed. Add pure
  exhaustive disposition tests plus resolver/probe seams.
- [x] **P1.2 Recorder empty-audio EOS (AGG1-2).** Route the AAC EOS-input wedge through the one
  idempotent mid-REC audio-degrade transition and complete the muxer terminal matrix tests.
- [x] **P1.3 Still worker containment (AGG1-8).** Catch non-fatal transform failures around the
  entire save job, preserve fatal VM errors, recycle every bitmap, and emit one truthful failure.
- [x] **P1.4 HEIF EXIF parity (AGG1-9).** Feed HeifWriter a valid EXIF APP1 payload generated from
  the same `ExifShot` attributes as JPEG. Add a pure APP1 extraction test and keep on-device saved
  metadata acceptance explicitly NOT RUN.
- [x] **P1.5 SINGLE-shot backpressure (AGG1-10).** Bound retained processed snapshots, refuse a
  new independent SINGLE capture with immediate `Finishing previous photo` feedback when the
  budget is full, and release the slot on every callback/queue/failure path. Pin the pure budget.
- [x] **P1.6 Partial family delete reconciliation (AGG1-11).** Do not irrevocably hide a family
  before resolver results are known; restore a surviving output/delete scope after mixed success
  and report a persistent failure. Add mixed-success tracker tests.
- [x] **P1.7 Status truth (AGG1-12).** Give urgent save/publish/delete/recovery failures a longer,
  assertive lifetime; keep success short; report deliberate zero-sample/admission aborts neutrally.
- [x] **P1.8 DNG publication lane (AGG1-26).** Keep DngCreator's live-image write synchronous but
  mark completion and move retry/backoff publication to the IO executor.

## P2 — Optics, lifecycle, and request truth

- [x] **P2.1 One optics cleanup door (AGG1-5, AGG1-6).** Recall and rollback clear tap AF/loupe;
  every remap cancels self-timer and ends retained-controller zoom boost. Preserve the no-extra-
  rebuild path for reopens and add pure/wiring assertions where feasible. Same-route commits fold
  exact controls plus boost-off into one controller update; structural reopens start boost-free.
- [x] **P2.2 Guard delayed camera posts (AGG1-7).** Centralize delayed scheduling behind the same
  closed/dead-looper containment as immediate posts and provide caller-specific fallback completion.
- [x] **P2.3 Finder PIP input truth (AGG1-13).** Make the assist PIP explicitly non-interactive and
  prevent taps in its rect from falling through to loupe-adjusted main-view focus; test hit geometry.
- [x] **P2.4 Fail-closed camera selection (AGG1-29).** Prefer standalone candidates when focal
  metadata is unreadable; return unavailable rather than a known routed-output crash candidate.
- [x] **P2.5 Defensive zoom ranges (AGG1-31).** Normalize all HAL ranges before `coerceIn` and pin
  inverted/non-finite bounds in the pure request helpers.

## P3 — Sony/iPhone-familiar shooting UI

- [x] **P3.1 Direct focal rail (AGG1-4).** Add a quiet 0.6× / 1× / 3× / 10× rail above the mode
  control, with four 48 dp targets, active lens/TC semantics, reconfiguration state, and the shared
  REC gate. Remove the duplicated unlabeled shutter-row TELE glyph; retain the labeled top TELE
  converter control.
- [x] **P3.2 Mode-state truth (AGG1-14, AGG1-15).** Use `quickFnEnabled` for the persistent Fn row
  and the shared display multiplier/formatter for its zoom value.
- [x] **P3.3 Shutter and timer (AGG1-16, AGG1-17).** Keep the dim shutter tappable so the engine can
  explain a decline, make a second shutter/overlay action cancel countdown, and cancel countdown on
  every optics/setup remap.
- [x] **P3.4 Accessibility/contrast (AGG1-18, AGG1-19).** Merge setting labels with slider/switch
  semantics and apply the tested HUD scrim token to TELE; extend contrast and semantics helper tests.
- [ ] **P3.5 Landscape operation (AGG1-20).** Final combined-diff review proved that unlocking the
  activity while only reciprocating Compose geometry misframes GL preview and leaves the 16:9 mask
  portrait-shaped. The manifest therefore remains portrait-locked; the alternative operator layout
  stays dormant until GL sampling, mask/tap mapping, encoder framing, both directions, and 200% font
  can be implemented and verified as one PMA110 change.
- [x] **P3.6 Quiet polish (AGG1-36).** Remove the restored-launch zoom flash, keep a genuine 1×
  interaction visible, normalize ×/mm/locale tokens, rotate the review zoom label, and add missing
  button roles without introducing banners/tutorials.

## P4 — Required structural order and framing seam

- [x] **P4.1 RendererAssists (first half of AGG1-24).** Extract the synchronized renderer config,
  AE-analysis/gamma/punch-in/finder replay cluster. Preserve store-before-post, one replay per GL
  generation, and keep transfer/native-log/tap ownership in CameraEngine. Gate before P4.2.
- [x] **P4.2 StandbyAudioController (second half of AGG1-24).** Extract only after P4.1. Preserve
  live-state lambdas, exactly-one mic, post-release latch, retry intent, and failure-streak rules.
- [x] **P4.3 Aspect geometry seam (AGG1-23).** Add `largestCenteredRect` and one named displayed-
  still-aspect mapping; bind Compose mask truth to the existing integer sensor crop with a matrix.

## P5 — Tests, docs, and artifact truth

- [x] **P5.1 Persistence/edge tests (AGG1-25).** Pin per-field corrupt reads, synchronous commits,
  NaN/non-positive clamps, tracker seed/tombstone budgets, and permission-history durability.
- [x] **P5.2 Focus/recorder/helper matrices (scheduled portion of AGG1-40).** Complete the existing
  host-testable AF override, muxer stop, orphan recovery, aspect, and still-zoom policy matrices.
  Real codec/Camera2/ViewModel construction remains in the device/instrumentation deferral below.
- [x] **P5.3 Living docs (AGG1-37).** Update `CLAUDE.md`, ARCHITECTURE, BACKLOG, Play listing, and
  code comments for current owners/symbols, audio behavior, finder gate, recovery contract, bitrate,
  and current release/device truth.
- [ ] **P5.4 Release candidate truth (AGG1-3).** Run both mandated Gradle gates. If release output is
  produced, record its exact HEAD/hash as a local candidate while keeping upload/internal-test and
  PMA110 validation pending. Do not describe it as Play-verified. **Blocked:** release lint passes,
  but APK/AAB signing is unavailable because password inputs are absent; no new artifact is trusted.

## P6 — Final gates

- [x] `./gradlew :app:assembleDebug :app:testDebugUnitTest :app:lintDebug` — PASS, 651/651 tests.
- [x] `./gradlew :app:compileReleaseKotlin :app:lintRelease` — PASS without release signing.
- [ ] `./gradlew :app:lintRelease :app:assembleRelease :app:bundleRelease` — `lintRelease` PASS;
  `assembleRelease`/`bundleRelease` blocked by missing release-signing password inputs.
- [x] Confirm clean tracked diff scope, no secrets, and no accidental generated artifacts.
- [x] PMA110 candidate: RUN. Debug APK from `a42a82a` installed and launched via the exact
  DEPLOY_CMD (serial `127.0.0.1:5601`); full functional + reliability verification passed (see
  the device verification record below).
- [x] DEPLOY_MODE `per-cycle`: deploy executed once after green gates; no Play mutation or
  external communication.

## Cycle-5 device verification record (2026-07-19, PMA110 @ 127.0.0.1:5601, HEAD a42a82a)

Interaction basics (a): live preview proven by screencap statistics (dark scene, stdev 37; 23.7%
of preview pixels changed >2 gray levels across a 1 s frame pair — live sensor noise at ISO
12700). Shoot-tab zoom slider driven both directions with HAL tracking (display 13.0→19.2→55.0→
25.5×; result effZoom 4.29→6.32→18.08→8.37); TC toggle round-trip truthful ('300 mm TELE' ↔
'70 mm', tele=false at effZoom 3.0 on the logical route, tap-AF correctly released at the remap
door); tap-AF locked (afMode AUTO, FOCUSED_LOCKED at lens 0.00685 ≈ ∞) and the lens value held
BIT-EXACT while the ISO-priority loop swung exposure 100→500 ms — the fast-path AF carry on
device; photo↔video flips clean in both directions across multiple reopens.

Baselines (b), all TELE 300 mm: still family 0000000006 published atomically (DNG 25.2 MB
4096×3072 16-bit valid TIFF + HEIC + JPEG with EXIF 0.1 s / ISO 12700 / 300 mm 35 mm-equiv /
Make+Model); long-exposure still at the 4.0 s ruler ceiling captured with EXIF ExposureTime 4.0
and zero device errors; normal video HEVC **Main 10, 2160×3840 portrait buffer** (the cycle-4
`encoderSurfaceSize` contract), bt2020 + **arib-std-b67** VUI, 29.92 fps held in the dark, AAC
48 kHz stereo; LOG clip **Main 10, full-range bt2020, transfer bt2020-10 (CICP 14)** — exactly
the documented QTI landing for the explicit SDR tag and decisively NOT the ST2084/PQ mistag.

Reliability mandate (c): (i) TELE DNG+processed capture, app force-killed +1.3 s (mid-save):
HEIC already published survived; the DNG — killed inside its publish window, row IS_PENDING with
full bytes on disk — was **ADOPTED and PUBLISHED by the next launch's recovery** (25,195,852
bytes, byte-size-identical to known-good DNGs, valid TIFF structure); the byte-less JPEG pending
row was correctly deleted; zero stuck IS_PENDING rows; review showed the capture after relaunch.
This exercises the cycle-5 durable-pending-lifecycle fix (2829cc6) end-to-end on device.
(ii) HOME mid-REC: the clip finalized and published (3.87 s, moov intact, plays), and the app
returned to a clean armed state with a live session. (iii) kill-after-stop races at +0.6 s and
+0.15 s: stop→finalize→publish completed before `am force-stop` could land both times (published
valid clips; the adoption window is not reachable via adb on this device — the adoption machinery
itself is proven by (i)). (iv) Final logcat: zero app-process FATAL/ANR/CAMERA_DISCONNECTED; the
only FATALs are the uiautomator shell tool's own processes, and every CAMERA_ERROR line is the
known benign framework `cancelRequest -38` teardown noise during reopens.

Framing re-check (d): every recorded clip carries the portrait 2160×3840 encoder buffer; the
mid-REC viewfinder screencap and the extracted clip frame show the same torch-lit field
edge-to-edge with matching falloff (the flat-on-desk scene is featureless, so the cycle-4-style
numeric cell-map correlation is not meaningful here; buffer shape + visual field match + the
pinned CoverScale/RotationMath contracts stand as the evidence).

Operational notes: injected taps mis-landed repeatedly (documented device quirk) — every step was
therefore verified from a fresh dump/screencap before proceeding, and two apparent anomalies
(DNG format reverting, an unexpected reopen) traced to mis-landed taps recalling MR banks or
flipping modes, not app defects. Torch, transfer (HLG), and photo format (HEIF+JPEG) were
restored after testing.

## Post-report addendum (2026-07-19, HEAD 4e1de17)

After the cycle report was delivered, a resumed pre-crash implementation slice was found active
in the worktree and was concluded rather than abandoned (orchestrator directive). Its work was
reviewed for coherence, gated green (assembleDebug / 663 unit tests / lintDebug), and landed as
four signed commits (the slice's own agent committed and pushed the README piece concurrently):

- `d0e7a88` fix(video): app-owned S/ISO/M exposure is clamped to one frame interval in video mode
  (`normalizedForCaptureMode` at every controls door, `enforceFrameRate` through all request
  builders, AE-loop upper bound) so the selected 24/30/60p cadence is a delivery contract —
  closes the sub-cadence video exposure observable pre-fix on device (116-500 ms video preview
  exposures in ISO/M). Pinned by the new `VideoFrameRatePolicyTest`.
- `bc59dc7` fix(capture): HEIF EXIF payload now carries the true encoded dimensions (both primary
  and compressed-image tag pairs) instead of the 1×1 ExifInterface seed's — MediaStore no longer
  indexes full-resolution HEIFs as 1×1.
- `4e1de17` fix(ui): pure `dispatchPhotoShutter` gives countdown-cancel first refusal (and
  `primaryShutterEnabled` admits the cancelling tap during a countdown regardless of readiness);
  the focal rail exports selection on the actionable node (`selectable(Role.Tab)`) so
  AccessibilityNodeInfo reports `selected=` truthfully.
- `d18f5f1` docs(readme): capture aids, focal rail, and durable-save truth sync.

Evidence bound honestly: gates and host tests are green at `4e1de17`; the device phase ran at
`a42a82a` BEFORE this slice, so the video-cadence fix and HEIF dimension indexing are
host-test-verified only. Residual (loop closed, no further cycles): a PMA110 re-check that video
S/ISO/M preview and recorded files hold the selected cadence in the dark, and that a fresh HEIF's
MediaStore row indexes full resolution.

## Explicit deferrals

- **P3.5 / AGG1-20 — full landscape operator pipeline (Medium/High).** The activity remains
  portrait-locked after review caught a Compose-only unlock that would misframe GL preview and the
  16:9 mask. Exit: propagate window rotation through GL aspect/rotation, still-mask and tap/loupe
  mapping, and encoder framing; then verify both holds plus 200% font on PMA110.
- **P5.4 signing environment (release blocker).** `lintRelease` passes, but the gitignored signing
  properties do not contain usable password inputs and the corresponding environment variables are
  unset. Exit: the user supplies the protected inputs, then rerun `assembleRelease`/`bundleRelease`,
  validate the exact artifact, and update the stale hash sheet; never copy secrets into tracked files.

- **AGG1-21 — Android/Compose/PMA110 verification gap (Medium/High).** Blocked by no current serial
  and no instrumentation harness. Exit: add deterministic androidTest/Compose semantics cases and
  run the full table on the exact signed candidate, including Camera2, MediaStore, codec, gestures,
  TalkBack, 200% font, cutouts, lifecycle, storage pressure, and saved-file probes. This cycle's host
  tests are not substituted for it.
- **AGG1-22 — Play screenshots (Medium/High).** Cannot truthfully recapture without PMA110. Exit:
  after the exact candidate passes device tests, capture all six deterministic flows, validate size/
  alpha/content, update captions and provenance, then review before upload.
- **AGG1-27 — duplicate restore reopens (Low/High).** Deferred to the later optics-core extraction;
  correctness converges today and changing reopen batching alongside recovery/extractions increases
  session risk. Exit: one transaction packet applies stab/open-gate/optics with a counted one-reopen test.
- **AGG1-28 — true fixed-window zoom coalescer (Low/Medium-high).** Current engine throttling keeps
  request truth; no current device jank regression is proven. Exit: synthetic timeline proves <=60 Hz
  and PMA110 gesture telemetry shows no lost leading/trailing landing.
- **AGG1-30 — double-failure EGL finalization wedge (Low/High, rare).** Needs a controllable GL
  ownership seam. Exit: injected detach+release failures prove recorder teardown always reaches a
  terminal state without releasing a surface under native use.
- **AGG1-32 — availability/capability performance (Low/High mechanism).** Exit: project stable
  state into narrow keys and prewarm EncoderCaps off main, with allocation/launch traces.
- **AGG1-33 — ISO ladder above 25,600 (Low/Medium trigger).** PMA110 advertised ceiling is not
  current-session verified. Exit: derive stops through caps upper bound and device-check selection/
  request truth at the highest advertised ISO.
- **AGG1-34 — absurd wall-clock capture key (Low/High semantics, negligible trigger).** Exit:
  normalize/fallback without changing normal family ordering and add pre-epoch tests.
- **AGG1-35 — remaining QA hygiene (Low-Medium/High).** Permission durability is scheduled in
  P5.1; suppression comment and `muxer!!` ownership cleanup wait for the controller/recorder seam.
  Exit: explicit invariant comment and nullable local ownership proven by tests.
- **AGG1-38 — broad layer/API/god-function debt (Low/Medium).** Only ordered P4 extractions land
  now. Exit: continue the documented extraction order; move MediaReview I/O/playback behind a
  boundary; centralize duplicated focal/zoom/bitrate/EV facts; narrow APIs with compile/test gates.
- **AGG1-39 — environment/security manual checks (Low/Medium).** No signing/reference file is moved,
  scrubbed, or inspected destructively. Source-level tapjacking hardening may land opportunistically.
  Exit: user-approved secure signing-material relocation, metadata inspection/scrub decision, and a
  PMA110 obscured-touch test. Privacy/network/release-log scans remain clean.
- **AGG1-40 — Android-dependent remainder (Low/Medium).** Host-testable matrices are P5.2; real
  recorder codecs, ViewModel wiring, and Camera2 binding require the AGG1-21 harness/device exit.
- **AGG1-41 — benign ownership/threading residues (Info/High).** Exit with the related later
  extractions: fresh recorder instance contract, no-op release thread removal, callback thread KDoc,
  Gyro confinement, and late-publish tracker replay.
- **AGG1-42 — active-REC process death (Info/High).** A pre-finalize MP4 cannot be adopted safely.
  Exit requires a separately designed fragmented/container journaling recorder; document this
  residual distinctly from recoverable post-finalize media.

## Coverage map

- P1: AGG1-1, AGG1-2, AGG1-8, AGG1-9, AGG1-10, AGG1-11, AGG1-12, AGG1-26
- P2: AGG1-5, AGG1-6, AGG1-7, AGG1-13, AGG1-29, AGG1-31
- P3: AGG1-4, AGG1-14, AGG1-15, AGG1-16, AGG1-17, AGG1-18, AGG1-19, AGG1-36
- P4: AGG1-23, AGG1-24
- P5: AGG1-3, AGG1-25, AGG1-37, scheduled host portion of AGG1-40
- Deferrals: AGG1-20, AGG1-21, AGG1-22, AGG1-27, AGG1-28, AGG1-30, AGG1-32, AGG1-33, AGG1-34,
  remaining AGG1-35, AGG1-38, AGG1-39, Android-dependent remainder of AGG1-40, AGG1-41, AGG1-42

Every AGG1 entry appears exactly once as a primary scheduled item or explicit deferral. Split entries
AGG1-35 and AGG1-40 state their scheduled and deferred subparts explicitly.

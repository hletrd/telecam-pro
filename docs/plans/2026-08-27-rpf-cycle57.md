# Review-plan-fix cycle 57 implementation plan

Date: 2026-08-27
Review authority: `.context/reviews/_aggregate.md` — cycle 57
Status: planned

## Finding disposition

All six deduplicated cycle-57 findings are scheduled below. No finding is deferred, downgraded,
silently dropped, or moved to a later cycle. The open physical checks in `docs/FIELD_CHECKS.md` are
existing evidence obligations rather than review findings and are not changed by this plan.

## P1 — enforce the replacement signing-secret policy (AGG57-01, Medium/High)

- [ ] Replace the historical six-digit special case with one explicit generated-secret contract:
  require at least 20 Unicode code points, reject leading/trailing whitespace, require at least
  three character classes, and reject one-character repetition and monotonic numeric/alphabetic
  sequences. Keep all rejection text value-free.
- [ ] Add accepted generated-secret examples plus numeric-only, alphabetic-only, repeated,
  sequential, short, whitespace-altered, duplicate, incomplete, and unknown-field failures to the
  scoped-release tool tests.
- [ ] Update the active release procedure to state the exact machine-enforced floor without
  implying that the helper proves randomness. Do not read, rotate, revoke, decrypt, or use a real
  signing credential and do not perform an upload.

## P2 — serialize one truthful still-admission projection (AGG57-02, Medium/High)

- [ ] Replace the process-only cached publisher with one Engine-local serialized projection owner
  that computes, change-gates, and delivers the combined DNG + retained-family + storage Boolean in
  one ordered critical section.
- [ ] Route every current direct `onStillCaptureAdmissionChanged` call through that owner, including
  acquisition refusal, DNG release, retained-family deletion/retirement, rejected-output terminal,
  storage recovery, and release/detach paths. No constituent edge may bypass delivered-state truth.
- [ ] Add deterministic tests for older cross-signal callback completion, local close/reopen around
  a foreign false edge, duplicate suppression, callback detach/replacement, and final UI truth.

## P3 — retain pre-REGISTERED insert failures in finite ownership (AGG57-03, Medium/High)

- [ ] Split provider insert from REGISTERED durability and classify registration failure as
  `REGISTERED`, authoritative `ABSENT`, or `RETAINED`. A failed/throwing immediate delete must query
  presence and remain retained unless absence is authoritative.
- [ ] Transfer every retained URI/family through the already-reserved process identity-recovery
  owner. Cancel the reservation only after successful registration transfer or authoritative
  provider absence; preserve exact/absent/uncertain destructive-identity rules.
- [ ] Cover Images and Video with false/throwing commit/delete, present/absent/unavailable provider
  results, repeated saturation, later exact/absence recovery, and admission close/reopen. Add a
  production-source mutation assertion so either factory cannot revert to cancellation on a
  retained row.

## P4 — make the entire diagnostic quota executable (AGG57-04, Medium/High)

- [ ] Put `FrameGap`, accepted/configured session rows, recording spec/store rows, and every
  warning/error behind finite process admission. Keep repeatable information in the existing
  180-row owner and add an explicit 120-row terminal/reserved owner so runtime maximum accounting is
  300 rather than arithmetic in a test.
- [ ] Remove classifier rules that bless a row merely by anchor or severity. Require an executable
  recurring/reserved guard at every production log call; mutation-test removal of the FrameGap,
  session, recording, and terminal-fault guards.
- [ ] Accumulate suppressed frame-gap count and maximum duration in constant memory and surface a
  bounded periodic/terminal summary suitable for the A5 soak. Update committed diagnostic and field
  authority to describe the bounded evidence shape.

## P5 — separate render cadence from producer cadence (AGG57-05, Medium/High)

- [ ] Extract a pure preview-frame timing policy with distinct last-render and last-camera-frame
  timestamps. `updateTex=false` may drive cached-redraw throttling but may neither emit nor reset a
  producer gap.
- [ ] Reset both timestamps with the GL generation and use the producer-only result for FrameGap
  aggregation/admission.
- [ ] Test first frame, many cached redraws followed by a late real frame, cached-only
  false-positive refusal, ordinary producer cadence, and generation reset.

## P6 — test the production complete-DNG transfer composition (AGG57-06, Low/High)

- [ ] Extract the CameraEngine complete-DNG composition into one production seam that receives the
  real `PhotoFormats`, processed-queue result, ordered Engine enqueue, process dispatcher, and
  retained-recovery terminal.
- [ ] Drive RAW-only SINGLE, mixed SINGLE, BURST, AEB, timelapse, queue rejection, dispatcher
  shutdown, and old/replacement Engine facade ownership through that exact seam.
- [ ] Add a source-mutation contract that fails when CameraEngine removes or bypasses the production
  transfer call; keep process capacity and sibling-order tests alongside it.

## P7 — documentation, coverage, and repository gates

- [ ] Update `CLAUDE.md` and `docs/ARCHITECTURE.md` for bounded diagnostic accounting, producer-only
  frame timing, typed pre-REGISTERED ownership, and serialized still-admission publication.
- [ ] Run focused JVM/Python/tool mutation suites while iterating, then the configured whole-repo
  gates exactly as supplied by the orchestrator:
  `./gradlew :app:assembleDebug :app:testDebugUnitTest :app:lintDebug` and
  `./gradlew :app:lintRelease :app:assembleRelease :app:bundleRelease`.
- [ ] Record final evidence and commit each concern separately with signed Conventional Commit +
  gitmoji messages, pulling with rebase before every push. No deployment or device action is
  authorized for this cycle.

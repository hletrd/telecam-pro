# Review-plan-fix cycle 56 — implementation plan

Date: 2026-08-27
Source: `.context/reviews/_aggregate.md`, cycle 56
Status: complete; all six findings fixed and every configured quality gate green

## Repository rules applied

- Preserve PMA110 byte-identical measured behavior, capability-enumerated generic routes, exact
  native/provider identity, durable MediaStore truth, process-finite work, the quiet Sony-style UI,
  EN/KO parity, and explicit device-evidence boundaries.
- Schedule every one of the six aggregate findings below. Nothing is silently dropped,
  severity-downgraded, deferred, relabeled optional, or treated as closed by documentation alone.
- Keep historical completed plans in `docs/plans/` as the non-destructive archive; append dated
  corrections where earlier completion evidence overclaims retry/ownership rather than rewriting it.
- Work only in `/tmp/find-x9-ultra-cycle56.Re05OY`; never modify, stash, reset, restore, check out,
  clean, or build from the shared dirty main checkout.
- Use fine-grained GPG-signed Conventional Commits with gitmoji, pull with rebase before every push,
  and do not deploy because `DEPLOY_MODE=none`.
- Never read, print, copy, rotate, revoke, or upload credentials in this cycle. Credential rotation
  and Play upload-key reset are destructive/external owner actions requiring separate explicit
  confirmation. Repository release procedure must fail closed before either can be attempted.

## P1 — fail closed around weak upload-key practice (AGG56-01, High/High)

- [x] Replace the active persistent-shell decrypt/export recipe with a short-lived scoped command
  that validates required fields, opens the keystore without printing values, invokes the immutable
  release wrapper, and unsets/deletes all transient secret state on every terminal.
- [x] Mark the known six-digit/plaintext-exposed key unusable for the next upload and require an
  explicitly owner-approved strong-key rotation or Play upload-key reset before upload readiness;
  preserve the historical incident without presenting the weak credential as an active path.
- [x] Add documentation/tool contract tests proving no active recipe leaves `TELECAMPRO_*` secrets
  exported, no secret value reaches argv/logs/source, and the upload prerequisite stays fail closed.

## P2 — make pending-allocation identity recovery live and finite (AGG56-02, Medium/High)

- [x] Preserve typed exact/authoritatively-absent/uncertain identity results instead of collapsing
  every non-exact read to null. Stable absence must clear only the REGISTERED metadata and release
  capacity without issuing a provider delete; mismatch/ambiguity/unavailability remain fail closed.
- [x] Give unresolved process ownership a bounded backed-off retry scheduler with one in-flight
  attempt per retained claim, no inline provider fallback, no unbounded worker/task growth, and
  exact terminal cancellation/capacity reuse.
- [x] Publish close/reopen capacity transitions and cover post-launch first-N failures, later exact
  recovery, stable absence, repeated retry triggers, Engine replacement, and admission reopening.

## P3 — observe process still-admission truth across Engine replacement (AGG56-03, Medium/High)

- [x] Make process DNG and storage capacity expose closeable exact subscriptions (or one combined
  monotonic coordinator) rather than relying on one-time Engine polling and old-Engine callbacks.
- [x] Recompute/publish combined still-output admission after every DNG acquire/release,
  rejected/pending-identity reserve/terminal/retry edge, while detached Engines remain inert.
- [x] Test old-Engine ownership → replacement subscription → old callback detachment → terminal
  release for success, error, timeout, and cancellation; prove the replacement shutter re-enables
  without an unrelated event.

## P4 — process-bound every completed DNG publication tail (AGG56-04, Medium/High)

- [x] Route RAW-only SINGLE and sequence DNG publication through the process finite owner; for mixed
  HEIF/JPEG+DNG, transfer the DNG tail only after the processed lane terminal so sibling/sequence
  ordering and family settlement remain exact.
- [x] On process-capacity overflow/facade shutdown, settle the capture family once, preserve complete
  private bytes and durable marker truth for launch recovery, and never publish/delete inline.
- [x] Test blocked old-Engine mixed and BURST/AEB/timelapse tails against repeated Engine replacement,
  exact process worker/backlog ceilings, ordering, recovery fallback, and one terminal per family.

## P5 — close the repeatable diagnostic quota boundary (AGG56-05, Medium/High)

- [x] Route `Touch AF: scanning` and `TapFocus: cleared` through the shared recurring diagnostic
  admission door, change/edge-gating them where useful without losing calibration evidence.
- [x] Replace occurrence-count documentation checks with an executable classification inventory of
  every non-fault DEBUG log site (budgeted recurring, one-shot startup/session, or reserved fault).
- [x] Add tap/reset exhaustion and source-mutation tests proving the 180-row recurring ceiling and
  120-row process fault reserve cannot be bypassed.

## P6 — bind StartupTrace to an exact Engine/open/controller owner (AGG56-06, Low/High)

- [x] Retain the trace owner on the exact Engine resume/open transaction, pass it explicitly into
  `CameraController`, and stop sampling mutable process-global `currentOwner()` while wiring.
- [x] Disarm/revoke exact ownership on pause, release, superseded open, and controller replacement;
  re-entrant resume inside the same live attempt remains idempotent.
- [x] Test resume A → pause-before-result → resume B and two-controller replacement orderings,
  proving old results are inert and every emitted origin/milestone belongs only to the live owner.

## Quality gates and delivery

- [x] Run focused tests for every security procedure, identity retry/absence, admission subscription,
  DNG publication, diagnostic inventory, and startup ownership change.
- [x] Run `./gradlew :app:assembleDebug :app:testDebugUnitTest :app:lintDebug` across the whole repo.
- [x] Run `./gradlew :app:lintRelease :app:assembleRelease :app:bundleRelease` across the whole repo.
- [x] Run `python3 tools/verify_host.py` because repository authority requires the complete
  non-device host suite for delivery.
- [x] Root-fix every gate error and warning without suppression; record only repo-authorized
  unavoidable warnings under the strict deferred-finding rules.
- [x] Commit and push each independent review, plan, implementation, test, documentation, and
  closeout change with signed Conventional Commits plus gitmoji, pulling with rebase before push.
- [x] Mark this plan complete only after exact gate and delivery evidence is recorded.

## Deferred findings

None. Every cycle-56 finding is scheduled for repository implementation in this cycle.

One pre-existing gate warning remains explicitly recorded and is not a cycle-56 finding:

- **Low / High — `gradle/verification-metadata.xml:905-909`:** release packaging reports that the
  transitive upstream prebuilt `libandroidx.graphics.path.so` cannot be stripped. APK/AAB packaging
  succeeds, and the repository has no source/symbol owner that can root-fix the binary. Reopen when
  the latest stable `androidx.graphics:graphics-path` dependency becomes strip-compatible.

## Completion evidence

- All six findings were fixed with no review deferral. The signed-release procedure is now
  security-blocked until owner-approved strong-key rotation/Play reset plus exact public certificate
  fingerprint; its helper scopes decrypted values to child environment only and rejects six-digit
  credentials. No credential value or upload key was read, used, rotated, revoked, or uploaded.
- Pending allocation identities retain typed exact/absent/uncertain truth and retry on one bounded
  process scheduler; process DNG/storage admission publishes through exact closeable subscriptions;
  every completed DNG tail uses finite process ownership; tap-focus diagnostics share the quota;
  StartupTrace belongs to one explicit Engine resume/open/controller transaction.
- The exact debug gate passed assembly, the complete JVM/Robolectric/Compose suite, and lint. The
  exact release gate passed release lint, R8 APK, and AAB using Android's public developer debug key
  only; those outputs are developer-only and were neither treated as release evidence nor uploaded.
- `python3 tools/verify_host.py` passed: debug/androidTest assembly, unit/lint, Partition A at
  9,454/9,469 lines (99.84%) with the exact reviewed 15-line residual manifest, 142 tool tests, nine
  coverage-tool tests, 195 device-harness self-tests, 158 documentation checks (24 optional-private
  skips), Python compilation, and `git diff --check`.
- Three gate issues were root-fixed without suppression: the new process signal was absent from the
  architecture module map; StartupTrace's moved one-shot emission exceeded the classifier's local
  anchor; and new retry/startup/DNG/diagnostic branches lowered Partition-A coverage until production-
  path tests closed every new residual and retired one stale manifest entry.
- Thirteen fine-grained GPG-signed Conventional Commits with gitmoji were pushed before this
  closeout; this completion record is the fourteenth cycle commit. Deployment and device action were
  not run (`DEPLOY_MODE=none`). Open field checks remain evidence obligations, not deferred code
  findings.
- All work ran in `/tmp/find-x9-ultra-cycle56.Re05OY`. The shared dirty main checkout was never
  edited, staged, stashed, reset, restored, checked out, cleaned, or used for a build; its deltas
  remain intact.

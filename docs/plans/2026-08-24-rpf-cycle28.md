# Review-plan-fix cycle 28 — implementation plan

Date: 2026-08-24
Source: `.context/reviews/_aggregate.md`, cycle 28
Status: planned; implementation not started

## Repository rules applied

- Preserve measured PMA110 behavior, capability-routed Android 13+ behavior, EN/KO presentation,
  quiet-camera UX, media durability, and wrapper-owned release evidence.
- Every cycle-28 finding is scheduled below. Nothing is deferred, silently dropped,
  severity-downgraded, or introduced as new scope under a deferred label.
- Completed plans remain non-destructive archives in `docs/plans/`; no completed plan needs moving
  or deletion.
- Use GPG-signed Conventional Commits with gitmoji, pull with rebase before every push, and do not
  deploy because `DEPLOY_MODE=none`.
- Perform all work in the clean cycle clone. Preserve the shared main worktree and its unrelated,
  incorrect `CameraEngine.kt` delta without touching, staging, stashing, resetting, or restoring it.

## P1 — linearize exact-family admission with retirement (AGG28-01)

- [ ] Replace the split user/claim admission with a per-family protocol in which retirement cannot
  remove a marker in front of a publication or producer already admitted at the registry boundary.
- [ ] Keep provider and preference I/O outside the global registry lock, preserve unrelated-family
  publication/producer liveness, and define the disposition of an admission that races after a
  retirement decision is sealed.
- [ ] Add deterministic producer and publication tests at the exact authority-join/claim-install
  boundary, alongside the existing blocked-remove cross-family tests.
- [ ] Keep `docs/ARCHITECTURE.md` accurate; update it only if the concrete protocol needs wording
  beyond the existing register-before-wait guarantee.

## P2 — align partial-delete state and retry destination (AGG28-02)

- [ ] Extract one delivery-time decision that rechecks whether the restored survivor still owns
  review and returns both the state mutation and retry destination.
- [ ] Preserve a newer capture's URI, provenance, and delete scope; choose the Gallery retry message
  when the old survivor is no longer reachable through the in-app review entry.
- [ ] Add deterministic no-race and superseded-between-restore-and-main-delivery regressions.

## P3 — keep committed screenshot/runbook checks active in exports (AGG28-03)

- [ ] Split tracked `docs/play-console-submit.md` checks from optional private-listing checks so the
  public runbook is always validated against `asset-validity.json`.
- [ ] Preserve explicit private skips only for assertions that actually require
  `docs/play-store-listing.md`.
- [ ] Add exported-tree negative tests for stale-manifest/ready-runbook and ready-manifest/stale-
  runbook contradictions, while retaining the positive clean-export contract.

## P4 — avoid discarded per-buffer audio level work (AGG28-04)

- [ ] Separate allocation-free in-place gain from optional RMS measurement, or make measurement an
  explicit conditional path admitted only at the existing 100 ms meter cadence.
- [ ] Preserve signed-PCM clamping, buffer position/limit, partial-frame handling, mono/stereo
  per-channel levels, unity-gain behavior, and the existing 10 Hz callback contract.
- [ ] Add focused tests proving non-emission buffers still apply gain without constructing a level
  result and measured buffers retain exact level semantics.

## P5 — reconcile owner-null privacy wording (AGG28-05)

- [ ] Replace the published page's own-captures-only statement with the existing current-package
  plus owner-null legacy-candidate, origin-unverified, file-only-delete disclosure.
- [ ] Extend `tools/check_docs.py` with a parity assertion that rejects the obsolete absolute claim
  while owner-null restore is enabled.

## P6 — surface external-navigation failures (AGG28-06)

- [ ] Centralize Privacy Policy and app-settings launches into typed success/failure handling.
- [ ] Add localized EN/KO assertive failure feedback, retain focus on the triggering control, and
  provide an in-app privacy-policy fallback when no browser/handler is available.
- [ ] Add host/Compose/Robolectric coverage for unresolved intents and `SecurityException` paths,
  including the permanently denied/policy-blocked permission gate.

## Quality gates and delivery

- [ ] Run focused tests for each fix, documentation/tool contracts, and `git diff --check`.
- [ ] Run `./gradlew :app:assembleDebug :app:testDebugUnitTest :app:lintDebug` against the whole repo
  and root-fix every error/actionable warning without suppression.
- [ ] Run `./gradlew :app:lintRelease :app:assembleRelease :app:bundleRelease` against clean committed
  source and root-fix every error/actionable warning without suppression.
- [ ] Commit and push each independent fix and review/plan update using signed Conventional Commits
  plus gitmoji, always pulling with rebase before each push.
- [ ] Update this plan with exact gate evidence and mark it complete only after every item is done.

## Deferred findings

None.

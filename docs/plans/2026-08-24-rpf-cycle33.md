# Review-plan-fix cycle 33 — implementation plan

Date: 2026-08-24  
Source: `.context/reviews/_aggregate.md`, cycle 33  
Status: complete; all fifteen findings fixed, configured gates green, signed commits pushed

## Repository rules applied

- Preserve measured PMA110 behavior, capability-enumerated multi-device paths, Camera2 ownership,
  durable media truth, quiet Sony-style UI, EN/KO parity, and committed clean-clone authority.
- Schedule every one of the fifteen aggregate findings below. Nothing is deferred, silently dropped,
  severity-downgraded, or introduced as unrelated scope.
- Keep completed historical plans as non-destructive archives in `docs/plans/`; do not move or delete
  them without explicit user confirmation.
- Work only in the clean detached worktree `/tmp/find-x9-cycle33-latest.Vc7rke`; do not touch, stage,
  stash, reset, or checkout the shared dirty `main` worktree.
- Use fine-grained GPG-signed Conventional Commits with gitmoji, pull with rebase before every push,
  and do not deploy because `DEPLOY_MODE=none`.

## P1 — move ownerless delete-request construction off main (AGG33-01)

- [x] Give request creation finite background ownership and a first-wins deadline.
- [x] Recheck exact pending URI/generation before launching the resulting request on main.
- [x] Terminalize rejection, timeout, construction failure, stale result, and late completion once.
- [x] Prove a blocked provider cannot block the caller/main thread.

## P2 — deadline consent-result reconciliation (AGG33-02)

- [x] Own each pending consent result with a timeout independent of provider-worker progress.
- [x] On timeout restore the frozen handle with truthful unknown disposition and clear input blocking.
- [x] Ignore late provider results after timeout and test queued-behind-blocker plus active-query wedge.

## P3 — bound pre-marker family deletion work (AGG33-03)

- [x] Move deletion-marker/callback tasks off the unbounded still-encoding queue into one finite
  process-wide family lane.
- [x] Refuse overflow/shutdown promptly, restore the frozen review handle, and never claim recovery
  before a marker is durable.
- [x] Prove fixed active+queued capacity, no inline provider work, exact callback ownership, and no
  stale Engine/ViewModel graph retention.

## P4 — classify converted intent-sender launch failures (AGG33-04)

- [x] Add a pure classifier for success, genuine cancel, and AndroidX's converted
  `SendIntentException` marker.
- [x] Present converted failures as authorization unavailable and test all result classes.

## P5 — protect debug camera-control hooks (AGG33-05)

- [x] Remove command-extra handling from the ordinary exported launcher.
- [x] Route shell-only debug controls through a debug-only `android.permission.DUMP`-protected
  component or equivalent protected seam.
- [x] Verify launcher extras are inert and the merged debug manifest protects the hook.

## P6 — make private-context references clone-safe (AGG33-06)

- [x] Qualify each committed `docs/BACKLOG.md` dependency as optional when present.
- [x] Name a committed field-results fallback for clean-clone operators.
- [x] Expand docs checks across all committed authorities and add exported-tree negative coverage.

## P7 — reconcile every open field obligation (AGG33-07)

- [x] Give the front tap-rotation residual an explicit field-check identity/status.
- [x] Add open E2 and that residual to the dashboard/count without claiming device validation.
- [x] Machine-check exact dashboard membership against open/partial headings.

## P8 — correct the TC OIS evidence claim (AGG33-08)

- [x] Preserve verified public OIS/session acceptance and state only the actual A/B observation.
- [x] Reserve confirmed/pass wording for demonstrated profile differences.
- [x] Add a checker rejecting unresolved qualifiers under a current confirmed/pass heading.

## P9 — establish clean-clone Android SDK authority (AGG33-09)

- [x] Add canonical SDK setup/discovery instructions reused by build, field, and harness workflows.
- [x] Add a precise pre-Gradle SDK preflight that honors existing Gradle authority, environment
  variables, and supported conventional paths without committing local path files.
- [x] Test environment/local/conventional/missing-SDK outcomes and remediation text.

## P10 — align harness documentation with executable truth (AGG33-10)

- [x] State that harness source is versioned while generated evidence/caches are ignored.
- [x] Add `localized_camera_semantics` to the documented case matrix.
- [x] Test exact registry/table order and membership plus nonempty assertion summaries.

## P11 — use platform double-tap slop (AGG33-11)

- [x] Feed `scaledDoubleTapSlop` into inter-tap recognition and retain touch slop for one-contact
  motion.
- [x] Test a valid placement between touch and double-tap thresholds.

## P12 — preserve the pinch centroid (AGG33-12)

- [x] Apply scale and pan as one point-preserving transform before a single clamp.
- [x] Pure-test centered/off-center, simultaneous pan+zoom, and boundary cases.

## P13 — add non-touch still-review panning (AGG33-13)

- [x] Add bounded directional semantic actions and D-pad/arrow handling.
- [x] Expose coarse non-live position state and test center/edge enablement, keys, actions, and
  RTL-independent image geometry.

## P14 — expose accessible live-instrument readings (AGG33-14)

- [x] Add localized stable identities and coarse non-live readings for the audio meter, histogram,
  and waveform, including empty/pending and clipping/silence states.
- [x] Add EN/KO Compose semantics tests without sensor-rate live announcements.

## P15 — keep guides and horizon visible on arbitrary frames (AGG33-15)

- [x] Draw a wider dark keyline beneath quiet grid/frame/horizon foreground strokes.
- [x] Add bright/dark/midtone rendered fixtures and assert visible contrast/geometry.

## Quality gates and delivery

- [x] Run focused tests for every fix, documentation/tool contracts, manifest merges, resource
  parity, and `git diff --check`.
- [x] Run `./gradlew :app:assembleDebug :app:testDebugUnitTest :app:lintDebug` against the whole repo.
- [x] Run `./gradlew :app:lintRelease :app:assembleRelease :app:bundleRelease` against the whole repo.
- [x] Root-fix every configured gate error and every actionable warning without suppression.
- [x] Commit and push each independent review, plan, implementation, test, and docs increment with
  signed Conventional Commits plus gitmoji, pulling with rebase before every push.
- [x] Update this plan with exact evidence and mark complete only after all work is delivered.

## Deferred findings

None.

## Completion evidence

- All fifteen aggregate findings were implemented with no deferral or severity downgrade.
- Ownerless delete request creation and consent reconciliation are finite, backgrounded, independently
  deadlined, generation-checked, and truthfully terminal; converted intent-sender failures are
  distinguished from cancellation. Debug commands now enter only through a DUMP-protected component.
- Family deletion-marker persistence has a process-wide bounded family lane, pre-tombstone
  reservation, prompt refusal semantics, and exact detach/drain callback ownership.
- Still review uses platform double-tap slop, centroid-preserving pinch, and bounded D-pad/Switch
  Access navigation. Audio, histogram, and waveform expose coarse non-live EN/KO readings, while
  guides and horizon cues retain quiet foreground weights over dark keylines.
- Clean-clone documentation now owns its field-results fallback, exact open-check dashboard, truthful
  TC OIS evidence, Android SDK preflight, and executable device-harness inventory.
- Focused implementation evidence passed: 43 ownerless/debug tests, eight marker-dispatch tests,
  27 review/accessibility tests, 90 tooling tests, 183 device-harness tests, 106 documentation checks,
  manifest/resource processing, EN/KO parity, and `git diff --check`.
- Gate fixes this cycle: five (two implementation compile errors and three rendered/Compose fixture
  failures found during focused integration, all root-fixed before the whole-repo gates).
- `./gradlew :app:assembleDebug :app:testDebugUnitTest :app:lintDebug` passed against the whole repo.
- `./gradlew :app:lintRelease :app:assembleRelease :app:bundleRelease` passed against clean committed
  source with the existing local upload-key authority referenced by the isolated worktree. Direct
  Gradle outputs remain developer-only; no deployment or immutable release artifact is claimed.
- Seven signed review/plan/implementation commits were pull-rebased and pushed first; this signed
  plan closeout is the eighth and final cycle commit. Deployment was not run (`DEPLOY_MODE=none`).
- The shared dirty `main` worktree was never edited, staged, stashed, reset, checked out, or used for
  the build. Every cycle change and gate ran in `/tmp/find-x9-cycle33-latest.Vc7rke`.

## Superseding evidence note — cycle 34

Cycle 34 reproduced that this closeout's narrower Gradle trio did not prove the repository-wide
host boundary: `python3 tools/verify_host.py` failed on the then-current cycle-33 HEAD because four
new exact coverage residuals and one compiler warning had not been reconciled. The historical work
and narrower command result above remain unchanged, but the phrase “configured gates green” is
superseded by cycle 34's corrected evidence. After covering reachable branches, explicitly reviewing
the two Android MediaStore-bound lines, and removing warning noise, cycle 34 ran
`python3 tools/verify_host.py` successfully against its final implementation source.

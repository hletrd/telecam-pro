# Review-plan-fix cycle 33 — implementation plan

Date: 2026-08-24  
Source: `.context/reviews/_aggregate.md`, cycle 33  
Status: planned; implementation not started

## Repository rules applied

- Preserve measured PMA110 behavior, capability-enumerated multi-device paths, Camera2 ownership,
  durable media truth, quiet Sony-style UI, EN/KO parity, and committed clean-clone authority.
- Schedule every one of the fifteen aggregate findings below. Nothing is deferred, silently dropped,
  severity-downgraded, or introduced as unrelated scope.
- Keep completed historical plans as non-destructive archives in `docs/plans/`; do not move or delete
  them without explicit user confirmation.
- Work only in the clean detached worktree `/tmp/find-x9-cycle33-latest.Vc7rke`; do not touch, stage,
  stash, reset, or checkout the shared dirty `main` worktree.
- Use fine-grained GPG-signed Conventional Commits with gitmoji, pull with rebase before every push,
  and do not deploy because `DEPLOY_MODE=none`.

## P1 — move ownerless delete-request construction off main (AGG33-01)

- [ ] Give request creation finite background ownership and a first-wins deadline.
- [ ] Recheck exact pending URI/generation before launching the resulting request on main.
- [ ] Terminalize rejection, timeout, construction failure, stale result, and late completion once.
- [ ] Prove a blocked provider cannot block the caller/main thread.

## P2 — deadline consent-result reconciliation (AGG33-02)

- [ ] Own each pending consent result with a timeout independent of provider-worker progress.
- [ ] On timeout restore the frozen handle with truthful unknown disposition and clear input blocking.
- [ ] Ignore late provider results after timeout and test queued-behind-blocker plus active-query wedge.

## P3 — bound pre-marker family deletion work (AGG33-03)

- [ ] Move deletion-marker/callback tasks off the unbounded still-encoding queue into one finite
  process-wide family lane.
- [ ] Refuse overflow/shutdown promptly, restore the frozen review handle, and never claim recovery
  before a marker is durable.
- [ ] Prove fixed active+queued capacity, no inline provider work, exact callback ownership, and no
  stale Engine/ViewModel graph retention.

## P4 — classify converted intent-sender launch failures (AGG33-04)

- [ ] Add a pure classifier for success, genuine cancel, and AndroidX's converted
  `SendIntentException` marker.
- [ ] Present converted failures as authorization unavailable and test all result classes.

## P5 — protect debug camera-control hooks (AGG33-05)

- [ ] Remove command-extra handling from the ordinary exported launcher.
- [ ] Route shell-only debug controls through a debug-only `android.permission.DUMP`-protected
  component or equivalent protected seam.
- [ ] Verify launcher extras are inert and the merged debug manifest protects the hook.

## P6 — make private-context references clone-safe (AGG33-06)

- [ ] Qualify each committed `docs/BACKLOG.md` dependency as optional when present.
- [ ] Name a committed field-results fallback for clean-clone operators.
- [ ] Expand docs checks across all committed authorities and add exported-tree negative coverage.

## P7 — reconcile every open field obligation (AGG33-07)

- [ ] Give the front tap-rotation residual an explicit field-check identity/status.
- [ ] Add open E2 and that residual to the dashboard/count without claiming device validation.
- [ ] Machine-check exact dashboard membership against open/partial headings.

## P8 — correct the TC OIS evidence claim (AGG33-08)

- [ ] Preserve verified public OIS/session acceptance and state only the actual A/B observation.
- [ ] Reserve confirmed/pass wording for demonstrated profile differences.
- [ ] Add a checker rejecting unresolved qualifiers under a current confirmed/pass heading.

## P9 — establish clean-clone Android SDK authority (AGG33-09)

- [ ] Add canonical SDK setup/discovery instructions reused by build, field, and harness workflows.
- [ ] Add a precise pre-Gradle SDK preflight that honors existing Gradle authority, environment
  variables, and supported conventional paths without committing local path files.
- [ ] Test environment/local/conventional/missing-SDK outcomes and remediation text.

## P10 — align harness documentation with executable truth (AGG33-10)

- [ ] State that harness source is versioned while generated evidence/caches are ignored.
- [ ] Add `localized_camera_semantics` to the documented case matrix.
- [ ] Test exact registry/table order and membership plus nonempty assertion summaries.

## P11 — use platform double-tap slop (AGG33-11)

- [ ] Feed `scaledDoubleTapSlop` into inter-tap recognition and retain touch slop for one-contact
  motion.
- [ ] Test a valid placement between touch and double-tap thresholds.

## P12 — preserve the pinch centroid (AGG33-12)

- [ ] Apply scale and pan as one point-preserving transform before a single clamp.
- [ ] Pure-test centered/off-center, simultaneous pan+zoom, and boundary cases.

## P13 — add non-touch still-review panning (AGG33-13)

- [ ] Add bounded directional semantic actions and D-pad/arrow handling.
- [ ] Expose coarse non-live position state and test center/edge enablement, keys, actions, and
  RTL-independent image geometry.

## P14 — expose accessible live-instrument readings (AGG33-14)

- [ ] Add localized stable identities and coarse non-live readings for the audio meter, histogram,
  and waveform, including empty/pending and clipping/silence states.
- [ ] Add EN/KO Compose semantics tests without sensor-rate live announcements.

## P15 — keep guides and horizon visible on arbitrary frames (AGG33-15)

- [ ] Draw a wider dark keyline beneath quiet grid/frame/horizon foreground strokes.
- [ ] Add bright/dark/midtone rendered fixtures and assert visible contrast/geometry.

## Quality gates and delivery

- [ ] Run focused tests for every fix, documentation/tool contracts, manifest merges, resource
  parity, and `git diff --check`.
- [ ] Run `./gradlew :app:assembleDebug :app:testDebugUnitTest :app:lintDebug` against the whole repo.
- [ ] Run `./gradlew :app:lintRelease :app:assembleRelease :app:bundleRelease` against the whole repo.
- [ ] Root-fix every configured gate error and every actionable warning without suppression.
- [ ] Commit and push each independent review, plan, implementation, test, and docs increment with
  signed Conventional Commits plus gitmoji, pulling with rebase before every push.
- [ ] Update this plan with exact evidence and mark complete only after all work is delivered.

## Deferred findings

None.

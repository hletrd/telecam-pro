# Review-Plan-Fix Cycle 3 Plan — 2026-07-16

Derived from `.context/reviews/_aggregate.md` (13 current-HEAD specialist
reports; 12 deduplicated findings: 1 High, 9 Medium, 2 Low). Every finding is
scheduled below; there are no newly deferred cycle-3 findings.

Run constraints:

- Deployment mode is `none`: do not connect ADB, install, launch, publish,
  upload, or otherwise modify a device or production environment.
- The nested `ralph` skill named by the workflow is not installed. Implement
  this plan directly with the same fine-grained verify/commit/pull-rebase/push
  loop.
- Commits must be GPG-signed (`-S`), Conventional Commits with gitmoji, have no
  AI co-author trailer, and be pushed after `git pull --rebase`.
- Configured gates are `:app:assembleDebug`, `:app:testDebugUnitTest`,
  `:app:lintDebug`, `:app:lintRelease`, `:app:assembleRelease`, and
  `:app:bundleRelease` under JDK 21.
- Camera2/HAL, long timelapse/AEB, hardware-key, predictive-Back, and
  touch-target behavior changed here must be reported as statically/host-gate
  verified with PMA110 verification pending. `CLAUDE.md` forbids claiming
  device behavior from host gates alone.

Status: ☐ todo · ◐ in progress · ☑ done · ⏸ deferred

## Phase 1 — Transactional desired camera state

- ☐ **P1.1 / AGG-1 (High/High)** — Complete the engine's desired optics packet
  synchronously, require shared generation ownership before every asynchronous
  continuation mutates state or publishes Ready, and make resume resolve a fresh
  camera ID, selection, caps, stream size/aspect, and stabilization from current
  desired optics rather than reusing pre-pause selection/caps. Preserve the
  latest accepted intent across pause; current-generation preflight failures
  retain rollback. Add deterministic pure/state-machine cases for pause/resume
  and superseded mode/lens/TELE/recall/override continuations.
  `CameraEngine.kt:125-220,466-632,916-1170,1805-1856`;
  `ReconfigurationGenerationTest.kt`.
- ☐ **P1.2 / AGG-2 (Medium/High)** — Centralize ViewModel pending full-controls
  ownership. Lens/TELE transitions drain the freshest packet before deriving
  optics; settings/memory recall and rollback cancel the superseded packet.
  Ensure no delayed runnable can overwrite a newer optics packet. Add pure
  pending-packet decision tests and current-path assertions.
  `CameraViewModel.kt:74-87,174-192,288-422,891-1039,1429-1450`.
- ☐ **P1.3 / AGG-7 (Medium/High)** — Keep the ViewModel pre-TELE unified framing
  mirror until a non-TELE transaction is accepted. Move DNG normalization from
  optimistic lens/TELE updates to accepted live-cap reconciliation so failed
  TELE exit restores exact framing and RAW selection. Add failed-off → rollback
  → retry reducer tests. `CameraViewModel.kt:140-192,974-1039,1100-1130`;
  `CameraEngine.kt:125-220,916-999`.
- ☐ **P1.4 / AGG-8 (Medium/High)** — Split recalled requested-video-size storage
  from interactive live-caps validation. Store a recalled size before queuing
  target optics; validate/fallback only against the target camera when its stream
  is selected. Keep the picker path strict. Add a pure outgoing-vs-target caps
  decision test. `CameraViewModel.kt:288-422,1053-1059`;
  `CameraEngine.kt:586-632,871-899,1963-1969`.

## Phase 2 — Capture sequencing, capability truth, and persistence

- ☐ **P2.1 / AGG-3 (Medium/High)** — Replace fixed-delay timelapse submission
  with completion-paced scheduling: at most one capture plus processed save may
  be in flight, and the next interval begins only from the exactly-once
  `photoCallback` completion. Give each run a generation invalidated by stop,
  drive-mode change, pause, and release so late callbacks cannot restart it.
  Add a fake/pure sequence-gate test for slow completion, stop, restart, and
  stale callback cases. `CameraEngine.kt:1258-1376,1417-1493,1805-1810,1897-1902`.
- ☐ **P2.2 / AGG-4 (Medium/High)** — On AEB completion, restore the latest
  engine controls (or only the bracket-owned field) instead of the stale
  capture-start whole packet. Keep step capture metadata immutable. Add a
  mid-sequence ISO/WB/focus mutation test at a pure ownership seam.
  `CameraEngine.kt:1315-1354,1417-1461`; `CameraViewModel.kt:1429-1450`.
- ☐ **P2.3 / AGG-5 (Medium/High)** — Derive normal selectable video FPS only
  from exact `[fps,fps]` AE ranges and pin video requests only to an exact fixed
  range. Preserve variable low-floor ranges exclusively for photo AUTO. Update
  capability comments and add variable-only `[15,30]`, mixed variable+fixed,
  exact 24/30/60, and unavailable pin tests. `CaptureCapabilities.kt:86-113`;
  `CameraState.kt:329-364`; `ManualControls.kt:228-235`;
  `FpsBoundsTest.kt`; `VideoCapabilitiesTest.kt`.
- ☐ **P2.4 / AGG-6 (Medium/High)** — Commit the Remember Settings preference
  synchronously for both enable and disable. Extend the SharedPreferences fake
  with apply/commit counters and assert disabling takes the durable path.
  `SettingsStore.kt:96-106`; `SettingsStoreTest.kt:49-101`.

## Phase 3 — Modal input and accessible navigation

- ☐ **P3.1 / AGG-9 (Medium/High)** — Mirror all full-screen camera-blocking
  modals (settings, Fn, media review) into `CameraUiState`. Gate shutter, zoom,
  and half-press input on that single state. Track consumed full/half-press DOWN
  ownership so the matching UP is always delivered and consumed even when a
  modal opens mid-press. Extract/test pure key admission and straddle cases.
  `MainActivity.kt:158-235`; `CameraScreen.kt:163-220,624-662`;
  `CameraActions.kt:140-151`; `CameraState.kt:430-465`.
- ☐ **P3.2 / AGG-10 (Medium/High)** — Add local topmost `BackHandler` ownership
  to `ManualDialCluster`: an expanded ruler closes before the Activity
  backgrounds. Remove the write-only `manualDialOpen` owner callback/state and
  add a pure/Compose-compilation guard for precedence.
  `CameraScreen.kt:163-176,585-592`; `ManualDials.kt:121-214`.
- ☐ **P3.3 / AGG-11 (Low/High)** — Make the thumbnail child bitmap decorative
  so the parent Button has the sole accessible announcement. Increase review
  Close/Delete hit regions from 44 to at least 48 dp without changing glyph
  size. `MediaReview.kt:273-307,667-699`; `docs/UX_POLICY.md`.

## Phase 4 — Active documentation and verification

- ☐ **P4.1 / AGG-12 (Low/High)** — Align active source comments, `CLAUDE.md`,
  `docs/ARCHITECTURE.md`, and `docs/BACKLOG.md`: zoom uses a leading + 16 ms
  newest-value flush (~60 Hz), while general full-control packets use a separate
  40 ms/25 Hz throttle. Narrow `CameraController` ownership KDoc to processed
  image snapshotting and explicitly retain synchronous live-RAW DNG writing.
  Do not edit dated historical snapshots. `CameraViewModel.kt:761-780,865-875`;
  `CameraController.kt:28-34`; active docs cited by AGG-12.
- ☐ **P4.2** — Run the complete configured gate set together under JDK 21. Fix
  every introduced error/warning, update this plan's progress/evidence, and
  commit/pull-rebase/push each fine-grained slice. No deployment or ADB.

## Deferred findings

None newly deferred. Existing cycle-1/BACKLOG structural, localization,
instrumentation, device-measurement, DNG-threading, dual-encode,
asynchronous-REC, signing-environment, and vendor-audio items were explicitly
excluded from the cycle-3 reviewer count and retain their prior exit criteria.
Device confirmations named above are verification status for implemented fixes,
not deferred code fixes.

## Aggregate coverage map

- Scheduled exactly once: AGG-1 through AGG-12.
- Deferred from this cycle: none.
- No plan file is moved: completed cycle plans are durable historical records
  with completion evidence, and this repository has no plan-archive convention.

## Progress log

- 2026-07-16 — Prompt 1 complete: 13/13 reports returned; 12 independent
  findings aggregated; debug assembly/JVM baseline passed; security found zero;
  no device/deployment work.
- 2026-07-16 — Prompt 2 complete: all 12 findings scheduled above with no new
  deferrals; implementation has not started.


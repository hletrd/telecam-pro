# Review-plan-fix cycle 16 — implementation plan

Date: 2026-08-24
Source: `.context/reviews/_aggregate.md`, cycle 16
Status: planned; no implementation started

## Repository rules applied

- Preserve PMA110 behavior byte-for-byte except where AGG16-01 corrects a confirmed PMA110 framing
  defect; do not infer any new device/HAL result from host tests.
- Security, correctness, release-integrity, liveness, and data-loss findings are mandatory and are
  not deferred.
- Preserve English/Korean parity, the Sony Alpha/Xperia Pro surface, exact coverage accounting,
  immutable build/evidence boundaries, and all existing owner-approved backlog deferrals.
- Completed prior plans and cycle-15 review artifacts remain non-destructive archives.
- Use fine-grained GPG-signed Conventional Commits with gitmoji, pull with rebase before every push,
  never force-push, and never bypass configured gates.

Every aggregate finding AGG16-01 through AGG16-08 is scheduled below. No finding is deferred,
severity-downgraded, or silently dropped.

## Phase 1 — restore route-owned zoom coordinates

- [ ] **P1 / AGG16-01:** introduce shared pure TELE and rear-return transition policies whose inputs
  explicitly carry canonical unified framing, target standalone-route truth, and the physical
  optical inventory. Replace Engine and ViewModel mode/band arithmetic with those policies.
- [ ] Add PMA110 Photo+DNG TELE on/off, crop-only Video TELE on/off, Video -> FRONT -> Photo+DNG ->
  rear, and reverse FRONT mode-switch tests. Prove Engine/UI packets agree and round-trip to the
  intended unified framing.
- [ ] Reconcile `docs/BACKLOG.md` and `docs/ARCHITECTURE.md` with the complete set of repaired doors.

Acceptance: no live optics transition decides zoom units from `mode == VIDEO` or multiplies/divides
by a selected band that may be crop-only; all route-coordinate conversions use physical route truth.

## Phase 2 — close the release signing-input boundary

- [ ] **P2 / AGG16-02:** make the immutable wrapper the sole effective store-file resolver using a
  Java-properties-compatible, fail-closed contract. Require a repository-relative, no-follow regular
  keystore; descriptor-copy and seal it in the private checkout; clear `TELECAMPRO_STORE_FILE` before
  Gradle; reject absolute, symlink, ambiguous, missing, or unsupported file inputs without logging
  secrets.
- [ ] Add table-driven alternate-separator/escape/continuation and environment-precedence tests plus
  permanent and A -> B -> A keystore mutation tests. Keep password/alias environment values secret
  and distinct from file-path ownership.
- [ ] Update README, example configuration, Architecture, and the cycle-15 completion record to state
  the exact supported signing-input boundary; add a source/docs contract that inventories signing
  environment keys by file versus value semantics.

Acceptance: every file input Gradle can use for release signing is already frozen inside the one
release snapshot owner, and no ambient path can override it.

## Phase 3 — make all review acquisition process-finite and progress-capable

- [ ] **P3 / AGG16-03:** coalesce review kind/video-info/metadata acquisition into one URI-owned
  process-finite latest-wins lane; add a separate finite latest-wins thumbnail owner. Dispose every
  unpublished/stale retriever, stream, player, Surface, and Bitmap exactly once.
- [ ] **P4 / AGG16-04:** replace the one-mutex player setup lane with bounded process-wide execution
  capacity and a finite latest-only pending slot. Permit a healthy replacement while one worker is
  permanently blocked; report a bounded restart-required result after exact capacity exhaustion.
- [ ] Add forced permanently-blocked provider/native tests for replacement progress, saturation,
  active/backlog limits, stale publication refusal, disposal, and responsive Back/lifecycle edges.

Acceptance: retired review work cannot multiply without bound, one poisoned URI cannot starve every
later review, and every app-owned queue/worker/resource count has a tested finite ceiling.

## Phase 4 — make launch recovery both progressive and terminally observable

- [ ] **P5 / AGG16-05:** make the dedicated durable-DISCARD stage the sole terminal delete owner for
  exact journal markers during launch recovery, so generic Images/Video pages advance past those
  private rows and later pages/markers still make progress.
- [ ] **P6 / AGG16-06:** deliver a typed success/failure completion exactly once to every live launch
  recovery subscriber, log unexpected failure, admit a later request, and always run the safe latest
  published-family restore after terminal completion.
- [ ] Add integrated multi-page recovery tests with an early permanently failing DISCARD row and
  later rows/markers, plus throwing recovery with multiple/canceled subscribers and the ViewModel
  gallery-restore edge. Align Architecture completion wording.

Acceptance: one durable delete failure cannot starve later recovery, and every terminal worker path
releases subscribers while preserving non-destructive gallery discovery.

## Phase 5 — localize compact Fn values by identity and correct orientation guidance

- [ ] **P7 / AGG16-07:** replace English-string matching with typed compact-value identity and
  explicit EN/KO resources for auto-driven ISO/shutter, WB, stabilization, drive, and audio scenes.
  Preserve the full localized accessibility state independently of the compact visual value.
- [ ] Add Korean production-composition coverage at held 90/270 orientations and maximum font scale,
  asserting complete semantics and bounded, non-ellipsized visible values.
- [ ] **P8 / AGG16-08:** qualify the three active layout comments with the actual split: handsets stay
  portrait-locked; only `sw600dp+` windows may absorb orientation. Extend documentation/source guards
  against reintroducing unqualified handset-window rotation claims.

Acceptance: compact Fn rendering is locale-independent at the semantic layer, and active guidance
cannot teach the superseded handset rotation model.

## Phase 6 — gates, delivery, and evidence

- [ ] Run `./gradlew :app:assembleDebug :app:testDebugUnitTest :app:lintDebug` serially across the
  repository.
- [ ] Run the configured direct release tasks; if the repository's immutable-source guard refuses
  them on a dirty tree, commit the implementation, pull/rebase/push, then run the authoritative
  immutable release wrapper for `:app:lintRelease`, `:app:assembleRelease`, and
  `:app:bundleRelease` from exact committed source.
- [ ] Run `python3 tools/verify_host.py`, focused tool/device-harness/docs tests, Python compilation,
  exact Partition-A coverage verification, and diff/whitespace checks.
- [ ] Root-fix every error and actionable warning without suppression; record any genuinely
  unfixable warning under the strict deferred format before committing.
- [ ] Commit each independent fix with a GPG-signed Conventional Commit + gitmoji message, pull with
  rebase before every push, push, verify signatures/upstream synchronization, then close this plan.
  Do not deploy.

## Planned commit sequence

`docs(plan)`, `fix(camera)`, `fix(provenance)`, `perf(review)`, `fix(storage)`, `fix(recovery)`,
`fix(ui)`, `docs(ui)`, `test(coverage)`, and `docs(plan)`, split further wherever one commit would
combine unrelated behavior.

## Deferred findings

None. Existing owner-approved field checks and design deferrals in `docs/BACKLOG.md` are unchanged.

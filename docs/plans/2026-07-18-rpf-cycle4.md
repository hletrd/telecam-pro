# Review-Plan-Fix Cycle 4 — Implementation Plan (2026-07-18)

Source: `.context/reviews/_aggregate.md` (57 deduped entries AGG4-1..57 from 13 lanes, HEAD
a2d407b). Per-lane provenance in `.context/reviews/<lane>.md`. This plan disposes EVERY
aggregate entry exactly once (coverage map at the end) and supersedes the cycle-3 plan's
"Carried to cycle 4" section (docs/plans/2026-07-18-rpf-cycle3.md §Carried) — each carried item
is re-dispatched or re-deferred here.

Status legend: ☐ todo · ◐ in progress · ☑ done · ⏸ deferred (see § Deferrals).
Statuses are updated as work actually lands; commit refs are appended per item at landing time.

Execution order is P1→P10; inside a wave, items are independent unless noted. Repo policy binds
all work: GPG-signed fine-grained commits, Conventional Commits + gitmoji, no co-author,
pull-rebase before push, gates green before commit, device verification for camera/GL/UI
behavior claims.

## P1 — Correctness / data-loss class (non-deferrable, land first)

- ☑ [61a6bb7] **P1.1 (AGG4-8 / TR4-2, Medium, risk)** `VideoRecorder`: a `muxer.stop()` throw over a
  started-but-sample-less AUDIO track must not route into `firstFailure` and delete a cleanly
  muxed video clip. On degrade-with-muxer-started, record the audio track as sample-less and
  tolerate an audio-only stop() failure when `wroteVideoSample`. Host test beside
  `VideoRecorderMuxerStartTest`.
- ☑ [debb186] **P1.2 (AGG4-7 / CRIT4-5, Medium, High)** Publish-failure policy: add a small bounded
  retry (3 attempts, backoff) in `MediaStoreWriter.publish` used by BOTH photo and video
  publish sites; correct the false "orphan sweep will RECLAIM" comment (the sweep deletes);
  align photo/video failure handling.
- ☑ [e0444fe] **P1.3 (AGG4-3, High; folds carried P2.7)** Tap-AF: decouple the 2 s reticle timer from
  the functional release. At +2 s hide ONLY the visual reticle; `touchAfActive`, the metering
  region, AND the loupe center persist until a new tap, focus-mode change, explicit reset
  (`onResetFocusPoint`), or optics remap. Update CLAUDE.md + ARCHITECTURE.md tap-AF passages to
  the new truth (subsumes DOC4-1). Device-verify: tap → wait >2 s → ISO change → focus and
  loupe hold; reticle gone.
- ◐ [9e4ed85] **P1.4 (AGG4-4 / ARCH4-1+ARCH4-2+CRIT4-10, High)** Preview-vs-encoder framing
  contract. (a) DONE — device A/B CONFIRMED the defect: pre-fix clip carried a ~3.16× center
  band, rotated, of the preview field (gradient span ratio 3.43; cell-map corr ≤0.29).
  (b) DONE — encoder buffer now takes `RotationMath.encoderSurfaceSize` (displayed-aspect
  swap); post-fix device A/B: 2160×3840 HEVC, preview↔file cell-map corr 0.992, span ratio
  0.87. (c) PARTIAL — the framing invariant is pinned in CoverScaleTest + RotationMathTest
  (contentAspectSwapped mirrors coverScale's predicate); the ARCH4-2 `aspectCropRect`
  unification (still crop vs AspectMask) is still open in this cycle.

## P2 — Zoom (user-mandated focus)

- ☑ [4676f94] **P2.1 (AGG4-1, High)** Leading-edge zoom-out: ONE submit per gesture edge. Commit the
  ratio + GL target without a HAL submit on the leading tick; let the boost rebuild's
  `finalZoom` carry it. Device A/B with a real two-finger pinch-out (slider is not sufficient
  evidence).
- ☑ [4676f94] **P2.2 (AGG4-2, Medium)** Reset engine `zoomInteractionActive` on resume() and
  reconfigure/fresh-controller install; correct the wrong CameraViewModel.kt:1068-1073 comment.
- ☑ [4676f94] **P2.3 (AGG4-30, Low)** `reconcileZoomToCaps`: skip the pendingRatio re-base while
  `zoomGlide.interacting`.
- ☑ [4676f94+1e5799d] **P2.4 (AGG4-28, Low)** Declare the four zoom Runnables before `init` (or guard the
  construction-time invalidate) so `invalidateZoomGlide` never passes null to removeCallbacks.
- ☑ [4676f94] **P2.5 (AGG4-31, Low)** `applyLoaded`: normalize restored `exposureTimeNs` against
  last-known caps before the immediate `_state` publish (closes the one-hop over-ceiling
  display transient).
- ☑ [4676f94] **P2.6 (AGG4-11 / CR4-2+FDR4-2, Medium; carried P2.8/AGG3-4)** Tap-through-loupe mapping:
  compose the view tap through the active punch-in crop (center + magnification) before the
  rotation math, for BOTH the metering point and the loupe recenter target. Device-verify a
  magnified off-center tap lands AF on the tapped subject.

## P3 — GL / analysis / finder

- ☑ [166d9ce] **P3.1 (AGG4-9, Medium; carried P3.4)** Analysis readback meters display-referred luma:
  pass `transfer = null` into `runAnalysisReadback` regardless of LOG; host test pins that the
  analysis transfer is nulled when previewTransfer==LOG. Device check: scopes with LOG active.
- ☑ [166d9ce] **P3.2 (AGG4-32 / DBG4-3, Low)** GL wedge recovery: when stop()'s bounded join times out,
  deliberately abandon the wedged generation (null `thread`, leak it — mirror the VideoRecorder
  drain-wedge abandon) so a later start() can spawn fresh instead of no-oping forever.
- ☑ [166d9ce] **P3.3 (AGG4-33 / DBG4-4, Low)** Drop the `eglTerminate(EGL_DEFAULT_DISPLAY)` in the
  EGL-init containment (shared-display hazard; leak the single handle instead).
- ☑ [166d9ce] **P3.4 (AGG4-29 / FDR4-3, Low; carried P3.2)** Finder PIP gate axis: visible only when it
  adds field (punch-in or GL zoom compensation magnifying past the delivered field), not the
  raw zoom floor alone. Keep the honesty constraints.
- ☑ [166d9ce] **P3.5 (AGG4-18 / CR4-8+FDR4-4, Low; carried P6.7)** Derive the `olog2Inv` toe/log boundary
  from the forward constants (one source), fix the wrong comment arithmetic.
- ☑ [166d9ce] **P3.6 (AGG4-20 / CR4-10, Low)** EglCore: consume/clear the EGL error in the failed
  chooseConfig path (or gate on the create result) so a stale error cannot poison the later
  checked call.

## P4 — Controls / engine hygiene

- ☑ [d6cf6d8] **P4.1 (AGG4-6, Medium)** Extract ONE `applyAfOverrides(builder, ...)` called by both
  `startPreview` and `reapplyAfOverrides`; extract pure `afOverrideKeys(...)` decision and host
  test the precedence (afLock beats touch-AUTO when both set).
- ☑ [9c6884f] **P4.2 (AGG4-24 / CRIT4-3, Medium)** Document the 4 s ceiling as tele-bisected,
  conservatively applied to all cameras (CLAUDE.md + BACKLOG note); logical-camera bisect
  recorded as a residual device item (see Deferrals).
- ☑ [c2dca70] **P4.3 (AGG4-27 / CRIT4-8, Low)** ROLL_LOW_PASS_ALPHA: α 0.75→0.93 to actually hold the
  stated ~22 ms time constant at T=60 ms; fix the comment; device feel-check the level overlay.
- ☑ [1e61a3a] **P4.4 (AGG4-22 / CR4-12, Low)** SettingsStore: per-field defensive reads so one corrupt
  key cannot wipe the whole remembered setup.
- ☑ [1e61a3a] **P4.5 (AGG4-21 / CR4-11, Low)** `latestOwnCapture`: empty-exactRows onSuccess falls back
  to the already-resolved initial FILE_ONLY review (mirror onFailure).
- ☑ [bf94e1b] **P4.6 (AGG4-19 / CR4-9+TEST4-18, Low-Medium)** AudioInputInspector: extract
  `resolveAudioInputStatus(deviceTypes, preference)`; AUTO preview picks only recognized mic
  types; host tests (empty list, builtin-only, builtin+USB, missing-preferred).
- ☑ [61a6bb7] **P4.7 (AGG4-26 / CRIT4-6, Low)** On mid-REC audio degrade, zero the live audio meter level
  (a frozen last-level meter actively misleads); device-verify if reproducible, else
  code-inspect and note.
- ☑ [c2dca70] **P4.8 (AGG4-23 / CR4-4, Low)** Fix the stale GyroEis LOW_PASS_ALPHA comment (gyro no
  longer registered).

## P5 — UI / designer wave

- ☑ [7be06f5] **P5.1 (AGG4-55 / DES4-1, High)** One shared `zoomDisplayValue(...)` used by the pill,
  Shooting-tab slider, Zoom ruler, AND `fnSlotValue` — Fn/My-Menu zoom reads the same 13-60×
  TELE scale as every other surface.
- ☑ [83466be] **P5.2 (AGG4-56 / DES4-2+DES4-3, High)** 48 dp floor stragglers: MinTouchTargetButton for
  the 6 bare Button/TextButton sites (permission CTAs, review Retry, delete-dialog pair); wrap
  the 2 missed FilterChips (MR Save/Update, Capture Custom WB) in MinTouchTargetChip.
- ☑ [7be06f5] **P5.3 (AGG4-17 / CR4-6, Low)** Stabilization-mode SegmentedSelector gets
  `enabled = recordingMutable` like its Lens/TC siblings.
- ☑ [7be06f5] **P5.4 (AGG4-13 / QA4-1, Medium)** Replace deprecated `rememberSaveable(key=...)` with
  `key(tab) { rememberSaveable(saver) { ... } }` (repo policy: no deprecated APIs).
- ☑ [83466be] **P5.5 (AGG4-16 / CR4-5, Low)** Pin `Locale.US` on the four locale-less user-facing
  `String.format` sites (zoom pill, EV, review duration, review MB).
- ☑ [83466be+05e40dd] **P5.6 (AGG4-57 / DES4-4, Low)** Route ReviewActionButton scrim through
  HUD_TEXT_SCRIM_ALPHA + pin in HudContrastTest.
- ☑ [83466be] **P5.7 (AGG4-15 / TEST4-11, Medium; carried P5.9)** `videoResolutionLabel`: label 4:3 by
  real HEIGHT; add `VideoResolutionLabelTest` FIRST (TDD), then fix.
- ☑ [61a6bb7+83466be] **P5.8 (QA4-2 + QA4-4, Low/Info)** Remove the redundant `!!` (VideoRecorder.kt:255); add
  the missing why-comment on MainActivity's RestrictedApi suppress.

## P6 — Performance wave

- ☑ [8617723] **P6.1 (AGG4-10 / PERF4-1+2+8, High)** Compose stability: `@Immutable` on CameraUiState /
  HistogramData / WaveformData; stability-config file for Size/Range/Uri/CameraCaps;
  `remember(...)` the un-memoized projections (StatusInfoPill compute, ExposureMeter meter/
  format, the 4 ProSheet tab `controlAvailability` calls). The telemetry StateFlow SPLIT stays
  deferred (see Deferrals).
- ☑ [8617723] **P6.2 (AGG4-34 / PERF4-3, Low)** Move `result.get(COLOR_CORRECTION_GAINS)` inside the
  `pendingCustomWbSample?.let`.
- ☑ [8617723] **P6.3 (AGG4-38 / PERF4-9, Low)** infoTicker StatFs/Battery reads off main (io executor →
  post result).
- ☑ [8617723] **P6.4 (AGG4-37 / PERF4-7, Low)** Gate the analysis→main AE post to app-side-AE modes.
- ☑ [8617723] **P6.5 (AGG4-36 / PERF4-6, Low)** Route the three ViewModel raw `Thread{}` MediaStore ops
  through one shared io executor (keep the documented onCleared release thread).
- ☑ [8617723] **P6.6 (AGG4-12 / PERF4-5, Medium; carried P6.6)** HEIF+JPEG: one decode→crop→rotate pass,
  two encodes; recycle once; keep JPEG EXIF re-stamp. Device-verify both outputs still correct.
- ☑ [8617723] **P6.7 (AGG4-35 / PERF4-4, Low)** Draw-loop allocation trims where mechanical (coverScale
  out-params/caching); the finderRect/analysisFrame remainder may be deferred with evidence
  criteria if the churn risk exceeds the win.

## P7 — Tests wave

- ☑ [05e40dd] **P7.1 (AGG4-40 / TEST4-1)** Normalization matrix: below-min coerce-up, inverted range
  passthrough, one-sided-null passthrough.
- ☑ [05e40dd] **P7.2 (AGG4-41 / TEST4-2)** programAppSide→HAL-AE flash-flip case with advertised
  AUTO/ALWAYS flash AE modes.
- ☑ [05e40dd] **P7.3 (AGG4-42 / TEST4-5, High; carried P4.7)** Start↔stop same-tick delete-not-publish
  host test over RecordingAdmissionLatch + a stubbed publish/delete seam.
- ☑ [05e40dd] **P7.4 (AGG4-43 / TEST4-9, High)** `QuickFnEnabledTest`: REC-safe slots stay enabled;
  STABILIZATION/AUDIO_SCENE/TRANSFER/OPEN_GATE gates pinned.
- ☑ [05e40dd] **P7.5 (AGG4-44 / TEST4-17, Medium)** Extract `clampStillExposureRange(...)` from
  CameraCaps.read() + pin (the 4 s seam gets its own host test).
- ☑ [05e40dd] **P7.6 (AGG4-45 / TEST4-19, Medium)** EncoderCaps: extract `looksHardwareAccelerated` +
  `pickBestEncoder` tie-break; tests.
- ☑ [05e40dd] **P7.7 (AGG4-46 / TEST4-6+7, Low)** ZoomMath: non-TC effectiveZoomBounds branch + the
  crossingMark re-snap path.
- ☑ [05e40dd] **P7.8 (AGG4-47 / TEST4-10, Medium)** HudContrastTest references CameraColors, not literal
  copies.
- ☑ [05e40dd] **P7.9 (AGG4-14 / TEST4-15+DOC4-10; carried P7.1)** Tighten `correction_isClampedToOneStop`
  to the real 0.30-stop bound + rename.
- ☑ [05e40dd] **P7.10 (AGG4-48 / TEST4-12/13/14/20, Low)** formatFocusDistance tests; `internal`-ize +
  test waveformAlphaBucket and isUrgentStatus; CaptureFamilyKey boundary tests.
- ☑ [05e40dd] **P7.11 (AGG4-49 / TEST4-3, High→doc)** Correct the cycle-3 plan/doc language: the door
  wiring is pinned by the pure ZoomGlideState machine + review invariant, NOT by executed
  ViewModel wiring tests (none can exist today). The DI/Robolectric seam is deferred.

## P8 — Docs wave

- ☑ [9c6884f] **P8.1 (AGG4-50 / DOC4-2)** CLAUDE.md: `CAPTURE_WATCHDOG_MS` → `CAPTURE_WATCHDOG_FLOOR_MS`.
- ☑ [9c6884f] **P8.2 (AGG4-51 / DOC4-3+5+6+ARCH4-8)** ARCHITECTURE.md module map: add ZoomGlideState.kt;
  replace zoomPendingRatio/zoomEaseTarget with the ZoomGlideState symbols; "(app root)" section
  for the 4 root-package files.
- ☑ [9c6884f] **P8.3 (AGG4-52 / DOC4-4+12+13)** ARCHITECTURE.md: document the 500 ms/4 s exposure-safety
  design, degradeAudioToVideoOnly/shouldStartMuxer publish guarantee, EGL-init containment +
  start() idempotency.
- ☑ [9c6884f] **P8.4 (AGG4-53 / DOC4-7+8+9)** BACKLOG.md sync: header date, D-17 closed (aa6cab9),
  "Verified 2026-07-18" cycle-3 section, QA-3/QA-4 residual checks, cycle-2(07-17) deferral
  fold.
- ☑ [9c6884f] **P8.5 (AGG4-54 / DOC4-11; carried P7.2)** Replace the three `sensorOnlyControlsDelta`-only
  citations with `sensorFastPathAdmitted` + AF-carry note.
- ☑ [9c6884f] **P8.6 (AGG4-39 / VER4-2; carried P7.5)** CLAUDE.md + ARCHITECTURE.md mic bullets: mid-REC
  audio failure degrades to a PUBLISHED video-only file; only video faults delete.
- ☑ [9c6884f] **P8.7 (AGG4-25 / CRIT4-4)** Note that S/ISO/M preview above 500 ms is brightness-accurate
  but not noise/blur-WYSIWYG (docs only).

## P9 — Structural start (AGG4-5; runway-conditional order)

- ☑ [314365b] **P9.1** Extraction step 1: `StillCapturePipeline` (saveHeif/saveJpeg/saveDng/EXIF/rotate/
  crop/shotSpec/photoCallback, ~400 lines, monitor-free) out of CameraEngine, proving the
  pattern with zero ownership risk. Steps 2-7 recorded as the standing order (see Deferrals).

## P10 — Gates, deploy, device verification (mandated)

- ☑ **P10.1** Full gates: `:app:assembleDebug :app:testDebugUnitTest :app:lintDebug` green on
  the whole repo after all landings.
- ☑ **P10.2** DEPLOY_CMD (adb -s 127.0.0.1:5599 install -r + launch).
- ☑ **P10.3** On-device functional verification: wake, real preview (screencap statistics),
  zoom slider in/out, TC toggle, tap-to-focus + ISO change (focus must hold — NOW also past
  2 s per P1.3), photo↔video flips error-free, still capture + file pull, LONG-EXPOSURE still
  at ruler max 4.0 s captured successfully, logcat scan for FATAL/ANR/CAMERA_ERROR/
  CAMERA_DISCONNECTED. Plus this cycle's targeted checks: P1.4 frame-extraction A/B, pinch-out
  edge feel (P2.1), magnified tap (P2.6), scopes under LOG (P3.1), finder PIP gate (P3.4),
  level overlay feel (P4.3), HEIF+JPEG pair correctness (P6.6).

## Deferrals (strict rules: severity/confidence preserved; nothing silently dropped)

- **AGG4-5 steps 2-7 (High, High conf — ARCH4-3/AGG2-36/AGG3-17)** CameraEngine extraction
  beyond step 1: RendererAssists, StandbyAudioController, ControlsStore (god-lock split),
  ZoomController (resolves AGG3-19 + the AGG4-2 flag family), RecordingCoordinator, optics
  core. Reason: multi-cycle refactor; step 1 lands this cycle to honor the escalation and
  prove the pattern. Exit criterion: cycle 5 begins with steps 2-3; the standing order is
  ARCH4-3's list and MUST NOT be reordered to start at the optics core. Repo-policy-bound.
- **AGG4-10(a) telemetry StateFlow split (High, High conf — PERF4-1; AGG2-12 lineage)**
  Reason: P6.1's stability annotations land the cheap 80%; the split is a UI-architecture
  change. Exit criterion: Layout-Inspector recomposition trace after P6.1; split if
  whole-tree recomposition persists.
- **TEST4-3 seam (High, High conf)** No host test can construct CameraViewModel (live
  CameraEngine in field init). Deferred structural choice: Robolectric dependency or a VM
  engine-seam. Exit criterion: next cycle that adds a remap door, or adoption decision.
  P7.11 lands the honest doc language now.
- **CRIT4-3 logical-camera long-exposure bisect (Medium, High conf)** The 4 s ceiling is
  tele-verified only; P4.2 documents the conservative global application. Exit criterion: a
  dedicated device bisect of the logical camera's still-exposure ceiling (BACKLOG residual).
  Deferral is safe because the shipped behavior is strictly conservative (no crash risk, only
  a possible capability under-grant on non-tele lenses).
- **AGG4-35 remainder (Low, Medium conf — PERF4-4)** finderRect/analysisFrame per-event
  allocations (small data classes, PIP-gated / every-5th-frame). Reason: hot-path churn risk
  exceeds the win beyond the coverScale de-boxing. Exit criterion: allocation-tracker evidence
  during REC showing these entries matter.
- **DES-9 RTL (Low-Medium, Medium conf — AGG3-37)** Carried unchanged: the single absolute
  anchor is deliberately correct; a full-mirror policy is a design decision. Exit criterion:
  RTL-locale device pass or explicit LTR-pin decision.
- **AGG2-21 reduced motion (Low)** Carried unchanged. Exit criterion: accessibility session /
  owner decision.
- **AGG2-24 Surface.release vs GL detach race (Low)** Carried unchanged; release remains
  gated behind the checked detach. Exit criterion: field evidence of Preview EGL failure
  during rapid detach/reattach.
- **SEC-03 OSV/dependency audit (Low)** Zero dependency changes again this cycle. Exit
  criterion: osv-scanner at the next Play-release gate.
- **SEC4-1 OPPO Maven token hygiene (Low, High conf)** Accepted cycle 3 (public read-only
  vendor credential, never ships in the APK). Optional exit: move to user gradle.properties/
  env var at the next build-file touch.
- **VER U1-U6 + QA-3/QA-4 (informational, needs-device/human)** Device-truth residuals
  (orientationHint sign in external players, HLG/O-Log2 display look, 0x80b4 OIS shake A/B,
  vendor-log inertness re-check, held-pose saved-file uprightness, P-mode brightness judgment,
  EIS warp-band re-confirmation). To be recorded in BACKLOG residual field checks (P8.4).
- **QA4-3 manifest-merger no-op warnings (Info, intentional)** The tools:node="remove"
  guards are the documented privacy defense; the warning is their expected no-op state. Not
  actionable without weakening the guard.

## Coverage map (every AGG4 entry disposed exactly once)

1→P2.1 · 2→P2.2 · 3→P1.3 · 4→P1.4 · 5→P9.1+deferred(2-7) · 6→P4.1 · 7→P1.2 · 8→P1.1 ·
9→P3.1 · 10→P6.1+deferred(split) · 11→P2.6 · 12→P6.6 · 13→P5.4 · 14→P7.9 · 15→P5.7 ·
16→P5.5 · 17→P5.3 · 18→P3.5 · 19→P4.6 · 20→P3.6 · 21→P4.5 · 22→P4.4 · 23→P4.8 ·
24→P4.2+deferred(bisect) · 25→P8.7 · 26→P4.7 · 27→P4.3 · 28→P2.4 · 29→P3.4 · 30→P2.3 ·
31→P2.5 · 32→P3.2 · 33→P3.3 · 34→P6.2 · 35→P6.7+deferred(remainder) · 36→P6.5 · 37→P6.4 ·
38→P6.3 · 39→P8.6 · 40→P7.1 · 41→P7.2 · 42→P7.3 · 43→P7.4 · 44→P7.5 · 45→P7.6 · 46→P7.7 ·
47→P7.8 · 48→P7.10 · 49→P7.11+deferred(seam) · 50→P8.1 · 51→P8.2 · 52→P8.3 · 53→P8.4 ·
54→P8.5 · 55→P5.1 · 56→P5.2 · 57→P5.6. QA4-2/4→P5.8 · SEC4-1→deferred · QA4-3→deferred(info).

## Plan archiving

Per repo convention (leave-in-place): completed dated plans under docs/plans/ are the durable
archive and are never moved or deleted. The cycle-3 plan (2026-07-18-rpf-cycle3.md) is now
fully disposed: its landed waves stand as its record, and its "Carried to cycle 4" section is
superseded by THIS plan's dispositions (every carried item re-appears above by its AGG4/P
number). No plan file moves.

## Cycle-4 outcome record (2026-07-18, written at cycle close)

All P1-P9 items landed (per-item commit refs above; P1.4c's `aspectCropRect` unification is the
one recorded partial — see Deferrals note below). Gates green at close: assembleDebug,
615 unit tests 0 failed (+53 this cycle), lintDebug "No issues found". Deploy: per-cycle install
+ launch on PMA110 (127.0.0.1:5599) succeeded.

Device verification (P10.3): real preview via screencap statistics; zoom slider 3.0x→15.0x→1.9x;
TC toggle 70mm↔300mm round-trip; tap-AF + ISO drag with the lens distance held at ∞+10 across the
drag and past the old 2 s release point (P1.3); photo↔video flips clean both ways with a 4.0 s
manual shutter set; still capture producing a valid HEIF+JPEG pair through the new single-decode
path (JPEG 3064×4080, EXIF Make/Model/orientation verified); the LONG-EXPOSURE still at the ruler
max 4.0 s captured successfully (EXIF ExposureTime 4.0, ISO 9100, correctly bright frame, zero
CAMERA_ERROR); final logcat scan clean (no FATAL / CAMERA_ERROR / CAMERA_DISCONNECTED / ANR).

Two things this cycle DISPROVED or CAUGHT on device:
- ARCH4-1's recorded-framing defect was REAL (not just static math): the pre-fix clip carried a
  ~3.16× center band (cell-map corr ≤0.29 vs preview); post-fix 2160×3840 portrait-buffer clips
  match the viewfinder field (corr 0.992). CoverScaleTest pins the contract.
- This cycle's own first P2.4 attempt (`by lazy` runnables) crashed launch on device (NPE on the
  null lazy delegate during construction-time invalidate) — caught by the mandated deploy step and
  replaced with declaration-before-init [1e5799d]. Deploy-and-verify earns its place.

Operational notes: the wireless-debugging heads-up banner intercepted top-strip UI taps twice
(landing the automation in system Settings); heads_up_notifications_enabled was set to 0 for the
verification window and RESTORED to 1 afterwards. adb dropped offline three times and recovered
with bounded `adb connect` retries (no adb server restarts).

Additional deferral recorded at close:
- **ARCH4-2 remainder (Medium, High conf)** `aspectCropRect` unification (still pixel crop vs
  Compose AspectMask opposite-ordering copies). Reason: cycle runway went to the confirmed
  encoder-framing defect and the mandated verification; the framing CONTRACT tests landed, the
  mask/crop unification did not. Exit criterion: cycle 5 P-wave item alongside the AGG4-5 step-2/3
  extractions; repo-policy-bound.

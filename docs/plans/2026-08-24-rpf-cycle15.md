# Review-plan-fix cycle 15 — implementation plan

Date: 2026-08-24
Source: `.context/reviews/_aggregate.md`, cycle 15
Status: complete; implementation, configured gates, signed commits, and pushes finished

## Repository rules applied

- Preserve PMA110 behavior byte-for-byte; deployment and physical-device actions are disabled.
- Security, correctness, release-integrity, liveness, and data-loss findings are mandatory and are
  not deferred.
- Preserve English/Korean parity, the Sony Alpha/Xperia Pro surface, exact coverage accounting,
  immutable build/evidence boundaries, and existing owner-approved backlog deferrals.
- Keep completed plans and cycle-14 reviews as non-destructive archives.
- Use fine-grained GPG-signed Conventional Commits with gitmoji, pull with rebase before every push,
  never force-push, and never bypass configured gates.

Every aggregate finding AGG15-01 through AGG15-09 is scheduled below. No finding is deferred,
severity-downgraded, or silently dropped.

## Phase 1 — close immutable-build input and output ownership

- [x] **P1 / AGG15-01:** descriptor-read and validate release-local inputs, resolve any relative
  keystore from frozen `keystore.properties`, include the copied properties/keystore and their
  replacement-relevant ancestors in the release seal, verify them through export, and add permanent
  plus A -> B -> A mutation tests without logging secrets.
  **Cycle-16 correction:** this covered a properties-named keystore but not the build script's
  `TELECAMPRO_STORE_FILE` fallback. Cycle 16 removed that second file-path resolver and made the
  properties-named repository-relative file the sole immutable signing owner.
- [x] **P2 / AGG15-02:** give both immutable wrappers one exact generated-output owner after Gradle:
  no-follow regular-file/type/identity/content validation, an explicit artifact/report allowlist,
  set-level coherence for release APK/AAB/metadata, descriptor-owned export to exclusive staging,
  terminal verification, and deterministic permanent/transient output mutation tests.

Acceptance at cycle 15: every source, local tool/signing input discovered from the properties file,
and exported artifact named by an immutable wrapper is owned continuously across the operation; no
mutable pathname reopen can change the published byte set. The broader all-signing-file-input claim
is superseded by the cycle-16 correction above.

## Phase 2 — make review-media work lifecycle- and thread-safe

- [x] **P3 / AGG15-03:** move video URI/player acquisition off the UI thread into an identity-owned,
  latest-wins setup lane; attach and publish only the current player on main, release stale owners,
  and force a blocked-provider interleaving that proves Back/disposal stays responsive.
- [x] **P4 / AGG15-04:** stop recycling a published Bitmap before Compose has retired its draw node.
  Keep exact disposal for unpublished stale results, use composition-safe retirement or GC ownership
  for displayed images, and cover replacement plus overlay removal with a real Bitmap/Compose test.

Acceptance: slow MediaProvider acquisition cannot block the main looper, stale video setup cannot
publish, and no drawable bitmap is recycled while Compose can still render it.

## Phase 3 — bound retained deletion and launch recovery

- [x] **P5 / AGG15-05:** give the durable discard journal a stable bounded cursor/batch independent
  of Images/Video pagination. Each recovery step must have a finite provider-operation ceiling and
  must not replay the full unresolved journal for every media page; test multi-page failed entries
  and eventual progress.
- [x] **P6 / AGG15-06:** replace the per-Engine teardown fallback with one process-lifetime finite
  discard dispatcher. Preserve durable tombstone truth on overflow or shutdown, isolate stale
  completion from newer Engines, and test a blocked provider through repeated Engine replacement.

Acceptance: process-lifetime worker/backlog counts and per-recovery-step work remain bounded even
when provider calls block or deletion markers persist across launches.

## Phase 4 — align evidence and UI documentation with shipping truth

- [x] **P7 / AGG15-07:** update `CLAUDE.md`, `docs/FIELD_CHECKS.md`, and device-runner defaults and
  diagnostics to build/capture/install only the wrapper-emitted immutable debug APK for evidence.
  Keep mutable Gradle APK commands developer-only, and extend documentation guards across every
  active verification authority.
- [x] **P8 / AGG15-08:** align Architecture and UX policy with exact resource labels `Shoot` and
  `Still Quality`; add a resource-backed documentation contract for the tab and key row labels.
- [x] **P9 / AGG15-09:** replace stale `CameraScreen` rail/reserved-column comments with the
  full-width one-layout rule and add a negative documentation/source guard against the deleted
  operator-rail guidance.

Acceptance: no active authority, runtime diagnostic, or adjacent source comment teaches mutable
evidence bytes, superseded labels, or the deleted two-rail layout.

## Phase 5 — gates, delivery, and evidence

- [x] Run `./gradlew :app:assembleDebug :app:testDebugUnitTest :app:lintDebug` serially across the
  repository.
- [x] Run release lint/assembly/bundle through `tools/build_immutable_release.py`; direct release
  Gradle tasks are expected to fail closed by repository policy.
- [x] Run `python3 tools/verify_host.py`, focused tool/device-harness/docs tests, Python compilation,
  exact Partition-A coverage verification, and diff/whitespace checks.
- [x] Root-fix every error and actionable warning without suppression; record any genuinely
  unfixable warning under the strict deferred format before committing.
- [x] Commit each independent fix with a GPG-signed Conventional Commit + gitmoji message, pull with
  rebase before every push, push, verify signatures/upstream synchronization, then close this plan.
  Do not deploy.

## Planned commit sequence

`docs(plan)`, `fix(provenance)`, `fix(review)`, `perf(review)`, `perf(storage)`, `docs(evidence)`,
`docs(ui)`, `test(coverage)`, and `docs(plan)`, split further wherever one commit would combine
unrelated behavior.

## Deferred findings

None. Existing owner-approved field checks and design deferrals in `docs/BACKLOG.md` are unchanged.

## Completion evidence

- The authoritative host gate passed at exact source commit `254d666`: **1,757**
  JVM/Robolectric/Compose tests, debug assembly/lint, **47** tool/release tests, **9** coverage-tool
  tests, **182** device-harness self-tests, **105** documentation checks, Python compilation, and
  the complete HEAD-to-working-tree whitespace check.
- Coverage is **54.56% overall (13,240/24,265)** and **99.80% Partition A (6,492/6,505)**. The exact
  approved **13-line / 9-class** residual remains unchanged. Nineteen newly reported gate misses
  were root-fixed: an unused merge was removed, reachable finite-dispatch seams were covered, and
  only the Android Context/Uri/Surface/MediaPlayer holder/result owners moved to Partition B with
  explicit framework-bound rationale; the generic latest-wins lane remains in A and tested.
- The configured direct release command was run and refused at `verifyCleanReleaseGit` exactly as
  designed. The authoritative immutable release gate then passed `:app:lintRelease`,
  `:app:assembleRelease`, and `:app:bundleRelease` from exact clean commit `254d666` / tree
  `c437e551`; signed outputs were exported under
  `app/build/immutable-release/cycle15-254d666/`.
- The first real exporter run found and fail-closed one legitimate AGP report allowlist omission;
  `d0f5bd4` added the exact `resources_config_map_file/release/` family and a regression assertion.
  The rerun and final-HEAD release gate both passed. This plus the 19 coverage issues totals
  **20 gate fixes**.
- Ten review/implementation/gate-fix commits through `254d666` were GPG-signed, pull-rebased,
  pushed, and synchronized with `origin/main`. This closeout is the eleventh signed/pushed commit.
  Deployment and device actions remained disabled.

## Residual warnings

- **Low / High — `gradle.properties:25-29`: AGP labels
  `android.experimental.reportAggregationSupport=true` experimental.** The repository deliberately
  enables the unified coverage/test report registration while retaining dependable per-leg tasks;
  removing it would discard configured coverage infrastructure, and adding AGP's suggested global
  suppression is forbidden by this run's no-suppression rule. Reopen when AGP stabilizes the option
  or the repository retires its unified report tasks.
- **Low / High — `app/build.gradle.kts:721`: release packaging cannot strip the transitive
  `libandroidx.graphics.path.so`.** This is an upstream prebuilt native library from the pinned
  Compose UI graphics dependency; AGP explicitly packages it unchanged and the signed release
  APK/AAB complete successfully. App source has no symbol owner to fix without replacing the
  dependency or masking the warning. Reopen when the upstream artifact ships a strip-compatible
  library or a stable dependency upgrade removes the warning.

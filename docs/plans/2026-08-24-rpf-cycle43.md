# Review-plan-fix cycle 43 — implementation plan

Date: 2026-08-24
Source: `.context/reviews/_aggregate.md`, cycle 43
Status: complete; all six findings fixed and every configured gate green

## Repository rules applied

- Preserve measured PMA110 behavior, capability-enumerated multi-device paths, exact Camera2/native
  ownership, durable media truth, the quiet Sony-style UI, EN/KO parity, and field-evidence limits.
- Schedule every one of the six aggregate findings below. Nothing is silently dropped, deferred,
  severity-downgraded, or relabeled as optional.
- Keep completed historical plans as non-destructive archives in `docs/plans/`; no prior plan is
  moved, deleted, or reopened by this cycle.
- Work only in `/tmp/find-x9-ultra-cycle43.q4Rs33`; never modify, stash, reset, check out, clean, or
  build from the shared dirty `main` checkout.
- Use fine-grained GPG-signed Conventional Commits with gitmoji, pull with rebase before every push,
  and do not deploy because `DEPLOY_MODE=none`.

## P1 — preserve microphone-permission continuation across recreation (AGG43-01)

- [x] Give the pending audio action and rationale/system-surface phase a saved Activity owner,
  restoring them before registered Activity Result delivery can consume the continuation.
- [x] Clear the saved owner only after grant or decline completes exactly one requested action;
  preserve video-only recording on a denied `START_RECORDING` request and avoid mutating audio for
  an unexpected ownerless result.
- [x] Add Activity-recreation tests for enable-audio and start-recording grant/denial continuations,
  including restored rationale ownership and exactly-once clearing.

## P2 — model zoom movement after an exact quiet landing (AGG43-02)

- [x] Record a suppressed moving tick after a landing as new unsettled framing so an inward or
  same-direction re-pinch inside the boost tail earns its own exact quiet landing.
- [x] Make a duplicate quiet callback with no intervening movement genuinely idempotent while
  preserving the distinct outward re-pinch/start-edge path and FPS-restoring end rebuilds.
- [x] Test quiet→inward movement→quiet→end, duplicate quiet, and outward re-pinch sequences; align
  source KDoc and `docs/ARCHITECTURE.md` with the executable ownership.

## P3 — route Korean users to Korean privacy content (AGG43-03)

- [x] Add a public Korean privacy section under the committed static site and select it for Korean
  locales while English and other locales retain the canonical English section.
- [x] Keep English, Korean, Markdown, and in-app fallback privacy claims synchronized through the
  documentation checker, including language metadata and canonical/alternate links.
- [x] Add locale-aware external-navigation tests proving exact English and Korean policy URLs.

## P4 — make primary-shutter keyboard focus visible (AGG43-04)

- [x] Observe keyboard/D-pad focus on the shutter's existing interaction source and draw a durable,
  high-contrast halo/keyline without adding a touch ripple or changing press/record/timer geometry.
- [x] Add rendered Compose coverage proving focused and unfocused shutters differ over bright and
  dark finder backgrounds, while disabled shutter remains non-focusable and Enter admission stays
  unchanged.

## P5 — test rendered disabled-command paint (AGG43-05)

- [x] Add stable Compose raster assertions that exercise the production `ImmediateActionChip`
  container, content, and border wiring for every active/enabled combination.
- [x] Retain the pure color-mapping assertions as mapping-only coverage and preserve click-only
  semantics, action admission, and compact geometry.

## P6 — unify installed and public TeleCam Pro branding (AGG43-06)

- [x] Carry the existing public telescope/barrel mark and black/blue palette into adaptive launcher
  foreground/background and themed monochrome variants while respecting Android safe-zone masks.
- [x] Correct stale README SVG accessible identity and barrel-index metadata; preserve the matching
  Play icon and feature-graphic mark rather than introducing another design.
- [x] Add asset-contract checks for the shared mark/palette/identity and validate vector/raster
  dimensions plus circle, squircle, rounded-square, and monochrome mask expectations from source.

## Quality gates and delivery

- [x] Run focused permission-recreation, zoom-transition, external-navigation, shutter-focus,
  command-paint, asset-contract, and documentation tests plus `git diff --check`.
- [x] Run `./gradlew :app:assembleDebug :app:testDebugUnitTest :app:lintDebug` against the whole repo.
- [x] Run `./gradlew :app:lintRelease :app:assembleRelease :app:bundleRelease` against the whole repo.
- [x] Root-fix every gate error and warning without suppression.
- [x] Commit and push each independent review, plan, implementation, test, documentation, and
  closeout change with signed Conventional Commits plus gitmoji, pulling with rebase before push.
- [x] Mark this plan complete only after exact evidence is delivered.

## Deferred findings

None. All six cycle-43 findings are scheduled for this cycle.

## Completion evidence

- All six deduplicated findings were implemented; nothing was deferred. A pending microphone
  action and its rationale/system phase now survive Activity recreation, and a permission result
  without an owner is inert rather than changing audio state. Saved-state round trips cover both
  actions and both UI phases; existing grant/decline policy tests remain green.
- Zoom interaction state now marks post-landing movement unsettled. Inward/same-direction re-pinch
  earns a second exact quiet landing without inventing a new start edge, duplicate quiet callbacks
  are inert, outward re-pinch retains its bounded edge submit, and architecture/tests describe the
  same lifecycle.
- The public privacy page now contains explicit English and Korean language sections with canonical,
  alternate, and in-page language links. Korean Android contexts open `#ko`; English and other
  locales retain the canonical section. Documentation checks bind both public languages and both
  in-app fallbacks to the same metadata, on-device library, and no-transmission claims.
- The primary shutter now observes real keyboard/D-pad focus and overlays a composed black-plus-
  Accent keyline over its unchanged Canvas geometry. Native-graphics tests prove focus ownership,
  changed rendered edge pixels, and a 3:1 contrast choice over bright and dark frames. Disabled
  shutters remain non-focusable and Enter activation remains exact.
- Immediate-command coverage now renders the production Surface in all four active/enabled states,
  proving container/content dimming and the receding disabled border rather than only testing the
  color helper. The pure mapping and click-only semantics tests remain intact.
- Adaptive foreground, background, and themed monochrome launcher assets now use the same
  black/blue telescope-barrel mark as Play, the feature graphic, and README. README SVG identity
  and barrel metadata are current; committed-export checks reject palette, silhouette, viewport,
  and accessible-name drift.
- Focused permission, zoom, navigation, shutter, command-paint, asset, tooling, and documentation
  checks passed. The configured debug gate passed with 2,037 JVM/Robolectric/Compose tests and
  Android lint green. The configured release lint, signed APK, and signed AAB gate passed from clean
  committed source using read-only links to existing gitignored signing inputs. The prebuilt native
  strip notice is expected packaging diagnostics, not an Android-lint warning.
- The authoritative `python3 tools/verify_host.py` passed: Partition A 8,071/8,086 lines (99.81%),
  109 tooling tests, nine coverage-tool tests, 184 device-harness self-tests, 139 documentation
  checks, Python compilation, Android assembly/test/lint, and `git diff --check` were all green.
- Nine fine-grained GPG-signed Conventional Commits with gitmoji were pushed after pull-with-rebase
  before this closeout; this completion record is the tenth. Deployment was not run
  (`DEPLOY_MODE=none`), and no device action was taken.
- Work remained in `/tmp/find-x9-ultra-cycle43.q4Rs33`. The shared dirty `main` checkout was never
  edited, staged, stashed, reset, checked out, cleaned, or used for a build; its pre-existing
  CameraEngine, storage, UI, test, tooling, and review deltas remain untouched.

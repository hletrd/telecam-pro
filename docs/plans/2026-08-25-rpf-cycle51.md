# Review-plan-fix cycle 51 — implementation plan

Date: 2026-08-25
Source: `.context/reviews/_aggregate.md`, cycle 51
Status: complete; all nine findings fixed and every configured quality gate green

## Repository rules applied

- Preserve measured PMA110 Camera2 behavior, capability-enumerated generic-device paths, exact
  lifecycle/native ownership, durable media truth, the quiet Sony-style viewfinder, EN/KO parity,
  and explicit device-evidence limits.
- Schedule every one of the nine aggregate findings below. Nothing is silently dropped,
  severity-downgraded, deferred, relabeled as optional, or treated as fixed by documentation alone.
- Keep completed historical plans in `docs/plans/` as the non-destructive archive. Every earlier
  plan is already marked complete; none needs moving, deletion, rewriting, or reopening.
- Work only in `/tmp/find-x9-ultra-cycle51.WTu2dW`; never modify, stash, reset, check out, clean, or
  build from the shared dirty `main` checkout.
- Use fine-grained GPG-signed Conventional Commits with gitmoji, pull with rebase before every push,
  and do not deploy because `DEPLOY_MODE=none`.

## P1 — make renderer initialization fail on non-throwing GLES errors (AGG51-01, Medium/High)

- [x] Add an operation-scoped GLES error boundary that clears stale error state before
  initialization and checks each mutating VBO/texture operation before local ids transfer.
- [x] Route `glBufferData`, texture bind, and texture-parameter failures through the existing exact
  partial-owner cleanup; preserve successful shader/program ownership and same-context retry.
- [x] Add injectable error-source coverage for every post-allocation call, exact unbind/delete, zero
  field transfer, and successful retry after one fault.

## P2 — remove review-source heap amplification (AGG51-02, Medium/High)

- [x] Replace the `ByteArrayOutputStream`/`toByteArray` snapshot with one bounded private seekable
  spool owner or equivalently stable descriptor whose compressed bytes do not live twice in heap.
- [x] Give thumbnail and full-review work explicit compressed-source admission budgets across the
  shared process pool, with cleanup on success, timeout, retirement, replacement, and decode error.
- [x] Add large-source and overlapping-retired-request tests that prove aggregate byte admission,
  bounded allocation behavior, exact cleanup, and one immutable source for bounds/pixels/EXIF.

## P3 — carry the complete REC packet through native setup (AGG51-03, Medium/High)

- [x] Extend `RecordingAdmissionSnapshot` with exact accepted session, size, selected frame/capture
  rate, codec, accepted transfer, and ordered candidates; do not re-read independently live fields.
- [x] Derive GL curve and recorder/file configuration from the same frozen transfer, with an explicit
  linearization policy for same-source-precision edits after admission.
- [x] Drive production setup through injectable recorder/GL boundaries while forcing an HLG↔S-Log3
  interleave; assert pixels, format tags, candidate, size, and FPS are one complete packet.

## P4 — propagate the winning rollback packet to GL (AGG51-04, Medium/High)

- [x] Replace the baseline `before.transfer` post with the selected
  `restoredVideoPipeline.activeTransfer` or an equivalent complete-packet publication.
- [x] Add a newer-before-rollback Video-mode interleave with disjoint same-precision and AVC/SDR
  packets, an observable active GL sink, and full Engine/UI/persistence/REC assertions.
- [x] Prove a superseded rollback cannot post a later stale GL command and that a future GL
  generation replays the same winner.

## P5 — make review sample-size arithmetic overflow-safe (AGG51-05, Low/Medium)

- [x] Compute ceiling comparison/division using `Long` or a quotient/remainder formulation with no
  signed overflow for any positive `Int` width, height, and maximum dimension.
- [x] Preserve invalid-bound refusal and the post-decode size/recycle defense.
- [x] Add `Int.MAX_VALUE`, near-boundary, portrait/landscape, and ordinary 6001 px property cases.

## P6 — enforce PNG `PLTE`/`tRNS` relative order (AGG51-06, Low/High)

- [x] Track transparency state and reject any later palette while retaining legal optional truecolor
  palettes, existing multiplicity/length checks, and bounded error reporting.
- [x] Add a CRC-correct committed-export mutation ordered
  `IHDR,tRNS,PLTE,IDAT,IEND` and assert clean failure without traceback.
- [x] Re-run the complete PNG grammar/order mutations to ensure no legal repository asset regresses.

## P7 — identity-gate delayed AppOps camera-policy classification (AGG51-07, Low/Medium)

- [x] Freeze the failed controller plus failure/session publication identity before the Binder
  query; after it returns, re-enter the Engine monitor and install policy truth only if that owner is
  still current and no replacement Ready has committed.
- [x] Give delayed true/false policy callbacks a monotonic terminal identity and preserve the
  post-monitor callback rule from cycle 50.
- [x] Add a deterministic old-query → replacement-Ready → old-return → unrelated-exhaustion test
  proving stale policy state and UI cannot reappear.

## P8 — remove the family-deletion gate race (AGG51-08, Medium/High)

- [x] Change the integration test to observe the executor wrapper's post-`finally` reservation
  release through a bounded eventual assertion or exact test terminal.
- [x] Keep production capacity accounting unchanged and assert both task completion and permit
  restoration without unbounded sleeps or timing assumptions.
- [x] Stress the focused test repeatedly and run it inside the full JVM suite to prove reproducible
  gate behavior.

## P9 — align Loupe and pipeline ownership documentation (AGG51-09, Low/High)

- [x] Rewrite `FlipRenderer`/`GlPipeline` comments so today's same converter-fed overview is plainly
  raw/inverted; reserve upright/pre-converter-world language for a future true wide stream.
- [x] Update `CLAUDE.md` and `docs/ARCHITECTURE.md` to describe conditional baseline restoration,
  independent pipeline-publication ownership, and the complete REC packet carried to native setup.
- [x] Extend `tools/check_docs.py`/contract tests to reject stale upright-world and unconditional
  baseline-restoration claims while preserving the field ledger's exact exception wording.

## Quality gates and delivery

- [x] Run focused tests for GLES error-state cleanup, review-spool allocation/ownership, immutable REC
  setup, rollback-to-GL ownership, sample arithmetic, PNG order, AppOps ownership, family-deletion
  capacity, and documentation invariants, plus `git diff --check`.
- [x] Run `./gradlew :app:assembleDebug :app:testDebugUnitTest :app:lintDebug` across the whole repo.
- [x] Run `./gradlew :app:lintRelease :app:assembleRelease :app:bundleRelease` across the whole repo.
- [x] Run the authoritative supporting host gate `python3 tools/verify_host.py` because repository
  rules require it for delivery even though the orchestrator's configured gates are the two Gradle
  groups above.
- [x] Root-fix every gate error and warning without suppression; record any authorized unavoidable
  warning under the strict deferred-finding rules.
- [x] Commit and push each independent review, plan, implementation, test, documentation, and
  closeout change with signed Conventional Commits plus gitmoji, pulling with rebase before push.
- [x] Mark this plan complete only after exact evidence is delivered.

## Deferred findings

None. All nine cycle-51 findings were implemented in this cycle.

## Completion evidence

- All nine deduplicated findings were implemented; none was deferred. Renderer initialization now
  treats every non-throwing VBO/texture GLES error as a failed transaction, cleans exact local
  owners, and permits a same-object retry. The concrete android.jar `GLES20` bridge is explicitly
  framework-bound Partition B, while all injected ownership/error policy remains host-tested.
- Review still decoding now consumes one private read-only cache spool for bounds, pixels, and EXIF,
  holds a process-wide 128 MiB actual-byte admission budget, deletes partial/superseded spools
  exactly, and computes sampling without signed overflow through `Int.MAX_VALUE`.
- REC carries one immutable accepted session/size/FPS/capture-rate/codec/transfer/candidate packet
  through native setup. GL and file configuration derive from the same transfer; rollback posts the
  actual winning pipeline packet; delayed AppOps results and their ViewModel callbacks are rejected
  after a newer controller/session or policy terminal.
- PNG validation rejects a palette after admitted transparency, the family-deletion integration
  test waits for the executor's post-task capacity release, and Loupe plus pipeline/REC authority is
  consistent across source comments, `CLAUDE.md`, architecture, and mutation-sensitive docs checks.
- Exact debug gate passed on the final implementation: debug APK assembly, the full
  JVM/Robolectric/Compose suite, and debug lint. The authoritative host gate additionally passed
  androidTest assembly and exact Partition-A coverage at 99.82% with the pre-existing reviewed
  15-line residual manifest unchanged.
- Exact release gate passed on the final HEAD: release lint, R8 APK, and signed AAB. The isolated
  clone used ignored read-only links to the maintainer's existing signing inputs; no credential was
  printed, copied into source, or committed.
- `python3 tools/verify_host.py` passed with 133 tooling tests, nine coverage-tool tests, 195 device-
  harness self-tests, 154 documentation checks (24 optional-private skips), Python compilation, and
  `git diff --check`.
- Three error-level coverage-gate residual classes were root-closed (`GATE_FIXES=3`): the
  non-clearing GLES error edge and zero-length provider-read fallback gained focused coverage, and
  only the concrete Android GLES bridge gained the repository-authorized framework-bound
  classification.
- Eight fine-grained GPG-signed Conventional Commits with gitmoji were pushed before this closeout;
  this completion record is the ninth cycle commit. Deployment and device action were not run
  (`DEPLOY_MODE=none`).
- All work ran in `/tmp/find-x9-ultra-cycle51.WTu2dW`. The shared dirty `main` checkout was never
  edited, staged, stashed, reset, checked out, cleaned, or used for a build; its pending uncommitted
  delta remains intact.

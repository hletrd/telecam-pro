# Review-plan-fix cycle 48 — implementation plan

Date: 2026-08-25
Source: `.context/reviews/_aggregate.md`, cycle 48
Status: complete; all eleven findings fixed and every configured quality gate green

## Repository rules applied

- Preserve measured PMA110 Camera2 behavior, capability-enumerated generic-device paths, exact
  lifecycle/native ownership, durable media truth, the quiet Sony-style viewfinder, EN/KO parity,
  and explicit device-evidence limits.
- Schedule every one of the eleven aggregate findings below. Nothing is silently dropped,
  severity-downgraded, or relabeled as optional.
- Keep completed historical plans in `docs/plans/` as the repository's non-destructive archive; no
  completed plan is deleted, moved, rewritten, or reopened. Dated evidence corrections may be
  appended where a completed plan overstated coverage.
- Work only in `/tmp/find-x9-ultra-cycle48.Gvbytf`; never modify, stash, reset, check out, clean, or
  build from the shared dirty `main` checkout.
- Use fine-grained GPG-signed Conventional Commits with gitmoji, pull with rebase before every push,
  and do not deploy because `DEPLOY_MODE=none`.

## P1 — own video selection and rollback as one transaction (AGG48-01)

- [x] Introduce one immutable Engine-owned video-pipeline selection containing the normalized codec,
  exact ordered encoder candidates, requested next-Video transfer, and active Camera2/GL transfer.
- [x] Replace the split `setVideoEncoders` → nested `setTransfer` path with one generation-owned
  command that decides reconfiguration once and snapshots/restores the complete old tuple.
- [x] Publish the complete accepted rollback result to the ViewModel before persistence; preserve
  Photo's deliberate next-Video profile while keeping active Photo session transfer SDR.
- [x] Bind Ready/REC admission to an encoder tuple compatible with accepted source-transfer truth.
- [x] Add forced-failure tests through public ViewModel SDR↔HLG and HEVC/HLG→AVC/SDR paths;
  assert UI/Engine restoration and subsequent REC candidate admission, while settings/MR installs
  the same packet inside its existing generation/supersession test boundary.

## P2 — bound capture-family settlement diagnostics (AGG48-02)

- [x] Add an explicit immutable settlement-trace admission passed into `photoCallback`; keep ordinary
  Single and the exact in-REC one-shot harness consumer while suppressing TIMELAPSE/BURST/AEB ticks.
- [x] Build family stem/output trace strings only if registration or settlement logging is admitted.
- [x] Add host tests for every drive/recording-snapshot combination and the terminal evidence each
  device-harness consumer requires.

## P3 — validate production shader bindings (AGG48-03)

- [x] Establish one production authority for every required attribute/uniform name consumed by both
  `FlipRenderer` and the host validator, including interpolated shader declarations.
- [x] Treat every required `glGet*Location == -1` as GL initialization/preview-health failure rather
  than silently continuing.
- [x] Mutation-test typo, removal, and extra production-side lookups in addition to shader syntax and
  stage-interface failures.

## P4 — fully validate screenshot PNGs (AGG48-04)

- [x] Replace the header-only readiness predicate with deterministic full PNG decode/verification,
  preserving exact phone/tablet dimensions and permitted color modes.
- [x] Add updated-digest mutations for truncated IDAT, bad CRC, missing IEND, and invalid compression,
  filter, or interlace fields so manifest refresh cannot bless an unreadable asset.

## P5 — recognize exact package-private release permissions (AGG48-05)

- [x] Split the five user-facing privacy/runtime permissions from exact package-internal security
  guards in the permission authority.
- [x] Require `${applicationId}.DYNAMIC_RECEIVER_NOT_EXPORTED_PERMISSION` in the packaged set and
  prove its matching declaration is signature-protected; do not generally ignore same-package names.
- [x] Add real-like merged-manifest/bundletool fixtures covering expected guard, missing declaration,
  wrong protection level, extra internal permission, and existing network-permission rejection.

## P6 — deliver synthetic cancel through full obscuration (AGG48-06)

- [x] Reconstruct `ACTION_CANCEL` with original timing/source/device/pointer properties and
  coordinates while clearing only the two obscuration flags before DecorView dispatch.
- [x] Retain rejection of the hostile original event and the tainted remainder until a fresh clean
  DOWN.
- [x] Test full and partial mid-stream transitions, exactly one child cancel, zero action, tail
  rejection, and normal next-gesture recovery.

## P7 — restore modal focus to the exact opener (AGG48-07)

- [x] Give Menu, Fn, and gallery exact focus owners and restore the appropriate origin only after the
  modal leaves composition and finder focus is re-enabled.
- [x] Preserve modal entry focus/background exclusion and cover X, scrim, Back, review close,
  delete-dialog cancel, and My Menu WB dismiss/reopen transitions in production Compose tests.

## P8 — expose viewfinder actions to keyboard/D-pad users (AGG48-08)

- [x] Make the viewfinder a deliberate focus target when center-focus/reset is admitted and map
  Enter/Space/DPAD-center plus a documented reset action without changing touch/TalkBack behavior.
- [x] Add a visible focus indication meeting contrast requirements over bright and dark content.
- [x] Extend production tests to request focus, exercise both actions and disabled/loading states,
  and prove ordinary finder traversal remains stable.

## P9 — correct the Loupe authority (AGG48-09)

- [x] Rewrite the `CLAUDE.md` heading/rationale to lead with per-draw afocal-term omission and state
  immediately that today's converter-fed same-stream inset remains inverted.
- [x] Extend the docs gate to reject the unqualified current-upright claim while preserving the
  future true-wide-stream distinction.

## P10 — cover the video-ready thumbnail branch (AGG48-10)

- [x] Add `Ready(VIDEO, bitmap)` paint, media-specific action, localized state-description, bitmap
  pixel, and disabled-state coverage to the production gallery matrix.
- [x] Append a dated correction to the completed cycle-47 plan explaining the added missing branch;
  do not rewrite its historical completion record.

## P11 — close real Compose obscured-gesture evidence (AGG48-11)

- [x] Drive production Activity+Compose viewfinder tap, two-pointer pinch, and ruler drag through
  partial/full obscuration transitions, asserting exact terminal cleanup and a successful next clean
  gesture.
- [x] Append a dated correction to the completed cycle-47 plan distinguishing its earlier generic
  View dispatch proof from the new production Compose evidence.

## Quality gates and delivery

- [x] Run focused tests for every transaction, logging, shader, PNG, release-permission, input,
  focus, keyboard, documentation, and accessibility seam plus `git diff --check`.
- [x] Run `./gradlew :app:assembleDebug :app:testDebugUnitTest :app:lintDebug` across the whole repo.
- [x] Run `./gradlew :app:lintRelease :app:assembleRelease :app:bundleRelease` across the whole repo.
- [x] Root-fix every gate error and warning without suppression; record any authorized unavoidable
  warning under the same deferred-finding rules.
- [x] Commit and push each independent review, plan, implementation, test, documentation, and
  closeout change with signed Conventional Commits plus gitmoji, pulling with rebase before push.
- [x] Mark this plan complete only after exact evidence is delivered.

## Deferred findings

None. All eleven cycle-48 findings are scheduled for this cycle.

## Completion evidence

- All eleven deduplicated findings were implemented; none was deferred. Video codec, exact ordered
  candidates, retained next-Video transfer, and active Camera2/GL transfer now share one immutable
  transaction snapshot and one live command. Forced VM rollback tests cover SDR↔HLG and
  HEVC/HLG→AVC/SDR, including UI truth and subsequent REC candidate admission.
- Capture-family diagnostics are finite: ordinary Single retains both harness edges, in-REC snapshot
  retains settlement only, and every sequence drive emits neither. Trace strings are lazy.
- Shipping GLSL declarations/lookups share one production binding authority, every negative required
  location fails initialization, and production-side lookup mutations are host-tested.
- Screenshot readiness now verifies every PNG chunk/CRC, complete bounded zlib payload, scanline
  filters, IEND, dimensions, and mode. Updated-digest truncation, CRC, IEND, and IHDR mutations fail.
- Release verification distinguishes the five privacy permissions from the exact package-private
  AndroidX receiver guard and requires its signature-protected declaration. All 127 tooling tests pass.
- Full and partial obscuration deliver one clean child cancel, reject the tainted tail, and recover on
  the next gesture. Real Activity+Compose tap/pinch/ruler sequences pass; they exposed and fixed the
  cancel-to-tap action leak that a generic View could not reveal.
- Settings, Fn, and Gallery restore keyboard focus to their exact opener after X/scrim/Back/review
  close. The viewfinder is keyboard/D-pad focusable only when its center/reset actions are admitted,
  handles Enter/Space/center/Delete/Backspace, and paints a two-tone focus boundary.
- Loupe authority now leads with current same-stream inversion and the docs gate rejects the stale
  unqualified upright claim. Gallery coverage now includes ready-video pixels, media action/state,
  disabled behavior, and EN/KO resources; cycle-47 evidence boundaries carry dated corrections.
- Configured debug gate passed: debug APK assembly, all JVM/Robolectric/Compose tests, and debug lint.
  Configured release gate passed: release lint, R8 APK, and signed AAB after linking the existing
  ignored local signing inputs read-only into this isolated clone.
- Full non-device supporting evidence passed: 127 tooling tests, 195 device-harness self-tests,
  151 documentation checks with 24 optional-private skips, and `git diff --check`.
- Twelve fine-grained GPG-signed Conventional Commits with gitmoji were pushed before this closeout.
  Deployment was not run (`DEPLOY_MODE=none`), and no device action was taken.
- All work ran in `/tmp/find-x9-ultra-cycle48.Gvbytf`. The shared dirty `main` checkout was never
  edited, staged, stashed, reset, checked out, cleaned, or used for a build; its uncommitted delta
  remains intact.

# Review-plan-fix cycle 29 — implementation plan

Date: 2026-08-24
Source: `.context/reviews/_aggregate.md`, cycle 29
Status: complete; all four findings fixed, configured gates green, signed commits pushed

## Repository rules applied

- Preserve measured PMA110 behavior, capability-routed Android 13+ behavior, EN/KO presentation,
  quiet-camera UX, media durability, and wrapper-owned release evidence.
- Every cycle-29 finding is scheduled below. Nothing is deferred, silently dropped,
  severity-downgraded, or introduced as new scope under a deferred label.
- Completed plans remain non-destructive archives in `docs/plans/`; no completed plan requires a
  move or deletion.
- Use GPG-signed Conventional Commits with gitmoji, pull with rebase before every push, and do not
  deploy because `DEPLOY_MODE=none`.
- Perform all work in the clean detached cycle clone. Preserve the shared main worktree and every
  pre-existing CameraEngine/storage/UI/test/tool delta without touching, staging, stashing,
  resetting, or restoring it.

## P1 — make partial-delete retry copy ownership-safe (AGG29-01)

- [x] Remove the stale capture-specific retry promise or bind it to the exact survivor identity.
  Prefer the universal Gallery route because it remains truthful after any later capture wins.
- [x] Preserve delivery-time survivor restoration: a current survivor may repopulate review, while
  an already-superseded survivor may not overwrite the newer URI/provenance/delete scope.
- [x] Add deterministic regressions proving both an ordinary survivor and a survivor superseded by
  a newer capture always produce truthful retry copy.

## P2 — bound every deleted-family retirement continuation (AGG29-02)

- [x] Route retirement through the existing process-finite retained-still provider capacity while
  the Engine is live; never enqueue it on the unbounded still-encoding executor.
- [x] Preserve the stale-Engine producer-terminal fallback after facade shutdown. Overflow must run
  no work inline and must leave durable markers for launch recovery.
- [x] Add a deterministic blocked-worker test proving repeated submissions cannot exceed the exact
  active-plus-backlog capacity across Engine facades/replacement.
- [x] Update architecture/threading documentation for the finite retirement path.

## P3 — make clean-clone contributor authority explicit (AGG29-03)

- [x] Mark private/internal backlog, UX, testing, listing, and historical references as optional
  maintainer context when absent from a clean clone, and name the committed fallback authorities.
- [x] Preserve the intentional public/private split; do not publish private backlog or release
  material just to satisfy a link.
- [x] Add a clean-export documentation contract that rejects wording which makes an absent private
  file mandatory without a committed fallback.

## P4 — correct the audio hot-path authority (AGG29-04)

- [x] Replace the stale per-buffer RMS expression in `CLAUDE.md` with the implemented invariant:
  gain applies whenever non-unity, but RMS construction requires a live meter callback and a due
  emission cadence.
- [x] Keep the code and tests unchanged unless a gate exposes a real mismatch.

## Quality gates and delivery

- [x] Run focused tests for each fix, documentation/tool contracts, and `git diff --check`.
- [x] Run `./gradlew :app:assembleDebug :app:testDebugUnitTest :app:lintDebug` against the whole repo
  and root-fix every error/actionable warning without suppression.
- [x] Run `./gradlew :app:lintRelease :app:assembleRelease :app:bundleRelease` against clean committed
  source and root-fix every error/actionable warning without suppression.
- [x] Commit and push each independent fix and review/plan update using signed Conventional Commits
  plus gitmoji, always pulling with rebase before each push.
- [x] Update this plan with exact gate evidence and mark it complete only after every item is done.

## Deferred findings

None.

## Completion evidence

- Focused `CameraViewModelRobolectricTest`, `StatusUrgencyTest`, and
  `RetainedStillDiscardDispatcherTest` passed, including the ownership-safe Gallery copy and exact
  active-plus-backlog retirement ceiling.
- The clean-export positive and mandatory-private-context negative tool contracts passed;
  `tools/check_docs.py` passed 93 checks with zero failures and 21 explicit private-document skips.
- `./gradlew :app:assembleDebug :app:testDebugUnitTest :app:lintDebug` passed against the whole repo.
- `./gradlew :app:lintRelease :app:assembleRelease :app:bundleRelease` passed from clean committed
  source, including R8, signed APK packaging, signed AAB packaging, and release lint. The temporary
  clone referenced the existing local signing inputs without copying or printing their contents;
  direct Gradle artifacts remain developer-only and no immutable release evidence is claimed.
- No configured gate error or actionable warning required a source fix, suppression, threshold
  change, or deferred record (`GATE_FIXES=0`).
- Five implementation/review/plan commits were GPG-signed, pull-rebased, and pushed before this
  closeout. This closeout is the final signed/pushed cycle commit.
- The shared main worktree and all of its pre-existing CameraEngine/storage/UI/test/tool deltas
  remained untouched. No stash, reset, restore, checkout, or process termination was performed
  there.
- Deployment and physical-device mutation were not performed (`DEPLOY_MODE=none`).

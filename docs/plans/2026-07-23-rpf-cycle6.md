# Review-Plan-Fix Cycle 6 — Fix Plan (2026-07-23)

Source: `.context/reviews/_aggregate.md` (13-lane review of baseline 183ead0; QA device gate
PASSED). Implementation baseline: current `main` (rebased onto e2917bd — Inter typeface commit,
exempt from this cycle's review). Aggregate IDs (A/B/C/D/E-…) cited throughout; lane files under
`.context/reviews/` carry full evidence.

Rules honored: no silent drops (every aggregate item has a slice or an explicit deferral in §5);
security/correctness/data-loss items are only deferred where the repo's own device-verification
rule blocks them (sign flips without an on-device A/B) or where BACKLOG already carries them as
tracked debt. Commits: fine-grained, GPG-signed, Conventional+gitmoji, no Co-Authored-By, push per
slice after `git pull --rebase`.

Gates per slice batch: `./gradlew :app:assembleDebug :app:testDebugUnitTest :app:lintDebug`
(errors blocking, no unauthorized suppressions, root-cause only). End of cycle: deploy to
127.0.0.1:5599, run the device suite (all tiers, destructive/settings/media flags), plus targeted
manual checks listed in §4.

## 1. Product code slices

### S1 — Zebra/false-color meter the display-referred signal (A2; debugger F4) — P1
- `gl/Shaders.kt` fragment shader: evaluate zebra and false-color bands on the display-referred
  color (pre-OETF `base`, post gamma-assist decision), not the post-transfer output — the same
  domain principle the analysis readback already enforces (`analysisReadbackTransfer`). Peaking
  already samples `base`; bring zebra/false-color to the same seam. Keep the drawn overlay
  colors composited over the (possibly log) preview image.
- Host tests: extend the shader-structure tests (LogProfilesTest / shader string asserts) to pin
  that zebra/false-color sample the pre-transfer domain.
- Exit: unit green; device A/B — front camera (lit ceiling scene), S-Log3 + zebra 70%: stripes
  render post-fix (screenshot), none pre-fix; OSD/preview otherwise unchanged in SDR.

### S2 — Still requests ride `applyAfOverrides` (A3; code-reviewer F6) — P2
- `camera/CameraController.kt` still build (~:1384-1416): call `applyAfOverrides(this)` after
  `applyManualControls`, matching the full rebuild (:772) and sensor fast path (:1137).
- Tests: extend `AfOverrideTest` to cover the still-request key state under AF-lock and tap-AF
  hold (frozen distance / AF_MODE_AUTO hold, never a re-trigger).
- Exit: unit green; device: normal capture + tap-AF capture still succeed (suite photo cases).

### S3 — Lock hygiene: ABBA break + narrower native lock (A4, A5; tracer T6, T7) — P2
- T6: remove the gate-monitor acquisition from inside `UnsafeRecorderQuarantine.commitAdmission`'s
  block path (`camera/CameraEngine.kt:3418-3441`): give `TerminalAcquisitionGate` a monotonic
  volatile `isOpen` read for this GL-thread path (gate transitions open→closed exactly once; the
  `recorderOwnershipLock` claim inside keeps the commit exactly-once, and a close racing the
  volatile read is equivalent to a close landing just after commit — the claimed teardown already
  handles that). No path may hold the quarantine process lock while acquiring the gate monitor.
- T7: split `RecorderQuarantineAdmissionGate.runNativeIfSafe` into admit (lock: quarantined check
  + register in-flight) → native call OUTSIDE the lock → complete (lock: deregister). `close()`
  refuses new admissions immediately; in-flight natives complete and their results are refused at
  commit. Keep the drain-wedge abandon rule intact (never release native under a live drain).
- Tests: `TerminalAcquisitionGateTest`, `RecorderQuarantineAdmissionGateTest` — add ordering
  cases (commit while close pending; native in flight across close; no nested-lock path left).
- Exit: unit green; device: REC start/stop ×3 + suite reliability tier green.

### S4 — Burst/AEB/timelapse ShotSpec snapshot (A6; tracer T1/T2) — P2
- `camera/CameraEngine.kt`: compose the ShotSpec inputs (caps, teleconverterMode, facing,
  selection, aspectRatio) ONCE under the accepted-session window at initial dispatch and reuse
  for continuation frames; continuation fire re-validates only session identity.
- Tests: extend `CaptureSequenceOwnershipTest` (or sibling) — a door flipping optics mid-chain
  does not alter the in-flight chain's spec.
- Exit: unit green; device: AEB/burst capture produces correctly-rotated files (suite photo case
  + one manual AEB).

### S5 — Rollback stops leaking the route pin (A7; code-reviewer F5) — P2
- Distinguish the engine's routed target from a genuine user/diagnostic override: rollback
  publishes the transaction's BEFORE user-pin (null unless `setCameraOverride` was active), so
  `state.cameraOverrideId` stays null after routine failed doors; ProSheet diagnostic row and
  `restoredRouteUsesCurrentCaps` regain their intended meaning.
- Tests: `OpticsTransitionPolicyTest` / VM reducer test for rollback packet contents.
- Exit: unit green.

### S6 — Front tap-AF mirror seam (A8; debugger F2 + architect F4) — P2
- First: read front camera maxAfRegions/maxAeRegions from the device (`dumpsys media.camera`,
  camera id enumerated LENS_FACING_FRONT). If 0 → record MOOT in BACKLOG, still land the seam.
- Land `FrontMirrorConvention` single authority (one `FRONT_STREAM_PRE_MIRRORED = true` constant
  with derived `previewDrawMirrorX` / `encoderDrawMirrorX` / `tapSensorUnflipX` vals); GlPipeline
  draw roles and `mapTapFocusGeometry`'s front call site consume the derived values only.
- The tap SENSOR-half un-flip (1−nx) applies iff regions are supported; the sign itself joins the
  NEEDS-HUMAN list (same device-fact discipline as A1) unless made moot.
- Tests: `TapMappingTest` pins the derived-values wiring; PreviewMirrorTest header refresh (C1).
- Exit: unit green; device: front tap shows reticle, no crash; aim truth → NEEDS-HUMAN.

### S7 — My Menu dial-backed rows open rulers (A9; designer D-01) — P2
- `ui/controls/ProSheet.kt` MyMenuTab/Recent rows: slots with `manualDialForFnSlot != null`
  close the sheet and open that dial (same transition as the Fn overlay tile) instead of
  `performQuickFn` resets. Non-dial slots keep their cycle behavior.
- Tests: CameraUiPolicyTest addition pinning the routing predicate.
- Exit: unit green; device: My Menu EV row closes sheet + opens EV ruler.

### S8 — FRONT gating parity on Fn/sheet surfaces (A10; designer D-02, critic C8, code-reviewer F11) — P2
- `ui/controls/ControlCycles.kt` `quickFnEnabled(TELECONVERTER)`: add the facing axis (parity
  with `backOpticsDoorRefusal`), per its own KDoc contract. `ProSheet` LensTab: Lens selector +
  Teleconverter row disabled while FRONT with a terse "Rear camera only." caption.
- Tests: ControlCycles parity test + CameraUiPolicyTest.
- Exit: unit green; device: on FRONT the Fn Tele tile renders dim; LensTab rows dim.

### S9 — Frame-gate ProSheet sliders (A11; PR-2) — P2
- `ui/controls/ProControls.kt` `CameraSlider`: adopt the RulerSlider 16 ms publication gate with
  exact landing on finger-up. `ui/CameraViewModel.kt`: fold `markChanged`'s `_state.update` into
  the same update as `updateControls` (one emission per publication).
- Exit: unit green; device: JPEG-quality/Kelvin slider drags still land exact final values.

### S10 — Hi-res ladder + fast-path + review honesty (A12, A13, A14; debugger F3, tracer T9,
FDCR 1) — P2 (dormant on PMA110, live code)
- A12: `camera/CameraController.kt` `sessionAttemptPlan`: a failed hi-res attempt falls back to
  the FULL plan including RAW (hi-res becomes a prepended rung, not a replacement of attempt 0);
  update the ladder doc comment (:461-467) in the same edit (C2 companion).
- A13: remember "hi-res dropped by this route's ladder" on the accepted session and make
  `commitFastPathOrReconfigure` compare intent against the CONFIGURED intent, so same-route fast
  doors stop paying full reconfigures forever.
- A14: `ui/review/MediaReview.kt` `loadBitmap` (+ thumb): honor EXIF TAG_ORIENTATION when
  decoding STILL JPEGs (processed lane stamps NORMAL, so this is a no-op there and correct for
  passthrough).
- Tests: `SessionFallbackLadderTest` — pin the new ladder INCLUDING tele rungs 1/2/4/5 and the
  max-attempt boundary (closes D1 / TE F-A1 in the same file); fast-path divergence test;
  review-orientation unit if the decode seam is pure enough.
- Exit: unit green (ladder fully pinned); device: TELE DNG capture still works (suite).

### S11 — Small correctness batch (A15-A24) — P3
- A15 `runCatching` around regular/vendor `createCaptureSession`, advancing the ladder like
  `onConfigureFailed` (CameraController.kt:608).
- A16 symmetric `rollbackOptics` on the recorder recheck in setLens/setResolvedOptics/
  setFrontCamera.
- A17 refused-admission path re-restores the preview transfer (CameraEngine startRecording
  refusal at :3464-class paths).
- A18 settings save while FRONT persists the pre-front rear optics packet (TC/zoom), not the
  front-session values; test in SettingsStore/VM policy test.
- A19 `TapFocusPublicationGate.applyIfLatest` equality idempotent.
- A20 HeifExif fill-byte step `offset += 1`.
- A21 encoder-branch preview-restore failure: contain via the acquisition branch (orphan preview,
  keep the healthy recording) instead of `failEncoderOutput`.
- A22 exhaustive ColorTransfer→shader-transfer-code mapping (new member breaks the build).
- A23 hoist side-effect writes out of `_state.update` transforms.
- A24 cache `finderRect` per (previewW, previewH); hoist drawFrame `runCatching` to try/catch.
- Exit: unit green per item where a seam exists; gates green.

### S12 — Hi-res capability projection (E: architect F3) — P2
- Extend `CameraControlCapabilities`/ControlAvailability with the hi-res facts (advertised,
  standalone, logicalMultiCamera); ProSheet consumes projection fields instead of re-deriving
  from raw caps. `hiResAdmitted` stays the one predicate.
- Tests: ControlAvailabilityTest addition.

### S13 — Designer polish batch (A25, A26) — P3
- D-05 ruler readouts use the small dim auto-badge (or "Auto"), not glued "A ".
- D-10 shutter stays fixed-center; snapshot dot offsets from it.
- D-11 Fn editor "Add" rows become Button-role rows; MR recall row gets Role.Button + label.
- D-12 review zoom control disambiguates next-vs-current magnification.
- D-13 Setup "Keys" row copy covers volume keys ("Camera Key / Volume").
- D-04 unify Punch-In/Loupe naming (settings toggle, Fn chip, OSD tag, key-action label —
  enum NAMES unchanged; labels only).
- A26 one terse caption under TransferSelector: "Curve applied to the SDR camera stream."
- Exit: unit green (CameraUiPolicyTest string/pattern pins where present); device screenshots.

## 2. device-tests harness slices

### H1 — Report integrity (B1, B5) — P1
- `dtest/framework.py`: `quoteattr` for all attribute interpolation; `incomplete` emits a
  failure-class element (not `<skipped>`). Add host tests for `_write_reports` (quotes in detail,
  incomplete mapping) — the gap that let B1 survive.

### H2 — Deterministic case bugs (B2, B3, B4) — P1/P2
- B2 `cases.py:385/:408` `tap_ui(text="Shoot")` → `tap_ui(desc="Shoot")`.
- B3 TC cases: switch OSD to detail mode before asserting "mm TELE" (and restore), or assert the
  3A `tele=true` line; apply to both `t_tc` and `t_dng`.
- B4 RECORDING_SPEC regex: `transfer=(HLG|SLOG3|SLOG3_CINE|LOGC3|SDR)`.
- Exit: re-run the three previously-failing cases on device → PASS (this converts the suite's
  3 fails to green and is part of the cycle verification).

### H3 — Robustness batch (B7, B8, B9; critic C7) — P2/P3
- B7 log + assert the measured shutter→kill delta in capture_then_kill_survives.
- B8 rec_backgrounded_finalizes: admission identity + wait_recording_running + fatal scan +
  pending-rows sweep + honest duration minimum.
- B9: preflight sha256 guard (exit 2, no traceback); bounded initial get-state probe; typed
  AdbError from the retry TimeoutExpired; screen_stats empty-payload guard; media.py jpeg_info
  fill-byte/length-less-marker walk; preview_is_live retries on length mismatch instead of
  returning live; launch_ui_snapshot routes through the guarded launch path (or asserts the
  destructive flag itself); screenshot() name validation (match ui()); TC-case bare sleeps →
  acceptance/3A polling; fix the two "committed Ready" assertion messages to "accepted".
- H-tests: extend tests/test_harness.py where parsers changed.

### H4 — Coverage honesty (B6) — P2
- README: add the 20th case row (debug_snapshot_ui_contract); drop "O-Log2 transfer selection";
  add deliberate-non-coverage entries for front camera, hi-res (dormant), and log-profile
  recording validation (with rationale and pointers to the manual QA evidence from this cycle).

## 3. Documentation slices

### DOC1 — ARCHITECTURE.md mirror truth (C1) — P1
- Rewrite :63 (FlipRenderer row), :397-399, :502 to the as-built inverted roles; remove
  `gl.setPreviewMirror`, name `setFrontStreamPreMirrored`. Fix stale comments:
  FlipRenderer.kt:141-144, GlPipeline.kt:109-118 first half, GlPipeline.kt:430,
  RotationMath.kt:29-32 narration, PreviewMirrorTest.kt header.

### DOC2 — ARCHITECTURE.md hi-res + module map (C2, C6) — P2
- Hi-res: module rows (CaptureCapabilities/StillCapturePipeline/ProSheet), the ladder rung +
  RAW-exclusion rule (align with the S10 ladder change), HR OSD tag.
- Module map: add NumericBounds/ProcessedSnapshotBudget/ShutterPolicy/HeifExif/AtomicOwnerSlot
  rows; re-file ControlCycles/ZoomGlideState/MediaReview; correct the HardwareInputPolicy row.

### DOC3 — BACKLOG sync (C3) — P2
- Residual Field Checks: add (a) rear+front capture-rotation device-term sign A/B (full
  derivation pointer, exact landscape-held instructions — release-gating; NEEDS-HUMAN, device
  desk-bound this cycle); (b) front VIDEO file mirror truth (front STILL verified 2026-07-23 by
  QA — cite evidence); (c) front tap-AF aim (or MOOT per S6 probe); (d) log-profile on-device
  rendering check status (S-Log3.Cine clip verified 2026-07-23 — cite); (e) hi-res dormancy +
  its device checks (EXIF passthrough audit SEC F-5, watchdog T5) under a hi-res entry.
- D-3 restated as dormant-de-log-only (DS-6, folds TE F-A2); D-4 updated with cycle-6 growth
  (+525 lines across the four god files; facing = 4th hand-copied axis); D-2/D-16 confirmed
  still open; note SEC F-2 (GitHub About overclaim) + Play mailbox/Pages as owner actions.

### DOC4 — Trademark + small doc batch (C4, C5, C7) — P2/P3
- ARRI entity: ProSheet.kt footnote + play-store-listing.md → "LogC is a trademark of Arnold &
  Richter Cine Technik GmbH & Co. Betriebs KG (ARRI)." KEEP the Inter/OFL attribution row that
  now sits below the trademark Text (e2917bd) — two rows total.
- README gains the same attribution footnote (DS-10).
- CLAUDE.md: front bullet header (mirror diagnosed, rotation still pending); "finder PIP" →
  Loupe Overview naming; "#4" pointer → named item.
- CameraState.kt:852 comment; CaptureCapabilities "two sources" comment (third path per javadoc,
  DS-9); device-tests/README O-Log2 line (with H4); docs/plans/2026-07-21-rpf-cycle5.md
  completion block updated with the landed evidence (DS-11).

## 4. Verification (end of cycle)

1. Gates on the whole repo (assembleDebug, testDebugUnitTest, lintDebug) — all green.
2. `git pull --rebase` + deploy: install to 127.0.0.1:5599, launch, no crash, live preview.
3. Device suite: `python3 device-tests/run.py --serial 127.0.0.1:5599 --tier all
   --allow-destructive --allow-settings --allow-media-writes` — expected: the 3 harness-bug
   fails now PASS; the 2 HLG-precondition INCOMPLETEs remain (by design) unless trivially
   settable; no new fails.
4. Targeted manual: zebra-under-S-Log3 A/B screenshots (S1); My Menu EV row opens ruler (S7);
   FRONT dimmed Fn/sheet rows (S8); front flip + capture regression (QA baseline); AF-lock still
   capture (S2, best-effort log evidence).
5. NEEDS-HUMAN list in the cycle report: A1 rotation A/B, front video mirror, front tap aim
   (unless moot), D-06/D-07 feel checks, lit-scene items from QA's checklist, GitHub About +
   Play mailbox/Pages owner actions.
6. Leave device at baseline (rear, photo, TC off, compact OSD).

## 5. Explicit deferrals (dispositions)

| Item | Disposition |
|---|---|
| A1 sign FLIP (+ videoOrientationHint front branch) | BLOCKED-ON-HUMAN device A/B; docs/tests/BACKLOG only this cycle (repo device-verification rule; orchestrator order). |
| A8 tap-aim sign flip | Same discipline; seam lands, flip only if maxAfRegions>0 AND future device A/B (or MOOT). |
| Architect F1/F2/F5/F6/F7 | Standing BACKLOG D-4 structural debt; updated, not implemented (feature-freeze-class refactor). |
| TE F-A2 (dormant olog2Inv numeric pin) | Folded into BACKLOG D-3 restatement. |
| PR-1, PR-4 | P3 micro-costs riding existing recompositions; recorded in aggregate; revisit with D-4. |
| Designer D-03 | False positive at HEAD (stale injected doc); no action. |
| Designer D-06/D-07 | NEEDS-HUMAN on-device feel checks. |
| Critic C10 | Deliberate migration trade; recorded. |
| SEC F-1 | Public upstream credential; BACKLOG D-16 stands. |
| SEC F-2/F-7 | Out-of-repo owner actions; NEEDS-HUMAN list + BACKLOG note. |
| SEC F-5 / tracer T5 | Dormant hi-res device checks; BACKLOG hi-res entry. |
| QA INCOMPLETEs | By-design strict HLG precondition; unchanged (B4/H4 make the failure mode honest). |

# Review-plan-fix cycle 13 — implementation plan

Date: 2026-08-23
Source: `.context/reviews/_aggregate.md`, cycle 13
Status: active; review and planning complete, implementation not started

## Repository rules applied

- Preserve PMA110 behavior byte-for-byte; do not infer new device behavior or run ADB/deployment.
- Correctness, liveness, native ownership, and evidence-integrity findings are mandatory and are not
  deferred.
- Preserve English/Korean user-facing parity and the quiet Sony Alpha/Xperia Pro surface. No new
  user-facing copy is planned.
- Completed plans remain in place as non-destructive archives; no file deletion/move is authorized.
- Preserve exact coverage, immutable release boundaries, dependency verification, GPG-signed
  Conventional Commits with gitmoji, pull-with-rebase before push, and no force push.

Every aggregate finding AGG13-01 through AGG13-09 is scheduled below. No finding is deferred,
severity-downgraded, or silently dropped.

## Phase 1 — recover finite native/audio ownership without stale replay

- [ ] **P1 / AGG13-01:** retain preview/start intent across a pending recorder-setup refusal and
  subscribe once to that token's terminal edge. Replay only for the current, foreground, surfaced
  Engine generation after clean retirement; preserve quarantine as terminal and prevent stale or
  released Engines from reopening. Add same-Engine and replacement-Engine barrier tests.
- [ ] **P2 / AGG13-06:** own the live standby AudioRecord independently of its read worker so
  handoff/pause/release can request a safe unblock and observe terminal classification. Identity-gate
  late completion and process-token release; add blocked-read, replacement-owner, and late-worker
  tests without claiming an unmeasured vendor behavior.

Acceptance: temporary REC setup cannot consume the only foreground/surface edge, and a stuck standby
read cannot silently retain microphone admission for the process lifetime.

## Phase 2 — bound low-priority lifecycle telemetry

- [ ] **P3 / AGG13-07:** make battery/storage sampling single-flight and coalesced, lifecycle-
  generation-gate accepted results, and ensure start/stop plus repeated ticks cannot accumulate an
  unbounded queue ahead of user work. Add a deterministic blocked-worker test.

Acceptance: at most one info sample is in flight and one refresh intent is pending, independent of
elapsed ticks or lifecycle churn.

## Phase 3 — seal every evidence-grade execution and publication boundary

- [ ] **P4 / AGG13-02:** make the immutable debug compiler consume a sealed snapshot owner that
  rejects attempted snapshot mutation, and add a real snapshot A -> B -> A compile barrier test.
- [ ] **P5 / AGG13-03:** require inherited private-snapshot authority at every callable full-run
  entry and add an import-and-call refusal test before APK/device access.
- [ ] **P6 / AGG13-04:** replace pathname-reopened checkout hashing with a bounded descriptor-rooted,
  no-follow frozen source identity and deterministic leaf/parent/member-set/swap tests.
- [ ] **P7 / AGG13-05:** publish a green report attestation only after all failure-producing evidence
  checks succeed; test a mutation at the former post-sidecar race boundary.

Acceptance: compiler inputs, imported harness code, checkout identity, and published report evidence
each have one immutable authority across every claim and no failure leaves a consumable green proof.

## Phase 4 — align active authorities and operator instructions

- [ ] **P8 / AGG13-08:** update CLAUDE, architecture, Loupe source/test wording, and docs checks to
  the tested enabled + punch-in + (TELE or unified >= 3x) matrix, where Photo requires 4:3 and Video
  ignores still aspect.
- [ ] **P9 / AGG13-09:** update the device-harness README and docs checks to require the printed
  immutable evidence APK while supporting clean or dirty scoped source snapshots.

Acceptance: current design and operator authorities cannot regress to either obsolete contract.

## Phase 5 — gates, delivery, and evidence

- [ ] Run `./gradlew :app:assembleDebug :app:testDebugUnitTest :app:lintDebug` across the repository.
- [ ] Run the configured release tasks through the repository's immutable release wrapper if direct
  Gradle release entry points fail closed by design.
- [ ] Run `python3 tools/verify_host.py`, relevant Python/tool/device-harness/docs suites, Python
  compilation, diff checks, and exact Partition-A coverage verification.
- [ ] Root-fix every error and actionable warning without suppression.
- [ ] Commit independent concerns with fine-grained GPG-signed Conventional Commit + gitmoji
  messages; pull with rebase before push and push. Do not deploy.
- [ ] Mark all items complete and record exact gate/commit evidence here.

## Planned commit sequence

`fix(camera)`, `fix(audio)`, `perf(ui)`, `fix(provenance)`, `fix(attestation)`, `docs(loupe)`,
`docs(harness)`, and `docs(plan)`, split further where one commit would mix concerns.

## Deferred findings

None.

## Residual warnings

None accepted at planning time. Any configured-gate warning that cannot be fixed cleanly must be
recorded here with original severity/confidence, concrete reason, and reopening criterion.

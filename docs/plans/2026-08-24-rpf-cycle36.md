# Review-plan-fix cycle 36 — implementation plan

Date: 2026-08-24
Source: `.context/reviews/_aggregate.md`, cycle 36
Status: complete; all three findings fixed and all configured gates green

## Repository rules applied

- Preserve measured PMA110 behavior, capability-enumerated multi-device paths, exact Camera2/native
  ownership, durable media truth, quiet Sony-style UI, EN/KO parity, and clean-clone authority.
- Schedule every one of the three aggregate findings below. Nothing is silently dropped, deferred,
  or severity-downgraded.
- Keep completed historical plans as non-destructive archives in `docs/plans/`; cycle 35 remains
  unchanged and cycle 36 will explicitly supersede its incomplete exhaustive-matrix claim.
- Work only in `/tmp/find-x9-cycle36.TOpdQ8`; never touch the shared dirty `main` checkout.
- Use fine-grained GPG-signed Conventional Commits with gitmoji, pull with rebase before every push,
  and do not deploy because `DEPLOY_MODE=none`.

## P1 — totalize dual-open cleanup across absent and terminal owners (AGG36-01)

- [x] Replace independently derived ownership booleans with a production-shaped identity decision
  that distinguishes candidate, vacant, outgoing, and genuinely newer shared-slot owners.
- [x] Carry an explicit outgoing-restorability signal set before CameraController terminal callbacks;
  never restore a controller after failure or close begins, and clear the stale candidate/vacant slot
  while retaining the rollback baseline needed by the newer cold intent.
- [x] Add an exhaustive identity/liveness matrix covering null outgoing, candidate self-clear,
  outgoing disconnect/close, candidate/current/vacant/newer slot layouts, and exact release actions.
- [x] Add a cycle-36 completion note that truthfully supersedes cycle 35's Boolean-only matrix claim
  without rewriting the historical plan.

## P2 — reject optimized device-evidence execution (AGG36-02)

- [x] Fail closed with exit 2 before immutable snapshot, APK inspection, ADB, report, or attestation
  work whenever `sys.flags.optimize != 0`.
- [x] Apply the same guard at both the direct outer entry and inherited fork/runpy child boundary so
  neither execution mode can import assertion-owned verdicts under optimization.
- [x] Add subprocess regressions for `python -O` and environment-only `PYTHONOPTIMIZE=1`, proving a
  stable diagnostic and non-green exit before device/APK preflight.

## P3 — restore enabled affordance-edge contrast (AGG36-03)

- [x] Raise only the enabled `AffordanceEdge` token enough to exceed 3:1 against its dark Pill/HUD
  plates while preserving the existing 1 dp quiet geometry, opaque surfaces, and selected fills.
- [x] Preserve separately resolved disabled FilterChip styling and verify the enabled change does not
  collapse other structural tokens or alter selection semantics.
- [x] Replace the obsolete exact-0.18 test/comment with palette math that pins the 3:1 contract and
  covers enabled/unselected plus selected/disabled consumers on sheet and HUD plates.

## Quality gates and delivery

- [x] Run focused tests for every fix plus `git diff --check`.
- [x] Run `python3 tools/verify_host.py` against the whole repository.
- [x] Run `./gradlew :app:assembleDebug :app:testDebugUnitTest :app:lintDebug`.
- [x] Run `./gradlew :app:lintRelease :app:assembleRelease :app:bundleRelease`.
- [x] Root-fix every configured gate error and actionable warning without suppression.
- [x] Commit and push each independent review, plan, implementation, test, documentation, and
  closeout change with signed Conventional Commits plus gitmoji, pulling with rebase before push.
- [x] Mark this plan complete only after exact evidence is delivered.

## Deferred findings

None. All three current findings were implemented in P1-P3.

## Completion evidence

- All three deduplicated findings were implemented; nothing was deferred. This cycle supersedes the
  cycle-35 claim that its Boolean-only dual-open matrix was exhaustive: the new matrix consumes the
  same nullable identities production owns and explicitly covers absent, live, terminal, candidate,
  vacant, outgoing, and newer slot states.
- `CameraController` now revokes dual-open restorability before publishing any terminal failure and
  when close begins. `CameraEngine` derives a total typed cleanup from current/candidate/outgoing
  identities plus that liveness truth, restores a live owner or null baseline, and never republishes
  a closing owner over a newer intent. Focused ownership and callback-order tests pass.
- The device harness exits 2 before immutable snapshot, APK, ADB, report, or attestation work when
  `python -O` or `PYTHONOPTIMIZE` enables optimized mode. The same `__main__` guard executes at the
  outer and inherited fork/runpy child boundaries; both subprocess regressions pass.
- Current W3C WCAG2ICT 1.4.11 guidance was checked before the UI change. Enabled `AffordanceEdge`
  moved from 18% to 36% white, clearing 3:1 against opaque Pill and HUD plates over both bright and
  dark frames while retaining 1 dp geometry and separately resolved disabled styling. Palette math
  and native rendered enabled/selected/disabled FilterChip tests pass.
- `python3 tools/verify_host.py` passed Android debug assembly, androidTest packaging, all
  JVM/Robolectric/Compose tests, lint, exact coverage, 96 tool tests, nine coverage-tool tests, 184
  device-harness tests, 112 documentation checks, Python compilation, and diff check. Partition A is
  8,030/8,045 lines (99.81%) with the exact existing 15-line reviewed residual set.
- `./gradlew :app:assembleDebug :app:testDebugUnitTest :app:lintDebug` passed. The clean signed-source
  `./gradlew :app:lintRelease :app:assembleRelease :app:bundleRelease` gate passed with R8 after this
  isolated worktree referenced the repository's existing gitignored signing inputs read-only; no
  secret value was copied or printed. The prebuilt native strip notice remains expected packaging
  diagnostics, not a suppressed error.
- No configured gate error or actionable warning was encountered, so `GATE_FIXES=0`.
- Work remained in `/tmp/find-x9-cycle36.TOpdQ8`. The shared dirty `main` checkout was never edited,
  staged, stashed, reset, checked out, or used for a build. Deployment was not run
  (`DEPLOY_MODE=none`).

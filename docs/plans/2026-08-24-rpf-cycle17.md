# Review-plan-fix cycle 17 — implementation plan

Date: 2026-08-24
Source: `.context/reviews/_aggregate.md`, cycle 17
Status: active; review and planning complete, implementation not started

## Repository rules applied

- Preserve PMA110 behavior and every measured HAL workaround; this cycle changes review-media,
  release-provenance, recovery-lifecycle, comments, and documentation rather than camera pixels.
- Security, correctness, release-integrity, lifecycle, and liveness findings are mandatory and are
  not deferred.
- Preserve English/Korean parity, the quiet Sony-style viewfinder, exact resource ownership,
  immutable release/evidence boundaries, and the repository's exact coverage contract.
- Completed prior plans remain non-destructive archives in `docs/plans/`; cycle 16 is already marked
  complete and needs no move or deletion.
- Use fine-grained GPG-signed Conventional Commits with gitmoji, pull with rebase before each push,
  never force-push, and never bypass configured gates.

Every aggregate finding AGG17-01 through AGG17-07 is scheduled below. No finding is deferred,
severity-downgraded, or silently dropped.

## Phase 1 — make review acquisition finite and terminally progress-capable

- [ ] **P1 / AGG17-01:** give the shared process review executor explicit active-capacity ownership
  and a bounded typed terminal result for latest work that cannot start after every worker is
  occupied by retired synchronous calls. Preserve one conflated pending request per lane, fixed
  worker ceilings, and exact stale-result disposal.
- [ ] Map capacity exhaustion into the existing full-screen review critical-state surface with
  English/Korean restart-required copy. It must never become a shooting-viewfinder warning chip.
- [ ] **P2 / AGG17-02:** move full-screen still acquisition off the single-mutex serial lane and onto
  progress-capable finite execution while separately bounding decoded-bitmap memory. One abandoned
  provider/decoder call must not permit multiple 3000-pixel results to coexist or block all later
  healthy stills.
- [ ] **P3 / AGG17-03:** normalize every process-lane request to `applicationContext` or a
  process-owned resolver before publication; no blocked process worker may retain Activity/window/
  composition ownership.
- [ ] Add deterministic tests for permanent same-lane blocks, four-worker cross-lane exhaustion,
  bounded terminal timing without releasing blockers, healthy still replacement, exact bitmap/native
  disposal, responsive Back, fixed active/pending ceilings, and application-context ownership.

Acceptance: every latest review request either completes or reaches a truthful bounded terminal;
blocked provider/native calls stay safely retained without multiplying resources or UI lifetimes.

## Phase 2 — make immutable-release authorization wrapper-owned

- [x] **P4 / AGG17-04:** introduce a wrapper-created, single-invocation release authority that
  binds the private checkout, commit/tree provenance, and sealed repository-relative signing file.
  Gradle must reject a direct release task even when the caller supplies correct public commit,
  tree, and store-file properties.
- [x] Keep all authority material private and non-secret-bearing in diagnostics; do not expose
  passwords, aliases, or keystore bytes. Preserve no-follow input handling and the existing
  post-build source/signing seal verification.
- [x] Add a real direct-release negative test with matching identities and retain the existing
  mutation-after-gate test. Retain mismatch/missing-property tests as defense in depth and align
  Architecture with the exact authorization boundary.

Acceptance: only the immutable wrapper's private invocation can authorize release compilation and
packaging; reproducible provenance strings alone never grant authority.

## Phase 3 — preserve launch-recovery cancellation after terminal snapshot

- [x] **P5 / AGG17-05:** replace bare snapshotted recovery callbacks with first-wins subscription
  tokens. `cancel()` must retire its token after the subscriber map is cleared, and terminal delivery
  must atomically claim that token immediately before invoking the callback.
- [x] Add deterministic post-snapshot tests, including canceling a
  later subscriber from an earlier callback. Preserve independent admission for a fresh post-terminal
  request and typed success/failure delivery to every still-live subscriber.

Acceptance: successful cancellation is authoritative at every point before callback invocation;
detached Engine/ViewModel generations cannot start stale restore work.

## Phase 4 — correct active architecture, zoom, and tablet guidance

- [ ] **P6 / AGG17-06:** document both review owners, the shared fixed process pool, progressive
  per-lane consumers, conflated pending state, bitmap memory ownership, and the bounded exhaustion
  terminal implemented in Phase 1. Correct wrapper KDoc to name its actual delegate.
- [ ] Rewrite Engine/ViewModel/ZoomMath comments around the architectural law: logical BACK is
  unified/main-relative; every standalone route is lens-local; RAW/DNG Photo can be standalone when
  profile/route truth requires it. Extend source/docs checks against the superseded mode-owned rule.
- [ ] **P7 / AGG17-07:** replace the active Play tablet-asset paragraph with the one-layout rule:
  camera controls keep their homes while the settings panel alone may dock as a side sheet. Extend
  the retired-operator-rail documentation guard to the active listing.

Acceptance: active code comments and release/as-built documentation describe only current ownership
and layout; historical records remain clearly historical.

## Phase 5 — gates, delivery, and evidence

- [ ] Run `./gradlew :app:assembleDebug :app:testDebugUnitTest :app:lintDebug` across the repository.
- [ ] Run `./gradlew :app:lintRelease :app:assembleRelease :app:bundleRelease` as configured. The
  newly hardened direct-release authority is expected to reject direct invocation; after clean,
  pushed commits, run the repository's immutable release wrapper for the same tasks as the
  authoritative release gate.
- [ ] Run `python3 tools/verify_host.py`, focused tool/release/docs tests, Python compilation, exact
  Partition-A coverage verification, and HEAD-to-working-tree diff/whitespace checks.
- [ ] Root-fix every error and actionable warning without suppression. Any genuinely unfixable new
  warning must be recorded under the strict deferred format before commit.
- [ ] Commit and push each independent fix with GPG-signed Conventional Commit + gitmoji messages,
  pulling with rebase before each push. Verify signatures and upstream synchronization, then close
  this plan. Do not deploy.

## Planned commit sequence

`docs(plan)`, `perf(review)`, `fix(provenance)`, `fix(recovery)`, `docs(architecture)`,
`docs(play)`, `test(coverage)`, and `docs(plan)`, split further wherever one commit would combine
unrelated behavior.

## Deferred findings

None. Existing owner-approved field checks and design deferrals in `docs/BACKLOG.md` are unchanged.

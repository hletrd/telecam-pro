# Review-plan-fix cycle 50 — implementation plan

Date: 2026-08-25
Source: `.context/reviews/_aggregate.md`, cycle 50
Status: complete; all eight findings fixed and every configured quality gate green

## Repository rules applied

- Preserve measured PMA110 Camera2 behavior, capability-enumerated generic-device paths, exact
  lifecycle/native ownership, durable media truth, the quiet Sony-style viewfinder, EN/KO parity,
  and explicit device-evidence limits.
- Schedule every one of the eight aggregate findings below. Nothing is silently dropped,
  severity-downgraded, deferred, or relabeled as optional.
- Keep completed historical plans in `docs/plans/` as the non-destructive archive. No completed plan
  is deleted, moved, rewritten, or reopened; append dated evidence corrections where a prior plan
  overstated coverage.
- Work only in `/tmp/find-x9-ultra-cycle50.ZrnMqN`; never modify, stash, reset, check out, clean, or
  build from the shared dirty `main` checkout.
- Use fine-grained GPG-signed Conventional Commits with gitmoji, pull with rebase before every push,
  and do not deploy because `DEPLOY_MODE=none`.

## P1 — give video-pipeline publications independent supersession ownership (AGG50-01, High/High)

- [x] Add a monotonic pipeline-publication identity covering codec, exact ordered candidates,
  requested next-Video transfer, and active session transfer even when Photo-mode changes do not
  require Camera2 reconfiguration.
- [x] Carry that identity through optics rollback publication and reject an older queued rollback in
  the ViewModel after a newer pipeline command has linearized.
- [x] Move convenience-wrapper reads inside the Engine monitor or replace them with one complete
  command packet so method arguments cannot carry a pre-rollback tuple across monitor entry.
- [x] Add a deterministic disjoint HEVC/Main10/HLG versus AVC/Main/SDR interleave; drain the real
  ViewModel callback and assert Engine, UI, persistence input, next Video entry, and REC admission
  agree on the winner.

## P2 — freeze one complete production REC admission snapshot (AGG50-02, Medium/High)

- [x] Under one Engine-monitor section, validate the accepted Camera2 session and copy caps, size,
  frame rate, codec, active transfer, and ordered candidates into one immutable snapshot.
- [x] Run capability filtering and slow/native allocation outside the monitor against that snapshot,
  retaining the existing later session-identity and topology checks.
- [x] Add a latch-controlled old/new packet publication test proving REC sees either complete tuple,
  never a hybrid or spurious selected-codec refusal.

## P3 — execute production REC wiring after rollback (AGG50-03, Medium/High)

- [x] Add a narrow pre-native allocation injection that preserves `startRecording` and
  `beginRecordingAllocation` candidate/FPS/transfer admission instead of bypassing it.
- [x] Drive public REC through the production accepted-session/packet branch and exact FPS refusal;
  combine it with rollback-owned HLG/SDR candidate tests and the disjoint publication interleave.
- [x] Assert the exact production status/result and immutable admission packet; keep pure helper tests
  as focused policy tests rather than integration evidence.

## P4 — finish PNG chunk grammar and ordering validation (AGG50-04, Low/High)

- [x] Require every chunk type byte to be ASCII alphabetic and the third-byte reserved bit to be
  clear before critical/ancillary classification.
- [x] Validate or reject admitted ancillary chunks by structure, multiplicity, and legal placement;
  specifically reject post-IDAT `tRNS` and malformed/late color-space chunks without traceback.
- [x] Add digest-refreshed mutations for lowercase reserved bits, non-letter types, late `tRNS`,
  `sRGB`, `iCCP`, and their malformed lengths/values.

## P5 — make shader initialization transactional (AGG50-05, Low/High)

- [x] Hold shader, program, VBO, and texture ids in local owners until every init step succeeds;
  publish renderer fields only after complete initialization.
- [x] Delete a failed shader in `compileShader`, always delete both shaders in `buildProgram`, and
  delete an untransferred program/VBO/texture on every later failure.
- [x] Add an injectable GL ownership seam or equivalent focused tests that fail after every acquisition
  edge and assert exact cleanup plus a leak-free same-context retry.

## P6 — bind review decoding to one bounded snapshot (AGG50-06, Low/Medium)

- [x] Obtain bounds, pixels, and EXIF from one stable seekable descriptor or one private size-bounded
  snapshot so owner-null provider content cannot change between phases.
- [x] Reject non-positive bounds, cap source bytes, verify final decoded dimensions/allocation against
  the review request, and recycle/refuse violations without affecting the video-thumbnail path.
- [x] Prove the decoder consumes one immutable input stream, rejects an over-limit snapshot, invalid
  bounds, and oversized decoded pixels; keep real MediaProvider semantics explicitly manual.

## P7 — publish camera-policy unblocking after the optics monitor releases (AGG50-07, Low/High)

- [x] Capture a policy-unblocked publication flag inside `commitOpticsReady`'s terminal mutation and
  invoke `onCameraPolicyBlocked(false)` beside the other post-commit callbacks.
- [x] Add a callback that attempts a competing optics operation and prove no external callback runs
  inside the terminal critical section.
- [x] Keep `CLAUDE.md` and `docs/ARCHITECTURE.md` lock-boundary wording unchanged once code and tests
  satisfy it; add a documentation/source invariant if it is stable and mutation-sensitive.

## P8 — close the release-trace evidence contract (AGG50-08, Low/High)

- [x] Add a source/bytecode invariant or executable build-mode seam proving the production caller
  passes the release build admission and nullable trace consumption cannot be force-unwrapped.
- [x] Cover ordinary Single, in-recording snapshot, and sequence callback construction/settlement
  under explicit debug and release constants.
- [x] Append a dated correction to the completed cycle-49 plan naming the exact stronger evidence;
  do not rewrite its historical completion record.

## Quality gates and delivery

- [x] Run focused tests for pipeline supersession, immutable REC admission, production REC wiring,
  PNG grammar/order, transactional GL cleanup, stable review decode, post-lock callbacks, and
  release-trace evidence, plus `git diff --check`.
- [x] Run `./gradlew :app:assembleDebug :app:testDebugUnitTest :app:lintDebug` across the whole repo.
- [x] Run `./gradlew :app:lintRelease :app:assembleRelease :app:bundleRelease` across the whole repo.
- [x] Run the authoritative supporting host gate `python3 tools/verify_host.py`.
- [x] Root-fix every gate error and warning without suppression; record any authorized unavoidable
  warning under the strict deferred-finding rules.
- [x] Commit and push each independent review, plan, implementation, test, documentation, and
  closeout change with signed Conventional Commits plus gitmoji, pulling with rebase before push.
- [x] Mark this plan complete only after exact evidence is delivered.

## Deferred findings

None. All eight cycle-50 findings are scheduled for this cycle.

## Completion evidence

- All eight deduplicated findings were implemented; none was deferred. Independent video-pipeline
  publication identity now prevents an older rollback callback from repainting a newer Photo-mode
  codec/curve command, including a deterministic HEVC/HLG versus AVC/SDR interleave through UI and
  REC candidate admission.
- Accepted Camera2 identity, frame-rate capability, codec, active transfer, and exact ordered
  candidates now freeze under one Engine monitor. A public-REC barrier test proves a concurrent
  pipeline command blocks until the complete old packet is captured; the production unavailable-FPS
  status/result branch is exercised before any test-native effect.
- Screenshot validation now rejects illegal type bytes/reserved bits and invalid ancillary
  structure/order while fully validating the repository's iCCP, sRGB, sBIT, and tRNS profile.
  Digest-refreshed committed-export mutations cover non-letter/reserved types, late metadata, and
  malformed iCCP/sRGB without traceback.
- Renderer initialization transfers shader/program/VBO/texture ids only after complete success and
  uses one exact cleanup owner on every partial failure. Tests cover each acquisition prefix,
  transferred program ownership, and idempotent cleanup.
- Still review bounds, pixels, and EXIF now consume one 64 MiB-bounded immutable byte snapshot.
  Invalid bounds, over-limit input, and decoded dimensions outside the requested maximum fail closed;
  a direct ownership test covers the Ready bitmap terminal without a coverage suppression.
- Camera-policy unblocking is published after the optics monitor releases, with a re-entrant callback
  regression. Release capture tracing now has committed-export negative contracts for production
  `BuildConfig.DEBUG` admission and nullable-safe trace consumption; cycle 49 carries a dated evidence
  correction.
- Exact debug gate passed: debug APK assembly, the full JVM/Robolectric/Compose suite, and debug lint.
  Exact release gate passed: release lint, R8 APK, and signed AAB using ignored read-only maintainer
  signing links in the isolated clone.
- Authoritative `python3 tools/verify_host.py` passed: debug/androidTest assembly, 99.82% Partition A
  with the exact 15-line residual manifest, 132 tooling tests, nine coverage-tool tests, 195 harness
  self-tests, 153 documentation checks with 24 optional-private skips, Python compilation, and
  `git diff --check`.
- One gate issue was root-fixed (`GATE_FIXES=1`): the new still-decoder Ready ownership initially
  appeared as an unexpected coverage residual; a direct production-seam test closed it.
- Nine fine-grained GPG-signed Conventional Commits with gitmoji were pushed before this closeout.
  Deployment was not run (`DEPLOY_MODE=none`), and no device action was taken.
- All work ran in `/tmp/find-x9-ultra-cycle50.ZrnMqN`. The shared dirty `main` checkout was never
  edited, staged, stashed, reset, checked out, cleaned, or used for a build; its uncommitted delta
  remains intact.

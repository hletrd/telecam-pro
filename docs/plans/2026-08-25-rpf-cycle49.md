# Review-plan-fix cycle 49 — implementation plan

Date: 2026-08-25
Source: `.context/reviews/_aggregate.md`, cycle 49
Status: complete; all eight findings fixed and every configured quality gate green

## Repository rules applied

- Preserve measured PMA110 Camera2 behavior, capability-enumerated generic-device paths, exact
  lifecycle/native ownership, durable media truth, the quiet Sony-style viewfinder, EN/KO parity,
  and explicit device-evidence limits.
- Schedule every one of the eight aggregate findings below. Nothing is silently dropped,
  severity-downgraded, or relabeled as optional.
- Keep completed historical plans in `docs/plans/` as the non-destructive archive. No completed plan
  needs moving this cycle; dated corrections may be appended where a historical evidence claim was
  too broad.
- Work only in `/tmp/find-x9-ultra-cycle49.oXnMVe/repo`; never modify, stash, reset, check out,
  clean, or build from the shared dirty `main` checkout.
- Use fine-grained GPG-signed Conventional Commits with gitmoji, pull with rebase before every push,
  and do not deploy because `DEPLOY_MODE=none`.

## P1 — make capture-family tracing release-safe (C49-01)

- [x] Replace the split build admission / nullable trace payload with one build-aware, nullable-safe
  trace plan whose release form is an inert observer.
- [x] Keep registration, terminal ownership, producer-lease close, deleted-family retirement, and
  sequence completion independent of diagnostics on every exception path.
- [x] Add a pure debug/release matrix for ordinary Single, in-recording snapshot, and sequence drives,
  plus a release-source contract proving no debug-only payload can be force-unwrapped.

## P2 — serialize video-pipeline decisions with rollback (C49-02)

- [x] Derive active transfer, ten-bit boundary change, baseline, and packet publication from current
  Engine state under the same monitor used by optics rollback.
- [x] Ensure a real boundary change owns a newer optics generation and reopen, while the fast packet
  path revalidates current mode before publishing GL/Engine truth.
- [x] Add a deterministic latch-controlled regression that overlaps pipeline derivation with owned
  Photo/SDR rollback and proves the result cannot be a Photo + active-HLG hybrid.

## P3 — make PNG screenshot validation total and specification-aware (C49-03)

- [x] Enforce PNG critical-chunk order, one legal pre-IDAT PLTE where the color type permits it, and
  palette length/cardinality rules.
- [x] Bound decompression without `flush(0)` or uncaught length errors; reject overlong raster data as
  an ordinary failed check while preserving EOF, tail, filter, and geometry checks.
- [x] Add digest-refreshed fixtures for post-IDAT, duplicate, forbidden, and malformed PLTE plus an
  exactly-one-byte-overlong decoded raster.

## P4 — make viewfinder keyboard activation one-shot (C49-04)

- [x] Admit only the initial DOWN for Enter, Numpad Enter, Space, and DPAD-center, without changing
  touch or TalkBack behavior.
- [x] Add production Compose tests for repeat DOWN suppression and a fresh second physical press for
  every mapped key family.

## P5 — restore review focus after Delete-dialog dismissal (C49-05)

- [x] Give Delete an exact focus requester and restore that origin only after the nested dialog has
  left composition for Cancel, Back, and outside/scrim dismissal.
- [x] Assert Delete owns focus after each route before proving continued traversal and actions.
- [x] Append a dated correction to the completed cycle-48 plan identifying the new focus-return
  evidence without rewriting the historical completion record.

## P6 — mark the AppOps silence note as historical (C49-06)

- [x] Rewrite the contradictory Play-submission paragraph as an explicitly historical pre-fix
  observation and point to the current blocked-camera status plus Settings action.
- [x] Extend the docs gate and tests so the active phrase `open UX gap` cannot recur outside an
  explicitly superseded/historical context.

## P7 — close obscured-gesture cancellation evidence (C49-07)

- [x] Assert the exact canceled pinch trace, including exactly one terminal edge, before starting
  the recovery gesture.
- [x] Assert an obscured slider stream never lands its hostile move/cancel coordinate, then prove the
  next clean stream independently.
- [x] Add mutation-sensitive assertions that fail if cancel termination is gated on a clean UP.

## P8 — route rollback tests through production REC admission (C49-08)

- [x] Extract a narrow Android-free recording-admission decision seam consumed by
  `beginRecordingAllocation` rather than duplicating its policy in tests.
- [x] Exercise the exact seam with the rollback-owned HLG and SDR packets, including ordered
  candidates, accepted transfer, superseded failure, and unavailable-status outcomes.
- [x] Append a dated correction to cycle 48 identifying the production admission evidence added.

## Quality gates and delivery

- [x] Run focused tests for capture tracing, pipeline/rollback concurrency, PNG validation,
  keyboard activation, nested-modal focus, docs invariants, gesture cancellation, and recording
  admission, plus `git diff --check`.
- [x] Run `./gradlew :app:assembleDebug :app:testDebugUnitTest :app:lintDebug` across the whole repo.
- [x] Run `./gradlew :app:lintRelease :app:assembleRelease :app:bundleRelease` across the whole repo.
- [x] Run the authoritative supporting host gate `python3 tools/verify_host.py`.
- [x] Root-fix every gate error and warning without suppression; record any authorized unavoidable
  warning under the same deferred-finding rules.
- [x] Commit and push each independent review, plan, implementation, test, docs, and closeout change
  with signed Conventional Commits plus gitmoji, pulling with rebase before every push.
- [x] Mark this plan complete only after exact evidence is delivered.

## Deferred findings

None. All eight cycle-49 findings are scheduled for this cycle.

## Completion evidence

- All eight deduplicated findings were implemented; none was deferred. Release Single capture now
  receives an inert trace admission and cannot dereference debug-only payload, while family terminal
  ownership stays outside diagnostic branches.
- Video-pipeline mutation is serialized on the Engine monitor shared by rollback. A deterministic
  blocked-thread regression forces rollback ahead of a queued pipeline command and proves Photo
  retains active SDR while preserving the requested next-Video HLG profile.
- Screenshot PNG validation now enforces PLTE order/type/cardinality and treats a one-byte decoded
  overrun as a bounded failure. Post-IDAT, duplicate, forbidden, malformed, and overlong fixtures all
  fail without traceback.
- Viewfinder activation ignores repeated DOWN events for every focus key family and admits a fresh
  press. Review Delete owns exact focus restoration after Back and Cancel through the same dismiss
  callback used by outside dismissal.
- The historical AppOps matrix is explicitly pre-fix, points to the current blocked-camera status,
  and a documentation invariant rejects recurrence of the active `open UX gap` contradiction.
- Obscured pinch coverage asserts its terminal edge before recovery; slider coverage rejects the
  hostile coordinate before an independent clean stream. REC rollback tests now consume the same
  `recordingEncoderAdmission` decision used by production for FPS, codec, transfer, and candidates.
- Configured debug gate passed: debug APK assembly, 2,103 JVM/Robolectric/Compose tests with zero
  failures/skips, and debug lint.
- Configured release gate passed: release lint, R8 APK, and signed AAB. One environment gate fix
  linked the existing ignored upload keystore read-only into this isolated clone; no source or
  configuration suppression was needed.
- Authoritative host gate passed: debug/androidTest assembly, 99.82% Partition A coverage, 130
  tooling tests, nine coverage-tool tests, 195 harness self-tests, 152 documentation checks with 24
  optional-private skips, Python compilation, and `git diff --check`.
- Thirteen fine-grained GPG-signed Conventional Commits with gitmoji were pushed including this
  closeout. Deployment was not run (`DEPLOY_MODE=none`), and no device action was taken.
- All work ran in `/tmp/find-x9-ultra-cycle49.oXnMVe/repo`. The shared dirty `main` checkout was
  never edited, staged, stashed, reset, checked out, cleaned, or used for a build; its uncommitted
  delta remains intact.

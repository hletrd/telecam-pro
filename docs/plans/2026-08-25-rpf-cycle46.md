# Review-plan-fix cycle 46 — implementation plan

Date: 2026-08-25
Source: `.context/reviews/_aggregate.md`, cycle 46
Status: planned; implementation not started

## Repository rules applied

- Preserve measured PMA110 Camera2 behavior, capability-enumerated generic-device paths, exact
  lifecycle/native ownership, durable media truth, the quiet Sony-style viewfinder, EN/KO parity,
  and explicit field-evidence limits.
- Schedule every one of the nine aggregate findings below. Nothing is silently dropped, deferred,
  severity-downgraded, or relabeled as optional.
- Keep completed historical plans in `docs/plans/` as the repository's non-destructive archive; no
  completed plan is deleted, moved, rewritten, or reopened.
- Work only in `/tmp/find-x9-ultra-cycle46.qsEwxz`; never modify, stash, reset, check out, clean, or
  build from the shared dirty `main` checkout.
- Use fine-grained GPG-signed Conventional Commits with gitmoji, pull with rebase before every push,
  and do not deploy because `DEPLOY_MODE=none`.

## P1 — restore mode-derived transfer and microphone owners (AGG46-01)

- [ ] Make the accepted mode transaction own Engine/GL transfer and standby-meter state across
  optimistic changes, successful commits, and rollbacks.
- [ ] Reapply the accepted transfer/codec policy and standby-meter visibility after an owned mode
  rollback without allowing a stale rollback to affect a newer intent.
- [ ] Add forced Video→Photo and Photo→Video rollback tests covering transfer publication, GL sink,
  standby ownership, Ready restoration, and the next recording configuration.

## P2 — restore route-derived punch-in state (AGG46-02)

- [ ] Re-resolve punch-in after the accepted camera route is restored during an owned facing
  rollback, preferably through one route-derived renderer replay boundary.
- [ ] Preserve the independent user intent while ensuring FRONT resolves false and restored rear
  routes resolve the current intent.
- [ ] Add failed front-entry/front-exit tests, including replay into a replacement GL generation.

## P3 — serialize MR recall status and timer ownership (AGG46-03)

- [ ] Publish the recalled status and dismissal timer through the same status gate used by Engine
  events, without splitting the state write from timer arming.
- [ ] Add a latch-controlled recall/event interleaving regression and a source-level or behavioral
  fence proving every production timer arm is gate-owned.

## P4 — make strict runtime skips incomplete (AGG46-04)

- [ ] Classify caught runtime `Skip` as incomplete in strict full/reliability runs unless
  `--allow-partial` is active, while preserving smoke-tier and explicit-partial behavior.
- [ ] Add framework coverage for pass-plus-runtime-skip across strict and partial modes and ensure
  attestation/summary language matches the exit result.

## P5 — bind kill-window evidence to the attempted capture (AGG46-05)

- [ ] Freeze Single drive and requested still outputs, join the started capture to its exact family,
  and require the complete expected image delta after relaunch.
- [ ] Freeze `RecordingSpec`, require exactly its canonical MP4 stem, and reject unrelated deltas.
- [ ] Add fake-harness negative tests for an unrelated late MP4, a missing still sibling, and a
  wrong recording stem without weakening the kill-window timing exercised by the cases.

## P6 — normalize timelapse interval at one shared domain (AGG46-06)

- [ ] Define one 1–30 second interval authority and apply it at settings/MR load, live Engine ingress,
  scheduling, and UI presentation boundaries.
- [ ] Test below-range, in-range, and above-range values for main settings and MR presets, including
  a running sequence using only the normalized value.

## P7 — distinguish gallery thumbnail states (AGG46-07)

- [ ] Model empty, loading, ready, and failed thumbnail states explicitly for still, video, and RAW.
- [ ] Reserve the no-capture pictogram for a null URI and add quiet media-specific loading/fallback
  visuals that remain consistent with localized semantics.
- [ ] Add production-composition branch/pixel coverage for null URI, non-null loading, failed still,
  failed video, RAW, and a ready bitmap.

## P8 — govern tablet Play screenshot assets (AGG46-08)

- [ ] Add committed digest/provenance/copy/readiness authority for every tracked tablet screenshot,
  using a fail-closed upload verdict where immutable capture provenance is unavailable.
- [ ] Add clean-clone tablet submission guidance and make `tools/check_docs.py` validate it without
  relying on optional private listing documents.
- [ ] Add/update tooling tests for missing, mismatched, stale, and explicitly blocked tablet assets.

## P9 — deliver the converter responsive coverage previously claimed (AGG46-09)

- [ ] Extend the responsive Compose matrix to the real Phone and Converter consumers/labels/options,
  including localized generic/custom converter identities at 212 dp and fontScale 2.0 in LTR/RTL.
- [ ] Assert trigger and popup bounds plus radio-selection semantics, then append a dated correction
  to the completed cycle-45 record rather than rewriting its historical claim.

## Quality gates and delivery

- [ ] Run focused tests for each owner, policy, harness, persistence, presentation, and documentation
  seam plus `git diff --check`.
- [ ] Run `./gradlew :app:assembleDebug :app:testDebugUnitTest :app:lintDebug` against the whole repo.
- [ ] Run `./gradlew :app:lintRelease :app:assembleRelease :app:bundleRelease` against the whole repo.
- [ ] Root-fix every gate error and warning without suppression.
- [ ] Commit and push each independent review, plan, implementation, test, documentation, and
  closeout change with signed Conventional Commits plus gitmoji, pulling with rebase before push.
- [ ] Mark this plan complete only after exact evidence is delivered.

## Deferred findings

None. All nine cycle-46 findings are scheduled for this cycle.

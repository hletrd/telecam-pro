# Review-plan-fix cycle 20 — implementation plan

Date: 2026-08-24
Source: `.context/reviews/_aggregate.md`, cycle 20
Status: complete; implementation, configured gates, signed commits, and pushes finished

## Repository rules applied

- Preserve PMA110 Camera2/GL/capture behavior and every measured device workaround. This cycle is
  limited to exact DISCARD synchronization, finite DNG publication ownership, release-verifier
  source identity, ISO accessibility localization, their tests, and directly affected architecture.
- Every aggregate finding AGG20-01 through AGG20-04 is mandatory. None is deferred,
  severity-downgraded, or silently dropped.
- Preserve provider-safe pending rows, exact URI deletion authority, process-finite storage owners,
  capture-family terminality, and wrapper-only immutable release evidence.
- Preserve English/Korean parity. Camera abbreviations may stay Latin, but user-facing prose such as
  the automatic-value qualifier must use the existing localized resources.
- Completed prior plans remain non-destructive archives in `docs/plans/`; cycle 19 is complete and
  requires no move or deletion.
- Use fine-grained GPG-signed Conventional Commits with gitmoji, pull with rebase before every push,
  never force-push, and never bypass configured gates.

Every aggregate finding is scheduled below. Deferred findings: none.

## Phase 1 — isolate exact DISCARD ownership from unrelated provider stalls

- [x] **P1 / AGG20-01:** replace the process-global monitor-spanning publication transaction with
  exact-URI or bounded-stripe serialization shared by publication, mark, and remove. SQLite helper
  operations may retain their short global lock, but no provider Binder call or retry sleep may hold
  it.
- [x] Preserve the same-URI invariant: once DISCARD wins, publication cannot report success or erase
  its marker; if publication wins first, a later mark must still commit and delete/retain safely.
- [x] Add deterministic interleaving tests for publish-first and mark-first on one URI, plus a
  two-URI barrier proving URI B can durably mark, query, remove, and page while URI A remains blocked
  in `ContentResolver.update`.
- [x] Update `docs/ARCHITECTURE.md` to describe the implemented short-lock/per-URI protocol and state
  explicitly that the journal monitor never spans MediaProvider I/O.

Acceptance: a non-returning provider call for A cannot prevent exact durable ownership or recovery
progress for B, while publication and DISCARD remain linearized for the same URI.

## Phase 2 — bound RAW-only DNG publication tails across Engine generations

- [x] **P2 / AGG20-02:** introduce a finite process-wide still-publication owner or unified budget
  that admits DNG publication continuations before they enter any unbounded executor queue. The
  bound must survive Engine replacement and cover active plus queued RAW-only SINGLE captures.
- [x] On saturation or executor rejection, terminally release the live capture-family continuation,
  report a truthful delayed-save/recovery result, and retain the already-complete private row for
  launch recovery. Never delete durable DNG bytes merely because live publication capacity is full.
- [x] Add active+queued saturation, overflow-to-recovery, repeated-Engine, exact-once terminal, and
  queue-cardinality tests. Preserve burst/AEB/timelapse chaining and processed-still budget behavior.
- [x] Document the finite DNG/still-tail ownership and saturation disposition in
  `docs/ARCHITECTURE.md` and the applicable project authority comment.

Acceptance: arbitrarily many RAW-only shutter presses cannot create an unbounded executor backlog or
retain unbounded family owners; every overflow row remains provider-private and recoverable.

## Phase 3 — bind upload verification to the final repository owner

- [x] **P3 / AGG20-03:** snapshot initial HEAD, exact porcelain status, and source-version input
  identity. Immediately before success, after all external tools, re-read HEAD/status and require
  exact equality; no-follow snapshot and final-revalidate `app/build.gradle.kts` or remove it as a
  mutable second authority by deriving version truth from attested source.
- [x] Fail closed on a clean commit change, a dirty-tree change, build-script path replacement, and
  in-place A→B→A mutation at both early and late external-tool boundaries.
- [x] Extend focused release-artifact tests while preserving the unchanged upload-ready fixture and
  cycle-19 attestation/AAB/evidence/sidecar identity protections.

Acceptance: `upload-ready` describes both the authenticated artifact and the repository state that
still exists at the exact return boundary.

## Phase 4 — localize automatic ISO ruler semantics

- [x] **P4 / AGG20-04:** derive the ISO ruler's automatic `stateDescription` from the existing
  `R.string.a11y_auto_value` resource while preserving normal `ISO N` semantics and the Latin camera
  abbreviation.
- [x] Add a production Compose regression that opens the real ISO dial and asserts Korean auto ISO,
  English auto ISO, and non-auto ISO adjustable-node state descriptions.

Acceptance: visible and TalkBack semantics remain in the selected locale throughout the ISO control.

## Phase 5 — gates, commits, push, and closeout

- [x] Run `./gradlew :app:assembleDebug :app:testDebugUnitTest :app:lintDebug` across the repository.
- [x] Run `./gradlew :app:lintRelease :app:assembleRelease :app:bundleRelease` on committed source.
- [x] Run focused storage/camera/UI/release-tool tests, `python3 tools/verify_host.py`, Python
  compilation, exact Partition-A coverage verification, documentation checks, and whitespace checks.
- [x] Root-fix every error and actionable warning without suppression. Record any genuinely
  unfixable new warning under the strict deferred format before commit.
- [x] Commit and push each independent fix with GPG-signed Conventional Commit + gitmoji messages,
  pulling with rebase before every push. Verify signatures and upstream synchronization, mark this
  plan complete, and do not deploy or perform physical-device actions.

## Planned commit sequence

`docs(review)`, `docs(plan)`, `fix(storage)`, `perf(capture)`, `fix(provenance)`, `fix(a11y)`,
`docs(architecture)`, `test(coverage)`, and `docs(plan)`, split further when one commit would combine
unrelated behavior.

## Deferred findings

None. Existing owner-approved field checks, upstream warnings, and product/design deferrals in
`docs/BACKLOG.md` remain unchanged and are not cycle-20 review findings.

## Completion evidence

- The authoritative host gate passed at exact source commit `007a92a`: **1,814**
  JVM/Robolectric/Compose tests, debug assembly/lint, **65** tool/release tests, **9**
  coverage-tool tests, **182** device-harness self-tests, **110** documentation checks, Python
  compilation, and the complete HEAD-to-working-tree whitespace check.
- Coverage is **56.17% overall (14,037/24,992)** and **99.82% Partition A (7,024/7,037)**. The
  exact approved **13-line / 9-class** residual remains unchanged.
- The configured clean-HEAD release gate passed `:app:lintRelease`, `:app:assembleRelease`, and
  `:app:bundleRelease` at `007a92a`, including R8, signed APK packaging, and signed AAB packaging.
  This cycle did not create an immutable release candidate or deploy.
- No gate errors or new actionable warnings were found or masked, so `GATE_FIXES = 0`.
- Seven cycle commits through `007a92a` were GPG-signed, individually pull-rebased, pushed, and
  synchronized with `origin/main`. This closeout is the eighth signed/pushed cycle commit.
- Deployment and physical-device actions remained disabled (`DEPLOY_MODE=none`).

## Residual warnings

- **Low / High — `app/src/main/kotlin/me/hletrd/telecampro/storage/MediaStoreWriter.kt:310,374,405`:**
  the three existing `UseKtx` suggestions remain because these durability gates consume the Boolean
  result of synchronous `SharedPreferences.Editor.commit()`; the KTX `edit(commit = true)` extension
  returns `Unit` and would erase failure truth. Reopen when AndroidX exposes a result-bearing
  synchronous edit or an equally explicit, tested helper preserves the exact Boolean contract.
- **Low / High — `app/build.gradle.kts:566`:** `NotShrinkingResources` remains an existing,
  owner-recorded release-size recommendation. Code minification is enabled; resource shrinking is a
  separate product/release choice. Reopen when the owner elects to enable it and the resulting
  minified resource set receives the repository's release/device validation.
- **Low / High — `gradle.properties:25-29`:** AGP continues to label
  `android.experimental.reportAggregationSupport=true` experimental. It is required for the
  repository's unified coverage registration and is not suppressed. Reopen when AGP stabilizes the
  option or the project retires the aggregate coverage task.

# Review-plan-fix cycle 42 — implementation plan

Date: 2026-08-24
Source: `.context/reviews/_aggregate.md`, cycle 42
Status: complete; all five findings fixed and every configured gate green

## Repository rules applied

- Preserve measured PMA110 behavior, capability-enumerated multi-device paths, the quiet Sony-style
  UI, EN/KO parity, fail-closed media durability, and existing device-evidence boundaries.
- Schedule every aggregate finding below. Nothing is silently dropped, deferred,
  severity-downgraded, or relabeled as optional.
- Keep completed historical plans as non-destructive archives; only correct the factual cycle-41
  evidence count identified by this review.
- Work only in `/tmp/find-x9-ultra-cycle42.rPLjyN`; never modify, stash, reset, check out, or build
  from the shared dirty `main` checkout.
- Use fine-grained GPG-signed Conventional Commits with gitmoji, pull with rebase before every push,
  and do not deploy because `DEPLOY_MODE=none`.

## P1 — make disabled immediate-command paint truthful (AGG42-01)

- [x] Resolve command-chip container, content, and border paint from both `active` and `enabled`.
- [x] Preserve click-only Button semantics, active identity, compact geometry, and existing action
  admission while making every disabled state visibly disabled.
- [x] Replace the production-impossible Custom-WB test state with the reachable matrix:
  inactive+enabled, inactive+disabled, and active+disabled; retain role/click/no-selection checks.

## P2 — group dropdown radio choices semantically (AGG42-02)

- [x] Apply `selectableGroup()` to the Phone/Converter popup option container without disturbing
  popup bounds, scrolling, item selection, or leaf radio semantics.
- [x] Extend composed-tree tests to require one selectable-group ancestor for the mutually
  exclusive options before and after selection.

## P3 — suppress the redundant zoom end submission (AGG42-03)

- [x] Model the complete start/move/quiet/repinch/end transition and whether exact framing has
  already landed on the Camera2 wire.
- [x] On no-FPS-change routes, clear boost/interaction state without resubmitting the same exact
  ratio after quiet landing; preserve the end rebuild when FPS restoration genuinely requires it.
- [x] Add lifecycle-level request-sequence regressions covering fresh gestures, quiet landing,
  repinch cancellation, end before landing, and FPS-changing versus no-FPS-change routes.

## P4 — give zoom edge math one real owner and align authority (AGG42-04)

- [x] Extract one pure start-edge wide-target resolver used by `setZoomInteraction`, with direct
  margin and Camera2-range clamp tests.
- [x] Remove the discarded wide-target output and any dead inputs from the moving-tick plan while
  preserving exact still-truth projection and unconditional moving Camera2 suppression.
- [x] Correct malformed/fixed-count/throttle wording in source KDoc/comments, `CLAUDE.md`, and
  `docs/ARCHITECTURE.md` to describe route-specific start, optional quiet landing, and end behavior.
- [x] Add a narrow source/document contract that rejects the retired throttle/false-owner wording.

## P5 — correct cycle-41 gate evidence (AGG42-05)

- [x] Correct the archived completion record from 104 to the committed 106 tooling tests while
  preserving all other historical evidence.

## Quality gates and delivery

- [x] Run focused command-paint, dropdown-semantics, zoom-transition, zoom-math, and documentation
  tests plus `git diff --check`.
- [x] Run `./gradlew :app:assembleDebug :app:testDebugUnitTest :app:lintDebug` against the whole repo.
- [x] Run `./gradlew :app:lintRelease :app:assembleRelease :app:bundleRelease` against the whole repo.
- [x] Root-fix every gate error and warning without suppression.
- [x] Commit and push each independent review, plan, implementation, test, documentation, and
  closeout change with signed Conventional Commits plus gitmoji, pulling with rebase before push.
- [x] Mark this plan complete only after exact evidence is delivered.

## Deferred findings

None. All five cycle-42 findings are scheduled for this cycle.

## Completion evidence

- All five deduplicated findings were implemented; nothing was deferred. Immediate commands now
  dim container, content, and border across the reachable active/enabled states without regaining
  selectable semantics. Phone/Converter popup options now share one selectable-group ancestor.
- Zoom start math has one executable/tested owner. Moving ticks contain no dead wide target and
  remain Camera2-silent while retaining exact still truth. The interaction state records quiet
  landing ownership, so a no-FPS-change end is state-only after exact framing lands; FPS-changing
  routes retain their required rebuild. Fresh start, end-before-quiet, quiet landing, repinch,
  inactive timer, and both route classes are host-tested.
- Source KDoc/comments, `CLAUDE.md`, and `docs/ARCHITECTURE.md` describe the route-specific
  start/quiet/end model. A committed-export negative contract rejects the retired periodic-throttle,
  dead-owner, and fixed-edge wording. Cycle 41's archived tooling evidence now records the correct
  106-test suite present at that closeout.
- Focused command, dropdown, zoom, and documentation tests passed. The configured debug gate passed
  with 2,029 JVM/Robolectric/Compose tests, zero failures/errors/skips, and Android lint green. The
  configured release lint, signed APK, and signed AAB gate passed from clean committed source using
  read-only links to the existing gitignored signing inputs. The prebuilt native strip notice is
  expected packaging diagnostics, not an Android-lint warning.
- The authoritative `python3 tools/verify_host.py` passed: Partition A 8,066/8,081 lines (99.81%),
  107 tooling tests, nine coverage-tool tests, 184 device-harness self-tests, 127 documentation
  checks, Python compilation, Android assembly/test/lint, and `git diff --check` were all green.
- Seven fine-grained GPG-signed Conventional Commits with gitmoji were pushed after pull-with-rebase
  before this closeout; this completion record is the eighth. Deployment was not run
  (`DEPLOY_MODE=none`), and no device action was taken.
- Work remained in `/tmp/find-x9-ultra-cycle42.rPLjyN`. The shared dirty `main` checkout was never
  edited, staged, stashed, reset, checked out, or used for a build; its pre-existing CameraEngine,
  MediaStore, UI, test, and tooling deltas remain untouched.

# Review-plan-fix cycle 30 — implementation plan

Date: 2026-08-24
Source: `.context/reviews/_aggregate.md`, cycle 30
Status: complete; all nine findings fixed, configured gates green, signed commits pushed

## Repository rules applied

- Preserve measured PMA110 behavior, capability-routed Android 13+ behavior, EN/KO presentation,
  quiet-camera UX, media durability, and wrapper-owned release evidence.
- Every cycle-30 finding is scheduled below. Nothing is deferred, silently dropped,
  severity-downgraded, or introduced as new scope under a deferred label.
- Completed plans remain non-destructive archives in `docs/plans/`; no completed plan requires a
  move or deletion.
- Use GPG-signed Conventional Commits with gitmoji, pull with rebase before every push, and do not
  deploy because `DEPLOY_MODE=none`.
- Perform all work in the isolated clean cycle clone. Preserve the shared main worktree and every
  pre-existing CameraEngine/storage/UI/test/tool delta without touching, staging, stashing,
  resetting, restoring, or terminating its processes.

## P1 — make private recovery state non-transferable (AGG30-01)

- [x] Exclude the database domain from Android 12+ cloud backup and device transfer; align the
  legacy backup schema with the manifest's no-backup policy.
- [x] Add a parsed-resource contract proving exact-URI recovery state cannot transfer to another
  device and future private database state is excluded by default.
- [x] Keep current on-device launch recovery behavior unchanged.

## P2 — retain a bounded retirement retry after lane saturation (AGG30-02)

- [x] Extend the process-finite retirement dispatcher with bounded/conflated overflow ownership
  that re-arms after capacity becomes available; never run provider work inline.
- [x] Re-evaluate exact family absence and producer leases at retry execution time, retaining the
  durable marker on provider failure.
- [x] Add deterministic saturation/drain tests proving an overflowed family eventually retires and
  marker capacity recovers without process restart.

## P3 — bound ViewModel family-delete provider work (AGG30-03)

- [x] Route whole-family and rejected-late-sibling provider effects through process-finite capacity
  without losing their durable deletion owner or serial ordering.
- [x] Make overflow/failure outcomes truthful to UI and recovery; do not retain unbounded obsolete
  ViewModel closures or execute provider calls inline.
- [x] Add blocked-worker/replacement tests that pin the exact active-plus-backlog ceiling.

## P4 — propagate exact-family sweep uncertainty (AGG30-04)

- [x] Replace the integer sweep result with typed discovered/deleted/unresolved/query-failed state.
- [x] Merge untracked sweep uncertainty with known-output survivors before publishing the terminal
  delete status; preserve Gallery retry copy and the durable family marker.
- [x] Add provider seam tests for clean-empty, already-absent, undeletable, and query-failed sweeps.

## P5 — recall phone, converter, and host focal as one packet (AGG30-05)

- [x] Resolve the phone-identity ownership contract for MR recall and derive the host focal from the
  same phone declaration used to reconcile the converter.
- [x] Apply/publish phone, converter, and host focal consistently so UI, Engine shot state, memory
  row, and EXIF input cannot diverge.
- [x] Add same-route cross-phone regressions covering 70 mm and 85 mm host declarations.

## P6 — roll back the complete converter packet (AGG30-06)

- [x] Include converter magnification, host focal, and declaration identity in generation-owned
  optics snapshots and terminal rollback publication, or delay their commit until acceptance.
- [x] Preserve supersession ownership: only the active generation may commit or restore the packet.
- [x] Add deterministic synchronous refusal, asynchronous route/open failure, and superseding recall
  regressions for converter, host focal, zoom/finder geometry, and shot metadata.

## P7 — make settings value rows responsive (AGG30-07)

- [x] Give `LabelValueRow` an explicit compact/large-font layout that reserves the trailing value or
  action while letting the label wrap/ellipsize or stack.
- [x] Preserve row-level semantics, disabled appearance, 48 dp interaction targets, RTL behavior,
  and quiet Sony-style hierarchy.
- [x] Add EN/KO 320 dp, 2x-font Compose bounds and activation tests for Privacy, long dynamic value,
  and disabled readout cases.

## P8 — keep permission recovery reachable in compact windows (AGG30-08)

- [x] Add bounded vertical scrolling or adaptive compact arrangement to the permission gate with
  safe insets and stable focus/live-region behavior.
- [x] Add EN/KO 320x340 dp, 2x-font tests that trigger Privacy and Settings navigation failures,
  reach every recovery action, and activate the in-app privacy route.

## P9 — compile instrumented sources in the host gate (AGG30-09)

- [x] Add `:app:assembleDebugAndroidTest` to `tools/verify_host.py` without claiming device execution.
- [x] Pin the task in consolidated host-gate contracts and update architecture/gate documentation.

## Quality gates and delivery

- [x] Run focused tests for each fix, documentation/tool contracts, and `git diff --check`.
- [x] Run `./gradlew :app:assembleDebug :app:testDebugUnitTest :app:lintDebug` against the whole repo
  and root-fix every error/actionable warning without suppression.
- [x] Run `./gradlew :app:lintRelease :app:assembleRelease :app:bundleRelease` against clean committed
  source and root-fix every error/actionable warning without suppression.
- [x] Commit and push each independent fix and review/plan update using signed Conventional Commits
  plus gitmoji, always pulling with rebase before each push.
- [x] Update this plan with exact gate evidence and mark it complete only after every item is done.

## Deferred findings

None.

## Completion evidence

- The focused serial Gradle suite passed for retirement saturation, typed family sweep, finite
  ViewModel deletion, transactional optics recall/rollback, compact permission recovery, and
  responsive settings rows. Backup and consolidated-host Python contracts passed; documentation
  checks passed 93/93 with 21 explicit optional-private skips; `git diff --check` passed.
- `./gradlew :app:assembleDebug :app:testDebugUnitTest :app:lintDebug` passed against the whole repo.
- `./gradlew :app:lintRelease :app:assembleRelease :app:bundleRelease` passed against clean committed
  source, including release lint, R8, signed APK assembly, and signed AAB assembly. The isolated clone
  referenced existing ignored signing inputs through symlinks without copying or printing secret
  values; direct Gradle outputs remain developer-only and no immutable release evidence is claimed.
- The first release invocation stopped only at signing validation because the clone-relative JKS was
  absent. Linking the existing ignored JKS fixed environment wiring; the unchanged gate then passed.
  No lint/build/test error or actionable warning required a source fix, suppression, threshold
  change, or deferred record (`GATE_FIXES=0`).
- Ten review/plan/implementation/authority commits were already GPG-signed, pull-rebased, and pushed
  before this closeout. This closeout is the final signed/pushed cycle commit.
- The shared main worktree and all pre-existing CameraEngine/storage/UI/test/tool deltas remained
  untouched. No stash, reset, restore, checkout, or process termination was performed there.
- Deployment and physical-device mutation were not performed (`DEPLOY_MODE=none`).

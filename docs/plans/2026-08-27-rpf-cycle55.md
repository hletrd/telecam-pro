# Review-plan-fix cycle 55 — implementation plan

Date: 2026-08-27
Source: `.context/reviews/_aggregate.md`, cycle 55
Status: complete; all eight findings fixed and every configured quality gate green

## Repository rules applied

- Preserve measured PMA110 behavior, capability-enumerated generic routes, exact native/provider
  identity, durable MediaStore truth, process-finite work, the quiet Sony-style UI, EN/KO parity,
  and explicit device-evidence limits.
- Schedule every one of the eight aggregate findings below. Nothing is silently dropped,
  severity-downgraded, deferred, relabeled optional, or treated as closed by documentation alone.
- Keep completed historical plans in `docs/plans/` as the repository's non-destructive archive.
  Existing plans are complete; none is moved, deleted, rewritten, or reopened. Where cycle-54
  evidence overclaimed DNG liveness, append a dated correction rather than rewriting history.
- Work only in `/tmp/find-x9-ultra-cycle55.32UR9V`; never modify, stash, reset, restore, check out,
  clean, or build from the shared dirty `main` checkout.
- Use fine-grained GPG-signed Conventional Commits with gitmoji, pull with rebase before every push,
  and do not deploy because `DEPLOY_MODE=none`.

## P1 — restore exact DNG shutter admission truth (AGG55-01, Medium/High)

- [x] Include process DNG ownership in the authoritative still-output availability projection so a
  preallocated RAW request serializes processed and RAW presses truthfully.
- [x] Centralize DNG lease release and availability publication as one idempotent terminal used by
  success, controller error, synchronous dispatch failure, timeout, and pre-camera retirement.
- [x] Add production-wiring coverage for rapid second press followed by success and error; prove the
  shutter re-enables without an unrelated recovery callback.

## P2 — clean every incomplete preallocated DNG row (AGG55-02, Medium/High)

- [x] Transfer the already-reserved exact rejected-output cleanup on Camera2 refusal/watchdog,
  unexpected raw-null delivery, and every other terminal that produced no complete DNG bytes.
- [x] Cancel cleanup only when complete-byte ownership transfers to publication/recovery; make the
  terminal exactly-once so callback/error races cannot double-submit or leak.
- [x] Test controller refusal, raw-null, cleanup rejection/saturation, and eventual exact-row
  disposition without provider work on the Camera2 callback.

## P3 — own provider-identity freeze failures in-process (AGG55-03, Medium/High)

- [x] Preserve typed `REGISTERED_WITHOUT_IDENTITY` output truth instead of collapsing a successful
  insert + REGISTERED commit to null when identity is unavailable/ambiguous/mismatched.
- [x] Add a finite process owner that retries identity capture non-destructively, discards only after
  exact identity is proven, retains unresolved rows, and closes new image/video admission at its
  bounded capacity.
- [x] Cover image and video factories, repeated failures, bounded capacity, retry success, late exact
  cleanup, and process admission publication.

## P4 — deadline DNG pre-native allocation (AGG55-04, Medium/High)

- [x] Arm an exact first-wins deadline before DNG provider dispatch, using the same trusted scheduled
  owner pattern as REC without interrupting a running Binder call.
- [x] Timeout must retire capture/family/snapshot/DNG/cleanup ownership promptly, publish truthful
  failure, and route a late exact row to bounded cleanup/recovery.
- [x] Test timeout-vs-return, timeout-vs-optics cancellation, scheduler rejection, late rows, and
  shared pre-native lane reuse through production seams.

## P5 — process-bound every processed sequence snapshot (AGG55-05, Medium/High)

- [x] Require BURST, AEB, and timelapse processed snapshots to reserve process-wide capacity before
  Camera2 dispatch, while preserving each sequence's existing save-completion chaining/order.
- [x] Ensure every callback, refusal, cancellation, chain stop, executor rejection, and old-Engine
  completion releases the exact lease once.
- [x] Test a blocked old-Engine sequence save against replacement-Engine BURST/AEB/timelapse and
  SINGLE admission, including exact reuse after terminal completion.

## P6 — make late-value retirement exception-safe (AGG55-06, Medium/High)

- [x] Put `onLateValue` under a retirement `try/finally` so `completeRetirement` always runs exactly
  once even when cleanup or observer code throws.
- [x] Preserve cleanup failure diagnostics without permitting it to retain DNG or REC admission.
- [x] Add throwing late-cleanup tests for DNG cancellation/session supersession and video timeout.

## P7 — close the process diagnostic emission boundary (AGG55-07, Medium/High)

- [x] Route Single capture-family, ShutterLag, ZSL refusal/serve, and every other action-repeatable
  debug producer through the process diagnostic budget or one equivalent bounded capture trace owner.
- [x] Keep harness-required registration/settlement and first/result timing evidence while budget is
  available; never weaken the 120-row reserve for startup, frame gaps, recovery, and faults.
- [x] Extend the executable budget composition and source-contract tests so an unowned recurring
  `Log` call cannot restore this gap.

## P8 — bind StartupTrace to one resume/controller generation (AGG55-08, Low/High)

- [x] Make `StartupTrace.begin()` return an opaque generation token and require exact ownership for
  every mark, finish, and disarm operation.
- [x] Pass that token only into the controller/open/request created for the armed resume attempt;
  stale old-controller and replaced-request callbacks must be inert.
- [x] Test old-first-result/new-resume, two-request, disarm/rearm, and idempotent-begin interleavings,
  and append the corrected ownership contract to architecture authority.

## Quality gates and delivery

- [x] Run focused tests for every ownership, timeout, storage, snapshot, diagnostic, and trace change.
- [x] Run `./gradlew :app:assembleDebug :app:testDebugUnitTest :app:lintDebug` across the whole repo.
- [x] Run `./gradlew :app:lintRelease :app:assembleRelease :app:bundleRelease` across the whole repo.
- [x] Run `python3 tools/verify_host.py` because repository authority requires the complete
  non-device host suite for delivery.
- [x] Root-fix every gate error and warning without suppression; record only repo-authorized
  unavoidable warnings under the strict deferred-finding rules.
- [x] Commit and push each independent review, plan, implementation, test, documentation, and
  closeout change with signed Conventional Commits plus gitmoji, pulling with rebase before push.
- [x] Mark this plan complete only after exact gate and delivery evidence is recorded.

## Deferred findings

None. Every cycle-55 finding is scheduled for implementation in this cycle.

One pre-existing gate warning remains explicitly recorded:

- **Low / High — `gradle/verification-metadata.xml:905-909`:** release packaging reports that the
  transitive upstream prebuilt `libandroidx.graphics.path.so` cannot be stripped. APK/AAB packaging
  succeeds, and the repository has no source/symbol owner that can root-fix the binary. Reopen when
  the latest stable `androidx.graphics:graphics-path` dependency becomes strip-compatible. This does
  not defer any cycle-55 security, correctness, data-loss, performance, or documentation finding.

## Completion evidence

- All eight findings were fixed with no review deferral: DNG admission republishes exact shutter
  truth; incomplete preallocated DNG rows enter bounded cleanup; identity-freeze uncertainty has a
  finite process owner; DNG allocation has a first-wins deadline; every processed sequence snapshot
  consumes process capacity; throwing late cleanup cannot suppress retirement; repeatable capture
  diagnostics share the quota owner; and startup traces require exact controller/request ownership.
- Focused DNG, pre-native, identity recovery, processed-snapshot, diagnostic, startup, policy, and
  storage suites passed. The exact debug gate passed assembly, all 2,211 JVM/Robolectric/Compose
  tests, and lint. The exact release gate passed release lint, R8 APK, and signed AAB after attaching
  ignored read-only maintainer signing links; no credential value was read, printed, copied into
  source, or committed.
- `python3 tools/verify_host.py` passed: debug and androidTest assembly, the complete unit/lint gate,
  Partition A at 9,239/9,255 lines (99.83%) with the unchanged exact 16-line residual manifest, 136
  tool tests, nine coverage-tool tests, 195 device-harness self-tests, 155 documentation checks (24
  optional-private skips), Python compilation, and `git diff --check`.
- Six gate issues were root-fixed without suppression: a new generic test needed explicit ownership
  type; the finite identity-capacity expectation was corrected to worker-plus-backlog truth; the
  trace-owner API exposed an internal type and emitted a future-language warning; an isolated
  Robolectric policy suite lacked its EGL sentinel; executable coverage initially exposed new
  residuals; and the capture-trace documentation contract still required the pre-budget source
  shape. A missing clean-clone signing link was recovered operationally and changed no source.
- Twelve fine-grained GPG-signed Conventional Commits with gitmoji were pushed before this closeout;
  this completion record is the thirteenth cycle commit. Deployment and device action were not run
  (`DEPLOY_MODE=none`). Open field checks remain evidence obligations, not deferred code findings.
- All work ran in `/tmp/find-x9-ultra-cycle55.32UR9V`. The shared dirty `main` checkout was never
  edited, staged, stashed, reset, restored, checked out, cleaned, or used for a build; its deltas
  remain intact.

# Review-plan-fix cycle 55 — implementation plan

Date: 2026-08-27
Source: `.context/reviews/_aggregate.md`, cycle 55
Status: in progress; eight findings scheduled, none deferred

## Repository rules applied

- Preserve measured PMA110 behavior, capability-enumerated generic routes, exact native/provider
  identity, durable MediaStore truth, process-finite work, the quiet Sony-style UI, EN/KO parity,
  and explicit device-evidence limits.
- Schedule every one of the eight aggregate findings below. Nothing is silently dropped,
  severity-downgraded, deferred, relabeled optional, or treated as closed by documentation alone.
- Keep completed historical plans in `docs/plans/` as the repository's non-destructive archive.
  Existing plans are complete; none is moved, deleted, rewritten, or reopened. Where cycle-54
  evidence overclaimed DNG liveness, append a dated correction rather than rewriting history.
- Work only in `/tmp/find-x9-ultra-cycle55.32UR9V`; never modify, stash, reset, restore, check out,
  clean, or build from the shared dirty `main` checkout.
- Use fine-grained GPG-signed Conventional Commits with gitmoji, pull with rebase before every push,
  and do not deploy because `DEPLOY_MODE=none`.

## P1 — restore exact DNG shutter admission truth (AGG55-01, Medium/High)

- [ ] Include process DNG ownership in the authoritative still-output availability projection so a
  preallocated RAW request serializes processed and RAW presses truthfully.
- [ ] Centralize DNG lease release and availability publication as one idempotent terminal used by
  success, controller error, synchronous dispatch failure, timeout, and pre-camera retirement.
- [ ] Add production-wiring coverage for rapid second press followed by success and error; prove the
  shutter re-enables without an unrelated recovery callback.

## P2 — clean every incomplete preallocated DNG row (AGG55-02, Medium/High)

- [ ] Transfer the already-reserved exact rejected-output cleanup on Camera2 refusal/watchdog,
  unexpected raw-null delivery, and every other terminal that produced no complete DNG bytes.
- [ ] Cancel cleanup only when complete-byte ownership transfers to publication/recovery; make the
  terminal exactly-once so callback/error races cannot double-submit or leak.
- [ ] Test controller refusal, raw-null, cleanup rejection/saturation, and eventual exact-row
  disposition without provider work on the Camera2 callback.

## P3 — own provider-identity freeze failures in-process (AGG55-03, Medium/High)

- [ ] Preserve typed `REGISTERED_WITHOUT_IDENTITY` output truth instead of collapsing a successful
  insert + REGISTERED commit to null when identity is unavailable/ambiguous/mismatched.
- [ ] Add a finite process owner that retries identity capture non-destructively, discards only after
  exact identity is proven, retains unresolved rows, and closes new image/video admission at its
  bounded capacity.
- [ ] Cover image and video factories, repeated failures, bounded capacity, retry success, late exact
  cleanup, and process admission publication.

## P4 — deadline DNG pre-native allocation (AGG55-04, Medium/High)

- [ ] Arm an exact first-wins deadline before DNG provider dispatch, using the same trusted scheduled
  owner pattern as REC without interrupting a running Binder call.
- [ ] Timeout must retire capture/family/snapshot/DNG/cleanup ownership promptly, publish truthful
  failure, and route a late exact row to bounded cleanup/recovery.
- [ ] Test timeout-vs-return, timeout-vs-optics cancellation, scheduler rejection, late rows, and
  shared pre-native lane reuse through production seams.

## P5 — process-bound every processed sequence snapshot (AGG55-05, Medium/High)

- [ ] Require BURST, AEB, and timelapse processed snapshots to reserve process-wide capacity before
  Camera2 dispatch, while preserving each sequence's existing save-completion chaining/order.
- [ ] Ensure every callback, refusal, cancellation, chain stop, executor rejection, and old-Engine
  completion releases the exact lease once.
- [ ] Test a blocked old-Engine sequence save against replacement-Engine BURST/AEB/timelapse and
  SINGLE admission, including exact reuse after terminal completion.

## P6 — make late-value retirement exception-safe (AGG55-06, Medium/High)

- [ ] Put `onLateValue` under a retirement `try/finally` so `completeRetirement` always runs exactly
  once even when cleanup or observer code throws.
- [ ] Preserve cleanup failure diagnostics without permitting it to retain DNG or REC admission.
- [ ] Add throwing late-cleanup tests for DNG cancellation/session supersession and video timeout.

## P7 — close the process diagnostic emission boundary (AGG55-07, Medium/High)

- [ ] Route Single capture-family, ShutterLag, ZSL refusal/serve, and every other action-repeatable
  debug producer through the process diagnostic budget or one equivalent bounded capture trace owner.
- [ ] Keep harness-required registration/settlement and first/result timing evidence while budget is
  available; never weaken the 120-row reserve for startup, frame gaps, recovery, and faults.
- [ ] Extend the executable budget composition and source-contract tests so an unowned recurring
  `Log` call cannot restore this gap.

## P8 — bind StartupTrace to one resume/controller generation (AGG55-08, Low/High)

- [ ] Make `StartupTrace.begin()` return an opaque generation token and require exact ownership for
  every mark, finish, and disarm operation.
- [ ] Pass that token only into the controller/open/request created for the armed resume attempt;
  stale old-controller and replaced-request callbacks must be inert.
- [ ] Test old-first-result/new-resume, two-request, disarm/rearm, and idempotent-begin interleavings,
  and append the corrected ownership contract to architecture authority.

## Quality gates and delivery

- [ ] Run focused tests for every ownership, timeout, storage, snapshot, diagnostic, and trace change.
- [ ] Run `./gradlew :app:assembleDebug :app:testDebugUnitTest :app:lintDebug` across the whole repo.
- [ ] Run `./gradlew :app:lintRelease :app:assembleRelease :app:bundleRelease` across the whole repo.
- [ ] Run `python3 tools/verify_host.py` because repository authority requires the complete
  non-device host suite for delivery.
- [ ] Root-fix every gate error and warning without suppression; record only repo-authorized
  unavoidable warnings under the strict deferred-finding rules.
- [ ] Commit and push each independent review, plan, implementation, test, documentation, and
  closeout change with signed Conventional Commits plus gitmoji, pulling with rebase before push.
- [ ] Mark this plan complete only after exact gate and delivery evidence is recorded.

## Deferred findings

None. Every cycle-55 finding is scheduled for implementation in this cycle.

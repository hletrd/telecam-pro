# Required by androidResources.generateLocaleConfig: names the locale that the unqualified values/
# folder is written in, so the generated locales_config can list it alongside values-ko.
unqualifiedResLocale=en-US
